name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: macos-26
    timeout-minutes: 30
    env:
      DEVELOPER_DIR: /Applications/Xcode_26.6.app/Contents/Developer
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Validate release and shell contracts
        run: |
          script/validate_release_version.sh
          bash -n script/*.sh script/lib/*.sh script/tests/*.sh
          script/tests/visual_dmg_contract_tests.sh
          script/tests/release_pipeline_contract_tests.sh
          script/tests/homebrew_release_tests.sh
          test "$(xcodebuild -version | sed -n '1p')" = "Xcode 26.6"

      - name: Test SiriusXMClient
        run: |
          swift test \
            --package-path Packages/SiriusXMClient \
            --scratch-path "$RUNNER_TEMP/siriusxmclient-tests"

      - name: Validate bundled animation and offline runtime contracts
        run: |
          bash script/test_update_download_contracts.sh
          bash script/test_animation_source_contracts.sh
          bash script/test_offline_review_launch_mode.sh
          bash script/test_scene_performance_director.sh
          bash script/test_sprite_atlas_geometry.sh
          python3 script/test_abyssal97_bundle.py
          python3 script/tests/scene_format2_tests.py
          swift test --package-path Packages/Canis97MotionSafety --scratch-path "$RUNNER_TEMP/motion-safety-tests"

      - name: Compile app and app tests without launching them
        run: |
          xcodebuild build-for-testing \
            -project SiriusMac.xcodeproj \
            -scheme Canis97 \
            -configuration Debug \
            -destination 'platform=macOS' \
            -derivedDataPath "$RUNNER_TEMP/canis97-derived-data" \
            -only-testing:Canis97Tests \
            -parallel-testing-enabled NO \
            CODE_SIGNING_ALLOWED=NO
