---
phase: 04-safe-skins-accessible-recovery
plan: "03"
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - SiriusMac/Skins/SkinAppearance.swift
  - SiriusMac/Skins/SkinPackageImporter.swift
  - SiriusMac/Skins/SkinManagementView.swift
  - SiriusMac/Player/CompactPlayerView.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMacTests/SkinPackageImporterTests.swift
  - SiriusMacTests/CompactPlayerPresentationTests.swift
  - SiriusMacTests/AccessibilityContractTests.swift
autonomous: true
requirements: [ACCS-02, SKIN-01, SKIN-02, SKIN-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A subscriber can open a native appearance-management surface, distinguish Native, bundled, and imported entries, select any valid entry, import another package, and see closed actionable validation results."
    - "A subscriber can remove an imported package; Native and bundled entries remain permanent catalog entries, and selected removal confirms Native durably before package deletion."
    - "Player > Use Native Appearance always restores the static Native value without resolving the current custom package, and remains enabled when selected metadata or package content is missing, corrupt, unsupported, or fails to render."
    - "Appearance management and recovery are keyboard- and VoiceOver-usable while the compact renderer retains every fixed native control, label/value, focus/state indicator, hit region, and Reduce Motion behavior."
    - "Management failures expose closed semantic categories and preserve a usable confirmed appearance, catalog, and selection record."
  artifacts:
    - path: "SiriusMac/Skins/SkinManagementView.swift"
      provides: "Native selection/import/removal/error management UI"
      exports: ["SkinManagementView", "SkinManagementRow", "SkinManagementErrorPresentation"]
    - path: "SiriusMac/SiriusMacApp.swift"
      provides: "Settings scene, Player management route, and package-independent Native recovery command"
    - path: "SiriusMac/Skins/SkinPackageImporter.swift"
      provides: "Imported-package removal transaction with selected-entry fallback ordering"
    - path: "SiriusMacTests/AccessibilityContractTests.swift"
      provides: "Compiled semantic/accessibility invariants across every appearance class and recovery route"
  key_links:
    - from: "Player > Manage Appearances"
      to: "SkinManagementView"
      via: "native Settings scene using the app-lifetime appearance/import controllers"
      pattern: "SkinManagementView"
    - from: "SkinManagementView selection"
      to: "SkinAppearanceController.select"
      via: "stable `SkinSelectionReference`; rows never pass manifest dictionaries or file URLs"
      pattern: "select"
    - from: "selected imported removal"
      to: "ManagedSkinStore"
      via: "confirm/persist Native first, then delete package; abort delete if fallback persistence fails"
      pattern: "removeImportedSkin"
    - from: "Player > Use Native Appearance"
      to: "ValidatedSkinAppearance.native"
      via: "direct static recovery method with no selected-package lookup"
      pattern: "restoreNativeAppearance"
  prohibitions:
    - statement: "A recovery failure may never strand the subscriber behind custom visuals or require knowledge of the skin package to return to Native."
      status: unverified
      flagged: true
    - statement: "Appearance-management errors may never blame the subscriber or expose untrusted archive, manifest, path, or decoder text as interface copy."
      status: unverified
      flagged: true
---

<objective>
Complete the subscriber-facing appearance lifecycle and accessible recovery: native management, selection, import result presentation, safe imported removal, and a package-independent Player-menu Native restore.

Purpose: Turn the safe contract and importer into a complete local personalization experience while proving that custom visuals cannot trap a subscriber or take ownership of controls and accessibility.
Output: Native management UI, deterministic removal behavior, permanent Player-menu recovery, accessible semantic regressions, and a complete source-coverage audit.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md
@.planning/phases/04-safe-skins-accessible-recovery/04-01-SUMMARY.md
@.planning/phases/04-safe-skins-accessible-recovery/04-02-SUMMARY.md
@.planning/phases/03-native-mac-listening-experience/03-07-SUMMARY.md
@SiriusMac/Skins/SkinAppearance.swift
@SiriusMac/Skins/SkinPackageImporter.swift
@SiriusMac/Player/CompactPlayerView.swift
@SiriusMac/SiriusMacApp.swift
@SiriusMac/Accessibility/AccessibilityAnnouncer.swift
@SiriusMac/Windows/CompactWindowController.swift
@SiriusMacTests/CompactPlayerPresentationTests.swift
@SiriusMacTests/AccessibilityContractTests.swift

<interfaces>
- `SkinAppearanceController` owns catalog entries, confirmed selected reference/value, selection generation, persistence, and direct `restoreNativeAppearance()` from Plans 04-01/04-02.
- `SkinImportCoordinator` owns serialized import transactions and `ManagedSkinStore`; UI consumes only closed `SkinImportResult`/`SkinPackageRejection` classifications.
- `CompactPlayerView` keeps `CompactPlayerAction` dispatch and all accessibility modifiers in native SwiftUI controls; `ValidatedSkinAppearance` supplies style and inert decoration only.
- `ListeningCommands` is the native Player command menu composition point. The restore action must call the appearance controller directly and remain independent of `ListeningSessionController` readiness and the selected package.
- `AccessibilityAnnouncer` accepts only `AccessibilityAnnouncementEvent`; appearance strings and validation text never enter this announcement boundary.
- `WindowLifecyclePolicy` owns the exact compact size and role; management/manifest values never reach it.
</interfaces>
</context>

## Flagged Planning Assumption

| ID | Probe | Status | Contract carried into execution |
|---|---|---|---|
| A-04-13 | SKIN-05 unclassified/manual review | flagged | Current repository policy forbids launching the app or an app/test host until a separate safety review authorizes a safe environment. This plan performs deterministic source/offline checks and compile-only validation, records the residual keyboard/VoiceOver/visual recovery walkthrough as unverified, and does not claim runtime UAT. |

<tasks>

<task type="auto">
  <name>Task 04-03-01: Manage native, bundled, and imported appearances in one accessible surface</name>
  <reversibility rating="reversible">The management surface is a private SwiftUI view over stable appearance/import controllers and can be reorganized without changing manifest or managed-storage contracts.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-01, D-02, D-13, D-14, and D-16 — expose lifecycle controls without transferring semantics, recovery, or persistence authority to skin data.
    - `SiriusMac/Skins/SkinAppearance.swift` — observe the app-lifetime catalog, selected reference, classification, and closed selection/recovery errors.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — invoke the one import coordinator and present only its closed result categories.
    - `SiriusMac/SiriusMacApp.swift` — add a native Settings scene and Player > Manage Appearances route using the same controllers as compact rendering.
    - `SiriusMac/Skins/SkinManagementView.swift` — create this view; rows receive stable semantic values and closures, never package contents or renderer behavior.
    - `SiriusMacTests/CompactPlayerPresentationTests.swift` — extend the selection-reference/classification authority contract without launching the test host.
    - `SiriusMac.xcodeproj/project.pbxproj` — register the management view source and preserve the established app/test graph.
  </read_first>
  <files>SiriusMac/Skins/SkinManagementView.swift, SiriusMac/Skins/SkinAppearance.swift, SiriusMac/SiriusMacApp.swift, SiriusMacTests/CompactPlayerPresentationTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <action>Per D-01, D-02, D-13, D-14, and D-16, create `SkinManagementView`, `SkinManagementRow`, and `SkinManagementErrorPresentation` in a native Settings scene. Show Native first, then bundled and imported sections in the catalog's deterministic order; each row exposes stable display name, classification, selected state, and a native Select button. Add Import Appearance… using the existing coordinator, one progress/cancel state, and closed actionable errors for invalid package, unsupported schema, unsafe content, over budget, cancelled, storage failure, and selection failure. Do not show raw archive/manifest/decoder/path text. Add Player > Manage Appearances… routed to the Settings scene. Keep all controls native with explicit labels, selected values, keyboard order, visible focus/state indicators, and at least the existing 32-point usable control regions. Extend compiled contracts to prove every classification uses `SkinSelectionReference` and the view receives no playback/session/accessibility mutation closure.</action>
  <acceptance_criteria>
    - One native surface lists Native, both bundled skins, and all validated imported skins with accurate selected/classification state.
    - Select and Import controls call the existing app-owned controllers and show only closed semantic progress/results.
    - The management view cannot access playback, authentication, window policy, accessibility announcements, raw manifests, or arbitrary files.
    - Player-menu and standard Settings routes open the same controller-backed management surface.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath "$(mktemp -d /tmp/sirius-mac-04-03-01.XXXXXX)" CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>A subscriber can inspect, select, and import every supported appearance class through one native, keyboard- and VoiceOver-described surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03-02: Remove imported packages without breaking confirmed selection</name>
  <reversibility rating="reversible">Removal ordering is private managed-store behavior behind the stable selection and import APIs.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-07, D-12, D-14, and D-16 — delete only app-owned packages, preserve confirmed selection on failure, and retain Native permanently.
    - `SiriusMac/Skins/SkinManagementView.swift` — add removal controls only to imported rows with native confirmation and closed errors.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — implement removal inside `ManagedSkinStore`/`SkinImportCoordinator`, never through the source archive URL.
    - `SiriusMac/Skins/SkinAppearance.swift` — remove a catalog entry only after storage deletion and maintain current-generation selection rules.
    - `SiriusMacTests/SkinPackageImporterTests.swift` — cover selected/nonselected removal, fallback-persistence failure, deletion failure, repeat removal, and concurrent selection/removal with injected storage seams.
  </read_first>
  <files>SiriusMac/Skins/SkinManagementView.swift, SiriusMac/Skins/SkinPackageImporter.swift, SiriusMac/Skins/SkinAppearance.swift, SiriusMacTests/SkinPackageImporterTests.swift</files>
  <behavior>
    - Removing a nonselected imported appearance deletes only its managed package and catalog entry; repeated removal returns an idempotent not-present success.
    - Removing the selected imported appearance first confirms/persists Native, then deletes the managed package and catalog entry.
    - If Native persistence fails, deletion does not begin and the prior imported appearance/package/selection remain intact.
    - If deletion fails after Native is confirmed, Native remains selected, the package remains cataloged, and a closed retryable removal error is shown.
    - Native and bundled rows have no removal action; concurrent selection/removal is serialized and generation checked.
  </behavior>
  <action>Per D-07, D-12, D-14, and D-16, add `removeImportedSkin(_:)` to the import/lifecycle actor and controller. Accept only an `.imported` stable reference already rooted in the managed Packages directory. For a nonselected entry, delete its managed directory then remove its catalog value. For the selected entry, call the ordinary validated Native selection/persistence path first; abort deletion when that durable confirmation fails. After confirmed Native, delete the imported directory and then remove the catalog value; on deletion failure retain the catalog entry and present a closed retryable error while Native stays active. Treat already-absent imported content as idempotent success after catalog reconciliation. In the UI, provide a native Remove button only on imported rows, confirmation naming the bounded display name, focus restoration after completion, and no destructive action on Native or bundled entries. Add deterministic injected-store compile contracts for each ordering and concurrency case.</action>
  <acceptance_criteria>
    - A subscriber can remove any imported appearance and can never remove Native or either bundled skin.
    - Selected removal cannot create a dangling selected reference or delete the package before Native is durably confirmed.
    - Failed persistence/deletion and repeat/concurrent removal have deterministic, recoverable outcomes.
    - Removal touches only the canonical managed directory resolved from a validated imported identifier.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath "$(mktemp -d /tmp/sirius-mac-04-03-02.XXXXXX)" CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>A subscriber can safely remove imported packages, including the selected one, without losing a valid confirmed appearance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03-03: Lock native recovery and accessibility invariants outside skin control</name>
  <reversibility rating="costly">The direct recovery route and native semantic-control boundary are cross-surface product invariants consumed by future appearance work.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-01, D-02, D-03, D-12, D-14, and D-15 — preserve app-owned semantics/focus/hit targets/geometry and guarantee package-independent Native recovery.
    - `SiriusMac/SiriusMacApp.swift` — keep `Use Native Appearance` as a top-level enabled Player command that calls the static recovery method directly.
    - `SiriusMac/Player/CompactPlayerView.swift` — audit every control, action, label/value/identifier, sort priority, focus/state visual, environment color scheme, and fixed frame after appearance injection.
    - `SiriusMac/Skins/SkinManagementView.swift` — ensure selection/import/removal/recovery states have native labels, values, keyboard order, and focus restoration.
    - `SiriusMacTests/CompactPlayerPresentationTests.swift` and `SiriusMacTests/AccessibilityContractTests.swift` — extend source-level semantic contracts across Native, both bundled skins, one imported fixture, invalid selection, render failure, and direct recovery.
  </read_first>
  <files>SiriusMac/SiriusMacApp.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Skins/SkinManagementView.swift, SiriusMacTests/CompactPlayerPresentationTests.swift, SiriusMacTests/AccessibilityContractTests.swift</files>
  <behavior>
    - `Use Native Appearance` is enabled independently of selected-package lookup and returns the static Native value for missing, corrupt, unsupported, decode-failed, or render-failed custom state.
    - Native, Signal Glow, Tape Deck, and an imported fixture expose the identical `CompactPlayerAction` set, control identifiers, labels/values, focus order, 32-point transport regions, and 400 x 288 frame.
    - Skin manifests cannot provide accessibility strings, action names, focus priorities, state copy, Reduce Motion behavior, or window metrics because none are schema keys or renderer inputs.
    - A selected style that cannot construct its inert decoration is rejected before publication or immediately replaced by Native while the Player recovery command remains available.
    - Management errors and recovery status are bounded app-owned copy and do not enter `AccessibilityAnnouncementEvent`.
  </behavior>
  <action>Per D-01, D-02, D-03, D-12, D-14, and D-15, make the Player command call `SkinAppearanceController.restoreNativeAppearance()` directly from the static Native catalog entry before any selected-custom resolution. Keep the command outside `CompactPlayerView`, always enabled when the listening shell exists, and independent of imported storage. In `CompactPlayerView`, retain one native control tree and apply appearance only to background/panel/accent/destructive colors, bounded integer spacing/radius, foreground scheme, and inert decorative image layers with hit testing and accessibility hidden. Preserve the exact 400 x 288 frame, 32-point transport frames, native buttons/toggles, accessibility identifiers/labels/values/order, focus/state indicators, and Reduce Motion behavior for every class. Reject an appearance before publication when its decoration cannot be resolved/decoded; if an already-selected managed asset becomes unusable, immediately render the static Native value and expose a closed recovery status without passing manifest text to announcements. Extend compiled semantic/source contracts across all appearance classes, corrupted selection, render failure, and direct recovery.</action>
  <acceptance_criteria>
    - Native recovery works without opening or decoding the selected custom package and cannot be hidden or disabled by skin visuals.
    - Every supported appearance renders the same app-owned semantic control and accessibility tree at the same fixed geometry/hit regions.
    - Invalid decoration or selected-package failure produces a usable Native appearance and bounded app-owned status.
    - The manual runtime review residue is recorded as A-04-13 rather than silently claimed or executed under the current safety block.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath "$(mktemp -d /tmp/sirius-mac-04-03-03.XXXXXX)" CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>The player has an unconditional native recovery route and one invariant accessible native control tree across every supported appearance.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates the following exact paths and symbols; executors may add private helpers only when they are recorded in the plan summary.

| Path | New symbols/data |
|---|---|
| `SiriusMac/Skins/SkinManagementView.swift` | `SkinManagementView`, `SkinManagementRow`, `SkinManagementErrorPresentation` |
| `SiriusMac/Skins/SkinPackageImporter.swift` | `ManagedSkinStore.removeImportedSkin(_:)`, `SkinImportCoordinator.removeImportedSkin(_:)` |
| `SiriusMac/Skins/SkinAppearance.swift` | `SkinAppearanceController.removeImportedSkin(_:)`, hardened `restoreNativeAppearance()` |
| `SiriusMac/SiriusMacApp.swift` | Settings appearance scene, Player > Manage Appearances…, Player > Use Native Appearance |
| `SiriusMacTests/CompactPlayerPresentationTests.swift` | Cross-class renderer/selection/recovery source contracts |
| `SiriusMacTests/AccessibilityContractTests.swift` | Cross-class native semantic/accessibility invariants |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Nostalgic local appearance remains declarative, bounded, accessible, and recoverable | 04-01, 04-02, 04-03 | COVERED | Selection/render tracer, hostile-input package lifecycle, and accessible recovery form one end-to-end capability. |
| REQ | ACCS-02 | Customization preserves semantic controls, hit targets, focus/state indicators, and Native fallback | 04-01, 04-03 | COVERED | Fixed native renderer and direct Player recovery. |
| REQ | SKIN-01 | At least two complete tested bundled skins | 04-01 | COVERED | Signal Glow and Tape Deck exact-minimum resource audit. |
| REQ | SKIN-02 | Import, validate, select, and remove declarative local packages | 04-02, 04-03 | COVERED | Two-pass import plus management/removal lifecycle. |
| REQ | SKIN-03 | Packages have no executable/remote/arbitrary/control authority | 04-01, 04-02 | COVERED | Exact manifest keys, local managed resolver, one native renderer. |
| REQ | SKIN-04 | Reject schema/path/type/resource/processing violations | 04-02 | COVERED | Exact closed policy and offline boundary/precision matrix. |
| REQ | SKIN-05 | Failure preserves prior appearance and provides built-in recovery | 04-01, 04-02, 04-03 | COVERED | Persistence-before-publication, atomic rollback, direct Native command. |
| RESEARCH | — | No RESEARCH.md exists because phase research is disabled | — | EXCLUDED | Locked CONTEXT.md supplies the archive, image, limits, cancellation, and storage contracts; no source item is omitted. |
| CONTEXT | D-01 | Appearance/decorative data only behind fixed controls | 04-01, 04-03 | COVERED | One validated style input and one native control tree. |
| CONTEXT | D-02 | Native controls/semantics/hit targets/focus/state remain authoritative | 04-01, 04-03 | COVERED | Cross-class compiled invariants. |
| CONTEXT | D-03 | Fixed 400 x 288 and no independent layout runtime | 04-01, 04-03 | COVERED | Schema omits window/control geometry. |
| CONTEXT | D-04 | ZIP `.siriusskin`, ZIPFoundation 0.9.20 transport only | 04-02 | COVERED | Exact SwiftPM linkage isolated to importer. |
| CONTEXT | D-05 | Two-pass inspect then streamed extraction | 04-02 | COVERED | Separate descriptor preflight and actual-byte extraction. |
| CONTEXT | D-06 | Reject encryption/path/collision/prefix/type violations | 04-02 | COVERED | Canonical path/type policy before extraction. |
| CONTEXT | D-07 | Atomic managed copy and source independence | 04-02, 04-03 | COVERED | Same-volume promotion and managed-only removal/rendering. |
| CONTEXT | D-08 | Strict versioned exact-key local-only manifest | 04-01, 04-02 | COVERED | One shared validator for bundled/imported resources. |
| CONTEXT | D-09 | Exact centralized archive/file/image/path budgets | 04-02 | COVERED | Inclusive checked constants and one-over tests. |
| CONTEXT | D-10 | Single-frame PNG/JPEG with Image I/O agreement | 04-02 | COVERED | UTI/extension/frame/dimension policy. |
| CONTEXT | D-11 | Cooperative 10-second deadline plus hard structural limits | 04-02 | COVERED | Injected clock/checkpoints and no preemption claim. |
| CONTEXT | D-12 | Full validation before selection; all failures preserve prior selection | 04-01, 04-02, 04-03 | COVERED | Generation, rollback, persistence-before-publication. |
| CONTEXT | D-13 | Two bundled skins use imported finite contract | 04-01 | COVERED | Shared manifest validator, catalog value, renderer. |
| CONTEXT | D-14 | Permanent non-removable Native fallback | 04-01, 04-03 | COVERED | Static Native catalog member and removal exclusion. |
| CONTEXT | D-15 | Native Player-menu recovery independent of selected skin | 04-01, 04-03 | COVERED | Direct static `restoreNativeAppearance()`. |
| CONTEXT | D-16 | Persist stable selection metadata only | 04-01, 04-02, 04-03 | COVERED | Exact three-key selection JSON; packages stay managed files. |

<verification>
- Run the Plan 04-01/04-02 offline selection, bundled-manifest, and package-policy executables before this plan's compile gate.
- Compile the final app and configured test sources once with one serialized `xcodebuild build-for-testing` lane, a fresh `mktemp` DerivedData directory, ad-hoc signing, and no app/test execution.
- Source-review Player command construction to prove Native recovery calls the static value without selected-package lookup; source-review `CompactPlayerView` to prove decoration is behind native controls and accessibility hidden.
- Record the SKIN-05 runtime keyboard/VoiceOver/visual walkthrough as the flagged A-04-13 residue under current project safety policy; no runtime UAT is authorized by this plan.
- Confirm every source-audit row is COVERED or explicitly EXCLUDED and every D-01 through D-16 citation appears in an owning task action.
</verification>

<success_criteria>
- A subscriber can choose two bundled skins, import/select/remove a validated local package, and recover to Native through normal macOS surfaces.
- Native semantics, controls, 32-point transport regions, readable focus/state indicators, 400 x 288 geometry, announcements, and Reduce Motion behavior remain identical across appearance classes.
- All six Phase 04 requirements and D-01 through D-16 are covered without a second renderer, unmanaged source dependency, runtime/live test, or deferred phase feature.
- The 13 spec-less edge-probe items are explicit flagged assumptions with concrete contracts; A-04-13 remains honestly unverified under the safety block.
</success_criteria>

<output>
Create `.planning/phases/04-safe-skins-accessible-recovery/04-03-SUMMARY.md` when done.
</output>
