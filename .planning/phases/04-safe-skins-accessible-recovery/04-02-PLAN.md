---
phase: 04-safe-skins-accessible-recovery
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - SiriusMac/Skins/SkinAppearance.swift
  - SiriusMac/Skins/SkinPackagePolicy.swift
  - SiriusMac/Skins/SkinPackageImporter.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMacTests/SkinPackageImporterTests.swift
  - script/tests/SkinPackagePolicyOfflineTests.swift
autonomous: true
requirements: [SKIN-02, SKIN-03, SKIN-04, SKIN-05]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A subscriber can choose one local `.siriusskin` archive, import it through a two-pass transaction, and select the validated app-owned copy through the same selected-appearance path as Native and bundled skins."
    - "Every archive entry, canonical path, declared resource total, actual streamed extraction byte, manifest key, local asset reference, image type/frame/dimension, cancellation point, and deadline check is accepted or rejected by one closed policy before selection changes."
    - "Managed rendering resolves only immutable app-owned package files after atomic promotion, and source archive access is closed before managed rendering begins."
    - "Invalid, cancelled, stale, decode-failed, over-budget, or promotion-failed work leaves the prior confirmed in-memory and durable selected appearance unchanged."
    - "Identical re-import is idempotent, an actor serializes import work, selection publication requires the current generation, and each transaction owns a unique staging directory."
  artifacts:
    - path: "SiriusMac/Skins/SkinPackagePolicy.swift"
      provides: "Pure canonical path, archive entry, resource budget, deadline, and rejection policy"
      exports: ["SkinPackageLimits", "CanonicalSkinPath", "SkinArchiveEntryDescriptor", "SkinPackagePolicy", "SkinPackageRejection"]
    - path: "SiriusMac/Skins/SkinPackageImporter.swift"
      provides: "ZIPFoundation transport adapter, Image I/O asset validation, managed atomic store, and serialized import coordinator"
      exports: ["SkinPackageImporter", "ManagedSkinStore", "SkinImportCoordinator", "SkinImportResult"]
    - path: "SiriusMacTests/SkinPackageImporterTests.swift"
      provides: "Compiled app-layer transaction, rollback, cancellation, and transport integration contracts"
    - path: "script/tests/SkinPackagePolicyOfflineTests.swift"
      provides: "Runnable source-level boundary/precision/path/resource policy tests"
  key_links:
    - from: "NSOpenPanel selection in SiriusMacApp.swift"
      to: "SkinImportCoordinator"
      via: "one explicit `.siriusskin` URL and one task; security-scoped access ends in defer"
      pattern: "SkinImportCoordinator"
    - from: "SkinPackageImporter"
      to: "SkinPackagePolicy"
      via: "pass-one metadata preflight and pass-two streamed byte accounting"
      pattern: "SkinPackagePolicy"
    - from: "SkinPackageImporter"
      to: "SkinManifestValidator"
      via: "validate exact manifest and referenced managed candidate assets before promotion"
      pattern: "SkinManifestValidator"
    - from: "ManagedSkinStore"
      to: "SkinAppearanceController"
      via: "atomic promotion result becomes catalog entry; selection persists only after current-generation resolution"
      pattern: "SkinImportResult"
  prohibitions:
    - statement: "A local appearance archive may never become an execution, networking, authentication, playback, persistence, accessibility, or arbitrary-file authority surface."
      status: unverified
      flagged: true
    - statement: "Importing a local appearance may never make rendering depend on the source archive or another user-controlled location after validation."
      status: unverified
      flagged: true
    - statement: "An import failure may never silently replace the last confirmed appearance or its durable selection."
      status: unverified
      flagged: true
---

<objective>
Expand the selected-appearance tracer into a hostile-input-safe local package lifecycle: choose, preflight, stream-extract, validate, atomically promote, catalog, and select one `.siriusskin` without weakening the existing appearance or app authority boundaries.

Purpose: Deliver the local creator package capability while treating archive metadata, paths, bytes, JSON, images, source URLs, cancellation, and concurrent requests as untrusted input.
Output: ZIPFoundation 0.9.20 transport, a pure safety policy, strict image/manifest validation, managed storage, serialized atomic import, safe failures, and focused offline/compile contracts.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md
@.planning/phases/04-safe-skins-accessible-recovery/04-01-SUMMARY.md
@SiriusMac/Skins/SkinAppearance.swift
@SiriusMac/Player/CompactPlayerView.swift
@SiriusMac/SiriusMacApp.swift
@SiriusMac/Library/LibraryStore.swift
@SiriusMac.xcodeproj/project.pbxproj

<interfaces>
- Plan 04-01 provides `SkinManifestValidator.validate(_:assetResolver:)`, `ValidatedSkinAppearance`, `SkinAppearanceCatalog`, `SkinSelectionReference`, and main-actor `SkinAppearanceController.select(_:)`; imports must terminate at these contracts rather than create a second renderer.
- `SkinSelectionStore` publishes only stable identifier/classification metadata after a confirmed selection and preserves the previous record on errors.
- `CompactPlayerView` accepts only `ValidatedSkinAppearance` visual data; it has no archive, source URL, storage, networking, or file-reading collaborator.
- `LibraryStore.saveIfNeeded` demonstrates the repository's confirmed-state pattern: catch durable failure, rollback, then republish only confirmed state. Package selection must preserve the same user-visible ordering.
- The Xcode app target currently consumes the local `SiriusXMClient` product. Add the exact remote package reference `https://github.com/weichsel/ZIPFoundation.git` at version `0.9.20` and link only the `ZIPFoundation` product to the app target.
</interfaces>
</context>

## Flagged Planning Assumptions

| ID | Probe | Status | Contract carried into execution |
|---|---|---|---|
| A-04-07 | SKIN-02 idempotency | flagged | Re-importing the same stable identifier and SHA-256 digest returns the existing managed entry without a duplicate directory or selection write; the same identifier with different validated content atomically replaces the managed package. |
| A-04-08 | SKIN-02 concurrency | flagged | `SkinImportCoordinator` serializes import transactions. Waiting cancellation returns cancelled; each active transaction has a unique staging directory; only the latest current selection generation may publish. |
| A-04-09 | SKIN-03 concurrency | flagged | Rendering consumes an already-decoded immutable `ValidatedSkinAppearance`; removal and replacement cannot grant manifest code or mutate the value being rendered, and selected removal changes to Native before deletion. |
| A-04-10 | SKIN-04 boundary | flagged | Every numeric maximum is inclusive: exact maximum passes and one unit over rejects. The 10-second check rejects at exactly 10,000,000,000 elapsed nanoseconds because work must finish before the deadline. |
| A-04-11 | SKIN-04 precision | flagged | Byte/count/path/dimension totals use overflow-reporting integer arithmetic; compression ratios use integer cross multiplication, nonempty data over zero compressed bytes is infinite/rejected, image pixel products use checked multiplication, and no value is rounded or clamped. |
| A-04-12 | SKIN-04 concurrency | flagged | Passes share only immutable limits. Per-import counters, clocks, file handles, and staging roots are transaction-local; the actor serializes promotion and catalog mutation so parallel work cannot merge budgets or directories. |

Canonical breadcrumb: generic archive traversal, decompression-bomb, injection, arbitrary-file, and data-retention concerns are canon security items covered by the targeted skin-import security review and offline/compiler gates; they are not duplicated as bespoke prohibition descriptors.

<tasks>

<task type="auto">
  <name>Task 04-02-01: Import one valid local package into managed storage and select it</name>
  <reversibility rating="costly">The `.siriusskin` ZIP transport and exact dependency version are user-visible package compatibility choices; replacing them requires coordinated importer, fixture, documentation, and creator-package changes.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-04, D-05, D-07, D-08, D-12, and D-16 — implement exact transport, two-pass extraction, managed ownership, strict manifest use, selection ordering, and metadata-only persistence.
    - `SiriusMac/Skins/SkinAppearance.swift` — consume the one validator/catalog/controller contract from Plan 04-01.
    - `SiriusMac/SiriusMacApp.swift` — add one native import command and safe semantic result presentation without changing session or playback ownership.
    - `SiriusMac.xcodeproj/project.pbxproj` — preserve existing package/target membership and add the exact ZIPFoundation 0.9.20 remote reference/product to the app target only.
    - `SiriusMac/Skins/SkinPackagePolicy.swift` and `SiriusMac/Skins/SkinPackageImporter.swift` — create these files; keep pure entry/path/budget decisions in the former and ZIP/ImageIO/FileManager effects in the latter.
  </read_first>
  <files>SiriusMac/Skins/SkinPackagePolicy.swift, SiriusMac/Skins/SkinPackageImporter.swift, SiriusMac/Skins/SkinAppearance.swift, SiriusMac/SiriusMacApp.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <action>Per D-04, add the exact ZIPFoundation 0.9.20 SwiftPM product strictly as archive transport. Define `SkinPackageLimits`, `CanonicalSkinPath`, `SkinArchiveEntryDescriptor`, `SkinPackagePolicy`, and closed `SkinPackageRejection` separately from `SkinPackageImporter`, `ManagedSkinStore`, `SkinImportCoordinator`, and `SkinImportResult`. Per D-05, first iterate all ZIPFoundation entries and build policy descriptors without extraction; after preflight, create a UUID staging directory under Application Support/Sirius Mac/Skins/.staging on the same volume as Packages, then extract accepted regular files with ZIPFoundation's chunk consumer, CRC verification enabled, explicit FileHandle writes, and actual emitted-byte counters. Require root `manifest.json`; call the Plan 04-01 exact-key `SkinManifestValidator` with a resolver rooted only at staging. Per D-07, compute a deterministic SHA-256 digest over canonical path plus content, atomically move a new package or replace an existing identifier under `Skins/Packages/<identifier>`, close and discard source/security-scoped access in `defer`, then insert the managed validated appearance into the shared catalog. Per D-12, select it only after promotion and current-generation persistence succeed. Add Player > Appearance > Import Appearance… using `NSOpenPanel` restricted to `.siriusskin`, exactly one selection, and one import task; present closed semantic success/failure categories without archive text, manifest strings, source paths, or storage details.</action>
  <acceptance_criteria>
    - A valid package takes the exact sequence preflight, fresh staged streamed extraction, manifest/asset validation, atomic managed promotion, catalog insertion, and selected-appearance confirmation.
    - The renderer receives only a validated managed appearance and never reads the source archive or staging directory.
    - The source URL/bookmark is absent from durable state, and source access ends on every success, failure, and cancellation path.
    - ZIPFoundation owns ZIP mechanics only; Sirius Mac owns every acceptance policy and semantic error.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath "$(mktemp -d /tmp/sirius-mac-04-02-01.XXXXXX)" CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>A subscriber can import and select one validated local `.siriusskin`, and playback renders only the atomically managed app-owned copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02-02: Enforce every archive, path, manifest, image, and budget boundary</name>
  <reversibility rating="costly">These exact inclusive limits and canonicalization rules define the accepted creator-package compatibility surface and require synchronized fixture/validator changes when revised.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-06, D-08, D-09, D-10, and D-11 — implement every rejection and exact numeric limit without substitution.
    - `SiriusMac/Skins/SkinPackagePolicy.swift` — keep path/resource/deadline arithmetic Foundation-only and directly runnable outside the app host.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — map ZIPFoundation metadata and streamed data into the pure policy; use Image I/O only after structural and encoded-byte checks pass.
    - `SiriusMacTests/SkinPackageImporterTests.swift` — create compiled integration contracts for two-pass ordering, managed-root resolution, Image I/O type/frame checks, and transaction rollback.
    - `script/tests/SkinPackagePolicyOfflineTests.swift` — create a runnable boundary/precision/collision matrix using generated descriptors and injected clock values only.
  </read_first>
  <files>SiriusMac/Skins/SkinPackagePolicy.swift, SiriusMac/Skins/SkinPackageImporter.swift, SiriusMacTests/SkinPackageImporterTests.swift, script/tests/SkinPackagePolicyOfflineTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <behavior>
    - Exact inclusive maxima pass and one unit over rejects for archive bytes, expanded bytes, entries, per-file bytes, manifest bytes, ratio, image width/height/pixels, path components, and UTF-8 path bytes.
    - Absolute, drive-prefixed, traversal, backslash, NUL, duplicate canonical, case-fold, NFC-equivalent, and file/directory-prefix-conflicting paths reject before extraction.
    - Encrypted entries, symlinks, and unsupported entry kinds reject; only accepted regular files emit bytes into staging.
    - Only root `manifest.json` plus referenced single-frame PNG/JPEG assets are accepted; Image I/O detected type and lowercase extension agree.
    - Cancellation/deadline checks occur before and after each entry and on every extraction chunk; synchronous Image I/O inspection is bounded structurally and is never described as preemptible.
  </behavior>
  <action>Per D-06, canonicalize each `/`-separated path by rejecting empty interior, `.`, `..`, leading slash, drive prefix, backslash, NUL, more than four components, and more than 240 UTF-8 bytes, then NFC-normalize and POSIX case-fold for duplicate/collision and prefix-conflict detection. Reject encryption, symlink, and non-file/non-directory entry types before extraction; count directories in the entry total but emit only regular files. Prove the resolved destination remains beneath the fresh staging root. Per D-09, centralize inclusive limits at 16 * 1024 * 1024 archive bytes, 64 * 1024 * 1024 actual expanded regular-file bytes, 128 entries, 8 * 1024 * 1024 actual bytes per regular file/image, 64 * 1024 manifest bytes, and 100:1 declared per-file and aggregate ratios. Use checked addition/multiplication and integer ratio cross-products; reject arithmetic overflow and nonempty output with zero declared compressed bytes. Per D-08, permit only exact version-1 keys and local canonical references; require every extracted asset to be referenced and every reference to exist beneath staging. Per D-10, accept only `.png`, `.jpg`, or `.jpeg` whose Image I/O UTI agrees, frame count equals one, width and height are each at most 4,096, and checked pixels are at most 16,777,216. Per D-11, inject `ContinuousClock`, set deadline to start + 10 seconds, and check cancellation/deadline around entries and chunks; structural limits remain the safety boundary. Add the complete offline exact/max/over/overflow/collision/deadline matrix and app-layer compile contracts.</action>
  <acceptance_criteria>
    - Every D-06/D-08/D-09/D-10/D-11 rejection has a closed `SkinPackageRejection` case and a focused offline or compiled integration case.
    - Streamed actual bytes, not declared expanded sizes, enforce per-file and total extraction budgets.
    - Ratio, total, and pixel math cannot overflow, round, clamp, or divide by zero into acceptance.
    - Temporary files are removed and the managed catalog/selection remain unchanged after every rejection.
  </acceptance_criteria>
  <verify>
    <automated>zsh -lc 'probe_dir=$(mktemp -d /tmp/sirius-mac-04-02-policy.XXXXXX); swiftc SiriusMac/Skins/SkinPackagePolicy.swift script/tests/SkinPackagePolicyOfflineTests.swift -o "$probe_dir/skin-policy-tests"; "$probe_dir/skin-policy-tests"'</automated>
  </verify>
  <done>Every hostile shape and exact resource boundary fails closed before it can alter managed packages, selection, or rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02-03: Serialize imports and make cancellation, re-import, promotion, and selection atomic</name>
  <reversibility rating="reversible">Generation arbitration and staging cleanup are private transaction mechanics behind the fixed import/result contract.</reversibility>
  <read_first>
    - `.planning/phases/04-safe-skins-accessible-recovery/04-CONTEXT.md` D-05, D-07, D-11, and D-12 — preserve two-pass ordering, managed promotion, cooperative interruption, and previous-selection rollback.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — use one actor-owned transaction queue, UUID staging roots, digest-based idempotency, and a narrow promotion critical section.
    - `SiriusMac/Skins/SkinPackagePolicy.swift` — reuse transaction-local counters and clock checks; do not introduce shared mutable budgets.
    - `SiriusMac/Skins/SkinAppearance.swift` — preserve main-actor selection generation and publish only current validated managed references.
    - `SiriusMac/SiriusMacApp.swift` — preserve the one explicit import task and route overlapping UI intents through the serialized coordinator.
    - `SiriusMacTests/SkinPackageImporterTests.swift` — add deterministic fake transport/file-operation/clock tests that compile without app launch or live file-provider access.
  </read_first>
  <files>SiriusMac/Skins/SkinPackageImporter.swift, SiriusMac/Skins/SkinPackagePolicy.swift, SiriusMac/Skins/SkinAppearance.swift, SiriusMac/SiriusMacApp.swift, SiriusMacTests/SkinPackageImporterTests.swift</files>
  <behavior>
    - Two simultaneous import requests execute as separate serialized transactions with distinct staging directories and counters.
    - Cancelling a waiting request creates no staging directory; cancelling active work at an entry/chunk check removes staging and leaves durable selection unchanged.
    - Re-importing equal identifier/digest returns the managed appearance without duplicate storage or selection writes.
    - A validated different digest for the same identifier replaces the package atomically; failed replacement leaves the old managed package resolvable.
    - A stale completion may leave a valid imported package available in the catalog but cannot replace the latest confirmed selected appearance.
  </behavior>
  <action>Per D-05, D-07, D-11, and D-12, make `SkinImportCoordinator` an actor that assigns request and selection generations, serializes archive transactions, and owns cleanup through `defer`. Allocate staging only when a request reaches the front of the queue. Compare identifier plus canonical content digest before promotion: return the existing managed value for equal content; for changed content, use same-volume replacement with backup/rollback semantics and publish the catalog entry only after the replacement succeeds. Never expose staging or source paths. Forward the validated managed result to the main-actor controller with the originating generation; discard selection publication when it is stale, cancelled, or persistence fails. Keep prior managed content on a failed replacement and prior in-memory/durable selection on every non-success. Add deterministic fakes that force cancellation before extraction, between chunks, after validation, during promotion, and during selection persistence, plus overlapping equal/different-ID requests.</action>
  <acceptance_criteria>
    - No interleaving can share counters, staging paths, FileHandles, promotion backups, or selected-appearance authority.
    - All cancellation and injected failure points clean staging and preserve the prior confirmed/durable selection.
    - Same-content re-import is idempotent and changed-content replacement is atomic with an intact rollback source.
    - Only the current successful selection generation changes the compact appearance.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath "$(mktemp -d /tmp/sirius-mac-04-02-03.XXXXXX)" CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>Repeated, overlapping, cancelled, replaced, and failed imports have deterministic storage and selection outcomes with no partial activation.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates the following exact paths and symbols; executors may add private helpers only when they are recorded in the plan summary.

| Path | New symbols/data |
|---|---|
| `SiriusMac/Skins/SkinPackagePolicy.swift` | `SkinPackageLimits`, `CanonicalSkinPath`, `SkinArchiveEntryDescriptor`, `SkinPackagePolicy`, `SkinPackageRejection` |
| `SiriusMac/Skins/SkinPackageImporter.swift` | `SkinPackageImporter`, `ManagedSkinStore`, `SkinImportCoordinator`, `SkinImportResult` |
| `SiriusMacTests/SkinPackageImporterTests.swift` | `SkinPackageImporterTests` app-layer transport/image/promotion/cancellation/rollback compile contracts |
| `script/tests/SkinPackagePolicyOfflineTests.swift` | `SkinPackagePolicyOfflineTests` executable covering boundary, precision, overflow, path collision, and deadline cases |
| `SiriusMac.xcodeproj/project.pbxproj` | Exact ZIPFoundation 0.9.20 package/product linkage and new source/test membership |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local source archive -> ZIP transport/policy | Untrusted metadata, paths, entry types, sizes, ratios, and emitted bytes cross into app processing. |
| Extracted candidate -> manifest/Image I/O validation | Untrusted JSON and encoded images are inspected before managed storage or rendering. |
| Staging -> managed package store | Only a completely validated same-volume candidate may be atomically promoted. |
| Managed package -> selected renderer | Only a validated immutable value and managed asset resolver cross into the compact renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-02-01 | Tampering | canonical archive paths | high | mitigate | D-06 exact path/type/collision/prefix policy before extraction plus beneath-root resolution. |
| T-04-02-02 | Denial of Service | archive expansion and image inspection | high | mitigate | D-09 checked inclusive hard limits, streamed actual-byte accounting, D-10 image bounds, D-11 cooperative checks. |
| T-04-02-03 | Elevation of Privilege | manifest capability surface | high | mitigate | D-08 exact-key closed schema and finite local decoration/style roles only. |
| T-04-02-04 | Information Disclosure | source and managed paths | medium | mitigate | D-07 scoped source access ends in defer; UI/results expose closed semantic categories only. |
| T-04-02-05 | Tampering | promotion/selection interleaving | high | mitigate | actor serialization, unique staging, same-volume atomic replacement, generation check, persistence-before-publication. |
</threat_model>

<verification>
- Run `script/tests/SkinPackagePolicyOfflineTests.swift` against fresh generated descriptors and injected clocks; it must exercise every exact limit, one-over case, overflow path, collision class, and deadline boundary without app launch.
- Compile the app and configured tests after dependency resolution with one serialized `xcodebuild build-for-testing` lane and a fresh `mktemp` DerivedData directory; do not execute any app/test host.
- Perform a targeted source security review of the four trust boundaries in the threat register, including every exit path's source-scope closure, staging cleanup, and selection rollback.
- Confirm ZIPFoundation appears only in `SkinPackageImporter.swift` and project linkage; policy, appearance values, renderer, and persistence do not import it.
- Record A-04-07 through A-04-12 as flagged assumptions in the summary.
</verification>

<success_criteria>
- A subscriber can import and select a valid local package, while every D-04 through D-12 safety/transaction rule is implemented and cited.
- SKIN-02, SKIN-03, SKIN-04, and SKIN-05 have executable source/offline/compile contracts without runtime or live SiriusXM activity.
- The importer owns no behavior beyond archive transport, validation, managed lifecycle, and selected-appearance handoff.
- The previous valid appearance and durable selection survive every invalid, cancelled, stale, decode, budget, promotion, and persistence failure.
</success_criteria>

<output>
Create `.planning/phases/04-safe-skins-accessible-recovery/04-02-SUMMARY.md` when done.
</output>
