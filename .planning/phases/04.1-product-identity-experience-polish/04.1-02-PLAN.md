---
phase: 04.1-product-identity-experience-polish
plan: "02"
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - SiriusMac/App/ProductIdentity.swift
  - SiriusMac/App/ProductIdentityMigration.swift
  - SiriusMac/Library/LibraryStore.swift
  - SiriusMac/Skins/SkinSelectionStore.swift
  - SiriusMac/Skins/SkinPackageImporter.swift
  - SiriusMac/Windows/CompactWindowController.swift
  - SiriusMacTests/LibraryStoreTests.swift
  - SiriusMacTests/SkinPackageImporterTests.swift
  - SiriusMacTests/ListeningSessionControllerTests.swift
  - script/tests/product_identity_migration_tests.swift
autonomous: true
requirements: []
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02: Existing non-secret library, appearance selection, managed skin, and valid window-frame state remains discoverable after the approved rename."
    - "D-02/D-10: Each migration is destination-first, semantic, idempotent, verifies its destination before marking completion, and leaves legacy sources intact on failure."
    - "D-02: The Keychain generic-password service/account and SiriusXMClient session boundary are never read, copied, renamed, deleted, or cosmetically migrated."
    - "D-09: A malformed selection or managed-package migration failure preserves Native recovery and the existing schema/package validator authority."
    - "D-12: Migration behavior has a standalone Foundation test lane plus compile-only app-test coverage; no app/test host or live service runs."
  artifacts:
    - path: "SiriusMac/App/ProductIdentityMigration.swift"
      provides: "Pure, injected migration transaction and completion-marker contract for app-owned non-secret state"
    - path: "script/tests/product_identity_migration_tests.swift"
      provides: "Standalone offline migration behavior matrix"
    - path: "SiriusMac/Library/LibraryStore.swift"
      provides: "Semantic legacy-library import into the approved Application Support root"
    - path: "SiriusMac/Skins/SkinSelectionStore.swift"
      provides: "Validated selection migration with Native fallback"
    - path: "SiriusMac/Skins/SkinPackageImporter.swift"
      provides: "Validated managed-package copy into the approved root"
    - path: "SiriusMac/Windows/CompactWindowController.swift"
      provides: "Valid-only frame autosave migration into product-owned names"
  key_links:
    - from: "SiriusMac/App/ProductIdentity.swift"
      to: "SiriusMac/App/ProductIdentityMigration.swift"
      via: "Current and legacy non-secret namespaces come only from ProductIdentity"
      pattern: "ProductIdentity.Legacy"
    - from: "SiriusMac/App/ProductIdentityMigration.swift"
      to: "SiriusMac/Library/LibraryStore.swift"
      via: "Injected semantic read/copy/verify/mark transaction"
      pattern: "ProductIdentityMigration"
    - from: "SiriusMac/Skins/SkinSelectionStore.swift"
      to: "SiriusMac/Skins/SkinPackageImporter.swift"
      via: "Only a validated managed package may back a migrated imported selection"
      pattern: "PersistedSkinSelection"
  prohibitions:
    - "No raw SQLite move/adoption, legacy-source deletion, or destination overwrite after completion."
    - "No Keychain/session/token migration or fallback through files, preferences, SwiftData, logs, or skin data."
    - "No Phase 04.2/05 work, renderer expansion, app launch, app-hosted tests, UI automation, or live SiriusXM activity."
---

<objective>
Preserve app-owned non-secret state across the approved D-02 identity change without touching provider credentials or weakening Phase 4 recovery and validation (D-09/D-10/D-12).

Purpose: Make the rename safe for existing local users before the Xcode product and bundle identity is switched.
Output: A pure migration transaction, standalone offline proofs, and wired library/appearance/frame compatibility consumers.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
@.planning/phases/04.1-product-identity-experience-polish/04.1-01-SUMMARY.md
@.planning/phases/04.1-product-identity-experience-polish/04.1-CONTEXT.md
@SiriusMac/App/ProductIdentity.swift
@SiriusMac/Library/LibraryStore.swift
@SiriusMac/Skins/SkinSelectionStore.swift
@SiriusMac/Skins/SkinPackageImporter.swift
@SiriusMac/Windows/CompactWindowController.swift

<interfaces>
`LibraryStore.makeDefaultModelContainer()` currently creates `Sirius Mac/Library.store` and performs a semantic legacy import. `SkinSelectionStore.init(applicationSupportDirectory:fileOperations:)` owns `appearance-selection.json`. `ManagedSkinStore.init(applicationSupportDirectory:fileOperations:)` owns the managed `Skins` root. `WindowLifecyclePolicy.frameAutosaveName` and `WindowFrameRestoration` own frame names and validity. Keep these ownership points; route their current/legacy namespaces through the approved identity registry and the new pure migration transaction.
</interfaces>
</context>

## Artifacts this phase produces

| File path | New symbol/artifact | Contract |
|---|---|---|
| `SiriusMac/App/ProductIdentityMigration.swift` | `ProductIdentityMigration`, `ProductIdentityMigrationOperations`, `ProductIdentityMigrationOutcome` | Destination-first, verify-before-mark, idempotent non-secret migration |
| `script/tests/product_identity_migration_tests.swift` | standalone migration test executable | Success, rerun, destination-wins, malformed-source, write/verify failure, and no-secret-hook cases |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and prove the pure non-secret migration transaction</name>
  <precondition>The approved brand artifact exists, has `status: approved`, and all current/legacy storage keys are concrete.</precondition>
  <read_first>
    - .planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
    - SiriusMac/App/ProductIdentity.swift
    - SiriusMac/Library/LibraryStore.swift
    - SiriusMac/Skins/SkinSelectionStore.swift
  </read_first>
  <files>SiriusMac/App/ProductIdentity.swift, SiriusMac/App/ProductIdentityMigration.swift, script/tests/product_identity_migration_tests.swift</files>
  <behavior>
    - A missing destination plus valid legacy semantic data writes and rereads the destination before an atomic completion marker is written.
    - A completed or already-populated destination never reimports or overwrites from legacy state.
    - Invalid input, cancelled work, write failure, or reread mismatch leaves the marker absent and legacy data untouched.
    - The operation surface has no Keychain, credential, token, cookie, session, network, or provider callback.
  </behavior>
  <action>Create a Foundation-only injected migration transaction keyed by ProductIdentity's approved current namespace and fixed legacy non-secret namespace (D-02). Make operations explicit for source existence/read, semantic validation/projection, destination write, destination reread/verification, and atomic marker write. Return a closed outcome for no source, migrated, already complete, destination authoritative, rejected source, and failed destination. Retain the legacy source in every outcome. Add a standalone test executable that fails first, then covers the behavior matrix without importing the app target, SwiftData, AppKit, Security, WebKit, AVFoundation, or SiriusXMClient (D-10/D-12).</action>
  <verify>
    <automated>IDM_TMP=$(mktemp -d /tmp/product-identity-migration.XXXXXX); trap 'rm -rf "$IDM_TMP"' EXIT; xcrun swiftc SiriusMac/App/ProductIdentity.swift SiriusMac/App/ProductIdentityMigration.swift script/tests/product_identity_migration_tests.swift -o "$IDM_TMP/product-identity-migration-tests"; "$IDM_TMP/product-identity-migration-tests"</automated>
  </verify>
  <acceptance_criteria>
    - Every migration outcome is closed and testable; success is impossible before destination reread verification and marker persistence.
    - Reruns and populated destinations cannot duplicate or overwrite user state.
    - No API in the migration file can accept or emit secret/provider material.
  </acceptance_criteria>
  <done>The pure migration transaction passes its complete standalone behavior matrix under D-02/D-10/D-12.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate library, selection, and managed appearances through their existing owners</name>
  <read_first>
    - SiriusMac/App/ProductIdentityMigration.swift
    - SiriusMac/Library/LibraryStore.swift
    - SiriusMac/Skins/SkinSelectionStore.swift
    - SiriusMac/Skins/SkinPackageImporter.swift
    - SiriusMacTests/LibraryStoreTests.swift
    - SiriusMacTests/SkinPackageImporterTests.swift
  </read_first>
  <files>SiriusMac/Library/LibraryStore.swift, SiriusMac/Skins/SkinSelectionStore.swift, SiriusMac/Skins/SkinPackageImporter.swift, SiriusMacTests/LibraryStoreTests.swift, SiriusMacTests/SkinPackageImporterTests.swift</files>
  <action>Route the library store, selection JSON, and managed skin root to the approved Application Support directory (D-02). For library rows, reuse semantic model reads and writes; never move or adopt opaque store files. For selection, accept only the existing exact keys/schema and require the referenced imported package to exist as a validated app-owned managed copy before preserving an imported selection. For managed packages, copy only files that already passed the Phase 4 validator/catalog contract, verify the destination projection, and leave source packages untouched on any failure. Destination data wins, completion markers are per storage kind, and failure collapses to existing in-memory/Native behavior rather than an empty invented state (D-09). Extend the existing tests with injected roots and file operations; tests must assert idempotence, destination precedence, failure preservation, Native recovery, and absence of any Keychain migration hook.</action>
  <verify>
    <automated>IDM_TMP=$(mktemp -d /tmp/product-identity-migration.XXXXXX); trap 'rm -rf "$IDM_TMP"' EXIT; xcrun swiftc SiriusMac/App/ProductIdentity.swift SiriusMac/App/ProductIdentityMigration.swift script/tests/product_identity_migration_tests.swift -o "$IDM_TMP/product-identity-migration-tests"; "$IDM_TMP/product-identity-migration-tests"; bash script/test_product_identity_contract.sh --migration</automated>
  </verify>
  <acceptance_criteria>
    - Valid legacy library records, selected appearance metadata, and validated managed packages are written once beneath the approved root.
    - Existing destination state is authoritative; malformed/missing packages produce Native recovery without deleting either root.
    - The Xcode app tests contain deterministic injected-root cases and remain compile-only until the repository safety hold is separately lifted.
  </acceptance_criteria>
  <done>All durable non-secret app storage owners preserve their semantic state under the approved namespace and retain Phase 4 safety.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate only valid window frames and lock the secret boundary</name>
  <read_first>
    - SiriusMac/App/ProductIdentity.swift
    - SiriusMac/App/ProductIdentityMigration.swift
    - SiriusMac/Windows/CompactWindowController.swift
    - SiriusMacTests/ListeningSessionControllerTests.swift
    - SiriusMac/Security/KeychainCredentialStore.swift
  </read_first>
  <files>SiriusMac/Windows/CompactWindowController.swift, SiriusMacTests/ListeningSessionControllerTests.swift, SiriusMacTests/LibraryStoreTests.swift, SiriusMacTests/SkinPackageImporterTests.swift</files>
  <action>Replace frame write names with the approved product-owned values while retaining the three legacy names only for read compatibility (D-02). Before normal AppKit attachment, copy a legacy frame string only when the current name is absent and `WindowFrameRestoration` accepts the frame for a visible screen. For the compact role preserve only the safe origin and always reapply 400×288; invalid/offscreen legacy frames are ignored and never marked as migrated values (D-09). Add source-level and compile-only app-test assertions that the Keychain service fallback/account and `SiriusXMClient` package/module/diagnostic boundary remain unchanged and that the migration types cannot call Security APIs (D-02/D-10).</action>
  <verify>
    <automated>bash script/test_product_identity_contract.sh --migration</automated>
  </verify>
  <acceptance_criteria>
    - Authentication, compact, and library frames use new write names and import at most one valid legacy value.
    - Compact restoration always yields 400×288 content and a safe visible origin.
    - The retained legacy Keychain service is allowlisted solely as a compatibility constant and no secret operation was added.
  </acceptance_criteria>
  <done>Non-secret window state migrates safely while the credential/session boundary is demonstrably untouched.</done>
</task>

</tasks>

<verification>
- Run the standalone migration executable with an isolated temporary output.
- Run `bash script/test_product_identity_contract.sh --migration`.
- Confirm app-hosted migration tests are present and compile later through Plan 06; do not execute them in this plan.
</verification>

<success_criteria>
- D-02/D-10: All four non-secret state families have explicit tested compatibility behavior and concrete approved destinations.
- D-09: Fixed geometry, validated packages, exact selection schema, and Native recovery remain invariant.
- D-12: Every executed check is Foundation/source-only; no app, app-hosted test, UI automation, or live service starts.
- Keychain and SiriusXMClient identities remain byte-for-byte unchanged except for explanatory allowlist assertions.
</success_criteria>

<output>
Create `.planning/phases/04.1-product-identity-experience-polish/04.1-02-SUMMARY.md` when done.
</output>
