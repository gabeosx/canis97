---
phase: 04.1-product-identity-experience-polish
plan: "06"
type: execute
wave: 4
depends_on: ["04.1-05"]
files_modified:
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
  - SiriusMacTests/SkinPackageImporterTests.swift
  - SiriusMacTests/SystemMediaControllerTests.swift
  - SiriusMacTests/WebAuthenticationBridgeTests.swift
  - SiriusMacUITests/SiriusMacUITests.swift
  - script/build_and_run.sh
  - script/native_single_instance_launcher.swift
  - script/live_compatibility_checkpoint.sh
  - script/tests/build_and_run_script_tests.sh
  - script/tests/build_and_run_tests.sh
autonomous: false
requirements: []
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02: Remaining unit/UI tests, exact executable paths, bundle IDs, process labels, environment keys, schemes, launch locks, caches, telemetry predicates, and Phase 05 live-checkpoint inputs use the approved app identity tuple."
    - "D-03: Final stale-name scans distinguish provider-facing SiriusXMClient/subscriber facts and migration/Keychain compatibility from app branding."
    - "D-06/D-10: Offline project/build-product inspection proves one ProductIcon target association, correct built Info.plist/icon values, and no competing icon authority."
    - "D-10/D-12: Source-only launcher tests and one serialized `xcodebuild build-for-testing` lane pass without app launch, app-hosted/UI test execution, or live SiriusXM activity."
    - "D-11/D-12: A human separately authorizes and completes the offline visual/VoiceOver review in a safe environment; autonomous execution never launches the app."
  artifacts:
    - path: "script/build_and_run.sh"
      provides: "Approved exact bundle/executable/scheme/log tuple while preserving source-only and build-only safety modes"
    - path: "SiriusMacUITests/SiriusMacUITests.swift"
      provides: "Approved exact identity assertions retained behind the explicit UI-test safety hold"
    - path: "SiriusMac.xcodeproj/project.pbxproj"
      provides: "Build-only app/test products whose Info.plist and icon are inspected offline"
  key_links:
    - from: "script/build_and_run.sh"
      to: "script/native_single_instance_launcher.swift"
      via: "Exact approved bundle URL, bundle identifier, and executable URL tuple"
      pattern: "APP_BUNDLE_IDENTIFIER"
    - from: "SiriusMacUITests/SiriusMacUITests.swift"
      to: "SiriusMac.xcodeproj/project.pbxproj"
      via: "UI-test bundle/target/executable expectations match the renamed app target"
      pattern: "expectedBundleIdentifier"
    - from: "script/test_product_identity_contract.sh"
      to: "built app Info.plist"
      via: "Built-product mode inspects bundle/product/icon/UTType values without executing the bundle"
      pattern: "--built-product"
  prohibitions:
    - "No app or test-host launch by the executor; no xcodebuild test, test-without-building, xctest, XCUIApplication.launch, UI-test runner, build_and_run.sh execution, or live SiriusXM request."
    - "No signing, notarization, stapling, publishing, checksums, Homebrew, updater, support bundle, DocC, App Store, song favorite, or playlist-service work."
    - "No legal-clearance or service-authorization conclusion and no Keychain/provider-client cosmetic migration."
---

<objective>
Close the identity migration with exact test/script consumers, objective offline build evidence, and the separately authorized human visual/accessibility review (D-02/D-03/D-06/D-10 through D-12).

Purpose: Prove the approved identity is coherent from source through the built app bundle without violating the repository launch hold, then make the required visual/VoiceOver decision explicit.
Output: Updated remaining test and launcher consumers, passing source-only launcher suites, a serialized build-for-testing product inspection, and human review approval or a precise issue list.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
@.planning/phases/04.1-product-identity-experience-polish/04.1-05-SUMMARY.md
@.planning/phases/04.1-product-identity-experience-polish/04.1-CONTEXT.md
@AGENTS.md
@script/build_and_run.sh
@script/native_single_instance_launcher.swift
@script/tests/build_and_run_script_tests.sh
@script/tests/build_and_run_tests.sh
@SiriusMacUITests/SiriusMacUITests.swift

<interfaces>
The approved decision keys `schemeName`, `displayName`, `executableName`, `appBundleIdentifier`, `moduleName`, `unitTestTargetName`, `uiTestTargetName`, `environmentPrefix`, `scriptPrefix`, `appLogSubsystem`, and `filesystemSlug` are the only app-identity inputs. The client package/module/subsystem and legacy Keychain service remain explicit allowlist entries. `script/build_and_run.sh` retains a source-only path and a build-only path; neither source-test command may call its run/debug/log/telemetry/verify launch cases.
</interfaces>
</context>

## Artifacts this phase produces

No new source files or public symbols are created. This plan completes the identity of existing test/launcher consumers and produces transient isolated DerivedData for review; the executor records its exact path in the checkpoint handoff and the plan summary.

<tasks>

<task type="auto">
  <name>Task 1: Finish unit/UI test identity and launch-safety assertions</name>
  <precondition>The approved Xcode module/target/bundle/scheme graph from Plan 05 passes cutover-source validation.</precondition>
  <read_first>
    - .planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
    - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
    - SiriusMacTests/SkinPackageImporterTests.swift
    - SiriusMacTests/SystemMediaControllerTests.swift
    - SiriusMacTests/WebAuthenticationBridgeTests.swift
    - SiriusMacUITests/SiriusMacUITests.swift
  </read_first>
  <files>SiriusMacTests/SelectedAuthenticationCompositionTests.swift, SiriusMacTests/SkinPackageImporterTests.swift, SiriusMacTests/SystemMediaControllerTests.swift, SiriusMacTests/WebAuthenticationBridgeTests.swift, SiriusMacUITests/SiriusMacUITests.swift</files>
  <action>Move the final four unit-test sources to the approved app-module import and approved app-owned identifiers, preserving exact Keychain/provider fixtures as named compatibility cases (D-02/D-03). Rename the UI-test class/safety messages and update its expected bundle, executable, window title, environment key, and exact-built-binary checks to the approved tuple. Keep the preflight refusal for any existing matching process, one-process postlaunch identity check, exact executable URL comparison, and guarded teardown. Do not execute XCUIApplication or any app/test host; the source remains compiled only by build-for-testing (D-10/D-12).</action>
  <verify>
    <automated>bash script/test_product_identity_contract.sh --final-source</automated>
  </verify>
  <acceptance_criteria>
    - Every unit-test source imports the approved app module and every app-owned fixture uses the approved identity or an explicit legacy compatibility case.
    - UI-test exact bundle/executable/window/environment values match the approved tuple.
    - All existing single-instance refusal/identity/teardown guards remain present.
    - No UI/app test is executed.
  </acceptance_criteria>
  <done>The complete unit/UI test source graph is identity-correct and retains the repository's exact-process safety contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update launcher consumers and produce objective offline build evidence</name>
  <precondition>Xcode 26.6 is available, no other xcodebuild process is running, and the existing ZIPFoundation dependency can resolve without changing package versions.</precondition>
  <read_first>
    - .planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
    - script/build_and_run.sh
    - script/native_single_instance_launcher.swift
    - script/live_compatibility_checkpoint.sh
    - script/tests/build_and_run_script_tests.sh
    - script/tests/build_and_run_tests.sh
    - AGENTS.md
  </read_first>
  <files>script/build_and_run.sh, script/native_single_instance_launcher.swift, script/live_compatibility_checkpoint.sh, script/tests/build_and_run_script_tests.sh, script/tests/build_and_run_tests.sh</files>
  <reversibility rating="costly">Exact launch/process/script identities must change with the app product or later build/release checks can inspect or terminate the wrong executable.</reversibility>
  <action>Replace build/launch/test/live-checkpoint app target, scheme, bundle, executable, process label, telemetry subsystem, environment prefix, cache/scratch basename, and lock basename with the approved concrete tuple (D-02). Keep the provider-client telemetry predicate unchanged and keep `live_compatibility_checkpoint.sh` as a Phase 05 input only; do not run it. Update fake-hook/source-only tests so they synthesize the approved paths and prove lock ownership, zero-before-launch, exact-binary identity, and build-only nonlaunch behavior without invoking the real app. Run those two source-only suites, then run exactly one serialized `xcodebuild build-for-testing` with a freshly created isolated DerivedData directory, `CODE_SIGNING_ALLOWED=NO`, the approved app scheme, and macOS destination. Do not select or execute tests. Inspect build settings and the built Info.plist/resource bundle to prove approved product/executable/module/bundle/UTType values, `ProductIcon` as the configured icon basename, one compiled icon authority, and intact app/unit/UI products (D-06/D-10/D-12). Print and retain the exact isolated built-app path for the following human checkpoint.</action>
  <verify>
    <automated>bash script/test_product_identity_contract.sh --final-source; bash script/tests/build_and_run_script_tests.sh; bash script/tests/build_and_run_tests.sh; BRAND_SCHEME=$(/usr/bin/plutil -extract selectedIdentity.schemeName raw .planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json); BRAND_DERIVED=$(mktemp -d /tmp/product-identity-build.XXXXXX); xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme "$BRAND_SCHEME" -destination 'platform=macOS' -derivedDataPath "$BRAND_DERIVED" CODE_SIGNING_ALLOWED=NO; IDENTITY_DERIVED_DATA_PATH="$BRAND_DERIVED" bash script/test_product_identity_contract.sh --built-product; printf 'human-review-derived-data: %s\n' "$BRAND_DERIVED"</automated>
  </verify>
  <acceptance_criteria>
    - Both source-only launcher suites pass using fake hooks and no real app process.
    - One build-for-testing lane produces app, unit-test, and UI-test products under an isolated path without executing them.
    - Built Info.plist/resources match the approved identity/UTType/icon tuple and contain one icon authority.
    - The exact built-app/DerivedData path is handed to the checkpoint; `live_compatibility_checkpoint.sh` was updated but not run.
  </acceptance_criteria>
  <done>All autonomous identity evidence is green under D-12 and the exact offline review build is ready for a separately authorized human inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Authorize and complete the visual, keyboard, and VoiceOver review</name>
  <read_first>
    - .planning/phases/04.1-product-identity-experience-polish/04.1-BRAND-DECISION.json
    - .planning/phases/04.1-product-identity-experience-polish/04.1-CONTEXT.md
    - AGENTS.md
    - SiriusMac/Testing/UITestHarness.swift
  </read_first>
  <files>SiriusMac/Testing/UITestHarness.swift, SiriusMac/Assets/ProductIcon.icon, SiriusMac/App/AboutProductView.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMac/Catalog/ListeningView.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Skins/SkinManagementView.swift</files>
  <action>Pause after all automated evidence is green. Ask the user to establish a safe review environment and separately authorize one serialized manual launch of the exact build-only product from Task 2 in its DEBUG offline presentation mode. The executor must not launch the app, start an app/test host, run UI automation, or contact SiriusXM. If authorization is withheld, keep the checkpoint blocked rather than substituting screenshots, source inspection, or an autonomous launch (D-11/D-12).</action>
  <what-built>The complete approved product identity, one Icon Composer source, coherent native/bundled presentation, safe non-secret migration, and credential-free offline presentation matrix.</what-built>
  <how-to-verify>
    1. Confirm there is no existing matching app/test process and explicitly authorize this manual safety review; use only the exact isolated build path printed by Task 2 and the approved DEBUG offline-presentation environment key.
    2. In Icon Composer, inspect ProductIcon at default, dark, and monochrome appearances and small sizes. Confirm one recognizable text-independent core, system masking, clean layer separation, and no provider/third-party imagery.
    3. Without launching, inspect the built app in Finder and Spotlight plus its Info.plist/About metadata. After the authorized offline launch, inspect its Dock and About presentation. Confirm the approved name/icon/bundle presentation and clear non-affiliation statement.
    4. Use the offline state selector to inspect every authentication state, compact empty/populated/pending/error state, every library collection plus empty/error state, and appearance management. Confirm copy is truthful and the app name dominates factual SiriusXM account/service wording.
    5. Inspect Native, Signal Glow, and Tape Deck in light/dark contexts. Confirm 400×288 content, readable metadata/status/error/focus states, stable native control geometry, minimum hit targets, and package-independent Native recovery.
    6. Navigate keyboard-only through sign-in, compact controls, menus, library tabs/rows/search, appearance management, and Native recovery. Confirm focus order/visibility and all documented shortcuts.
    7. With VoiceOver, confirm product/window labels, controls, values, selected/playing/favorite state, announcements, reading order, and full long-text values. Confirm appearances do not alter the accessibility tree.
    8. Enable Reduce Motion and confirm state transitions/reveal behavior remain understandable without required animation.
    9. Quit the reviewed offline process and confirm no app/test host remains. Record approval or list each failing surface/state/appearance precisely.
  </how-to-verify>
  <verify>
    <automated>bash script/test_product_identity_contract.sh --final-source</automated>
    <human-check>Human confirms the nine-step review against the exact build-only product after separate safety authorization.</human-check>
  </verify>
  <acceptance_criteria>
    - The user explicitly authorized the launch safety review before any app process started.
    - All D-11 icon/product/surface/appearance/contrast/keyboard/VoiceOver/focus/hit-target/Reduce-Motion checks pass, or exact issues are returned for correction.
    - The review used only the offline presentation composition and caused no Keychain, WebKit, media, provider, or live-network activity.
    - No reviewed app/test process remains afterward.
  </acceptance_criteria>
  <done>The human approves the complete visual/accessibility review matrix and confirms clean process shutdown; otherwise Phase 04.1 remains open with a precise issue list.</done>
  <resume-signal>Reply `safety authorized and approved` after all nine checks pass, `not authorized` to keep the phase blocked, or provide the exact issues found.</resume-signal>
</task>

</tasks>

<verification>
- Source-only identity, migration, presentation, appearance, cutover, launcher, and built-product checks all pass.
- Exactly one serialized build-for-testing command succeeds and no app/test binary executes.
- Human review is separately authorized and approved against the complete D-11 matrix.
- No Keychain, WebKit, AVFoundation/live media, or SiriusXM network activity occurs in validation.
</verification>

<success_criteria>
- D-02/D-03: Every app/test/script/runtime identity consumer is coherent, provider facts are narrow, and legacy compatibility is explicit.
- D-06/D-10: The built product objectively contains the approved bundle/UTType/icon identity with one authoritative icon source.
- D-11: Authentication, compact, library, appearance, icon, keyboard, VoiceOver, focus, contrast, hit targets, and Reduce Motion receive an approved deliberate review.
- D-12: Autonomous execution remains launch-free and the only launch is separately human-authorized, offline, serialized, and cleaned up.
- Phase 04.2 and Phase 05 boundaries remain untouched.
</success_criteria>

<output>
Create `.planning/phases/04.1-product-identity-experience-polish/04.1-06-SUMMARY.md` when done.
</output>
