---
phase: 04.2-expressive-skin-architecture-bundled-themes
plan: "03"
type: execute
wave: 3
depends_on: ["04.2-02"]
files_modified:
  - SiriusMac/Player/CompactPlayerPresentation.swift
  - SiriusMac/Player/CompactPlayerView.swift
  - SiriusMac/Testing/UITestHarness.swift
  - SiriusMac/Skins/SkinManagementView.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMacTests/CompactPlayerPresentationTests.swift
  - SiriusMacTests/AccessibilityContractTests.swift
  - script/tests/ExpressiveSkinPhaseContract.swift
autonomous: true
requirements: []
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Populated, empty, loading, playback-error, and appearance-recovery states render app-authored content coherently in every validated expressive layout while the semantic source order stays fixed (D-05, D-19)."
    - "Long Unicode channel, program, artist, appearance-name, and recovery values keep their complete app-authored Help/VoiceOver values while visual lines and ellipsis remain bounded by the approved layout capacity (D-03, D-05, D-06)."
    - "Keyboard focus, VoiceOver order/labels/values, app-owned focus rings, 32-point targets, disabled-state truth, and Reduce Motion behavior remain consistent across Native, legacy, bundled, and imported appearances (D-05, D-06, D-07, D-19)."
    - "The appearance manager presents Native, Bundled, and Imported sections with exact app-owned selection/import/removal/recovery copy, atomic busy/error states, native scrolling, and full accessible names (D-04, D-17, D-19)."
    - "Native recovery stays available in the Player menu and compact overflow route and restores Native chrome/400 x 288 immediately after validation, decode, render, restoration, or fit failure (D-17)."
    - "Automated phase verification remains source-only, Foundation-only, or build-for-testing; deliberate visual/accessibility UAT remains explicitly deferred to the separately gated Plan 04.2-04 (D-19)."
  artifacts:
    - path: "SiriusMac/Player/CompactPlayerView.swift"
      provides: "All-state semantic slot registry, long-text constraints, motion policy, and recovery copy across expressive layouts"
    - path: "SiriusMac/Testing/UITestHarness.swift"
      provides: "Credential-free cartesian review controls for Native, a deterministic schema-version-1 fixture, both schema-version-2 bundles, all three schema-version-3 bundles, and compact populated/empty/loading/error/long-text/appearance-failure states"
    - path: "SiriusMac/Skins/SkinManagementView.swift"
      provides: "Exact native appearance-management states, copy, focus, scrolling, atomic import/removal behavior, and recovery affordances"
    - path: "SiriusMacTests/AccessibilityContractTests.swift"
      provides: "Static app-owned accessibility, focus, action, copy, and Native recovery authority contracts"
    - path: "script/tests/ExpressiveSkinPhaseContract.swift"
      provides: "Launch-free whole-phase source/resource contract audit"
  key_links:
    - from: "CompactPlayerPresentation"
      to: "CompactPlayerView semantic slots"
      via: "closed semantic values projected into fixed app-owned views before validated visual placement"
      pattern: "CompactPlayerPresentation|CompactSkinSemanticSlot"
    - from: "CompactPlayerView"
      to: "SkinAppearanceController.restoreNativeAppearance"
      via: "render/window recovery callback independent of failed package content"
      pattern: "onAppearanceRecovery|restoreNativeAppearance"
    - from: "SkinManagementView"
      to: "SkinImportCoordinator and SkinAppearanceController"
      via: "atomic import/select/remove tasks and current semantic reference only"
      pattern: "importAndSelect|removeImportedSkin|select"
    - from: "Player menu and compact overflow"
      to: "restoreNativeAppearance"
      via: "unconditional app-owned Native recovery routes"
      pattern: "Use Native Appearance|restoreNativeAppearance"
  prohibitions:
    - statement: "Appearance data may never supply subscriber-facing semantic text, accessibility strings, focus order, enabled state, action names, recovery choices, menus, or announcements."
      status: unverified
      flagged: true
    - statement: "A rejected, cancelled, stale, partially validated, or removal-failed package may never replace the previous confirmed appearance or render a partial catalog row/state."
      status: unverified
      flagged: true
---

<objective>
Harden the expressive renderer and management surfaces across every documented UI state, accessibility contract, recovery route, and long-content boundary, then compile and audit the integrated phase without launching the app.

Purpose: Turn the schema and three themes into a dependable subscriber experience while preserving app-owned semantics, atomic management, and the repository safety hold.
Output: Complete state/copy/accessibility behavior, hardened appearance management, launch-free phase contract, and final compile-only integration evidence ready for separately gated UAT.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-RESEARCH.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-01-SUMMARY.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-02-SUMMARY.md
@SiriusMac/Player/CompactPlayerPresentation.swift
@SiriusMac/Player/CompactPlayerView.swift
@SiriusMac/Testing/UITestHarness.swift
@SiriusMac/Skins/SkinManagementView.swift
@SiriusMac/SiriusMacApp.swift

<interfaces>
- `CompactPlayerPresentation.project` emits confirmed/empty/pending/error semantic values and `retainingConfirmedContent(from:)` owns prior-confirmed retention. The appearance layer does not change playback truth.
- `CompactPlayerView` owns all compact control identifiers, labels, Help text, accessibility values/sort priorities, action dispatch, and recovery callback; schema-version 3 only supplies a validated visual plan.
- `OfflineReviewHarness` is a DEBUG-only, credential-free, in-memory composition. Extend its `OfflineReviewSurface` and add an orthogonal appearance fixture selector so human review can exercise every required state against Native, one deterministic schema-version-1 appearance, the existing schema-version-2 bundles, and all three schema-version-3 bundles without provider or durable-store access.
- `SkinManagementView` owns Native/Bundled/Imported sections plus import/removal task state; `SkinAppearanceController` and `SkinImportCoordinator` own durable selection and package transactions.
- `ListeningCommands` owns the Player menu and direct `Use Native Appearance` route. `CompactListeningSlice` owns the view/window recovery callback.
- Plans 04.2-01/02 supply three actual expressive appearances, strict package validation, fixed semantic registry, finite window policies, and complete Native fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04.2-03-01: Complete compact populated, empty, loading, error, long-text, focus, and motion states</name>
  <reversibility rating="reversible">State presentation and private SwiftUI layout helpers remain behind unchanged semantic presentation/action and schema contracts.</reversibility>
  <read_first>
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md` — implement D-03, D-05, D-06, D-07, D-17, and D-19 across every compact state.
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md` — implement E1, E2, E5, and E6 explicit resolutions plus exact Copywriting, typography, line limits, focus, target, motion, and recovery rules.
    - `SiriusMac/Player/CompactPlayerPresentation.swift` — modify only app-owned empty copy/projection values required by the approved contract; retain semantic state and confirmed-content ownership.
    - `SiriusMac/Player/CompactPlayerView.swift` — modify every state in the validated semantic slot registry while retaining source order and appearance-only placement.
    - `SiriusMac/Testing/UITestHarness.swift` — modify the existing DEBUG-only `OfflineReviewHarness`, `OfflineReviewSurface`, and compact fixture projection so the full deterministic state/theme matrix is reachable without credentials, provider calls, playback, or durable state.
    - `SiriusMacTests/CompactPlayerPresentationTests.swift` — modify compiled contracts for every theme/state/long-text/motion/recovery combination.
    - `SiriusMacTests/AccessibilityContractTests.swift` — modify static accessibility/focus/identifier/target/source-order authority checks.
  </read_first>
  <files>SiriusMac/Player/CompactPlayerPresentation.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Testing/UITestHarness.swift, SiriusMacTests/CompactPlayerPresentationTests.swift, SiriusMacTests/AccessibilityContractTests.swift</files>
  <behavior>
    - Empty presentation uses `Nothing Playing` and `Choose a channel in the Library to start listening.`, keeps Show Library available, and exposes no stale media content.
    - Pending playback uses `Loading playback`, may retain confirmed identity/artwork, and disables unavailable transport without changing labels or focus order.
    - Playback failure uses `Playback couldn’t start.` plus exactly the mapped Try Again, Sign In Again, or Refresh Library action; appearance failure uses `This appearance is unavailable. Native appearance has been restored.` and direct Native recovery.
    - A confirmed channel renders all eight semantic slots; channel identity is one visual line, program and artist are at most two visual lines each, and tail ellipsis retains complete Help and VoiceOver values.
    - Required controls keep non-overlapping 32 x 32-or-larger frames, visible 4-point focus clearance, fixed identifiers/labels/order, accurate disabled state, and app-owned overflow/recovery reachability.
    - Theme changes use at most one 150ms opacity crossfade; Reduce Motion makes theme/size changes immediate and every state/focus/recovery cue remains understandable without motion.
    - Offline review exposes Native, a deterministic schema-version-1 appearance, Signal Glow, Tape Deck, Pixel Desk, Pocket Disc, and Aqua Vista independently from populated, empty, pending, playback-error, long-Unicode, and appearance-failure surfaces using synthetic local values only.
  </behavior>
  <action>Start with compiled table-driven contracts across Native, schema versions 1 and 2, Pixel Desk, Pocket Disc, and Aqua Vista for populated, empty, pending, playback-error, and appearance-recovery presentations plus longest allowed Unicode values. Per D-05/D-06 and the UI-SPEC, make the eight semantic slot views structurally stable across states: present exact app-authored copy, keep the same identifiers/labels/values/source order, render all confirmed slots, clear stale media for a truly empty state, retain only previously confirmed identity/artwork during pending replacement, disable unavailable actions truthfully, and expose only the mapped recovery action. Use one-line channel identity and two-line program/artist limits with tail truncation, complete `.help`, and complete accessibility label/value. Preserve minimum targets and 4-point focus clearance in every approved size; render the app-owned focus/state accent above decoration. Per D-07, apply only an opacity transition capped at 150ms when Reduce Motion is false, and no animation when true; never animate position, size interpolation, scale, bounce, parallax, playback truth, focus, or recovery. Per D-17, change the recovery message to the exact Copywriting Contract, publish Native through the existing callback, and ensure Native chrome/400 x 288 policy wins as one complete state. Extend the DEBUG-only offline review composition with `OfflineReviewAppearanceFixture` cases for Native, a deterministic asset-free schema-version-1 manifest validated in memory through the production validator, Signal Glow, Tape Deck, Pixel Desk, Pocket Disc, and Aqua Vista plus `OfflineReviewSurface` cases for long Unicode and appearance failure; present appearance and state as orthogonal selectors so every combination is reachable, use only in-memory catalog/persistence and synthetic presentation values, and keep the launch environment incapable of provider, authentication, playback, or durable storage access. Add static source contracts that every decoration/drag layer is accessibility-hidden and noninteractive, that the documented control identifiers and sort order remain present, and that the offline review matrix contains every expected appearance and state. Compile tests only; runtime human evaluation remains deferred to Plan 04.2-04 (D-19).</action>
  <acceptance_criteria>
    - Every compact state uses the exact approved app-owned copy and mapped action in every appearance layout.
    - Long Unicode content respects one-line/two-line visual limits while Help and VoiceOver retain the complete app-authored values.
    - Semantic source/focus/VoiceOver order, identifiers, enabled-state truth, 32-point targets, and focus clearance remain stable across Native, one schema-version-1 fixture, both schema-version-2 bundles, and all three schema-version-3 bundles.
    - Appearance/render/window failure immediately presents complete Native state and exact recovery truth.
    - Motion is a maximum 150ms opacity-only treatment and is absent under Reduce Motion.
    - `OfflineReviewHarness` exposes the complete seven-appearance by six-state matrix with deterministic local values and no provider, credential, playback, or durable-store dependency.
  </acceptance_criteria>
  <verify>
    <automated>zsh -lc 'dd_path=$(mktemp -d /tmp/canis97-04.2-03-states.XXXXXX); xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme Canis97 -destination "platform=macOS" -derivedDataPath "$dd_path" CODE_SIGNING_ALLOWED=NO'</automated>
  </verify>
  <done>Every compact state, long-content boundary, focus/accessibility contract, motion setting, and recovery path is complete across the appearance family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04.2-03-02: Complete appearance management and lock the integrated phase with a launch-free contract</name>
  <reversibility rating="reversible">Management copy/layout and the private launch-free audit remain behind existing selection/import/removal transaction APIs.</reversibility>
  <read_first>
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md` — implement D-04, D-05, D-17, D-18, and D-19 without broadening package or management authority.
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md` — implement E3/E4/E6 explicit resolutions and exact appearance-list/import/removal/error copy.
    - `SiriusMac/Skins/SkinManagementView.swift` — modify Native/Bundled/Imported sections, empty/import/removal/error states, focus, scrolling, long names, and buttons.
    - `SiriusMac/SiriusMacApp.swift` — modify only app-owned Appearance menu/Native recovery copy and route needed by the UI contract.
    - `SiriusMacTests/AccessibilityContractTests.swift` — modify static management/menu/focus/copy/accessibility authority checks.
    - `script/tests/ExpressiveSkinPhaseContract.swift` — create this modified/new launch-free source/resource audit across all phase artifacts.
    - `SiriusMac/Skins/SkinAppearance.swift` — read the stable controller/catalog interfaces from prior plans; the task does not modify them.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — read the stable atomic import/remove interfaces and package budgets; the task does not modify them.
  </read_first>
  <files>SiriusMac/Skins/SkinManagementView.swift, SiriusMac/SiriusMacApp.swift, SiriusMacTests/AccessibilityContractTests.swift, script/tests/ExpressiveSkinPhaseContract.swift</files>
  <behavior>
    - The native List always shows separate Native and Bundled sections; Imported shows `No imported appearances yet.` followed by `Import a local .siriusskin package to add one.` when empty.
    - The primary local package action is `Select Appearance`; zero, one, and many imported rows remain individually selectable/removable, scrollable, keyboard reachable, and VoiceOver reachable with selected state visible.
    - During import, competing import/removal actions are disabled, a labelled progress indicator is visible, and Cancel Import remains available.
    - Rejection, unsupported schema, cancellation, selection failure, and removal failure keep the previous confirmed appearance and use app-owned error copy; catalog/UI never show a partial package.
    - Display names remain validator-capped at 64 characters, tail-ellipsize visually, and retain complete accessible row/removal names; confirmation uses Remove Appearance?, Remove {name} from this Mac?, Remove {name}, and Keep {name}.
    - Player menu and compact overflow keep app-owned Native recovery reachable regardless of selected layout or package failure.
  </behavior>
  <action>Start with static contract failures for the complete E3/E4/E6 resolutions. In `SkinManagementView`, use the exact UI-SPEC copy and section behavior: Native/Bundled/Imported native List sections, the two-line Imported empty message, `Select Appearance`, native scroll behavior, app-owned selected indicator, 64-character visual ellipsis with complete accessible labels, labelled import progress, Cancel Import, competing-action disablement, and exact removal confirmation buttons `Remove {name}` and `Keep {name}`. Continue routing selection/import/removal only through `SkinAppearanceController`/`SkinImportCoordinator`; preserve durable-save-first selection, serialized removal, idempotent absent-package reconciliation, and previous valid state for every failure (D-04/D-18). Consolidate error presentation around the exact general import/selection message `This appearance couldn’t be used. Choose another package or select Native.` while retaining semantic internal classifications; never display raw paths, package payloads, or validation internals. In `SiriusMacApp`, retain `Use Native Appearance` as an unconditional Player-menu action and ensure the compact app-owned overflow exposes the same recovery route without a package lookup (D-17). Create `ExpressiveSkinPhaseContract.swift` to inspect actual source/resources and assert exact theme set/tokens/sizes, schema versions 1/2 compatibility markers, eight-slot/source-order/control identifiers, app-owned copy, 32-point/focus/motion constants, Native recovery routes, package budget/accessor linkage, zero font binaries, and current safety-prohibited command absence from executable scripts. This audit is evidence only and does not replace production validation or launch the app. Per D-19, record human visual/accessibility checks as deferred to Plan 04.2-04 rather than claiming automated visual proof.</action>
  <acceptance_criteria>
    - Native, Bundled, and Imported sections, zero/one/many rows, selected state, import progress/cancel, errors, removal confirmation, and long names follow the exact UI contract with native keyboard/VoiceOver reachability.
    - Import, selection, and removal remain atomic; failure leaves the prior valid appearance and no partial row/state.
    - Both app-owned Native recovery routes remain reachable and invoke `restoreNativeAppearance()` directly.
    - The launch-free phase contract passes against actual source/resources and detects missing themes, semantic slots/identifiers, recovery routes, package safety links, exact copy, or prohibited font/resource authority.
    - Automated verification opens no app, test host, UI runner, live provider route, browser, or network connection.
  </acceptance_criteria>
  <verify>
    <automated>swift script/tests/ExpressiveSkinPhaseContract.swift SiriusMac SiriusMacTests SiriusMac.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>Appearance management and the integrated expressive-skin source/build contract are complete and ready for separately authorized human UAT.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols / contents |
|---|---|
| `SiriusMac/Player/CompactPlayerView.swift` | Private stable semantic-slot state helpers and app-owned opacity/Reduce Motion treatment; no new public type |
| `SiriusMac/Player/CompactPlayerPresentation.swift` | Exact approved empty-state body field/value if needed by the stable semantic projection; no appearance authority |
| `SiriusMac/Testing/UITestHarness.swift` | New DEBUG-only `OfflineReviewAppearanceFixture` including `legacySchema1`; new `OfflineReviewSurface.compactLongText` and `.compactAppearanceFailure` cases; orthogonal appearance/state picker wiring, in-memory schema-version-1 validation, and synthetic fixture projections |
| `SiriusMac/Skins/SkinManagementView.swift` | Exact E3/E4 management states/copy and private row/confirmation presentation helpers; no new public authority |
| `SiriusMacTests/CompactPlayerPresentationTests.swift` | New appearance-family state matrix, long Unicode, target/focus, motion, and Native recovery test methods |
| `SiriusMacTests/AccessibilityContractTests.swift` | New static source-order, identifier, Help/VoiceOver, management, menu/overflow recovery, and decoration-isolation test methods |
| `script/tests/ExpressiveSkinPhaseContract.swift` | New `ExpressiveSkinPhaseContract`, source/resource assertions, and standalone launch-free entry point |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Appearance visuals to semantic state | Layout/chrome may vary, while status, actions, labels, enabled state, focus order, and recovery remain trusted app values. |
| Imported package transaction to management UI | UI receives only validated semantic rows and closed outcomes; raw package content/path/error details stay behind the importer. |
| Failed appearance to Native recovery | Failure must atomically replace custom visuals/window policy and keep recovery reachable through app-owned navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-03-01 | Spoofing | Skin-authored copy/state | high | mitigate | Render only app-authored semantic copy/actions/labels and retain fixed identifiers/order per D-05. |
| T-04.2-03-02 | Elevation of Privilege | Decoration/drag accessibility | high | mitigate | Keep decoration and drag layers noninteractive/accessibility-hidden and app focus treatment above them per D-06/D-11. |
| T-04.2-03-03 | Tampering | Partial import/selection/removal state | high | mitigate | Preserve existing atomic coordinator/controller transactions and expose only fully validated catalog rows per D-03/D-18. |
| T-04.2-03-04 | Denial of Service | Failed appearance/recovery | medium | mitigate | Keep two unconditional app-owned Native routes and apply complete Native appearance/window policy per D-17. |
| T-04.2-03-05 | Information Disclosure | Management error UI | medium | mitigate | Map failures to closed app copy and exclude paths, payloads, assets, validation internals, and provider/session data. |
</threat_model>

<verification>
- Run the launch-free whole-phase source/resource contract and both dedicated manifest audits from Plans 04.2-01/02.
- Run one final serialized `xcodebuild build-for-testing` with a fresh temporary DerivedData path and `CODE_SIGNING_ALLOWED=NO`; inspect resource presence without executing any built product.
- Review only the owned state/management/test files for exact UI copy, fixed source order, 32-point targets, focus clearance, motion policy, atomic management, and direct Native routes.
- Confirm that human visual/accessibility evaluation is explicitly pending Plan 04.2-04 and is not marked passed by source or build evidence.
- Do not run `xcodebuild test`, `test-without-building`, `xctest`, `XCUIApplication.launch()`, UI automation, `script/build_and_run.sh`, any app/test host, or any live SiriusXM operation.
</verification>

<success_criteria>
- Every documented compact and appearance-management state is implemented with exact app-owned copy and native keyboard/VoiceOver behavior.
- Long content, focus, target size, disabled state, Reduce Motion, and recovery contracts remain intact across Native, legacy, bundled, and imported appearances.
- Import/select/remove UI remains atomic and truthful for zero/one/many and all closed failures.
- Launch-free source/resource audits and build-for-testing compile evidence pass; human visual/accessibility UAT remains explicitly gated rather than inferred.
</success_criteria>

<output>
Create `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-03-SUMMARY.md` when done.
</output>
