---
phase: 04.2-expressive-skin-architecture-bundled-themes
plan: "01"
subsystem: compact-player-skins
tags: [swiftui, appkit, declarative-skins, validation, accessibility]
dependency_graph:
  requires: [phase-04-safe-skins]
  provides: [schema-v3-layout-plan, pixel-desk-tracer, finite-compact-window-policy]
  affects: [skin-import, compact-player, compact-window]
tech_stack:
  added: []
  patterns: [closed-schema-validation, app-owned-semantic-rendering, finite-window-geometry]
key_files:
  created:
    - SiriusMac/Skins/Bundled/PixelDesk.json
    - script/tests/ExpressiveSkinContractOfflineTests.swift
  modified:
    - SiriusMac/Skins/SkinAppearance.swift
    - SiriusMac/Skins/SkinPackageImporter.swift
    - SiriusMac/Player/CompactPlayerView.swift
    - SiriusMac/Windows/CompactWindowController.swift
    - SiriusMac/SiriusMacApp.swift
decisions:
  - Schema-v3 layouts compile to a closed, fully validated plan before rendering or window policy.
  - Semantic controls, accessibility, and window behavior remain application-owned; manifests provide only validated visual placement and finite tokens.
  - Compact restoration stores only a versioned finite origin and invalid state immediately routes through Native recovery.
metrics:
  duration: 44m
  completed: 2026-08-28
status: complete
actuals:
  tokens: 72439
  tasks: 3
  commits: 6
---

# Phase 04.2 Plan 01: Pixel Desk Schema-v3 Tracer Summary

Pixel Desk now proves a single production path from a strict schema-v3 manifest to app-owned semantic rendering, a finite 432 × 304 compact window, and safe Native recovery.

## Completed Tasks

1. Added the closed v3 layout, silhouette, size, typography, decoration, slot, focus, drag, and asset contracts; registered Pixel Desk and rendered it through the shared compact-player path.
2. Moved imported-package asset accounting to the compiler-owned version-aware accessor, preserving canonical-path, image, digest, managed-copy, and atomic-selection checks.
3. Added versioned compact-position storage and pure frame geometry; size transitions clamp complete titled frames and invalid restoration invokes the app-owned Native route.

## Verification

- `swift script/tests/ExpressiveSkinContractOfflineTests.swift SiriusMac/Skins/Bundled/PixelDesk.json SiriusMac/Skins/Bundled/SignalGlow.json SiriusMac/Skins/Bundled/TapeDeck.json` — passed.
- `xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme Canis97 ... CODE_SIGNING_ALLOWED=NO` — compiled app, unit-test, and UI-test products without launching them; PixelDesk.json, SignalGlow.json, and TapeDeck.json were present in the built app resources.

## Decisions Made

- V1 and v2 retain the legacy `legacyStack`/`nativeRect` 400 × 288 layout plan.
- V3 package dimensions, hit regions, screens, persistence keys, and window flags never cross the validation boundary as raw values.
- The compact window stays titled, key-capable, nonresizable, and rectangular for events while skins draw their apparent silhouette inside transparent content.

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 1 - Bug] Corrected the new importer contract test's repository lookup.
- **Found during:** Task 3 compile-only verification.
- **Fix:** Resolved the source path locally in `SkinPackageImporterTests` so the test target compiles without relying on another test class's private helper.
- **Commit:** `24fcb03`

## Known Stubs

None.

## Self-Check: PASSED

- All eleven task files exist and all six task commits are present in Git history.
