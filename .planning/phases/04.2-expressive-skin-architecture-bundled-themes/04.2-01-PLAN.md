---
phase: 04.2-expressive-skin-architecture-bundled-themes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SiriusMac/Skins/SkinAppearance.swift
  - SiriusMac/Skins/SkinPackageImporter.swift
  - SiriusMac/Skins/Bundled/PixelDesk.json
  - SiriusMac/Player/CompactPlayerView.swift
  - SiriusMac/Windows/CompactWindowController.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMacTests/CompactPlayerPresentationTests.swift
  - SiriusMacTests/SkinPackageImporterTests.swift
  - SiriusMacTests/ListeningSessionControllerTests.swift
  - script/tests/ExpressiveSkinContractOfflineTests.swift
autonomous: true
requirements: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A subscriber can choose Pixel Desk and the compact player renders its validated layout, apparent silhouette, typography, chrome, semantic controls, and 432 x 304 content size through the production selected-appearance path (D-01, D-04, D-12, D-13)."
    - "Native plus schema-v1 and schema-v2 appearances retain their existing legacyStack/nativeRect rendering and exact 400 x 288 content size (D-01, D-09, D-20)."
    - "A schema-v3 document reaches rendering and window policy only as a fully validated CompactSkinLayoutPlan with all eight semantic slots, bounded decoration references, and at least one valid drag region (D-02, D-03, D-11)."
    - "The app continues to own every action, enabled state, semantic value, keyboard route, focus order, VoiceOver label, playback truth, drag gesture, and NSWindow operation (D-05, D-06, D-08, D-10)."
    - "An unsupported, malformed, hostile, stale, or unrenderable expressive appearance preserves the previous confirmed appearance or publishes package-independent Native recovery through the existing controller (D-03, D-17, D-18)."
  artifacts:
    - path: "SiriusMac/Skins/SkinAppearance.swift"
      provides: "Strict schema-v3 compiler and validated expressive layout contract alongside unchanged schema-v1/v2 branches"
      exports: ["CompactSkinLayoutVariant", "CompactSkinSilhouetteVariant", "CompactSkinSizeVariant", "CompactSkinSemanticSlot", "CompactSkinTypographyToken", "CompactSkinRect", "CompactSkinTypography", "CompactSkinDecorationReferences", "CompactSkinLayoutPlan"]
    - path: "SiriusMac/Player/CompactPlayerView.swift"
      provides: "Stable semantic-source-order renderer driven only by a validated expressive layout plan"
    - path: "SiriusMac/Windows/CompactWindowController.swift"
      provides: "Finite controller-owned content-size transitions, transparent titled chrome, frame clamping, and versioned position restoration"
      exports: ["CompactWindowPositionRecord", "CompactWindowGeometry", "CompactWindowPositionStore"]
    - path: "SiriusMac/Skins/Bundled/PixelDesk.json"
      provides: "Complete schema-v3 Pixel Desk tracer theme"
    - path: "script/tests/ExpressiveSkinContractOfflineTests.swift"
      provides: "Launch-free exact-schema and hostile-geometry contract executable"
  key_links:
    - from: "SkinManifestValidator.validate"
      to: "ValidatedSkinAppearance.layoutPlan"
      via: "exact schema-version dispatch and nested exact-key validation"
      pattern: "validateVersion3|CompactSkinLayoutPlan"
    - from: "ValidatedSkinAppearance.layoutPlan"
      to: "CompactPlayerView"
      via: "fixed semantic registry placed into validated slot frames"
      pattern: "CompactSkinSemanticSlot"
    - from: "ValidatedSkinAppearance.layoutPlan"
      to: "CompactWindowController"
      via: "finite size policy rather than manifest dimensions or raw NSWindow flags"
      pattern: "CompactSkinSizeVariant|updateAppearance"
    - from: "SkinPackageImporter"
      to: "SkinManifestValidator.referencedAssetPaths"
      via: "one version-aware asset list used for exact-file accounting, decode checks, digest, promotion, reload, and renderability"
      pattern: "referencedAssetPaths"
  prohibitions:
    - statement: "Appearance packages may never carry executable behavior, selectors, constraints, networking, remote URLs, arbitrary-file references, raw window flags, or behavior-bearing accessibility data."
      status: unverified
      flagged: true
    - statement: "A package-provided dimension or frame may never reach NSWindow without closed-token resolution and complete layout validation."
      status: unverified
      flagged: true
---

<objective>
Create the production schema-v3 tracer: one complete Pixel Desk appearance travels from the existing catalog and selection controller through strict validation, the native semantic renderer, and the compact-window controller while legacy packages remain unchanged.

Purpose: Prove the phase's hardest constraint first — visual layout and silhouette may transform while behavior, accessibility, window authority, package safety, and Native recovery remain app-owned.
Output: Strict schema-v3 types and compiler, one end-to-end bundled theme, version-aware importer accounting, finite compact-window transitions, and launch-free structural/build evidence.
</objective>

<assumption_delta_decision>
primary_noun: selected appearance
decision: no-change
rationale: Choosing a theme changes the value resolved by the existing SkinAppearanceController selected-appearance aggregate; it does not replace the aggregate's identity, classification model, persistence record, or app composition root. Add a contract test that each theme selection preserves SkinSelectionReference identity/classification and still resolves through the same catalog/controller path.
</assumption_delta_decision>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-RESEARCH.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md
@SiriusMac/Skins/SkinAppearance.swift
@SiriusMac/Skins/SkinPackagePolicy.swift
@SiriusMac/Skins/SkinPackageImporter.swift
@SiriusMac/Player/CompactPlayerPresentation.swift
@SiriusMac/Player/CompactPlayerView.swift
@SiriusMac/Windows/CompactWindowController.swift
@SiriusMac/SiriusMacApp.swift

<interfaces>
- `SkinManifestValidator.validate(_:classification:assetResolver:)` currently performs exact schema-version dispatch for versions 1 and 2 and returns `ValidatedSkinAppearance`; retain that single public entry point and add version 3 behind it.
- `ValidatedSkinAppearance.renderableAppearance(_:)` atomically replaces the complete selected appearance with `.native` when a declared decoration is unusable; extend its decoration enumeration rather than creating partial fallback.
- `SkinAppearanceController.select(_:)` persists before ordinary publication, while `restoreNativeAppearance()` publishes package-independent Native immediately; theme choice continues through these methods.
- `CompactPlayerPresentation` supplies semantic state only. `CompactPlayerAction` is the closed action vocabulary and `transportControlSize` is 32 points.
- `CompactPlayerView` already receives a validated appearance and app-owned callbacks. Its source order, labels, values, identifiers, Help text, and action dispatch remain authoritative.
- `WindowAttachmentView` owns the narrow `CompactWindowController`; `CompactListeningSlice` supplies it as a background after constructing the selected appearance view.
- `SkinPackageImporter.validateCandidate` validates the manifest, computes exact referenced-file accounting, validates every image, promotes, revalidates, and verifies the digest before selection.
</interfaces>
</context>

## Spec-less probe note

Phase 04.2 has no SPEC and no assigned requirement IDs, so the deterministic fallback generated no probe-derived edge predicates. No edge predicate is invented here. The values/safety prohibitions in frontmatter come from locked CONTEXT decisions and remain descriptor-less, flagged, and unverified until execution evidence exists.

<tasks>

<task type="tracer">
  <name>Task 04.2-01-01: Render and size Pixel Desk through one production schema-v3 path</name>
  <reversibility rating="costly">Schema version 3 becomes a public package contract; changing its exact vocabulary later requires compatibility handling across validators, installed packages, resources, renderer, and tests.</reversibility>
  <read_first>
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md` — implement D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-11, D-12, D-13, D-17, and D-20 exactly.
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md` — use the approved token names, Pixel Desk palette, eight-slot source order, 432 x 304 size, 4-point grid, 32-point controls, 4-point focus clearance, and 80 x 20 drag minimum.
    - `SiriusMac/Skins/SkinAppearance.swift` — preserve exact schema-v1/v2 branches, catalog/controller selection authority, finite surface treatment, and complete Native renderability fallback while modifying this file.
    - `SiriusMac/Player/CompactPlayerPresentation.swift` — preserve the semantic presentation/action contracts consumed by the modified view.
    - `SiriusMac/Player/CompactPlayerView.swift` — refactor this modified file into fixed semantic source order with visual placement driven only by a validated plan.
    - `SiriusMac/Windows/CompactWindowController.swift` — extend this modified narrow AppKit seam for the finite appearance size and transparent titled surface.
    - `SiriusMac/Skins/Bundled/PixelDesk.json` — create this modified/new resource as the first complete schema-v3 contract consumer.
    - `SiriusMac.xcodeproj/project.pbxproj` — register the new JSON resource in the app bundle without changing target identities, dependencies, package products, or test-host wiring.
  </read_first>
  <files>SiriusMac/Skins/SkinAppearance.swift, SiriusMac/Skins/Bundled/PixelDesk.json, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Windows/CompactWindowController.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <action>Per D-01/D-02/D-03/D-20, add an exact schemaVersion 3 decoder branch with exact top-level and nested key sets. Define the closed enums `CompactSkinLayoutVariant` (`legacyStack`, `desktopUtility`, `discConsole`, `aquaPod`), `CompactSkinSilhouetteVariant` (`nativeRect`, `pixelNotched`, `discPod`, `bubbleCapsule`), `CompactSkinSizeVariant` (`legacy400x288`, `desktop432x304`, `console384x320`, `capsule448x304`), `CompactSkinSemanticSlot` (the eight UI-SPEC slots), and `CompactSkinTypographyToken` (`systemDefault`, `systemRounded`, `systemMonospaced`). Define integer `CompactSkinRect`, exact three-role `CompactSkinTypography`, exact decoration references (`backdrop`, `chromeFrame`, `displayPlate`, and zero-to-three `ornaments`), and non-optional `CompactSkinLayoutPlan`. Validate one frame for each required slot, all semantic measurements on the 4-point grid, bounds, interactive-frame non-overlap, 32 x 32 favorite/library/overflow floors, transport capacity for three 32-point controls plus app-owned gaps, readable one-line channel and two-line metadata capacity, 4-point focus clearance inside the silhouette safe region, no more than six resolved local decorations, and at least one noninteractive drag rectangle of 80 x 20 or larger that is in bounds and disjoint from interactive frames. Preserve schema versions 1 and 2 decode and rendering unchanged at legacyStack/nativeRect/400 x 288 (D-01/D-09/D-20). Per D-05/D-06, refactor `CompactPlayerView` so app-created artwork, channel identity, metadata, favorite, status, transport, library, and overflow regions exist once in the fixed UI-SPEC source/VoiceOver/keyboard order; place them visually from validated frames, keep every existing identifier/action/value/Help string app-owned, keep decorations and drag regions behind controls with hit testing disabled and accessibility hidden, and retain the existing complete Native fallback. Per D-08/D-09, extend the window controller to accept only the validated finite size/silhouette policy, keep the compact window titled, key-capable and nonresizable, make its content/background transparent without exposing click-through holes or raw style-mask authority, and apply equal content minimum/maximum values. Create `PixelDesk.json` with the exact UI-SPEC palette, `desktopUtility`/`pixelNotched`/`desktop432x304`, monospaced display/status typography, the documented eight slot frames and valid drag rectangle, and original app-drawn pixel bevel/dither ornament selected by the public silhouette token; register it in `bundledCatalog` and the app resource phase through the same validator used by every appearance (D-04/D-12/D-13). Do not add a bundled-only control tree or renderer.</action>
  <acceptance_criteria>
    - Pixel Desk is a complete selectable bundled appearance whose JSON validates through `SkinManifestValidator.validate(... classification: .bundled)` and whose selected reference still resolves through `SkinAppearanceController`.
    - Pixel Desk renders at 432 x 304 with all eight app-owned semantic regions, visible 4-point focus clearance, 32-point interactive targets, an app-owned drag region, original pixel ornament, and the fixed semantic source order.
    - Native, Signal Glow, Tape Deck, and schema-v1/v2 imported packages retain legacyStack/nativeRect and 400 x 288 behavior.
    - The compact NSWindow remains titled, key-capable, nonresizable, and rectangular for event handling while the drawn Pixel Desk silhouette appears inside transparent content.
    - Unknown schema-v3 keys/tokens and invalid required layout values fail before catalog publication or selection.
  </acceptance_criteria>
  <verify>
    <automated>zsh -lc 'dd_path=$(mktemp -d /tmp/canis97-04.2-01-tracer.XXXXXX); xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme Canis97 -destination "platform=macOS" -derivedDataPath "$dd_path" CODE_SIGNING_ALLOWED=NO'</automated>
  </verify>
  <done>One production-quality Pixel Desk path proves schema-v3 selection, native semantic rendering, finite window sizing, and legacy compatibility end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04.2-01-02: Apply the same strict v3 compiler and asset accounting to imported packages</name>
  <reversibility rating="costly">The version-aware referenced-asset accessor becomes part of the installed-package validation contract; later changes require coordinated package compatibility and managed-copy revalidation.</reversibility>
  <read_first>
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md` — apply D-02, D-03, D-04, D-17, and D-18 to imported packages without changing budgets or authority.
    - `SiriusMac/Skins/SkinAppearance.swift` — modify the new schema-v3 compiler from Task 04.2-01-01 and expose one version-aware referenced-asset accessor.
    - `SiriusMac/Skins/SkinPackagePolicy.swift` — preserve all existing archive/path/symlink/count/byte/image/deadline limits and inclusive arithmetic semantics as a pattern source.
    - `SiriusMac/Skins/SkinPackageImporter.swift` — modify exact-file accounting, candidate/managed validation, image checking, digest, promotion, reload, and renderability around the version-aware accessor.
    - `SiriusMacTests/SkinPackageImporterTests.swift` — modify with schema-v3 imported-package, hostile-layout, extra-file, and atomic-selection compilation contracts.
    - `SiriusMacTests/CompactPlayerPresentationTests.swift` — modify with classification/identity and legacy-compatibility contracts.
    - `script/tests/ExpressiveSkinContractOfflineTests.swift` — create this modified/new launch-free executable with exact v3 JSON and geometry fixtures.
  </read_first>
  <files>SiriusMac/Skins/SkinAppearance.swift, SiriusMac/Skins/SkinPackageImporter.swift, SiriusMacTests/SkinPackageImporterTests.swift, SiriusMacTests/CompactPlayerPresentationTests.swift, script/tests/ExpressiveSkinContractOfflineTests.swift</files>
  <behavior>
    - A schema-v3 package with zero through six canonical local decoration references uses the same validator and validated layout plan as Pixel Desk.
    - Every declared v3 decoration is included in exact-file accounting, type/frame/dimension validation, digest generation, managed promotion revalidation, managed reload, and renderability fallback.
    - Missing, extra, duplicated, escaping, symlinked, remote, active-URL, unsupported-type, over-budget, undecodable, or unreferenced files fail before selection and preserve the prior confirmed appearance.
    - Invalid frames include negative coordinates, out-of-bounds rectangles, off-grid measurements, duplicate/missing semantic slots, interactive overlap, undersized targets, insufficient text capacity, inadequate focus clearance, absent/overlapping drag regions, excess decoration layers, and unsupported finite tokens.
    - Schema versions 1 and 2 keep their exact accepted keys, asset accounting, appearance values, and legacy geometry.
  </behavior>
  <action>Start with failing launch-free and compiled test sources for the behaviors above. Per D-04/D-18, replace the importer's schema-version-1-only `referencedAssetPaths(in:)` decode with one accessor owned by the strict manifest compiler that returns canonical raw local references for the detected supported schema. Use the same returned set for assetResolver membership, exact-file equality, ImageIO validation, content digest, promotion revalidation, managed reload, and `renderableAppearance`; preserve every existing `SkinPackageLimits` value, overflow-reporting arithmetic, CRC-checked extraction, rollback, generation, and durable-save-first selection behavior. Map unsupported version 3 to the existing closed compatibility error and all hostile schema-version-3 values to semantic package rejection without exposing raw package data. Add imported schema-version-3 fixtures that cover all boundary and failure categories, prove the prior valid reference/catalog state remains confirmed on rejection, prove Native recovery is independent of the package, and implement the assumption-delta invariant that native/bundled/imported theme choice retains exact `SkinSelectionReference` identity and classification through the existing controller. The standalone offline executable may mirror exact schema and geometry constraints for runnable evidence but must take actual manifest files as inputs where available and must not become production validation authority.</action>
  <acceptance_criteria>
    - Bundled and imported schema-v3 documents compile to the same `ValidatedSkinAppearance` and `CompactSkinLayoutPlan` types.
    - Exact-file accounting recognizes every allowed v3 decoration reference and rejects every undeclared package file.
    - Every hostile geometry/asset fixture leaves the selected reference and durable confirmed appearance unchanged.
    - Archive, path, symlink, entry-count, byte, image-dimension, decode, CRC, processing-deadline, rollback, and generation guarantees remain present at their current bounds.
    - The launch-free executable passes exact-token, boundary, overlap, drag, layer-count, and legacy-version cases without opening an app or test host.
  </acceptance_criteria>
  <verify>
    <automated>swift script/tests/ExpressiveSkinContractOfflineTests.swift SiriusMac/Skins/Bundled/PixelDesk.json SiriusMac/Skins/Bundled/SignalGlow.json SiriusMac/Skins/Bundled/TapeDeck.json</automated>
  </verify>
  <done>Imported and bundled schema-v3 packages share one strict compiler, asset-accounting path, semantic renderer input, and atomic recovery contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04.2-01-03: Make finite size transitions and restoration atomic and Native-recoverable</name>
  <reversibility rating="costly">The versioned compact-position record and finite transition policy affect restored user window placement; changing them later requires a coordinated migration and controller update.</reversibility>
  <read_first>
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md` — implement D-08, D-09, D-10, D-11, D-17, and D-19 exactly.
    - `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-UI-SPEC.md` — preserve top-left, select the containing screen, clamp the complete frame, center only with no valid origin, and restore Native for invalid state.
    - `SiriusMac/Windows/CompactWindowController.swift` — modify window geometry, attachment input, position persistence, and recovery callback.
    - `SiriusMac/SiriusMacApp.swift` — modify `CompactListeningSlice` only to pass the current validated finite policy and package-independent Native recovery callback into `WindowAttachmentView`.
    - `SiriusMacTests/ListeningSessionControllerTests.swift` — modify compile-time window policy tests for all four sizes, top-left preservation, screen choice, full-frame clamping, invalid records, and Native recovery routing.
    - `SiriusMacTests/CompactPlayerPresentationTests.swift` — modify static integration contracts for appearance-to-window policy and direct Native fallback.
  </read_first>
  <files>SiriusMac/Windows/CompactWindowController.swift, SiriusMac/SiriusMacApp.swift, SiriusMacTests/ListeningSessionControllerTests.swift, SiriusMacTests/CompactPlayerPresentationTests.swift</files>
  <behavior>
    - Native/legacy, Pixel Desk, Pocket Disc, and Aqua Vista resolve only to 400 x 288, 432 x 304, 384 x 320, and 448 x 304 respectively.
    - A size change preserves the visible top-left point when possible, converts content size to the complete titled frame, selects the screen containing that point, clamps the complete frame to `visibleFrame`, sets equal content minimum/maximum values, and persists one schema-versioned origin record.
    - A missing record centers the selected valid appearance; a malformed, unsupported, nonfinite, off-screen, or incompatible record triggers direct Native selection and the 400 x 288 Native policy.
    - The compact window retains normal key/main behavior, native title/traffic-light ownership, nonresizable policy, and rectangular hit testing at every size.
    - A valid drag rectangle invokes only the app-owned window drag gesture and remains behind semantic controls and outside the accessibility tree.
  </behavior>
  <action>Per D-10/D-17, add `CompactWindowPositionRecord` with schemaVersion 1 plus finite `x`/`y`, `CompactWindowPositionStore`, and pure `CompactWindowGeometry` helpers for content-to-frame sizing, top-left preservation, containing-screen choice, axis clamping, and invalid-record classification. Update `CompactWindowController` and `WindowAttachmentView` so `CompactListeningSlice` passes only `ValidatedSkinAppearance.layoutPlan.windowPolicy` plus `restoreNativeAppearance()` as an app-owned recovery callback; raw manifest dictionaries, frame rectangles, style masks, and persistence keys stay out of the view. Apply the full frame once, then set equal content minimum/maximum values and persist the clamped top-left. Keep the current role-scoped close observer, Always on Top behavior, key/main eligibility, titlebar/traffic lights, nonresizable/full-screen restrictions, and current Native/legacy migration. On invalid versioned restoration, call the recovery closure, apply Native 400 x 288 chrome/policy, and expose the existing app-owned recovery status rather than centering the failed custom appearance. Per D-11, use SwiftUI `WindowDragGesture` only on validated clear drag rectangles behind controls with activation events allowed; no custom mouse tracking or click-through holes. Add exhaustive deterministic source/test contracts for each size, multi-screen top-left/clamp cases, exact-edge fits, invalid coordinates/versions, re-selection idempotency, and the direct Native route. Compile those tests only under the current safety hold.</action>
  <acceptance_criteria>
    - Each supported appearance policy produces exactly its approved content size and a complete frame contained by one `NSScreen.visibleFrame`.
    - Valid transitions preserve top-left placement when possible and persist only a versioned finite origin; invalid restoration selects Native at 400 x 288.
    - Imported values never choose arbitrary dimensions, min/max values, style masks, screen identifiers, persistence keys, or resize behavior.
    - Dragging is app-owned, accessibility-hidden, behind controls, and disjoint from every interactive slot.
    - A final build-for-testing compiles the app, unit-test, and UI-test products without launching any process from those products.
  </acceptance_criteria>
  <verify>
    <automated>zsh -lc 'dd_path=$(mktemp -d /tmp/canis97-04.2-01-window.XXXXXX); xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme Canis97 -destination "platform=macOS" -derivedDataPath "$dd_path" CODE_SIGNING_ALLOWED=NO'</automated>
  </verify>
  <done>Finite expressive sizes transition and restore atomically inside visible screens, with invalid state routed directly to package-independent Native recovery.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols / contents |
|---|---|
| `SiriusMac/Skins/SkinAppearance.swift` | `CompactSkinLayoutVariant`, `CompactSkinSilhouetteVariant`, `CompactSkinSizeVariant`, `CompactSkinSemanticSlot`, `CompactSkinTypographyToken`, `CompactSkinRect`, `CompactSkinTypography`, `CompactSkinDecorationReferences`, `CompactSkinLayoutPlan`, private `SkinManifestVersion3`, `SkinManifestValidator.validateVersion3`, and the version-aware `referencedAssetPaths` accessor |
| `SiriusMac/Player/CompactPlayerView.swift` | Private `CompactSkinSilhouetteShape`, `CompactSkinMaterialLayer`, and the initial `pixelNotched` material helper around the stable semantic-slot renderer |
| `SiriusMac/Windows/CompactWindowController.swift` | `CompactWindowPositionRecord`, `CompactWindowGeometry`, `CompactWindowPositionStore`, finite appearance-policy update/recovery input on `CompactWindowController` and `WindowAttachmentView` |
| `SiriusMac/Skins/Bundled/PixelDesk.json` | Complete Pixel Desk schema-v3 manifest with exact tokens, palette, slot frames, typography, decorations, and drag region |
| `script/tests/ExpressiveSkinContractOfflineTests.swift` | `ExpressiveSkinContractOfflineTests`, its exact schema/geometry fixtures, and launch-free entry point |
| `SiriusMacTests/CompactPlayerPresentationTests.swift` | New schema compatibility, selection identity/classification, semantic registry, and Native fallback test methods |
| `SiriusMacTests/SkinPackageImporterTests.swift` | New v3 exact-file accounting, hostile layout/asset, and atomic rejection test methods |
| `SiriusMacTests/ListeningSessionControllerTests.swift` | New finite-size, top-left preservation, visible-frame clamp, versioned restoration, and Native recovery test methods |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local JSON/archive to validated appearance | Entire package input is untrusted until exact keys, finite tokens, geometry, asset references, and existing package budgets pass. |
| Validated layout to SwiftUI controls | Visual placement crosses into rendering, while action/accessibility/playback authority stays app-owned. |
| Validated finite policy to NSWindow | Only closed size/silhouette policy crosses; raw manifest geometry and window flags remain untrusted data. |
| Managed package to selected appearance | Durable selection publishes only after managed-copy revalidation and persistence authority remain current. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.2-01-01 | Tampering | Nested schema-v3 layout objects | high | mitigate | Exact key sets, closed enums, complete slot accounting, bounds/overlap/focus/drag validation, and rejection before catalog publication per D-02/D-03. |
| T-04.2-01-02 | Elevation of Privilege | Semantic renderer | high | mitigate | Instantiate app-owned controls once in stable source order; package data supplies only validated visual frames/tokens per D-05/D-06. |
| T-04.2-01-03 | Elevation of Privilege | Compact NSWindow bridge | high | mitigate | Resolve finite app-owned size/silhouette policy before the controller; retain titled/key-capable/nonresizable behavior and raw-flag exclusion per D-08/D-09/D-10. |
| T-04.2-01-04 | Denial of Service | Imported decoration/assets | high | mitigate | Preserve inclusive archive/image/decode/deadline budgets and exact-file accounting through version-aware references per D-18. |
| T-04.2-01-05 | Spoofing | Failed custom appearance | medium | mitigate | Replace the complete appearance/window policy with permanent Native and app-authored recovery copy per D-17. |
</threat_model>

<verification>
- Run the standalone expressive schema/geometry contract against Pixel Desk plus both schema-v2 compatibility resources; it must execute without an application or test host.
- Run serialized `xcodebuild build-for-testing` with a fresh temporary DerivedData path and `CODE_SIGNING_ALLOWED=NO`; this compiles configured products only.
- Inspect the built app resource bundle for `PixelDesk.json`, `SignalGlow.json`, and `TapeDeck.json` without opening the app.
- Review the diff for exact schema-v1/v2 compatibility, centralized asset-reference accounting, fixed semantic source order, finite window-policy input, and direct Native fallback.
- Do not run `xcodebuild test`, `test-without-building`, `xctest`, `XCUIApplication.launch()`, UI automation, `script/build_and_run.sh`, any app/test host, or any live SiriusXM operation.
</verification>

<success_criteria>
- Pixel Desk is selectable and materially changes validated layout, silhouette, chrome, typography, and size through one real production path.
- Schema-v3 packages remain closed declarative data/assets and imported packages use the same compiler, renderer, recovery, and asset accounting as bundled resources.
- Native and schema-v1/v2 appearances remain exact legacy 400 x 288 compatibility cases.
- Finite size transitions, multi-screen clamping, versioned restoration, drag regions, and Native recovery remain app-owned and compile under focused coverage.
</success_criteria>

<output>
Create `.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-01-SUMMARY.md` when done.
</output>
