---
phase: 02-authorized-live-listening
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md
  - .planning/phases/02-authorized-live-listening/02-RESEARCH.md
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift
autonomous: true
requirements: [CAT-01, CAT-02, PLAY-01, PLAY-04, META-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production provider code changes only when 02-LIVE-CONTRACT.md says `Gate Result: SUPPORTED`; unsupported, ambiguous, protected, or incomplete evidence halts this plan without editing provider operations."
    - "Every fixed request operation and strict decoder corresponds to a supported capability row and sanitized contract fact; no endpoint, host, method, provider field, optional key step, or schema branch is inferred or fabricated."
    - "All three 02-RESEARCH.md Open Questions are explicitly closed against the canonical sanitized artifact before downstream catalog/playback/metadata implementation starts."
    - "The resolved-media handoff is selected from the observed AVFoundation/resource requirements, remains ephemeral and redacted, and exposes no ordinary public URL/header/key/resource value."
    - "Provider-specific deterministic fixtures are invented from semantic type/cardinality contracts, not copied or sanitized from live bodies."
  artifacts:
    - path: ".planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md"
      provides: "Canonical supported non-exhaustive operation, schema, AVFoundation, and handoff contract"
      contains: "Gate Result: SUPPORTED"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift"
      provides: "Individually named exact request cases for supported Phase 02 capabilities only"
      contains: "SiriusXMRequestContract"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift"
      provides: "Strict status/control/schema classifiers for the supported catalog, tune/resource, and metadata operations"
      contains: "LiveListeningAdapter"
  key_links:
    - from: "02-LIVE-CONTRACT.md"
      to: "SiriusXMRequestContract"
      via: "each implemented case cites one supported operation purpose and fixed contract fact"
      pattern: "SUPPORTED"
    - from: "02-LIVE-CONTRACT.md"
      to: "LiveListeningAdapter"
      via: "invented fixtures mirror only semantic type/cardinality and closed outcome facts"
      pattern: "Semantic Shape"
    - from: "LiveListeningAdapter"
      to: "LiveListeningModels"
      via: "strict provider decoding collapses immediately to semantic values and opaque handoff constraints"
      pattern: "unsupported"
  prohibitions:
    - statement: "This plan must not make a live provider request, add a capability absent from COVERAGE.md, claim provider-exhaustive coverage, or preserve raw checkpoint evidence."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Convert the supported sanitized live result into exact internal compatibility contracts and a safe ephemeral media handoff before feature expansion.

Purpose: Ensure every downstream provider-dependent implementation is evidence-backed, deterministic, repairable, and blocked by one canonical gate.
Output: Resolved research statuses, fixed named request cases, strict decoders, selected handoff seam, and invented provider-contract regression fixtures.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-RESEARCH.md
@.planning/phases/02-authorized-live-listening/02-PATTERNS.md
@.planning/phases/02-authorized-live-listening/COVERAGE.md
@.planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md
@.planning/phases/02-authorized-live-listening/02-02-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift
@Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift
@Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift

<interfaces>
- `02-LIVE-CONTRACT.md` is the sole provider-contract evidence source and must say supported. `COVERAGE.md` remains the bounded capability inventory, not an exhaustive private API map.
- Plan 02-01 supplies provider-neutral semantic models and red contract suites; Plan 02-02 supplies the sanitized current operation/resource/AVFoundation facts.
- `SiriusXMRequestContract` currently contains only the two established Phase 01 operations and exact authorization/host/cache behavior.
- Provider request construction, decoding, media values, and compatibility failure stay internal. Ordinary public models remain semantic and secret-safe.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `02-RESEARCH.md` — marks Open Questions 1-3 resolved-supported against the canonical artifact; no question is closed by planner assumption.
- `LiveListeningModels.swift` — replaces the reversible handoff seam only with the narrowest evidence-compatible opaque app-integration type.
- `SiriusXMRequestContract.swift` and `LiveListeningAdapter.swift` — add exact supported operations/strict preflight and decoding without public runtime methods.
- The three Wave 0 package suites — refine invented semantic contracts into evidence-backed fixed-contract tests.

### New files and symbols

- No new planning artifact: `02-LIVE-CONTRACT.md` was created by the live checkpoint and is refined only with cross-references/decision status.

### Public API changes

- No ordinary public provider wire surface. If an app-integration handoff is required, it is SPI-scoped, non-Codable, permanently redacted, memory-only, and generation-bound.

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-03-01: Resolve the three research questions and encode only supported fixed contracts</name>
  <precondition>`02-LIVE-CONTRACT.md` exists, says `Gate Result: SUPPORTED`, contains resolved catalog/tune/resource/AVFoundation/metadata evidence, and contains no `Execution: HALT` marker.</precondition>
  <reversibility rating="costly">Fixed internal operations and the app-integration handoff become the compatibility authority used by all later Phase 02 capabilities, though provider details remain contained behind adapters.</reversibility>
  <read_first>
    - `02-LIVE-CONTRACT.md` — use every fixed fact exactly and stop on missing, ambiguous, or unsupported evidence.
    - `COVERAGE.md` — implement only rows marked supported or not-required; never add capability scope.
    - `02-RESEARCH.md` Open Questions 1-3 — close each question explicitly with a canonical-artifact citation.
    - `SiriusXMRequestContract.swift` — preserve individually named operations, exact authorized-host policy, credential validation, cache bypass, and redirect denial.
    - `AuthenticationFlowAdapter.swift` — reuse status/content-type/control preflight and terminal unsupported conversion.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md, .planning/phases/02-authorized-live-listening/02-RESEARCH.md, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift</files>
  <behavior>
    - Test 1: each new request case maps one-to-one to a supported operation purpose and exact method/authorized-host-policy/path-template fact from the artifact.
    - Test 2: absent/unsupported operations have no request case; a not-required key or artwork step creates no request and returns its closed semantic fallback.
    - Test 3: every decoder performs status/content-type/control preflight before the exact supported shape and maps unknown/malformed/extra-semantic branches to closed unsupported.
    - Test 4: the selected media handoff can configure only the supported Apple playback mechanism and cannot encode, render, persist, or return resource/header/key material through ordinary public API.
    - Test 5: the research file identifies Open Questions 1-3 as resolved-supported with links to the precise sanitized artifact sections and no raw evidence.
  </behavior>
  <action>Begin by asserting the canonical gate precondition and reconciling every supported/not-required/unsupported coverage row with `02-LIVE-CONTRACT.md`. If a minimum catalog, selected tune/resource, AVFoundation, or metadata row is unsupported or ambiguous, stop with the typed compatibility result and leave production provider files unchanged. Otherwise add one individually named request case per supported operation using exactly the recorded fixed method, authorized-host policy, and path template; do not generalize them into a dynamic request builder. Add strict status/content-type/control preflight and supported schema classifiers in `LiveListeningAdapter`, with unknown input terminal.

Choose the media handoff from the evidence, in this order: an SPI closure/factory that returns a fully configured Apple player item without exposing values; otherwise an SPI Apple playback-session protocol that retains any supported resource-loader responsibility internally. Do not add an ordinary public URL/header/key accessor or select a fallback media engine. Record the choice and rationale in the canonical artifact. Update all three research questions to resolved-supported and cite the artifact sections for current fixed contracts, AVFoundation behavior, and the chosen handoff.</action>
  <acceptance_criteria>
    - Every provider operation/decoder branch is traceable to one sanitized supported fact and every absent/unsupported row stays absent.
    - Open Questions 1-3 are explicitly resolved from the checkpoint, not from prior assumptions.
    - The selected handoff matches observed requirements and has no ordinary render/encode/persist/material accessor.
    - Request construction remains fixed, host-restricted, ephemeral, redirect-denying, and non-generic.
    - This task performs no live provider work.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient</automated>
  </verify>
  <done>All three research questions are closed and each supported content operation plus the ephemeral Apple media handoff is encoded behind one evidence-backed internal compatibility boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-03-02: Lock the supported contracts with invented deterministic fixtures</name>
  <reversibility rating="reversible">Invented compatibility fixtures can evolve alongside internal adapters without exposing provider payloads or changing user data.</reversibility>
  <read_first>
    - `02-LIVE-CONTRACT.md` Semantic Shape and Failure Classification sections — create artificial values matching only recorded type/cardinality/optionality facts.
    - `LiveCatalogAdapterTests.swift`, `LivePlaybackCoordinatorTests.swift`, and `MetadataRefreshCoordinatorTests.swift` — refine the Wave 0 semantic contracts rather than replacing them.
    - `SanitizedNativeResponseFixtures.swift` and `RedactionTests.swift` — preserve the artificial-marker rule and reject provider/account/resource structures.
    - `LiveListeningAdapter.swift` — exercise each supported success and terminal preflight/unknown-shape branch.
  </read_first>
  <files>Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift</files>
  <behavior>
    - Test 1: invented catalog fixtures cover the supported collection/item cardinalities plus D-01 positive filtering and terminal unknown classifications.
    - Test 2: invented tune/resource/key fixtures cover exactly the supported or not-required sequence, protection/control stops, and opaque handoff redaction.
    - Test 3: invented metadata/artwork fixtures cover only supported fields/optionality and keep text/artwork failure independent.
    - Test 4: redirects, HTML/control content, authorization loss, 403/429, malformed JSON, unknown shape, cancellation, and generation supersession stop before later scripted steps.
    - Test 5: fixture source contains no copied provider value, account/session data, raw URL/resource/key value, or response capture.
  </behavior>
  <action>Extend the three pre-existing Wave 0 suites with invented fixture values that conform to the sanitized type/cardinality/optionality contract while remaining visibly artificial. Exercise every supported request/decoder sequence and not-required branch, status/content-type/control preflight, terminal unknown shape, optional/missing presentation, opaque handoff redaction, cancellation, and exact operation counts. Do not paste, transform, hash, or redact a live body into tests. Keep D-01 through D-12 semantic cases from Plan 02-01 intact and layer fixed compatibility cases beneath them. Add no network-enabled test.</action>
  <acceptance_criteria>
    - Each supported operation and strict decoder has success plus terminal drift/control coverage.
    - Not-required and unsupported rows make zero provider-operation calls.
    - All fixtures are invented, deterministic, offline, and structurally secret-safe.
    - The full package suite passes and continues to cover every Wave 0 requirement.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient</automated>
  </verify>
  <done>The live-supported fixed contracts are reproducible through invented offline fixtures without retaining provider evidence or widening API coverage claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Sanitized live artifact → fixed request code | Missing or ambiguous facts must halt rather than become inferred provider behavior. |
| Provider response → strict semantic adapter | Untrusted status/content/schema/control signals must collapse before public/app state. |
| Internal resolved resource → Apple playback layer | Ephemeral material must remain inside a redacted SPI handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure | handoff/models/fixtures | high | mitigate | SPI opaque factory/session, permanent redaction, no encoding/material accessor, and invented fixtures. |
| T-02-02 | Information Disclosure / Tampering | fixed requests | high | mitigate | Evidence-backed individual cases, exact host policy, ephemeral transport, redirect denial, and no generic builder. |
| T-02-03 | Tampering | provider schema/control | high | mitigate | Preflight before strict decoding; unknown, malformed, and protected results terminate unsupported. |
| T-02-04 | Elevation of Privilege | unsupported capability | high | mitigate | Top-level supported precondition and zero code path for unsupported/not-required operations. |
| T-02-05 | Repudiation | API coverage claims | medium | mitigate | Canonical non-exhaustive artifact and one-to-one coverage-row traceability. |
</threat_model>

<verification>
- The canonical supported artifact is checked before any production provider edit.
- Research Open Questions 1-3 are explicitly resolved and cross-referenced.
- Full offline package tests cover every supported exact contract, not-required zero-call path, drift/control stop, and handoff redaction.
</verification>

<success_criteria>
- Downstream plans can implement catalog, playback, recovery, and metadata without provider guesses.
- Unsupported/ambiguous results cannot enter fixed provider code.
- API coverage remains limited to the decided Phase 02 capability matrix.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-03-SUMMARY.md` when done.
</output>
