---
phase: 02-authorized-live-listening
plan: "14"
type: execute
wave: 13
depends_on: ["02-13", "02-15"]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMacTests/AuthenticationPresentationModelTests.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
autonomous: true
gap_closure: true
requirements: [CAT-03, PLAY-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A new authentication attempt cannot consume or save a credential until every prior explicit sign-out cleanup has completed."
    - "A blocked older erase followed by a successful new attempt leaves the new credential stored after all earlier cleanup work finishes."
    - "The app shows one fixed cleanup-in-progress state and cannot begin restore, WebView sign-in, handoff, or retry while cleanup is outstanding."
    - "Overlapping sign-out requests still coalesce, later explicit cleanup can retry after failure, and ordinary operation failures still never erase credentials."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      provides: "Cleanup-before-authentication ordering"
      contains: "cleanupTask"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift"
      provides: "Blocked erase/new save survival regression"
      contains: "SignOutTests"
    - path: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      provides: "Truthful cleanup-in-progress presentation gate"
      contains: "finishingCleanup"
  key_links:
    - from: "SessionCoordinator.attemptSession"
      to: "cleanupTask.value"
      via: "await outstanding cleanup before acquiring the next attempt lease or credential"
      pattern: "cleanupTask"
    - from: "AuthenticationPresentationModel.clearLocalSession"
      to: "ComposedAuthenticationPresentationFlow.signOut"
      via: "one in-flight cleanup state that rejects new auth actions"
      pattern: "finishingCleanup"
  prohibitions:
    - statement: "An older cleanup task must never erase a credential saved by a newer successful session."
      status: unverified
      flagged: true
    - statement: "Cleanup sequencing tests must use in-memory stores/cleaners and must not read or erase the real app Keychain or WebView store."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Prevent an older explicit cleanup from deleting a later successful session and make that ordering visible in the app.

Purpose: CR-01 explains the earlier unexpected sign-out: detached cleanup can race a later save. This plan serializes the lifecycle and proves the new durable credential survives.
Output: Cleanup-before-attempt ordering, a blocked-erase regression, and a fixed cleanup-in-progress native state.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-REVIEW.md
@.planning/phases/02-authorized-live-listening/02-VERIFICATION.md
@.planning/phases/02-authorized-live-listening/02-13-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-15-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
@SiriusMac/Authentication/AuthenticationPresentationModel.swift

<interfaces>
- `SessionCoordinator.signOut()` retires actor state synchronously, creates one coalesced `cleanupTask`, and awaits injected credential/browser cleaners.
- `SessionCoordinator.attemptSession()` currently checks only `attemptLease`; it must also respect outstanding cleanup before reading `CredentialSource`.
- `AuthenticationPresentationModel` already uses `attemptID` and `isAttemptInFlight` to reject overlapping UI work; expose that cleanup state truthfully rather than leaving stale Ready/rejected copy on screen.
</interfaces>
</context>

## Artifacts this phase produces

- Serialized cleanup and authentication within the sole session actor.
- A deterministic erase/save order regression using an injected blocking credential store.
- Native cleanup-in-progress presentation and action gating.
- No new storage identity, Keychain query, browser cleanup method, or automatic retry.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-14-01: Prove an older blocked erase cannot delete a later credential</name>
  <reversibility rating="costly">The ordering governs every explicit sign-out and subsequent authentication attempt in the reusable client.</reversibility>
  <read_first>
    - `02-REVIEW.md` CR-01 and `02-VERIFICATION.md` second gap — exact detached cleanup/new save race.
    - `SessionCoordinator.swift` `attemptSession` and `signOut` — preserve memory-first retirement and cleanup coalescing.
    - `SignOutTests.swift` existing overlapping/retry/blocked revalidation tests.
    - `02-12-SUMMARY.md` — preserve persistence-gated durable readiness while adding cleanup ordering.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift</files>
  <behavior>
    - Start sign-out with an injected erase blocked; a new attempt performs zero source/authentication/entitlement/save calls while erase remains blocked.
    - Release erase; the new attempt then runs in order, saves once, returns durable active, and the stored synthetic generation remains the new one.
    - Overlapping sign-outs share one erase/cleanup; a later explicit cleanup after failure still starts a new operation.
    - An ordinary revalidation failure continues to perform zero erase calls.
  </behavior>
  <action>Add the blocking-store regression first, including a store-side generation marker that can assert the final saved generation without exposing credential bytes. At the start of `attemptSession()`, await any outstanding cleanup before acquiring an attempt lease or asking the credential source for material. Re-check cancellation and actor state after the await. Preserve sign-out's memory-first behavior, overlapping cleanup coalescing, explicit retry after cleanup failure, and the operation-failure preservation behavior from 02-08. Do not solve the race with a timer, detached retry, extra Keychain identity, or best-effort erase.</action>
  <acceptance_criteria>
    - No authentication collaborator is called while the prior erase is blocked.
    - After unblock, event order is erase-old then read/verify/entitle/save-new.
    - Final injected store generation is the new session and remains present after the old sign-out returns.
    - Existing cleanup coalescing, failure reporting, cancellation, and explicit-only erasure tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests</automated>
  </verify>
  <done>Cross-process cleanup can no longer erase material established by the next successful session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-14-02: Keep native reauthentication blocked until cleanup is finished</name>
  <reversibility rating="reversible">The cleanup-in-progress copy and action gate are local presentation behavior over the package ordering invariant.</reversibility>
  <read_first>
    - `AuthenticationPresentationModel.swift` sign-out/clear actions, `startAttempt`, and presentation copy.
    - `SelectedAuthenticationCompositionTests.swift` fresh cleanup, explicit retry, and restored sign-out tests.
    - `AuthenticationPresentationModelTests.swift` single-flight and fixed-copy assertions.
    - Locked user intent — no retry, Sign Out, or Clear Local Session is permitted during a bounded terminal UAT result.
  </read_first>
  <files>SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift</files>
  <behavior>
    - Sign Out or Clear Local Session immediately displays one fixed finishing-cleanup state.
    - Restore, Sign In, Retry, Use This Window's Session, and another non-coalesced cleanup cannot start while that state is in flight.
    - Cleanup success settles signed out; cleanup failure settles the existing explicit warning; neither auto-starts WebView or native authentication.
    - A later explicit sign-in begins only after cleanup returns and uses the fresh WebView lifecycle from 02-13.
  </behavior>
  <action>Add a `finishingCleanup` closed presentation state and enter it for both explicit sign-out and clear-local-session before awaiting the composed client. Retain `isAttemptInFlight` as the single-flight gate and make every authentication action return nil during cleanup. After completion, map only the existing signed-out/cleanup-warning outcomes and schedule no next action. Extend composition tests with a blocked cleanup fake followed by one explicit successful sign-in, asserting no WebView load, credential transfer, or native call occurs before release.</action>
  <acceptance_criteria>
    - Cleanup progress is visible and fixed-copy only.
    - Every authentication action is inert until cleanup resolves.
    - No automatic follow-up is scheduled on success or failure.
    - The post-cleanup explicit attempt uses the new-session generation and reaches the package only once.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh &amp;&amp; bash script/tests/build_and_run_tests.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests -only-testing:SiriusMacTests/WebAuthenticationBridgeTests CODE_SIGNING_ALLOWED=NO</automated>
  </verify>
  <done>The native app cannot overlap cleanup and reauthentication, and tells the user when local cleanup is still finishing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Older detached cleanup → newer session persistence | An authorized erase must not cross the generation boundary into later material. |
| Native cleanup UI → authentication actions | Presentation must not allow a new credential handoff while destructive cleanup is outstanding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-14-01 | Tampering | `CredentialStore.erase` ordering | high | mitigate | Await outstanding cleanup before source read/save; prove final synthetic generation survives. |
| T-02-14-02 | Denial of Service | session durability | high | mitigate | Serialize lifecycle instead of allowing old cleanup to remove a valid new session. |
| T-02-14-03 | Replay | post-cleanup WebView attempt | high | mitigate | Permit only one later explicit attempt and require the fresh-session rotation delivered by 02-13. |
| T-02-14-04 | Information Disclosure | cleanup tests | high | mitigate | In-memory generation markers and fixed outcomes only; no material, Keychain, WebView, or OSStatus access. |
| T-02-14-05 | Repudiation | cleanup state | medium | mitigate | Fixed finishing/success/warning states make the destructive boundary observable. |
</threat_model>

<verification>
- Run the package sequencing tests first; run app-host tests only after the standalone auth matrix and fake launcher matrix are green.
- Confirm no test creates `KeychainCredentialStore`, `WebAuthenticationBridge()` live composition, provider transport, or app launch.
- Re-run the 02-08 session-preservation cases to ensure ordinary failures still do not erase.
</verification>

<success_criteria>
- CR-01 is closed at its root cause with a deterministic blocked-erase/new-save regression.
- The new credential remains durable after every older cleanup has finished.
- UI actions cannot overlap cleanup and authentication.
- No live account action is needed to prove the repair.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-14-SUMMARY.md` when done.
</output>
