---
phase: 02-authorized-live-listening
plan: "16"
type: execute
wave: 14
depends_on: ["02-14"]
files_modified:
  - SiriusMac/Listening/PlaybackCoordinator.swift
  - SiriusMacTests/PlaybackInstallationOrderTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
autonomous: true
gap_closure: true
requirements: [PLAY-01, PLAY-02, PLAY-03]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An AVPlayerItem already ready when observation is installed requests play exactly once after, never before, runtime installation."
    - "The same initial-ready ordering works for initial tune, live-edge resume, and recovered items."
    - "Late, cancelled, superseded, wrong-channel, and old-recovery readiness callbacks cannot request play or publish state."
    - "The SiriusMacTests project group references the declared MetadataPresentationTests file object with no dangling identifier."
  artifacts:
    - path: "SiriusMac/Listening/PlaybackCoordinator.swift"
      provides: "Initial-ready staging across observe/install/play"
      contains: "observeAndInstall"
    - path: "SiriusMacTests/PlaybackInstallationOrderTests.swift"
      provides: "Synchronous-ready-before-observe-return regressions"
      contains: "PlaybackInstallationOrderTests"
    - path: "SiriusMac.xcodeproj/project.pbxproj"
      provides: "Consistent test group file reference"
      contains: "020700020000000000000001"
  key_links:
    - from: "AVFoundationItemObservation"
      to: "PlaybackCoordinator.observeAndInstall"
      via: "initial/new status callback staged until exact item installation"
      pattern: "initial"
    - from: "observeAndInstall"
      to: "runtime.requestPlay"
      via: "matching observation identity plus installed generation"
      pattern: "requestPlayForReadyItem"
  prohibitions:
    - statement: "A readiness callback may never call requestPlay before the matching item is installed or after its generation/observation is superseded."
      status: unverified
      flagged: true
    - statement: "Tests may not resolve provider media, contact SiriusXM, create production authentication/Keychain composition, or claim audibility."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Close the already-ready AVFoundation race for tune and recovery and repair the dangling Xcode test reference.

Purpose: CR-02 leaves a normal cached/rapid item installed but permanently idle. The same path breaks recovery. This plan consumes initial readiness only after exact installation and keeps stale callbacks inert.
Output: Initial-status observation, pending-ready staging, production-shaped ordering tests, and a consistent test project group.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-REVIEW.md
@.planning/phases/02-authorized-live-listening/02-VERIFICATION.md
@.planning/phases/02-authorized-live-listening/02-09-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-14-SUMMARY.md
@SiriusMac/Listening/PlaybackCoordinator.swift
@SiriusMacTests/PlaybackInstallationOrderTests.swift

<interfaces>
- `PlaybackPlayerRuntime.observe` may invoke `onReady` synchronously before it returns; the coordinator must treat that as a real early-ready event.
- `PlaybackCoordinator.observeAndInstall` is shared by initial tune, live-edge resume, and `installRecoveredItem`.
- `isCurrentItem` currently requires the installed generation and observation identity for actionable callbacks; preserve those guards and add a pending pre-install readiness state.
- `AVFoundationItemObservation` must observe the item's initial status as well as later transitions, while its `deliveredReady` flag prevents duplicate readiness delivery.
</interfaces>
</context>

## Artifacts this phase produces

- One shared initial-ready/install/play state machine for initial, resume, and recovery items.
- Deterministic synchronous-observer tests that do not load media.
- A corrected Xcode group reference for the already-built metadata test source.
- No new player, playback engine, provider operation, media URL access, or optimistic state.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-16-01: Stage an already-ready item until exact installation</name>
  <reversibility rating="costly">The ordering is private but central to all tune/resume/recovery playback paths and stale-callback safety.</reversibility>
  <read_first>
    - `02-REVIEW.md` CR-02 and `02-VERIFICATION.md` first/third gaps — exact initial-ready and recovery failure.
    - `PlaybackCoordinator.swift` `AVFoundationItemObservation`, `observeAndInstall`, `requestPlayForReadyItem`, and `isCurrentItem`.
    - `PlaybackInstallationOrderTests.swift` install-gated runtime — extend it to fire ready synchronously inside `observe` before returning.
    - `02-CONTEXT.md` D-05 through D-08 — one player, confirmed state, same-channel bounded recovery, and cancellation.
  </read_first>
  <files>SiriusMac/Listening/PlaybackCoordinator.swift, SiriusMacTests/PlaybackInstallationOrderTests.swift</files>
  <behavior>
    - Initial tune with synchronous ready during observation records observed, installed, then exactly one play request; no play occurs before install.
    - Live-edge resume follows the same order and re-resolves exactly once.
    - Recovery with synchronous initial ready installs and requests play once while preserving the incident until confirmed playing.
    - A pending early-ready callback from a cancelled/superseded observation is discarded and cannot affect the new item.
  </behavior>
  <action>Add runtime regressions that invoke the ready callback synchronously inside `observe` before returning the observation handle. Update the production KVO to request both initial and subsequent item status. In `observeAndInstall`, establish the new observation identity before subscribing and stage a matching ready signal when its item is not yet installed. After `runtime.install(item)` completes, consume the staged signal once through the existing generation/channel/observation/incident guard and request play. Clear pending readiness in every invalidation, pause-before-ready, stop, switch, session-end, and failed-observation path. Keep `.playing` publication exclusive to confirmed player callbacks and use the same path for recovery.</action>
  <acceptance_criteria>
    - Synchronous initial-ready events request play exactly once after install in tune, resume, and recovery tests.
    - No test event log contains play-requested before installed.
    - Duplicate initial/new ready delivery remains idempotent.
    - Stale, cancelled, and superseded callbacks remain inert.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/PlaybackInstallationOrderTests CODE_SIGNING_ALLOWED=NO</automated>
  </verify>
  <done>Initial and recovered items start reliably even when AVFoundation reports ready before observer installation returns.</done>
</task>

<task type="auto">
  <name>Task 02-16-02: Repair the MetadataPresentationTests project reference</name>
  <reversibility rating="reversible">This is a one-line Xcode group reference correction with no source or target membership change.</reversibility>
  <read_first>
    - `02-REVIEW.md` WR-01 — exact dangling group child and declared 24-character file-reference ID.
    - `SiriusMac.xcodeproj/project.pbxproj` PBXFileReference, SiriusMacTests PBXGroup, and PBXSourcesBuildPhase entries for MetadataPresentationTests.
  </read_first>
  <files>SiriusMac.xcodeproj/project.pbxproj</files>
  <action>Replace only the dangling SiriusMacTests group child with the already-declared MetadataPresentationTests PBXFileReference identifier. Preserve the existing PBXBuildFile, test build-phase membership, E4/E1 project/target graph, product references, and every other object identifier. Do not regenerate the project or reorder unrelated entries.</action>
  <acceptance_criteria>
    - The group child identifier exactly matches the declared MetadataPresentationTests file reference.
    - The existing test build file still points to the same declared file reference.
    - Project lint/list succeeds with no dangling-reference warning.
  </acceptance_criteria>
  <verify>
    <automated>plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -list</automated>
  </verify>
  <done>Xcode resolves MetadataPresentationTests consistently in both the group and test build phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| AVPlayerItem KVO → coordinator installation | Early callbacks must not bypass item identity and installation ordering. |
| Old observation/recovery → current player | Superseded callbacks must not request playback on the current single player. |
| Project group → test source membership | A dangling visual reference must not conceal which test source is built. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-16-01 | Tampering | early readiness callback | high | mitigate | Stage by exact observation/generation and consume only after matching install. |
| T-02-16-02 | Denial of Service | initial/recovered playback | high | mitigate | Observe initial status and test synchronous ready for tune/resume/recovery. |
| T-02-16-03 | Spoofing | stale observation identity | high | mitigate | Preserve channel, generation, observation, and recovery-incident guards. |
| T-02-16-04 | Information Disclosure | media runtime tests | high | mitigate | Fake handoff/runtime only; no media URL, headers, key, provider, or AVAsset load. |
| T-02-16-05 | Repudiation | Xcode test graph | low | accept | Exact declared identifier plus project lint/list makes membership inspectable; no runtime security boundary changes. |
</threat_model>

<verification>
- The no-host auth matrix must pass before the focused Xcode test host is allowed to run.
- Playback tests use the injected fake resolver/runtime and never instantiate production auth, provider transport, media handoff, or Keychain.
- Do not claim live audibility; that remains a bounded UAT observation.
</verification>

<success_criteria>
- CR-02 and its recovery consequence are closed by production-shaped tests.
- PLAY-01/02/03 no longer fail on already-ready item timing.
- WR-01 is removed without project regeneration.
- No live app or provider action occurs in this autonomous plan.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-16-SUMMARY.md` when done.
</output>
