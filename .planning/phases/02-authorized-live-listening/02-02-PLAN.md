---
phase: 02-authorized-live-listening
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - script/live_compatibility_checkpoint.sh
  - SiriusMac/Listening/LiveContractObservation.swift
  - SiriusMacTests/ListeningCompositionTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - .planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md
  - .planning/phases/02-authorized-live-listening/COVERAGE.md
  - .planning/phases/02-authorized-live-listening/02-RESEARCH.md
autonomous: false
requirements: [CAT-01, CAT-02, PLAY-01, PLAY-04, META-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Task 02-02-02 is the first Phase 02 task allowed to contact or observe live SiriusXM content beyond the established Phase 01 authentication/entitlement sequence."
    - "Pre-checkpoint setup is provider-agnostic and can only launch the existing authenticated app, enforce one-run/stop rules, and accept allow-listed semantic evidence; it contains no provider operation, endpoint, field, resource, or real content call."
    - "The owner runs one bounded catalog observation and one owner-selected tune through the existing authenticated app session without another login, automated credential action, or repeated attempt."
    - "A `SUPPORTED` result records only implementation-required fixed request facts, semantic schema/cardinality classifications, AVFoundation behavior, and closed failure/protection status in 02-LIVE-CONTRACT.md; no raw evidence is retained."
    - "Any unsupported, ambiguous, protected, unauthorized, rate-limited, or secret-bearing condition produces a typed `UNSUPPORTED` outcome and halts Phase 02 provider-dependent execution before Plan 02-03."
  artifacts:
    - path: "script/live_compatibility_checkpoint.sh"
      provides: "Safe single-run launcher and deterministic preflight with no provider contract"
      contains: "live compatibility checkpoint"
    - path: "SiriusMac/Listening/LiveContractObservation.swift"
      provides: "Closed sanitized evidence/result vocabulary with no raw-data sink"
      contains: "LiveContractObservation"
    - path: ".planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md"
      provides: "Canonical sanitized provider-capability and handoff evidence used by all downstream plans"
      contains: "Gate Result"
  key_links:
    - from: "script/live_compatibility_checkpoint.sh"
      to: "script/build_and_run.sh --telemetry"
      via: "offline tests and session-availability checks complete before one owner-visible launch"
      pattern: "build_and_run.sh"
    - from: "LiveContractObservation"
      to: "02-LIVE-CONTRACT.md"
      via: "only closed support, request-contract, semantic-shape, protection, and AVFoundation classifications may become durable"
      pattern: "SUPPORTED|NOT REQUIRED|UNSUPPORTED"
    - from: "02-LIVE-CONTRACT.md"
      to: "Plan 02-03"
      via: "a top-level SUPPORTED gate is the required precondition for fixed provider code"
      pattern: "Gate Result: SUPPORTED"
  prohibitions:
    - statement: "The checkpoint must never retain raw requests/responses, credentials, account identifiers, cookies, headers, media/resource/key locations or values, HAR/browser storage, AVFoundation access/error logs, or secret-bearing errors."
      status: unverified
      flagged: true
    - statement: "The checkpoint must never continue after a protected-control, authorization, rate-limit, unknown-host/redirect, DRM ambiguity, or unsupported-contract result."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Resolve the live-provider and AVFoundation uncertainty through the first and only bounded owner-visible compatibility checkpoint.

Purpose: Replace every provider-dependent planning assumption with sanitized evidence or a typed unsupported stop before any fixed content operation enters production code.
Output: Provider-neutral checkpoint tooling, one bounded authenticated run, canonical 02-LIVE-CONTRACT.md, refined COVERAGE.md, and explicit research-question status.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-RESEARCH.md
@.planning/phases/02-authorized-live-listening/02-VALIDATION.md
@.planning/phases/02-authorized-live-listening/COVERAGE.md
@.planning/phases/02-authorized-live-listening/02-01-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-UAT.md
@script/build_and_run.sh

<interfaces>
- Plan 02-01 supplies all four offline Wave 0 suites plus semantic result/fake seams and proves that production cannot perform Phase 02 content work.
- Phase 01 supplies the sole user-operated authentication flow, current entitlement authority, ephemeral session, cleanup, and closed telemetry; this plan must reuse it and must not request or automate another login.
- `COVERAGE.md` enumerates the only content capabilities this checkpoint may classify. It is not an exhaustive provider API inventory.
- `02-LIVE-CONTRACT.md` does not exist before this run. The checkpoint creates it only after the live attempt, using the fixed sanitized schema described below.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `ListeningCompositionTests.swift` and `project.pbxproj` — prove/register the provider-neutral observation sink and single-run checkpoint setup without adding provider code.
- `COVERAGE.md` — replaces pending rows only with `SUPPORTED`, `NOT REQUIRED`, or `UNSUPPORTED` from the bounded run.
- `02-RESEARCH.md` — replaces each of the three Open Questions with a sanitized resolved/unsupported status and a link to the canonical contract artifact; it retains no raw evidence.

### New files and symbols

- `live_compatibility_checkpoint.sh` — preflight/launch wrapper with no provider contract or authentication automation.
- `LiveContractObservation.swift` — closed evidence structures and a sink that cannot accept raw request/response/resource/error material.
- `02-LIVE-CONTRACT.md` — sanitized gate result, supported operation-purpose facts, semantic shapes, AVFoundation result, and chosen-handoff evidence inputs.

### Public API changes

- None. The checkpoint records facts; Plan 02-03 decides and implements the reversible app-integration handoff from `SUPPORTED` evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-02-01: Build the provider-neutral checkpoint shell and sanitized evidence sink</name>
  <reversibility rating="reversible">The launcher and closed observation vocabulary are temporary compatibility tooling and can change without affecting the client API.</reversibility>
  <read_first>
    - `02-01-SUMMARY.md` — assert all Wave 0 suites and the static no-provider-work check pass before adding the shell.
    - `script/build_and_run.sh` — wrap the existing telemetry launch without adding authentication or provider request behavior.
    - `SafeDiagnosticEvent.swift` — follow the closed enum/no-payload-sink pattern.
    - `02-VALIDATION.md` Manual-Only Verifications — encode every preflight and stop rule.
    - `COVERAGE.md` — model only its existing pending capability rows; do not expand provider scope.
  </read_first>
  <files>script/live_compatibility_checkpoint.sh, SiriusMac/Listening/LiveContractObservation.swift, SiriusMacTests/ListeningCompositionTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: the checkpoint launcher runs package/app suites first, accepts one explicit owner launch, and has no credential entry, login retry, provider request, endpoint, or operation configuration.
    - Test 2: `LiveContractObservation` accepts only capability, support disposition, fixed request-contract facts needed for later exact code, semantic type/cardinality shape, closed failure/protection class, and AVFoundation audible/transport classification.
    - Test 3: observation types have no initializer or property for raw body, request/response object, token, cookie, authorization header, account ID, resource/key location or value, raw Error, or AVFoundation log.
    - Test 4: repeated begin, cancellation, sign-out, or terminal observation closes the single run and rejects later observations.
  </behavior>
  <action>Create a shell that runs both deterministic suites and then invokes the established telemetry launcher exactly once after explicit owner confirmation. It must not construct or configure content requests, discover endpoints, inspect browser storage, automate login, or perform any live action during this task. Add closed `LiveContractObservation` value types and a single-run sink whose accepted fields are support disposition, operation purpose, fixed method/authorized-host-policy/path-template facts required to implement an exact contract, invented semantic shape aliases with type/cardinality, closed protection/failure class, and AVFoundation behavioral result. Values that could carry raw traffic, credentials, account data, media/key locations, response content, or free-form errors must be impossible to submit. Extend the offline composition tests for single-run close/reject behavior and register the source once.</action>
  <acceptance_criteria>
    - Setup completes without a live SiriusXM content request or provider-contract edit.
    - The shell cannot trigger login, select a channel, build a request, or repeat a run.
    - The evidence sink is structurally unable to retain raw or secret-bearing evidence.
    - All Wave 0 and existing Phase 01 tests remain green before the checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' &amp;&amp; bash -n script/live_compatibility_checkpoint.sh</automated>
  </verify>
  <done>The owner-visible run has a deterministic, single-use, provider-neutral shell and a semantic evidence sink, while no Phase 02 live content access has occurred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 02-02-02: Run the first bounded authenticated catalog-to-AVFoundation investigation</name>
  <precondition>Plan 02-01 suites and Task 02-02-01 automated verification are green, and the app reports the existing Phase 01 session as authenticated and entitled without requesting a new login.</precondition>
  <read_first>
    - `02-VALIDATION.md` Manual-Only Verifications — follow exactly one catalog observation, one owner-selected tune, and every stop rule.
    - `COVERAGE.md` — classify only the listed capability rows and do not claim provider-exhaustive coverage.
    - `LiveContractObservation.swift` — emit only the closed evidence fields its API permits.
    - `02-RESEARCH.md` Open Questions 1-3 — the checkpoint must resolve each question to supported, unsupported, or still ambiguous/unsupported.
    - `.planning/phases/00-authentication-feasibility-gate/.continue-here.md` — historical feasibility is context only, never a current endpoint/schema assertion.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md, .planning/phases/02-authorized-live-listening/COVERAGE.md, .planning/phases/02-authorized-live-listening/02-RESEARCH.md</files>
  <action>Run `script/live_compatibility_checkpoint.sh` and reuse only the already-authenticated, entitled app session. This task is the first Phase 02 point permitted to contact or observe live SiriusXM content. Perform one owner-visible catalog observation and one tune of a channel the owner explicitly selects. During this single run, bounded transient in-memory inspection may correlate first-party public application behavior with request method, authorized host policy, path template, semantic response shape/cardinality, tune/resource/key requirements, metadata/artwork availability, and AVFoundation behavior. Collapse each observation immediately through `LiveContractObservation`; do not write or display the underlying traffic. Use a `mktemp -d` run directory if transient tooling needs scratch space, keep it outside the repository, and remove it when the run ends.

Stop immediately without a follow-up request on a new-login requirement, unknown redirect/host, CAPTCHA, MFA, 403, 429, rate-limit/bot/control signal, authorization or entitlement loss, malformed/unknown contract, DRM/access-control ambiguity, unsupported media handoff, or any need for spoofing, bypass, raw durable capture, or secret-bearing output. Pause/resume/stop is observed only after one authorized resource reaches AVFoundation; resume is evaluated as current-live-edge behavior per D-04, not time shifting.

After teardown, create `02-LIVE-CONTRACT.md` with a top-level `Gate Result: SUPPORTED` only if catalog, one selected tune/resource, AVFoundation, and the minimum metadata contract all have sufficient sanitized evidence. Otherwise write `Gate Result: UNSUPPORTED`, the first closed failure domain, and `Execution: HALT`; do not approve Plan 02-03. For a supported result, record only fixed implementation-required request facts, invented semantic aliases/types/cardinalities, whether key authorization is required, the safe-handoff evidence constraints, and owner-confirmed audible/live-edge/stop behavior. Update the existing `COVERAGE.md` rows without adding capabilities. Update all three `02-RESEARCH.md` Open Questions to resolved-supported or resolved-unsupported and link them to `02-LIVE-CONTRACT.md`; never paste raw evidence.</action>
  <what-built>A provider-neutral checkpoint shell and semantic sink that can conduct one bounded owner-authorized live investigation without a raw evidence channel.</what-built>
  <how-to-verify>
    1. Confirm both automated suites are green and the app reports the existing authenticated/entitled session; stop if it asks for login or session transfer.
    2. Launch the single-use checkpoint and permit exactly one catalog observation.
    3. Select one channel yourself and permit exactly one tune/resource attempt; observe audibility, pause silence, current-live-edge resume, stop/clear, and best available metadata.
    4. Stop on the first listed protected, ambiguous, unauthorized, rate-limited, unknown-contract, or secret-bearing condition.
    5. Confirm the player item and transient run directory are cleared, then inspect only the sanitized `02-LIVE-CONTRACT.md`, `COVERAGE.md`, and research statuses.
    6. Approve only if the canonical artifact says `Gate Result: SUPPORTED` and contains no raw evidence; otherwise confirm `Execution: HALT`.
  </how-to-verify>
  <acceptance_criteria>
    - Exactly one catalog observation and one owner-selected tune occur through the existing session, with no login automation, repeat, channel switch, or fallback engine.
    - All three Research Open Questions receive a sanitized resolved-supported or resolved-unsupported status.
    - `02-LIVE-CONTRACT.md` is canonical, non-exhaustive, contains only sanitized implementation facts, and drives every later provider decision.
    - Unsupported/ambiguous/protected outcomes publish a typed unsupported result and halt every provider-dependent later plan.
    - A supported result confirms the minimum catalog/tune/resource/AVFoundation/metadata path and safe-handoff constraints without retaining raw evidence.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md</automated>
    <human-check>Owner confirms one bounded existing-session run produced either a sanitized SUPPORTED contract plus audible/live-edge/stop evidence, or the first closed UNSUPPORTED stop with no follow-up request.</human-check>
  </verify>
  <resume-signal>Type `approved` only after the sanitized artifact is written and the live item/transient directory are cleared, or report the first closed unsupported stop. Do not paste provider, account, request, response, resource, key, or error details.</resume-signal>
  <done>The live gate is resolved from one bounded owner-visible run; supported sanitized evidence can proceed to Plan 02-03, while unsupported or ambiguous evidence halts provider-dependent execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Existing authenticated app session → first content investigation | One owner action crosses from settled authorization into live content work without another login. |
| Transient live traffic → semantic observation sink | Raw provider/account/resource material must collapse in memory to allow-listed facts and never become durable. |
| Sanitized live contract → downstream implementation | Only a supported, non-exhaustive artifact may authorize fixed content code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure | transient observation/artifacts | high | mitigate | Closed sink, no raw display/write, temporary directory teardown, semantic artifact scan, and no free-form errors. |
| T-02-02 | Information Disclosure / Tampering | unexpected host/redirect | high | mitigate | Existing direct-host/session policy, unknown-destination stop, and no follow-up request. |
| T-02-03 | Denial of Service / Repudiation | live activity | high | mitigate | One run, one catalog observation, one owner-selected tune, no retry/login automation, and terminal 403/429/control stop. |
| T-02-04 | Elevation of Privilege | stale catalog/session | high | mitigate | Existing current authenticated/entitled session is a precondition; cached presence cannot satisfy tune authorization. |
| T-02-05 | Tampering | provider contract ambiguity | high | mitigate | Ambiguity becomes typed unsupported plus execution halt; no inferred operation or handoff. |
</threat_model>

<verification>
- Plan 02-01 and Task 02-02-01 complete entirely offline before the first live content action.
- Exactly one bounded live run creates the canonical sanitized contract or an unsupported halt.
- All three research questions and every pending coverage row receive a closed status without provider-exhaustive claims.
</verification>

<success_criteria>
- No provider-dependent production code exists before the checkpoint.
- Every downstream plan has a single canonical supported/unsupported evidence gate.
- Unsupported or protected behavior stops safely; supported behavior records enough sanitized fixed-contract facts for deterministic implementation.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-02-SUMMARY.md` when done.
</output>
