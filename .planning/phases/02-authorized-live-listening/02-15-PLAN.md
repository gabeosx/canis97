---
phase: 02-authorized-live-listening
plan: "15"
type: execute
wave: 11
depends_on: ["02-11"]
files_modified:
  - script/lib/single_instance_launcher.sh
  - script/build_and_run.sh
  - script/tests/build_and_run_tests.sh
autonomous: true
gap_closure: true
requirements: [PLAY-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two concurrent launch invocations can open the exact built SiriusMac bundle at most once."
    - "The launcher waits for zero old SiriusMac processes; if they do not drain, it opens nothing and closes every copy before returning failure."
    - "After launch, exactly one PID exists and its executable path equals the exact just-built binary; any mismatch closes all copies and fails."
    - "Telemetry streaming is started and confirmed alive before launch, so automatic restore and every fixed auth stage can be observed."
    - "Build-only never kills, opens, or launches SiriusMac."
  artifacts:
    - path: "script/lib/single_instance_launcher.sh"
      provides: "Atomic lock, drain, exact launch, and invariant enforcement"
      contains: "acquire_launch_lock"
    - path: "script/build_and_run.sh"
      provides: "Single supported build/run entry point"
      contains: "single_instance_launcher"
    - path: "script/tests/build_and_run_tests.sh"
      provides: "Fake-process concurrent launcher matrix"
      contains: "concurrent"
  key_links:
    - from: "script/build_and_run.sh"
      to: "script/lib/single_instance_launcher.sh"
      via: "all launch-capable modes use one locked exact-path protocol"
      pattern: "single_instance"
    - from: "telemetry process"
      to: "exact app launch"
      via: "stream starts and is checked before the one launch call"
      pattern: "telemetry"
  prohibitions:
    - statement: "No launch mode may use `open -n`, accept two or more matching processes, or treat any same-name executable path as the intended build."
      status: unverified
      flagged: true
    - statement: "Launcher tests must use fake process/open/log commands and must never start or kill a real SiriusMac process."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Replace duplicate-prone launching with one atomic, exact-binary, telemetry-first protocol.

Purpose: Earlier UAT started multiple app copies from different build roots, making authentication state and Keychain identity impossible to reason about. This plan makes one process a mechanically enforced invariant.
Output: Testable shell launcher library, corrected build/run script, and a no-launch fake-process matrix.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-UAT.md
@.planning/debug/token-validity-before-resignin.md
@script/build_and_run.sh

<interfaces>
- `script/build_and_run.sh` builds exactly `/tmp/sirius-mac-derived-data/Build/Products/Debug/SiriusMac.app` and knows its exact executable path.
- Launch-capable modes are `run`, `--logs`, `--telemetry`, `--verify`, and debugger execution. Build-only is a pure build path.
- The helper must accept explicit command hooks/paths for shell tests; production passes system `pgrep`, `pkill`, `ps`/path inspection, `open`, and `log` commands. No test-only branch may enter app code.
</interfaces>
</context>

## Artifacts this phase produces

- One atomic lock spanning drain/build/launch verification for launch-capable invocations.
- Exact zero-before/one-after process and executable-path assertions.
- Telemetry-before-launch ordering for bounded UAT.
- A fake-process launcher suite that never opens or kills the real app.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-15-01: Prove two concurrent invocations open the exact bundle once</name>
  <reversibility rating="reversible">The launcher helper is a local development/UAT script and does not change app runtime architecture.</reversibility>
  <read_first>
    - `build_and_run.sh` — current pre-build `pkill`, `open -n`, and count-at-least-one behavior.
    - Locked user intent — atomic lock, zero-old wait, exact one PID/path, close-all-on-failure, and build-only no-launch are non-negotiable.
    - `02-UAT.md` — prior live evidence came from an ambiguous launch environment and must not be repeated.
  </read_first>
  <files>script/lib/single_instance_launcher.sh, script/tests/build_and_run_tests.sh</files>
  <behavior>
    - Concurrent invocations contend on one atomic directory lock; exactly one fake open occurs and the loser exits without opening.
    - A fake old process that drains allows one open only after the zero observation.
    - A fake old process that does not drain produces zero opens, invokes close-all, and exits nonzero.
    - Post-launch zero, two, or wrong-executable-path states invoke close-all and exit nonzero; exact one PID/path succeeds.
    - Build-only path produces zero close, open, log, or path-inspection calls.
  </behavior>
  <action>Create a sourceable shell helper with explicit arguments for lock path, process name, app bundle, exact binary, drain deadline, and command hooks. Acquire the lock with atomic directory creation and release it through a trap. For launch-capable work, request termination of existing same-name processes, poll to exact zero with a finite deadline, and abort without launch if zero is not reached. Invoke the app bundle exactly once without a new-instance flag, then poll to exactly one PID and compare its resolved executable path byte-for-byte with the built binary. On any invariant failure, terminate all same-name copies, wait for zero, and return nonzero. Write fake command/state tests first; they must operate entirely inside a temporary directory and never invoke system process or launch commands.</action>
  <acceptance_criteria>
    - The concurrent fake schedule records one open total.
    - Every pre/post count and path failure records close-all and zero surviving fake processes.
    - Lock cleanup occurs on success, error, and interruption.
    - The test file contains a guard that fails if a real process/open command is reached.
  </acceptance_criteria>
  <verify>
    <automated>bash script/tests/build_and_run_tests.sh --helper-only</automated>
  </verify>
  <done>The launch protocol itself enforces zero old processes and one exact new process under concurrent invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-15-02: Route every run mode through telemetry-first single-instance launch</name>
  <reversibility rating="reversible">All behavior remains inside the repository's development launcher and can be changed without app migration.</reversibility>
  <read_first>
    - `script/lib/single_instance_launcher.sh` from Task 1 — use it rather than duplicating process logic.
    - `build_and_run.sh` mode switch — preserve build and supported mode names while changing launch ordering.
    - Locked user intent — start the closed-label telemetry stream before launch and leave at most the intended app open.
  </read_first>
  <files>script/build_and_run.sh, script/tests/build_and_run_tests.sh</files>
  <behavior>
    - Run/logs/telemetry/verify modes all acquire the same lock and use the same exact launch invariant.
    - Telemetry mode starts the narrow authentication/client predicate, verifies the stream process is alive, then performs the single launch.
    - Build-only performs the build and executable existence check with no process cleanup or launch.
    - No supported mode contains a new-instance launch flag or an at-least-one process assertion.
  </behavior>
  <action>Source the helper from `build_and_run.sh` and move every launch-capable mode onto it. Acquire the lock before any process cleanup and hold it through build, launch, and verification so simultaneous script invocations cannot both open. In telemetry mode, start only the fixed authentication/client log predicate before launch, retain its PID for cleanup when the script exits, verify it has not exited, and then call the one launcher path. Keep build-only isolated from all process/log/launch work. Preserve the exact derived-data bundle and binary paths and print the chosen bundle only after a successful build. Extend fake tests to assert cross-mode ordering and call counts; run a real build-only check only after fake tests pass.</action>
  <acceptance_criteria>
    - Every launch-capable mode shares the atomic helper and exact-path assertion.
    - Telemetry event order is stream-start, stream-alive, terminate-old, zero-old, open-once, one-exact-binary.
    - Build-only launches and kills nothing.
    - Failure leaves zero SiriusMac copies; success leaves exactly the intended copy.
  </acceptance_criteria>
  <verify>
    <automated>bash script/tests/build_and_run_tests.sh &amp;&amp; ./script/build_and_run.sh --build-only</automated>
  </verify>
  <done>The repository has one supported, telemetry-first launcher that cannot intentionally create duplicate SiriusMac instances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent shell invocation → LaunchServices | Multiple operators/scripts must not create multiple app processes. |
| Built bundle path → running PID | Same process name or bundle identifier is insufficient; executable identity must match. |
| Automatic restore → telemetry stream | Restore happens at launch, so telemetry must already be listening. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-15-01 | Spoofing | running app identity | high | mitigate | Resolve the sole PID executable and compare to the exact built binary. |
| T-02-15-02 | Denial of Service | duplicate app instances | high | mitigate | Atomic lock, exact zero-before/one-after counts, and close-all on invariant failure. |
| T-02-15-03 | Repudiation | automatic restore trace | high | mitigate | Start and verify narrow fixed-label telemetry before launch. |
| T-02-15-04 | Tampering | launcher tests | medium | mitigate | Fake command hooks in a temporary state directory; guard against real process/launch calls. |
| T-02-15-05 | Information Disclosure | telemetry | high | mitigate | Keep the predicate restricted to closed authentication/client categories delivered by 02-12/13. |
</threat_model>

<verification>
- Run fake launcher tests before build-only. Neither command may launch SiriusMac.
- Static shell syntax checking and fake call logs must cover every supported mode.
- Do not execute run, telemetry, logs, debug, or verify mode during this autonomous plan.
</verification>

<success_criteria>
- Concurrent launch invocations open once.
- Non-draining, wrong-count, and wrong-path states open zero or close all and fail.
- Build-only never launches.
- The later UAT can begin telemetry before a single exact app process.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-15-SUMMARY.md` when done.
</output>
