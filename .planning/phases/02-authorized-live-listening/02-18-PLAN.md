---
phase: 02-authorized-live-listening
plan: "18"
type: execute
wave: 16
depends_on: ["02-16", "02-17"]
files_modified:
  - .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md
autonomous: false
gap_closure: true
requirements: [CAT-01, CAT-02, CAT-03, PLAY-01, PLAY-02, PLAY-03, PLAY-04, META-01, META-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Successful 02-AUTH-UAT.md and green playback Gate 5 are hard prerequisites; no WebView sign-in or credential handoff is permitted in this plan."
    - "The offline preflight wraps every Xcode invocation with exact zero SiriusMac processes before and after, closes leaks, preserves Keychain/browser state, and never launches the production app."
    - "Incremental Gate 6 requests separate owner authorization, launches one exact newly built bundle once, and requires automatic restore-completed before any catalog or listening action."
    - "Missing, invalid, unavailable, rejected, or incomplete restore—or any WebView appearance—writes one fixed blocked stage and HALTS without another login."
    - "After restore only, one catalog refresh, one owner-selected entitled channel tune, natural start/pause/live-edge resume/stop, and current text/artwork observation are bounded and sequential."
    - "No forced recovery, expiry, sleep/wake, network interruption, stall, challenge, retry, cleanup action, or second launch occurs; at most the one exact intended app remains open."
  artifacts:
    - path: ".planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md"
      provides: "Sanitized automatic-restore and bounded-listening evidence"
      contains: "Restore and Listening Checkpoint"
  key_links:
    - from: ".planning/phases/02-authorized-live-listening/02-AUTH-UAT.md"
      to: "automatic restore checkpoint"
      via: "successful durable credential established by Plan 02-17"
      pattern: "status: passed"
    - from: "every Xcode preflight invocation"
      to: "script/lib/single_instance_launcher.sh --guard-app-host"
      via: "zero-before/zero-after inert host wrapper with close-all-on-leak"
      pattern: "guard-app-host"
    - from: "script/build_and_run.sh --telemetry"
      to: "automatic Keychain restoration"
      via: "one exact newly built binary with telemetry alive before launch"
      pattern: "restore-completed"
    - from: "restore-completed"
      to: "catalog → selection → tune → controls → metadata"
      via: "single bounded listening path unlocked only by successful restore"
      pattern: "catalog-refresh|tune|playing|paused|stopped"
  prohibitions:
    - statement: "Never open or operate a WebView sign-in, transfer another credential, retry authentication, invoke Sign Out/Clear Local Session, or retrieve secret Keychain data."
      status: unverified
      flagged: true
    - statement: "Never run an Xcode invocation outside the zero-process guard or launch the production app during offline preflight."
      status: unverified
      flagged: true
    - statement: "Never force recovery/expiry/failure conditions, vary provider requests, capture traffic, inspect secrets, or leave duplicate/wrong-binary processes."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Prove durable restoration and the repaired listening path in a separate owner-authorized checkpoint without another login.

Purpose: Authentication success is valuable only if the exact next build restores it and reaches real listening. Separating this checkpoint prevents a playback defect from being confused with sign-in and guarantees each stage advances only on prior evidence.
Output: `02-UAT-RECHECK.md` containing sanitized preflight, automatic-restore, catalog, listening-control, and current-metadata outcomes while preserving the original UAT and auth checkpoint.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-UAT.md
@.planning/phases/02-authorized-live-listening/02-AUTH-UAT.md
@.planning/phases/02-authorized-live-listening/02-17-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-16-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-15-SUMMARY.md
@script/build_and_run.sh

<interfaces>
- `02-AUTH-UAT.md` must have `status: passed` with authentication, entitlement, persistence, and item-existence completion; a blocked artifact prohibits this plan.
- `02-16-SUMMARY.md` is Gate 5 evidence that the already-ready playback race is fixed and tested without production launch.
- `script/lib/single_instance_launcher.sh --guard-app-host -- <command>` owns exact zero before/after every Xcode preflight command and always closes leaks.
- `script/build_and_run.sh --telemetry` is the sole production launcher; it must build and open one exact bundle without a new-instance flag.
- Automatic restoration is the only allowed authentication path. WebView presentation or any restore failure is terminal and cannot route to login.
- `02-UAT.md` and `02-AUTH-UAT.md` remain unchanged. This plan writes only `02-UAT-RECHECK.md`.
</interfaces>
</context>

## Artifacts this phase produces

- A guarded integrated offline preflight with zero processes before/after every Xcode invocation.
- A distinct Gate 6 restoration/listening artifact referencing the successful authentication checkpoint.
- One bounded real catalog/listening/metadata observation after automatic restore.
- A precise blocked restore or listening stage with no second login when success is not reached.

## Source Coverage Audit

| Source | ID | Focus | Coverage |
|---|---|---|---|
| GOAL | — | Find entitled channels and reliably listen to one live stream with clear state/current metadata | Gate 6 bounded restore/listening path |
| REQ | CAT-01, CAT-02 | Entitled linear lineup, identity, presentation, freshness | One refresh and visible selected row after restore |
| REQ | CAT-03, PLAY-04 | Distinct auth/catalog/resolution/playback failures | Fixed stage stop; no cached authorization inference |
| REQ | PLAY-01, PLAY-02 | One coordinator supports tune/start/pause/live-edge resume/stop | One bounded control sequence |
| REQ | PLAY-03 | Bounded recovery remains cancellation-aware | Offline Gate 5; no live fault induction |
| REQ | META-01, META-02 | Current text/artwork and independent stale semantics | One current observation; deterministic expiry suite in preflight |
| RESEARCH | — | AVFoundation behavior is empirical; provider operations remain fixed | Natural start observation only; no fallback engine/request variation |
| CONTEXT | D-01–D-06 | Entitled catalog, live-edge controls, one confirmed player | One refresh/selection/tune/control sequence |
| CONTEXT | D-07–D-09 | Bounded distinct recovery/failures | Offline regressions and immediate fixed-stage stop |
| CONTEXT | D-10–D-12 | Independent current/stale metadata | Current live observation plus existing deterministic suite |

<tasks>

<task type="auto">
  <name>Task 02-18-01: Prove the integrated offline path with zero app-host leakage</name>
  <reversibility rating="reversible">This task runs inert verification and writes only sanitized preflight results while preserving the durable credential.</reversibility>
  <precondition>`02-AUTH-UAT.md` has `status: passed`, and `02-16-SUMMARY.md` records Incremental Gate 5 as green.</precondition>
  <read_first>
    - `02-AUTH-UAT.md` — assert complete durable authentication and do not alter it.
    - `02-16-SUMMARY.md` — assert the playback race, project reference, build-only, and zero-process gate passed.
    - `02-15-SUMMARY.md` — use the exact app-host guard and launcher contracts.
    - `02-UAT.md` — preserve the original blocked evidence unchanged.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</files>
  <action>Assert the successful auth artifact and all prerequisite summaries. Run the no-host authentication/package matrix, fake launcher matrix, and package suite first. Before each focused Xcode authentication/WebView/playback/metadata app-host invocation, use the Plan 02-15 `--guard-app-host` wrapper: close any SiriusMac process without app UI actions, prove exact zero, run the inert test host, then always prove exact zero afterward. If a host leaks, close all copies, record preflight failed, and halt. Run `plutil` project lint outside the guard because it cannot launch an app, run `xcodebuild -list` inside the guard, and finish with build-only. Never use a launch-capable mode, Sign Out/Clear Local Session, Keychain read/erase, WebView, or provider request. Create `02-UAT-RECHECK.md` with fixed PASS/FAIL rows, a reference to `02-AUTH-UAT.md`, and `status: awaiting_owner_authorization` only after all checks pass.</action>
  <acceptance_criteria>
    - Successful Gate 4 and Gate 5 artifacts are asserted before any command.
    - No-host/package and fake launcher gates pass before the guarded app-host matrix.
    - Every Xcode app-host invocation, including `xcodebuild -list`, starts and ends at exact zero; `plutil` lint may run outside the guard because it cannot launch an app, while leaks are closed and halt the plan.
    - Build-only launches nothing, and durable Keychain/browser state remains untouched.
    - Preflight failure cannot reach the owner checkpoint or a launch-capable command.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^status: passed$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-16-SUMMARY.md &amp;&amp; bash script/test_offline_auth_matrix.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient &amp;&amp; bash script/tests/build_and_run_tests.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer bash script/lib/single_instance_launcher.sh --guard-app-host -- xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests -only-testing:SiriusMacTests/WebAuthenticationBridgeTests -only-testing:SiriusMacTests/PlaybackInstallationOrderTests -only-testing:SiriusMacTests/MetadataPresentationTests CODE_SIGNING_ALLOWED=NO &amp;&amp; plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer bash script/lib/single_instance_launcher.sh --guard-app-host -- xcodebuild -project SiriusMac.xcodeproj -list &amp;&amp; ./script/build_and_run.sh --build-only</automated>
  </verify>
  <done>Integrated offline evidence is green, all test hosts returned to zero, durable auth is untouched, and Gate 6 awaits separate authorization.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 02-18-02: Authorize one automatic-restore and bounded-listening checkpoint</name>
  <reversibility rating="reversible">The checkpoint launches one exact build and performs ordinary bounded listening controls without changing credentials or inducing faults.</reversibility>
  <read_first>
    - `02-UAT-RECHECK.md` — preflight must be PASS and awaiting separate authorization.
    - `02-AUTH-UAT.md` — the durable credential must already be proven; no WebView action is allowed.
    - `script/build_and_run.sh` and `02-15-SUMMARY.md` — use only the exact telemetry-first launcher.
    - `02-CONTEXT.md` D-01 through D-12 — preserve entitled lineup, live-edge controls, bounded failure, and independent metadata semantics.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</files>
  <action>Pause for the exact resume signal below. After authorization, use `script/build_and_run.sh --telemetry` to launch one exact newly built bundle once. Confirm zero-before, telemetry-alive, exact one PID, and exact executable path. Allow only automatic Keychain restoration. If restore is missing, invalid, unavailable, rejected, incomplete, or a WebView appears, record the fixed stage with `status: blocked`, stop, do not sign in or hand off a credential, do not create the plan summary, and halt Phase 02. If and only if `restore-completed` appears, perform one catalog refresh, ask the owner to select one entitled channel, tune once, observe natural confirmed start, pause once, resume once at live edge, stop once, and observe current channel/program-or-song text plus best available artwork. Record fixed PASS/BLOCKED/NOT OBSERVED results only. Stop at the first fixed failure; do not retry, relaunch, invoke cleanup, force recovery/expiry/sleep/network/stall conditions, capture traffic, inspect secrets, or vary provider requests. Leave at most the one exact intended app open.</action>
  <instructions>
    1. Confirm integrated preflight PASS and wait for the exact owner resume signal.
    2. Launch one exact new build through telemetry-first single-instance enforcement.
    3. Wait only for automatic restore. If restore does not complete or WebView appears, record the fixed stage and halt without login.
    4. After restore-completed only, perform the single refresh/selection/tune/start/pause/live-edge-resume/stop/current-metadata sequence.
    5. Stop at the first fixed failure and leave no more than the one exact intended process.
  </instructions>
  <acceptance_criteria>
    - Exactly one authorized production launch occurs, with one exact PID/path and no WebView login or handoff.
    - Restore failure or WebView appearance produces a precise blocked artifact and no second login.
    - Restore success precedes every catalog/listening action.
    - The listening path is bounded to one refresh, one selection/tune, one natural control sequence, and one current text/artwork observation.
    - No forced fault/recovery/expiry, retry, relaunch, cleanup, secret inspection, or request variation occurs.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q '^status: passed$' .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q '^restore: completed$' .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q '^catalog_refresh: passed$' .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q '^listening_controls: passed$' .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q '^current_metadata: passed$' .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</automated>
    <human-check>Confirm automatic restoration occurred without a WebView and the recorded one-channel listening sequence matches what was heard/seen without sensitive material.</human-check>
  </verify>
  <verification>Gate 6 passes only after automatic restore and the bounded listening/current-metadata path; every blocked result halts without another login.</verification>
  <done>One separately authorized launch proves restoration and the repaired listening path without repeating authentication.</done>
  <resume-signal>Type exactly: Authorize one Phase 02 restore and listening checkpoint</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable Keychain item → automatic restore | The app may consume the saved opaque credential; the agent may observe only fixed restore semantics. |
| Xcode app host → production process set | Inert preflight must start/end at zero and preserve durable authentication state. |
| Exact restored app → fixed provider operations | Catalog and one listening path run only after restore-completed. |
| AVFoundation/metadata → owner observation | Natural confirmed state may be observed; secret media material and raw provider data may not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-18-01 | Information Disclosure | restored credential and media handoff | high | mitigate | Fixed labels only; no Keychain secret read, logs/capture, URLs, headers, bodies, or raw errors. |
| T-02-18-02 | Replay | WebView/second login | high | mitigate | Automatic restore is exclusive; WebView appearance halts and credential handoff is prohibited. |
| T-02-18-03 | Spoofing | duplicate/wrong binary | high | mitigate | Atomic exact launcher, one PID/path, close-all on invariant failure. |
| T-02-18-04 | Tampering | persisted auth during preflight | high | mitigate | Process-only guard, no Sign Out/Clear Local Session, no Keychain/browser access. |
| T-02-18-05 | Denial of Service | provider/account or forced recovery | high | mitigate | One natural bounded path, no induced faults/retries/relaunch, immediate terminal stop. |
| T-02-18-06 | Elevation of Privilege | speculative provider behavior | high | mitigate | Fixed catalog/tune/metadata operations only; no request variation or protected-control workaround. |
</threat_model>

<verification>
- `02-AUTH-UAT.md status: passed` and Gate 5 are mandatory before preflight or owner authorization.
- Every Xcode invocation is guarded zero-before/zero-after; any leak is closed and halts before production launch.
- The production launch occurs only after separate owner authorization and permits automatic restore only.
- Confirm `02-UAT.md` and `02-AUTH-UAT.md` remain unchanged and the new artifact contains no sensitive or dynamic provider material.
</verification>

<success_criteria>
- Incremental Gate 6 proves the saved credential restores in the exact next build without another login.
- One entitled channel completes the natural tune/start/pause/live-edge resume/stop sequence after restoration.
- Current text/artwork are observed without coupling metadata failure to audio or forcing expiry.
- A failure at any stage yields decisive sanitized evidence and halts without repeated authentication.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-18-SUMMARY.md` only after `02-UAT-RECHECK.md` has `status: passed`; a blocked artifact halts the phase without a summary.
</output>
