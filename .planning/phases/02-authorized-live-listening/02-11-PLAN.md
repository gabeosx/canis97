---
phase: 02-authorized-live-listening
plan: "11"
type: execute
wave: 10
depends_on: ["02-10"]
files_modified:
  - .planning/phases/02-authorized-live-listening/02-UAT.md
autonomous: false
gap_closure: true
requirements: [CAT-01, CAT-02, CAT-03, PLAY-01, PLAY-02, PLAY-03, PLAY-04, META-01, META-02]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "No owner action is requested until Plans 02-08 through 02-10 are summarized and all named offline package/app regressions pass without production app, browser, provider, or real Keychain activity."
    - "Fresh explicit owner authorization permits at most one bounded native end-to-end check; planning and previous approvals do not authorize it, and an unavailable session/login is a stop or user-operated native authentication gate rather than permission to erase state or automate a browser."
    - "The one check uses the real native row-selection path and, if safely available, covers entitled catalog refresh, one selected tune, install/ready/confirmed AVFoundation start, pause, live-edge resume, stop, current text, and best available artwork."
    - "The checkpoint never intentionally induces provider, network, decoder, protected-control, recovery, session-loss, or five-minute metadata-expiry faults. Naturally observed failures must preserve the session and receive only a closed semantic classification; unforced recovery/expiry/failure cases are recorded honestly as NOT FORCED with deterministic test backstops."
    - "Keychain material and the authenticated session are preserved after the check: Stop ends audio, while Sign Out and Clear Local Session are not clicked or invoked unless the owner separately requests that destructive lifecycle action."
    - "The UAT artifact records only PASS/FAIL/NOT FORCED semantic outcomes and offline test references—never account/channel/provider identifiers, credentials, cookies, tokens, request/response data, URLs, headers, media/key material, artwork bytes, raw errors, or browser state."
    - "Catalog boundary/adjacency/empty/encoding/ordering/number-precision behavior, command/recovery idempotency/concurrency, metadata empty/Unicode behavior, and session-preservation failure matrices remain proven by deterministic offline suites; owner UAT supplements rather than replaces them."
  artifacts:
    - path: ".planning/phases/02-authorized-live-listening/02-UAT.md"
      provides: "Sanitized owner-visible result for one bounded native Phase 02 flow plus honest offline backstop references"
      contains: "Phase 02 Consolidated Native UAT"
  key_links:
    - from: "Plans 02-08 through 02-10 focused regressions"
      to: "fresh owner authorization"
      via: "blocking precondition; no live action before offline green"
      pattern: "NOT FORCED"
    - from: "native ListeningView row selection"
      to: "catalog, playback, and metadata presentation"
      via: "one real selected channel and one bounded control sequence"
      pattern: "PASS"
    - from: "checkpoint completion"
      to: "02-UAT.md"
      via: "closed semantic statuses and backstop test names only"
      pattern: "Sanitized"
  flagged_assumptions:
    - id: "SP-EDGE-CAT-01-MANUAL"
      statement: "Owner confirms the actual entitled native lineup in this single UAT; strict entity admission remains backed by `LiveCatalogAdapterTests`."
      flagged: true
    - id: "SP-EDGE-CAT-02-BOUNDARY-ADJACENCY-EMPTY-ENCODING-ORDERING-PRECISION"
      statement: "These deterministic catalog edges are not manufactured against the provider; `LiveCatalogAdapterTests` is their authority and UAT checks only the naturally visible ordering/presentation."
      flagged: true
    - id: "SP-EDGE-CAT-03-MANUAL"
      statement: "If no natural catalog failure occurs, failure/cache non-authority is NOT FORCED and backed by package/app tests rather than an invented outage."
      flagged: true
    - id: "SP-EDGE-PLAY-01-MANUAL"
      statement: "AVFoundation becomes owner-observed only if the single native tune actually loads and behaves; otherwise it remains NOT OBSERVED."
      flagged: true
    - id: "SP-EDGE-PLAY-02-MANUAL"
      statement: "Owner-visible controls share the composed session; structural one-player ownership remains backed by focused offline composition/order tests."
      flagged: true
    - id: "SP-EDGE-PLAY-03-IDEMPOTENCY-CONCURRENCY"
      statement: "Recovery/command races are not induced live; focused offline tests are the authority unless a safe natural recovery occurs."
      flagged: true
    - id: "SP-EDGE-PLAY-04-CONCURRENCY"
      statement: "Distinct concurrent failure mapping and session preservation are proven offline; naturally observed live failure may supplement but is never forced."
      flagged: true
    - id: "SP-EDGE-META-01-EMPTY-UNICODE"
      statement: "Empty and Unicode metadata are deterministic decoder/presentation tests; the one live cut is not expected to exhibit every edge."
      flagged: true
    - id: "SP-EDGE-META-02-MANUAL"
      statement: "Healthy-audio independence is owner-visible; 90-second/300-second expiry is NOT FORCED unless it occurs naturally and remains backed by injected-time tests."
      flagged: true
  prohibitions:
    - statement: "No live/provider/app launch occurs without a new explicit owner authorization issued after the offline preflight passes."
      status: unverified
      flagged: true
    - statement: "The checkpoint must not use an agent browser, manipulate a DOM, inspect browser storage, capture traffic/HAR, vary provider requests, bypass controls, or retry after a stop condition."
      status: unverified
      flagged: true
    - statement: "The checkpoint must not invoke Sign Out/Clear Local Session, delete Keychain material, discard the active token/session, or treat an ordinary 4xx/compatibility failure as logout."
      status: unverified
      flagged: true
    - statement: "The checkpoint must not force sleep/wake, network loss, stalls, decoder failure, provider errors, rate limits, protected controls, long expiry waits, or a second tune to manufacture coverage."
      status: unverified
      flagged: true
    - statement: "No sanitized artifact may claim AVFoundation, recovery, stale expiry, artwork, or session-preservation behavior that was not actually observed or deterministically tested."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Perform one consolidated, owner-authorized native acceptance check after every offline gap repair is green, while preserving the authenticated session and recording honest sanitized evidence.

Purpose: Replace repeated one-shot break/fix attempts with one bounded end-to-end decision point that distinguishes observed behavior from deterministic backstops.
Output: `02-UAT.md` containing only closed semantic outcomes for catalog, selection, playback, metadata/artwork, session preservation, and unforced recovery/freshness cases.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md
@.planning/phases/02-authorized-live-listening/COVERAGE.md
@.planning/phases/02-authorized-live-listening/02-REVIEW.md
@.planning/phases/02-authorized-live-listening/02-VERIFICATION.md
@.planning/phases/02-authorized-live-listening/02-08-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-09-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-10-SUMMARY.md

<interfaces>
- `COVERAGE.md` and `02-LIVE-CONTRACT.md` remain the decided API/contract boundary. This checkpoint records app behavior only and does not add or vary provider operations.
- Plans 02-08, 02-09, and 02-10 provide session preservation/concurrent resolver safety, install-before-ready playback, and actual metadata/artwork/expiry wiring. Their focused tests are mandatory preflight evidence.
- The owner supplies explicit authorization and visible native UI actions. The executor may build/launch only after that authorization; browser automation and DOM/network capture are outside this checkpoint.
- `02-UAT.md` is a sanitized semantic record, not a provider capture, compatibility contract rewrite, requirement-completion update, or authorization token for another run.
</interfaces>
</context>

## Artifacts this phase produces

### New file

- `02-UAT.md` — `Phase 02 Consolidated Native UAT` with frontmatter for date/status and a fixed result table: Offline Preflight, Session Reuse, Catalog, Row Selection, AVFoundation Start, Pause, Resume Live, Stop, Current Text, Artwork, Natural Failure/Session Preservation, Natural Recovery, Natural Freshness Expiry, Safety Teardown. Values are only `PASS`, `FAIL`, `NOT FORCED`, `NOT OBSERVED`, or `BLOCKED` plus closed test/classification references.

### No source or contract changes

- No Swift source, test, project, Keychain, browser session, `COVERAGE.md`, `02-LIVE-CONTRACT.md`, requirement checkbox, ROADMAP completion marker, or provider operation is changed by this plan.

## Specless Edge Coverage

| Requirement edge | Resolution in this gap set |
|---|---|
| CAT-01 unclassified/manual | One native entitled-lineup owner check in this plan; strict admission remains `LiveCatalogAdapterTests`. |
| CAT-02 boundary | Existing/new focused catalog boundary regressions run in preflight. |
| CAT-02 adjacency | Focused ordering/category adjacency regressions run in preflight. |
| CAT-02 empty | Empty lineup and missing optional fields remain deterministic tests; visible empty state is checked only if naturally present. |
| CAT-02 encoding | Invented Unicode catalog strings remain decoder/presentation tests. |
| CAT-02 ordering | Owner observes natural order; exact category/number/name/identity order remains deterministic test authority. |
| CAT-02 precision | Integral number admission/rejection remains strict package-test authority; no provider edge is manufactured. |
| CAT-03 unclassified/manual | Natural visible failure is classified if it occurs; otherwise NOT FORCED with non-authoritative-cache test backstop. |
| PLAY-01 unclassified/manual | One bounded native start/pause/live-edge-resume/stop sequence; AVFoundation remains NOT OBSERVED if it does not complete. |
| PLAY-02 unclassified/manual | Same composed native control surface; one-player ownership remains structural/focused-test evidence. |
| PLAY-03 idempotency | Duplicate signal/stop/session-end behavior remains focused offline evidence. |
| PLAY-03 concurrency | Recovery/command race remains focused offline evidence unless naturally observed, never forced. |
| PLAY-04 concurrency | Per-operation failure/redirect/session preservation remains focused offline evidence; natural failure may supplement. |
| META-01 empty | Channel fallback/unavailable remains deterministic decoder/presentation evidence. |
| META-01 Unicode encoding | Unicode preservation remains deterministic decoder/presentation evidence. |
| META-02 unclassified/manual | Owner observes metadata alongside healthy audio; stale/unavailable deadlines are NOT FORCED with injected-time backstop. |

## Multi-Source Coverage Audit

| Source | ID | Feature / decision | Gap plan | Status |
|---|---|---|---|---|
| GOAL | — | Entitled lineup plus reliable one-stream listening with truthful metadata | 02-08 through 02-11 | COVERED |
| REQ | CAT-01 | Entitled standard/app-only linear lineup | 02-11 | COVERED |
| REQ | CAT-02 | Stable identity and presentation/freshness edges | 02-11 preflight/UAT | COVERED |
| REQ | CAT-03 | Visible failures; cache never authorizes playback | 02-08, 02-11 | COVERED |
| REQ | PLAY-01 | Tune/start/pause/resume/stop | 02-09, 02-11 | COVERED |
| REQ | PLAY-02 | One serialized playback owner | 02-09, 02-11 | COVERED |
| REQ | PLAY-03 | Bounded cancellation-aware recovery | 02-08, 02-09, 02-11 | COVERED |
| REQ | PLAY-04 | Distinct actionable failure domains | 02-08, 02-09, 02-11 | COVERED |
| REQ | META-01 | Active channel/current text/best artwork | 02-10, 02-11 | COVERED |
| REQ | META-02 | Metadata independent from audio; stale/unavailable | 02-10, 02-11 | COVERED |
| RESEARCH | — | Preserve exact fixed provider operations and opaque media/artwork boundaries | 02-08, 02-10 | COVERED |
| RESEARCH | — | Do not claim AVFoundation from compilation/fakes | 02-09, 02-11 | COVERED |
| RESEARCH | — | Fixed 30/90/300 metadata policy; no provider hint inference | 02-10 | COVERED |
| RESEARCH | — | No runtime DOM/browser architecture or access-control workaround | 02-08 through 02-11 | COVERED |
| CONTEXT | D-01, D-02, D-03 | Exact lineup, ordering, stale browse/non-authority | 02-08, 02-11 | COVERED |
| CONTEXT | D-04, D-05, D-06 | Live-edge controls, one player, confirmed serialized state | 02-09, 02-11 | COVERED |
| CONTEXT | D-07, D-08, D-09 | Same-channel bounded recovery and distinct failures | 02-08, 02-09, 02-11 | COVERED |
| CONTEXT | D-10, D-11, D-12 | Independent metadata, best text/artwork, channel fallback | 02-10, 02-11 | COVERED |

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 02-11-01: Authorize and perform one consolidated native Phase 02 UAT</name>
  <precondition>Plans 02-08, 02-09, and 02-10 have complete summaries; all automated commands below pass with fakes/in-memory stores only; no production app process was launched by preflight; and no live authorization has been inferred from earlier approvals.</precondition>
  <reversibility rating="reversible">The bounded check changes only transient native playback/presentation state and writes a sanitized result; it deliberately preserves authentication material.</reversibility>
  <read_first>
    - `02-08-SUMMARY.md`, `02-09-SUMMARY.md`, and `02-10-SUMMARY.md` — confirm all offline repairs/tests are complete and retain their no-live evidence boundary.
    - `02-VERIFICATION.md` failed truths and behavior-unverified catalog item — this single checkpoint targets those outcomes without inventing faults.
    - `02-REVIEW.md` CR-01 through CR-06 and WR-01/WR-02 — every finding must have a green focused backstop before authorization is requested.
    - `02-LIVE-CONTRACT.md` and `COVERAGE.md` — preserve fixed supported operations, stop rules, session safety, and AVFoundation NOT OBSERVED until this run actually observes it.
    - `02-CONTEXT.md` D-01 through D-12 — use live-radio, confirmed-state, bounded-recovery, and independent-metadata semantics.
    - This plan's Specless Edge Coverage table — never force a provider condition merely to turn a deterministic edge into live evidence.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-UAT.md</files>
  <action>First run the automated preflight exactly as scoped below. It may build focused test hosts, but it must use invented/fake transports, in-memory credential stores, injected clocks, and fake playback runtimes. If any command fails, stop before requesting owner authorization and return to gap repair. Do not run the full unfiltered app suite, launch the production app, access the real app Keychain, open a browser, or contact a provider during preflight.

After green preflight, pause and ask the owner for a fresh explicit authorization phrase for one bounded native Phase 02 UAT. Earlier approvals, planning invocation, stored login, and browser research do not count. Explain that the check will preserve the session/Keychain, use at most one selected-channel tune, make no browser/DOM capture, and stop on any protected/unknown condition. If authorization is not granted, do not launch or contact anything.

Once authorized, build and launch only the native Sirius Mac app. Reuse the existing stored/native session if it restores successfully. If user-operated native authentication is required, stop at the authentication gate and let the owner interact with the app's existing sign-in UI; do not automate, inspect, or capture its WebView. Do not invoke Sign Out or Clear Local Session. If authentication cannot reach entitled state safely, record `BLOCKED` and end without a content request or cleanup.

For an entitled session, perform at most one native lineup refresh, let the owner select one ordinary admitted linear row, and confirm that the actual row selection starts metadata. Permit at most one Tune. If the resolved item reaches confirmed playing, observe audible start, Pause silence, Resume Live as a new current-live-edge resolution, Stop, current/best-available text, and best-available artwork. Do not switch channels or retry a failed tune. Stop immediately on CAPTCHA/MFA/control, 403/429/rate-limit, unknown redirect/host, unknown/malformed contract, authorization/entitlement ambiguity, DRM ambiguity, secret-bearing output, or any proposed request variation/bypass.

Do not manufacture catalog failure, network loss, sleep/wake, stall, resource expiry, decoder failure, rate limit, protected control, session loss, or 90/300-second metadata expiry. If one occurs naturally, record only its existing closed semantic classification and whether the app remained entitled without credential cleanup. Otherwise mark the row `NOT FORCED` and cite the exact green offline suite/test category. Recovery and freshness remain `NOT FORCED` unless safely naturally observed; never infer them from healthy playback.

After the one sequence, leave the session authenticated, use Stop to end audio, close only the test-launched app if the owner approves normal closure, and do not clear local state. Write `02-UAT.md` using only the fixed statuses and test/classification labels described above. Omit channel/account/provider identifiers, times that identify listening activity, request/response facts, URLs, headers, tokens, cookies, media/key material, artwork bytes, raw errors, screenshots, logs, and traffic captures. Do not edit `COVERAGE.md`, `02-LIVE-CONTRACT.md`, requirement checkboxes, or Phase 02 completion state.</action>
  <what-built>Session-preserving concurrent live resolution, install-before-ready initial/recovery playback, and fully wired metadata/artwork/expiry behavior with focused offline regressions.</what-built>
  <instructions>
    1. Run the focused automated preflight. Stop before authorization if any command fails.
    2. Ask the owner for the exact fresh authorization signal; do not infer consent from earlier runs.
    3. After authorization, use only the native app and existing/user-operated native authentication flow.
    4. Refresh once, select one row, and Tune at most once. Observe controls and metadata only if the tune succeeds.
    5. Never force recovery, failure, or expiry; record `NOT FORCED` plus deterministic backstop evidence when absent.
    6. Stop audio without signing out or clearing local state, then write only the sanitized semantic UAT record.
  </instructions>
  <verification>The owner reviews `02-UAT.md`, confirms the fixed semantic statuses match the one native sequence, and confirms that no second attempt, forced fault, browser/DOM capture, Sign Out, Clear Local Session, or Keychain/session deletion occurred.</verification>
  <how-to-verify>
    1. The executor runs the exact focused preflight; all commands pass before asking for any authorization.
    2. The owner explicitly authorizes one bounded native Phase 02 UAT after reading the scope and stop conditions.
    3. The app reuses/restores the native session or pauses for user-operated native authentication; no agent-browser/DOM/network capture is used.
    4. In the entitled native view, refresh once, select one row, tune once, and—only if confirmed playing—observe start, pause, live-edge resume, stop, current text, and best-available artwork.
    5. Do not force failure/recovery/expiry. Record naturally observed closed behavior or `NOT FORCED` plus the named deterministic backstop.
    6. Confirm Stop ends audio while the app remains entitled and neither Sign Out nor Clear Local Session was invoked.
    7. Inspect only sanitized `02-UAT.md`; it must distinguish observed, not observed, blocked, and not-forced outcomes without sensitive data.
  </how-to-verify>
  <acceptance_criteria>
    - Fresh explicit authorization is obtained only after all focused offline preflight commands pass.
    - No more than one catalog refresh, one row selection, and one Tune occur; a failure is not retried.
    - Actual native selection reaches metadata lifecycle; successful playback is claimed only after owner-observed confirmed behavior.
    - Session/Keychain material is preserved and no explicit cleanup command is invoked.
    - Recovery, stale expiry, and provider failures are never forced; unobserved cases cite deterministic backstops honestly.
    - `02-UAT.md` contains only closed semantic statuses and makes no unsupported AVFoundation or provider claim.
  </acceptance_criteria>
  <verify>
    <automated>plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter LiveCatalogAdapterTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter LivePlaybackCoordinatorTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter MetadataRefreshCoordinatorTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/PlaybackInstallationOrderTests -only-testing:SiriusMacTests/MetadataPresentationTests CODE_SIGNING_ALLOWED=NO</automated>
    <human-check>Owner confirms the single native sequence's sanitized outcomes and that the authenticated session/Keychain were preserved with no second attempt, forced fault, browser capture, Sign Out, or Clear Local Session.</human-check>
  </verify>
  <resume-signal>After green preflight, explicitly authorize with `Authorize one bounded native Phase 02 UAT`. After the run, reply `approved` only if `02-UAT.md` accurately distinguishes PASS/FAIL/NOT FORCED/NOT OBSERVED/BLOCKED and the session remains preserved; otherwise report only the closed semantic failure label.</resume-signal>
  <done>One consolidated owner-authorized native UAT is recorded honestly and sanitized, with the session preserved and every unforced live edge backed by focused offline evidence rather than invented provider behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Offline verified code → fresh live authorization | Passing tests do not authorize live/provider activity; only the owner can open the bounded check. |
| Native authenticated session → one content flow | Existing local authorization is reused without exposing, deleting, or treating it as provider-contract evidence. |
| Live behavior → UAT artifact | Sensitive/runtime facts must collapse to closed semantic statuses with no capture channel. |
| Unforced conditions → acceptance decision | Recovery/failure/expiry must remain honestly unobserved and rely on deterministic backstops rather than manufactured faults. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-11-01 | Elevation of Privilege | live authorization | high | mitigate | Blocking fresh owner authorization after green preflight; prior approvals and planning are explicitly non-authorizing. |
| T-02-11-02 | Information Disclosure | native session/UAT | high | mitigate | No browser/traffic capture, no raw logs/errors, fixed semantic statuses only, and session/Keychain remain local. |
| T-02-11-03 | Denial of Service | provider/account activity | high | mitigate | One refresh/selection/tune ceiling, no retry/channel switch/forced fault, and immediate protected/unknown stop. |
| T-02-11-04 | Tampering | acceptance evidence | high | mitigate | Distinguish PASS, FAIL, NOT FORCED, NOT OBSERVED, and BLOCKED; cite focused tests for unforced edges and never infer live outcomes. |
| T-02-11-05 | Elevation of Privilege | session cleanup | high | mitigate | Do not invoke Sign Out/Clear Local Session; Stop controls audio only and ordinary failure preserves active authorization. |
| T-02-11-06 | Spoofing | browser/provider behavior | high | mitigate | Native app only, no DOM manipulation or browser automation, exact existing provider contract, and no request variation/bypass. |
</threat_model>

<verification>
- The checkpoint is blocking and non-autonomous; no planning artifact or earlier approval consumes the required authorization.
- All offline suites run before the owner is asked to do anything live.
- The final record separates observed native behavior from deterministic backstops and leaves AVFoundation NOT OBSERVED if the one tune does not prove it.
- Phase completion and requirement checkboxes remain unchanged until a later verifier evaluates `02-UAT.md` plus source/test evidence.
</verification>

<success_criteria>
- One consolidated UAT replaces repeated one-shot break/fix attempts and preserves the subscriber's local session.
- The actual native catalog-selection-playback-metadata path is checked at most once under fresh authorization.
- No protected condition, provider fault, recovery event, or stale deadline is deliberately induced.
- The sanitized artifact is sufficient for honest re-verification without containing sensitive provider/account material.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-11-SUMMARY.md` when done; do not mark Phase 02 complete until `$gsd-verify-work`/the verifier passes.
</output>
