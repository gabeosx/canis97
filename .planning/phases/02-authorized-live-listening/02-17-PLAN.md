---
phase: 02-authorized-live-listening
plan: "17"
type: execute
wave: 14
depends_on: ["02-14", "02-15"]
files_modified:
  - .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md
autonomous: false
gap_closure: true
requirements: [CAT-03, PLAY-04]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Incremental Gates 1 through 3 are hard prerequisites: the no-host/package matrix, fake launcher matrix, and guarded app-host matrix are green before any live action."
    - "Incremental Gate 4 launches exactly one known SiriusMac binary with telemetry already streaming and permits at most one owner-operated fresh WebView sign-in and one credential handoff."
    - "The checkpoint proves the fixed sequence through native authentication completed, entitlement completed, persistence completed, and metadata-only Keychain item existence; it performs no listening work."
    - "Any terminal result before complete persistence writes one fixed stage to 02-AUTH-UAT.md and HALTS the phase; Plan 02-17 is not complete and Plans 02-16/18 cannot execute."
    - "No catalog refresh, channel selection, tune, play, pause, resume, stop, metadata observation, recovery/expiry induction, relaunch, or second login occurs in this plan."
    - "On successful persistence, at most the one exact intended app remains open; its Keychain and browser state are preserved for the guarded playback work and later restoration checkpoint."
  artifacts:
    - path: ".planning/phases/02-authorized-live-listening/02-AUTH-UAT.md"
      provides: "Sanitized authentication-only checkpoint evidence and downstream success gate"
      contains: "Authentication Checkpoint"
  key_links:
    - from: "02-12/13/14/15 summaries"
      to: "owner authorization checkpoint"
      via: "green Incremental Gates 1, 2, and 3 are required before launch"
      pattern: "Incremental Gate"
    - from: "script/build_and_run.sh --telemetry"
      to: "one exact running binary"
      via: "telemetry-first locked launch with zero-before/one-exact-after enforcement"
      pattern: "telemetry"
    - from: "fixed authentication stage oracle"
      to: "02-AUTH-UAT.md"
      via: "first terminal label or complete auth/entitlement/persistence sequence"
      pattern: "native-authentication|entitlement|session-persistence"
    - from: "02-AUTH-UAT.md status passed"
      to: "Plans 02-16 and 02-18"
      via: "downstream execution is unreachable unless persistence and metadata-only item existence are proven"
      pattern: "status: passed"
  prohibitions:
    - statement: "Never read, print, hash, decode for output, export, screenshot, inspect, or log credential/cookie/header/body/URL/account/provider material."
      status: unverified
      flagged: true
    - statement: "Never retry authentication, click Sign Out/Clear Local Session, open Web Inspector, capture traffic, or vary an endpoint after a terminal result."
      status: unverified
      flagged: true
    - statement: "Never perform catalog, playback, metadata, recovery, expiry, or restore-relaunch work in this authentication-only checkpoint."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Prove authentication, entitlement, and durable credential persistence in one owner-authorized checkpoint before the playback repair.

Purpose: The previous UI proved only that the WebView looked signed in while the native transaction was rejected. This checkpoint yields one decisive fixed stage and creates a hard incremental gate before any further source work.
Output: `02-AUTH-UAT.md` containing sanitized preflight status and either one precise blocked stage or a complete authentication/entitlement/persistence success sequence.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-UAT.md
@.planning/phases/02-authorized-live-listening/02-12-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-13-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-14-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-15-SUMMARY.md
@script/build_and_run.sh

<interfaces>
- `script/test_offline_auth_matrix.sh` is the no-host Gate 1 verifier and must pass without inspecting or launching SiriusMac.
- `script/tests/build_and_run_tests.sh` is the fake-process Gate 2 verifier and must never reach real process or launch commands.
- `02-14-SUMMARY.md` is Gate 3 evidence: its Xcode app-host matrix passed with exact zero before and after.
- `script/build_and_run.sh --telemetry` is the only production launch path. It owns the lock, zero-before, telemetry-before-launch, exact one PID, and exact executable-path checks.
- Closed auth labels decide the branch: web selection, native authentication, entitlement, persistence. No raw log line or visible WebView account state is authentication evidence.
- `02-UAT.md` remains unchanged. This plan writes only `02-AUTH-UAT.md`.
</interfaces>
</context>

## Artifacts this phase produces

- A Gate 4 authentication artifact separate from listening evidence.
- One owner-authorized result that either proves durable authentication or stops at the exact fixed boundary.
- Metadata-only Keychain item-existence evidence after, and only after, the full fixed sequence succeeds.
- No catalog, playback, metadata, recovery, expiry, or restore claim.

## Source Coverage Audit

| Source | ID | Focus | Coverage |
|---|---|---|---|
| GOAL | — | Authorized listening requires a durable reusable session before playback work | 02-17 gates 02-16 and 02-18 |
| REQ | CAT-03 | Entitlement/auth failures are visible and never authorize later work | Fixed terminal stage plus hard halt |
| REQ | PLAY-04 | Authentication and entitlement failures remain distinct/actionable | Closed stage oracle recorded once |
| RESEARCH | — | Fixed direct operations; no speculative variation or bypass | One attempt, fixed requests, immediate stop |
| CONTEXT | D-03 | Cached/catalog state cannot authorize listening | Catalog is prohibited in this checkpoint |
| CONTEXT | D-08, D-09 | Protected/terminal auth states stop and remain distinct | Gate 4 terminal halt |
| CONTEXT | D-01–D-07, D-10–D-12 | Catalog/playback/recovery/metadata behavior | Scoped to completed plans and later 02-16/18; not implemented here |

## Authentication Checkpoint Oracle

| First closed sequence | Decisive meaning | Required action |
|---|---|---|
| web selection missing/malformed/ambiguous/reset failed | Fresh WebView extraction/session boundary failed | Record fixed stage; HALT |
| web transferred → native authentication unauthorized/forbidden | Native profile boundary rejected | Record fixed stage; HALT |
| native authentication completed → entitlement unauthorized/forbidden | Native profile accepted; entitlement boundary rejected | Record fixed stage; HALT |
| authentication completed → entitlement completed → persistence failed | Provider stages passed; durable storage failed | Record fixed stage; HALT |
| authentication completed → entitlement completed → persistence completed → Keychain item exists | Durable authentication proven | Mark Gate 4 passed; permit 02-16 |

<tasks>

<task type="auto">
  <name>Task 02-17-01: Reconfirm incremental offline gates without launching SiriusMac</name>
  <reversibility rating="reversible">This task runs deterministic no-host/fake verification and writes only fixed preflight status.</reversibility>
  <read_first>
    - `02-12-SUMMARY.md` and `02-13-SUMMARY.md` — Gate 1 no-host/package matrix must be green.
    - `02-15-SUMMARY.md` — Gate 2 fake launcher matrix and app-host guard contract must be green.
    - `02-14-SUMMARY.md` — Gate 3 guarded app-host matrix must be green with zero-before/zero-after evidence.
    - `02-UAT.md` — retain this original blocked UAT unchanged.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-AUTH-UAT.md</files>
  <action>Assert all four prerequisite summaries exist and their self-checks passed. Re-run only the complete no-host authentication matrix, fake-process launcher matrix, and build-only. Do not run Xcode, invoke a test host, inspect a real process, launch the app, read Keychain, instantiate WebKit, or contact the provider. Create `02-AUTH-UAT.md` with `status: awaiting_owner_authorization` and a fixed PASS/FAIL table for Gates 1 through 3. If any check fails, record the failed gate and halt before the human checkpoint.</action>
  <acceptance_criteria>
    - No-host/package, fake launcher, guarded app-host summary evidence, and build-only are green in that order.
    - No production process, Keychain item, browser state, WebView, or provider request is touched.
    - `02-AUTH-UAT.md` contains only fixed suite names/statuses and preserves `02-UAT.md` byte-for-byte.
    - Failure halts before owner authorization and before any launch-capable command.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-authorized-live-listening/02-12-SUMMARY.md &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-13-SUMMARY.md &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-14-SUMMARY.md &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-15-SUMMARY.md &amp;&amp; bash script/test_offline_auth_matrix.sh &amp;&amp; bash script/tests/build_and_run_tests.sh &amp;&amp; ./script/build_and_run.sh --build-only</automated>
  </verify>
  <done>Gates 1 through 3 remain green, no app was launched, and the authentication artifact awaits explicit owner authorization.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 02-17-02: Authorize one fresh authentication-only native checkpoint</name>
  <reversibility rating="reversible">One bounded owner-operated sign-in may create a durable local credential, while the agent observes only fixed semantic stages.</reversibility>
  <read_first>
    - `02-AUTH-UAT.md` — confirm preflight is PASS and status is awaiting owner authorization.
    - `script/build_and_run.sh` and `02-15-SUMMARY.md` — use only the telemetry-first exact launcher.
    - Authentication Checkpoint Oracle above — stop at the first fixed terminal sequence.
    - User decision — this checkpoint proves authentication only; listening and restoration are separate.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-AUTH-UAT.md</files>
  <action>Pause for the exact resume signal below. After authorization, launch only through `script/build_and_run.sh --telemetry`. Verify its zero-before, telemetry-alive, exact-one-PID, and exact-executable-path invariants; on mismatch, close all copies, record the process stage, and halt. Ask the owner to operate exactly one fresh WebView sign-in and invoke the credential handoff once. Observe only fixed labels. At the first terminal oracle row, write the fixed stage and `status: blocked`, stop every interaction, do not create the plan summary, and halt Phase 02 so dependent plans remain unreachable. Do not retry, relaunch, sign out, clear local session, inspect the WebView, capture traffic, change a request, refresh catalog, select/tune/play, observe listening metadata, or induce recovery/expiry. If and only if native authentication, entitlement, and persistence all emit completed, perform one metadata-only Keychain item-existence query for the fixed app service/account without requesting secret data. Mark `status: passed` only when that item exists. Leave at most the one exact intended app open on success and preserve its durable/browser state.</action>
  <instructions>
    1. Confirm the preflight table is PASS, then wait for the exact owner resume signal.
    2. Start telemetry first and launch one exact binary through the locked helper; abort and close all copies on any count/path mismatch.
    3. Ask the owner to complete one fresh sign-in and one handoff; stop at the first fixed oracle result.
    4. After the complete fixed success sequence only, query Keychain item existence metadata without retrieving secret data.
    5. Mark passed only for authentication completed, entitlement completed, persistence completed, and item exists. Otherwise record blocked and halt the phase.
  </instructions>
  <acceptance_criteria>
    - At most one exact app process and one fresh owner sign-in/handoff occur.
    - A fixed terminal failure produces `status: blocked`, no summary, and no downstream execution.
    - `status: passed` requires the complete fixed sequence plus metadata-only item existence.
    - No catalog, playback, metadata, recovery, expiry, relaunch, second login, cleanup action, inspection, or capture occurs.
    - Success leaves at most one exact intended process and preserves durable state for Plans 02-16 and 02-18.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^status: passed$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^native_authentication: completed$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^entitlement: completed$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^persistence: completed$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md &amp;&amp; rg -q '^keychain_item: exists$' .planning/phases/02-authorized-live-listening/02-AUTH-UAT.md</automated>
    <human-check>Confirm the artifact matches the one fixed terminal stage or complete durable-auth sequence and contains no sensitive material or listening claim.</human-check>
  </verify>
  <verification>Gate 4 passes only for the complete durable sequence; every other result is a precise blocked artifact and mandatory phase halt.</verification>
  <done>One fresh attempt proves durable authentication with fixed sanitized evidence; only then may Plan 02-16 execute.</done>
  <resume-signal>Type exactly: Authorize one fresh Phase 02 authentication checkpoint</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Owner-operated WebView → volatile native handoff | The owner may create one credential; the agent must never inspect or retain it. |
| Exact app binary → fixed provider operations | Only the repaired auth/entitlement contract may run once after offline proof. |
| Keychain → checkpoint evidence | Only fixed item existence metadata may be observed after the full success sequence. |
| Authentication artifact → downstream execution | A blocked or incomplete artifact must make Plans 02-16/18 unreachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-17-01 | Information Disclosure | live credential/session evidence | high | mitigate | Owner-only entry, fixed labels, metadata-only existence, no secret retrieval/output/capture. |
| T-02-17-02 | Replay | repeated sign-in/handoff | high | mitigate | Exactly one fresh attempt and handoff; immediate terminal stop; no retry or relaunch. |
| T-02-17-03 | Spoofing | duplicate/wrong app process | high | mitigate | Atomic launcher, exact one PID/path, close-all and halt on failure. |
| T-02-17-04 | Elevation of Privilege | speculative provider behavior | high | mitigate | Fixed auth/entitlement operations only; no request variation or control workaround. |
| T-02-17-05 | Tampering | downstream gate | high | mitigate | `status: passed` plus all completed fixed fields are required; no summary on blocked result. |
| T-02-17-06 | Denial of Service | provider/account controls | high | mitigate | One attempt, no listening/recovery work, and stop on challenge/protected/rate-limit outcomes. |
</threat_model>

<verification>
- Incremental Gates 1, 2, and 3 must be green before authorization or production launch.
- No Xcode app-host runs in this plan. The only production launch is the owner-authorized telemetry-first exact binary.
- On any blocked result, record the exact fixed stage, do not create `02-17-SUMMARY.md`, and halt the phase.
- Confirm `02-UAT.md` remains unchanged and `02-AUTH-UAT.md` contains no dynamic or sensitive material.
</verification>

<success_criteria>
- One owner attempt localizes failure precisely or proves native authentication, entitlement, and persistence.
- Gate 4 passes only with metadata-only evidence that the durable item exists.
- No listening or restore behavior is attempted or inferred.
- Plan 02-16 is reachable only after complete durable-auth success.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-17-SUMMARY.md` only after `02-AUTH-UAT.md` has `status: passed`; a blocked artifact halts the phase without a summary.
</output>
