---
phase: 02-authorized-live-listening
plan: "17"
type: execute
wave: 15
depends_on: ["02-14", "02-15", "02-16"]
files_modified:
  - .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md
autonomous: false
gap_closure: true
requirements: [CAT-01, CAT-02, CAT-03, PLAY-01, PLAY-02, PLAY-03, PLAY-04, META-01, META-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No app launch, Xcode test host, real Keychain metadata check, or provider call occurs until the complete standalone synthetic matrix is green."
    - "The live checkpoint launches exactly one known SiriusMac binary with telemetry already streaming, then permits at most one owner-operated fresh WebView sign-in and one credential handoff."
    - "The first terminal fixed label ends the attempt with no retry, cleanup, inspector, capture, speculative provider change, or induced fault."
    - "If authentication, entitlement, and persistence all complete, Keychain existence is checked by metadata only and the same session may proceed through one catalog refresh, one selected tune, and one unforced control/metadata observation."
    - "Automatic restore on a second launch requires separate owner authorization and is not implied by this checkpoint."
  artifacts:
    - path: ".planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md"
      provides: "Sanitized fixed-label recheck evidence"
      contains: "Closed Outcome"
  key_links:
    - from: "script/test_offline_auth_matrix.sh"
      to: "owner authorization checkpoint"
      via: "green no-host matrix is a hard prerequisite"
      pattern: "offline auth matrix"
    - from: "script/build_and_run.sh --telemetry"
      to: "one exact running binary"
      via: "telemetry-first locked launcher from 02-15"
      pattern: "telemetry"
    - from: "fixed auth terminal label"
      to: "02-UAT-RECHECK.md"
      via: "closed stage oracle with immediate stop"
      pattern: "native-authentication|entitlement|session-persistence|restore"
  prohibitions:
    - statement: "Never read, print, hash, decode for output, export, screenshot, inspect, or log credential/cookie/header/body/URL/account/provider material."
      status: unverified
      flagged: true
    - statement: "Never retry authentication, click Sign Out/Clear Local Session, open Web Inspector, capture traffic, or vary an endpoint after a terminal result."
      status: unverified
      flagged: true
    - statement: "Never leave more than the one intended exact app process open."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Run one decisive, owner-authorized native recheck after every offline repair is green.

Purpose: One fresh login should answer the exact stage question and, only if fully successful, validate the live listening path without repeated sign-ins or speculative diagnosis.
Output: A sanitized closed-label UAT recheck that records supported, blocked-at-stage, or unobserved outcomes and preserves the original 02-UAT.md.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-VALIDATION.md
@.planning/phases/02-authorized-live-listening/02-UAT.md
@.planning/phases/02-authorized-live-listening/02-12-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-13-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-14-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-15-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-16-SUMMARY.md
@script/build_and_run.sh

<interfaces>
- `script/test_offline_auth_matrix.sh` is the no-host gate and must pass before any Xcode test or app launch.
- `script/build_and_run.sh --telemetry` owns the exact single-instance, exact-binary, telemetry-before-launch invariant.
- Closed auth labels decide the branch: web selection, native authentication, entitlement, persistence, and restore. No raw log line or WebView content is evidence.
- `02-UAT.md` remains the immutable prior blocked attempt. Write the new result to `02-UAT-RECHECK.md` using fixed semantic outcomes only.
</interfaces>
</context>

## Artifacts this phase produces

- A green offline preflight record bound to Plans 02-12 through 02-16.
- One owner-authorized live result with exact terminal-stage semantics.
- Metadata-only persistence evidence after, and only after, a fully successful transaction.
- One bounded catalog/playback/metadata observation if the authentication path succeeds.

## Bounded UAT Oracle

| First closed sequence | Decisive meaning | Required action |
|---|---|---|
| web transferred → native auth unauthorized/forbidden | Profile boundary rejected; Keychain is irrelevant | Stop |
| native auth completed → entitlement unauthorized/forbidden | Credential was accepted for profile; entitlement contract/state is the focus | Stop |
| auth completed → entitlement completed → persistence failed | Authentication worked; durable storage failed | Stop |
| auth completed → entitlement completed → persistence completed | Transaction is durable-ready | Metadata-only Keychain existence check, then bounded listening checks |
| next separately authorized launch → restore completed | Persistence identity and reuse work | Record only after separate authorization |
| visibly signed-in web UI → web credential missing/malformed/ambiguous | Web extraction policy/session drift | Stop |

<tasks>

<task type="auto">
  <name>Task 02-17-01: Prove every offline repair before requesting owner authorization</name>
  <reversibility rating="reversible">This task runs deterministic verification and writes only sanitized preflight status.</reversibility>
  <read_first>
    - Summaries 02-12 through 02-16 — assert each self-check and exact no-live constraints.
    - `02-VALIDATION.md` — use SwiftPM for package logic and focused XCTest only after the no-host matrix is green.
    - `script/test_offline_auth_matrix.sh` — this must be the first executable gate.
    - `script/tests/build_and_run_tests.sh` — duplicate-process matrix must be green before launch authorization.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</files>
  <action>Run the standalone authentication matrix first and stop immediately if it fails. After it is green, run the package session/auth suites, fake launcher tests, focused app authentication/WebView/playback tests, project lint, and build-only. Do not run a launch-capable script mode. Create `02-UAT-RECHECK.md` with a sanitized preflight table and `status: awaiting_owner_authorization`; retain no command output beyond pass/fail and fixed suite names. If an Xcode test host leaves a SiriusMac process, close all copies through the single-instance helper, verify zero, record preflight failed, and stop before the checkpoint.</action>
  <acceptance_criteria>
    - Standalone complete matrix is the first green gate.
    - Package, app, playback, launcher, project, and build-only checks pass after it.
    - Zero SiriusMac processes remain before owner authorization is requested.
    - The preflight artifact contains fixed statuses only and preserves the prior UAT file.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient &amp;&amp; bash script/tests/build_and_run_tests.sh &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests -only-testing:SiriusMacTests/WebAuthenticationBridgeTests -only-testing:SiriusMacTests/PlaybackInstallationOrderTests CODE_SIGNING_ALLOWED=NO &amp;&amp; plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; ./script/build_and_run.sh --build-only</automated>
  </verify>
  <done>Every synthetic and build repair is green, no app remains running, and the owner has not yet been asked to sign in.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 02-17-02: Authorize and perform one fresh diagnostic native UAT</name>
  <reversibility rating="reversible">The action is one bounded owner-controlled session and records only sanitized semantic evidence.</reversibility>
  <read_first>
    - `02-UAT-RECHECK.md` — confirm offline preflight is PASS and status is awaiting owner authorization.
    - `script/build_and_run.sh` and its 02-15 summary — use only the telemetry-first exact launcher.
    - Bounded UAT Oracle above — stop at the first matching fixed sequence.
    - Locked user intent — one fresh sign-in, one handoff, no retry/cleanup/inspection/capture, and separate authorization for restore relaunch.
  </read_first>
  <files>.planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</files>
  <action>Pause and request the exact resume signal below. After authorization, start the narrow closed-label telemetry stream before launch through the locked launcher. Verify exactly one PID and the exact built executable path; if that invariant fails, close all copies and stop. Ask the owner to operate the WebView for exactly one fresh sign-in and click the session handoff exactly once. Observe only fixed labels. At the first terminal oracle row, stop all interaction and record that row. Do not retry, sign out, clear local session, inspect the WebView, capture traffic, change requests, or induce failures.

If and only if profile, entitlement, and persistence all complete, run a metadata-only Keychain existence query for the fixed app service/account without requesting secret data. Then allow one catalog refresh, one owner-selected entitled channel, one tune, one natural start/pause/live-edge-resume/stop sequence, and one current text/artwork observation. Stop on the first fixed failure and do not force recovery/expiry. Do not relaunch to test restore unless the owner separately authorizes that exact same build. Leave at most the one intended exact app open and write only PASS/BLOCKED/NOT OBSERVED fixed results to the recheck artifact.</action>
  <instructions>
    1. Confirm the preflight artifact says PASS, then wait for the exact owner resume signal.
    2. Start telemetry first and launch through the exact single-instance script; abort and close all copies on any count/path mismatch.
    3. Ask the owner to complete one fresh sign-in and one handoff; stop at the first fixed oracle result.
    4. Only after complete auth/entitlement/persistence success, perform metadata-only persistence confirmation and the single bounded listening sequence.
    5. Record only fixed semantic results and do not relaunch for restore without a separate authorization.
  </instructions>
  <acceptance_criteria>
    - One exact app process exists throughout, or all copies are closed and the UAT is blocked.
    - At most one fresh owner sign-in and one handoff occur.
    - The first terminal auth label determines the recorded root boundary without a retry.
    - Persistence is queried by existence metadata only after complete success; secret retrieval is never requested.
    - Listening checks are bounded to one refresh/selection/tune/control sequence and never induce faults.
  </acceptance_criteria>
  <verify>
    <automated>bash script/tests/build_and_run_tests.sh &amp;&amp; test -f .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md &amp;&amp; rg -q "^status: (passed|blocked|awaiting_separate_restore_authorization)$" .planning/phases/02-authorized-live-listening/02-UAT-RECHECK.md</automated>
    <human-check>Confirm the UAT record matches the one fixed terminal label or successful bounded listening observation and contains no sensitive material.</human-check>
  </verify>
  <verification>The recheck artifact names one fixed terminal stage or the bounded success path, and process validation leaves at most the one exact intended app open.</verification>
  <done>One fresh attempt yields decisive sanitized evidence, with no repeated sign-in and no ambiguous stage collapse.</done>
  <resume-signal>Type exactly: Authorize one fresh diagnostic native UAT</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Owner-operated WebView → volatile native handoff | The owner may create one credential; the agent must never inspect or retain it. |
| Exact app binary → provider | Only the repaired fixed contracts may run, once, after offline proof. |
| Keychain → UAT evidence | Only fixed item existence metadata may be observed after full success. |
| App process set → UAT interpretation | More than one process makes session/telemetry evidence invalid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-17-01 | Information Disclosure | live credential/session evidence | high | mitigate | Owner-only entry, fixed labels, no secret retrieval/output/capture, sanitized artifact scan. |
| T-02-17-02 | Replay | repeated sign-in/handoff | high | mitigate | Exactly one fresh attempt and handoff; immediate terminal stop; no retry. |
| T-02-17-03 | Spoofing | duplicate/wrong app process | high | mitigate | Atomic launcher, exact one PID, exact executable path, close-all on failure. |
| T-02-17-04 | Elevation of Privilege | speculative provider behavior | high | mitigate | Fixed operations only; no endpoint/header/body variation or access-control workaround. |
| T-02-17-05 | Tampering | Keychain state | high | mitigate | Metadata-only existence after full success; no Sign Out/Clear Local Session; restore relaunch separately authorized. |
| T-02-17-06 | Denial of Service | provider/account controls | high | mitigate | One attempt, no forced failures/recovery, and stop on protected/rate-limit/challenge outcomes. |
</threat_model>

<verification>
- Offline gates precede every Xcode/app/live action.
- UAT consumes only closed stage labels and owner observation; raw logs and screenshots are not artifacts.
- On a blocked auth result, catalog/playback/metadata remain BLOCKED or NOT OBSERVED, never inferred.
- On success, restore remains untested until a separate exact-build authorization.
</verification>

<success_criteria>
- The sign-in issue is localized to web extraction, profile, entitlement, persistence, or restore with one attempt.
- A full success proves durable Keychain presence by metadata only.
- Phase 02 listening behavior is observed once if authentication succeeds, without forced edge conditions.
- No duplicate app, secret evidence, repeated login, or speculative provider action occurs.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-17-SUMMARY.md` when done.
</output>
