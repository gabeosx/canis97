---
phase: 02-authorized-live-listening
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift
  - SiriusMac/Catalog/ListeningPresentationModel.swift
  - SiriusMac/Catalog/ListeningView.swift
  - SiriusMac/Authentication/AuthenticationView.swift
  - SiriusMacTests/ListeningCompositionTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01, refresh exposes only currently entitled standard and app-only channel-linear records; Xtra, replay, on-demand, ambiguous, and unsupported entities never appear as playable channels."
    - "Per D-02, every record has a stable nonempty SiriusXM identity; number, name, description, category, and artwork reference are independently optional and never replace identity."
    - "Per D-02, empty filtered results are a successful empty lineup only when the provider collection itself was valid; missing/null/malformed collections fail closed."
    - "Per D-02, duplicate identical identities collapse deterministically, conflicting duplicates fail the refresh, integral channel numbers reject overflow/fractional coercion, Unicode presentation survives without lossy truncation, and ties end with stable identity ordering."
    - "Per D-03, a refresh failure preserves the last valid snapshot as explicitly stale while exposing the new failure; stale presence never authorizes tuning."
    - "The minimal native entitled view shows deterministic category/number ordering, freshness, empty/failure state, and one selected stable identity without issuing provider calls from SwiftUI."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift"
      provides: "Semantic channel, catalog snapshot, freshness, entitlement, artwork-reference, and catalog-failure models"
      contains: "LiveCatalogSnapshot"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift"
      provides: "Invented fixture coverage for filtering, identity, optionals, edges, ordering, freshness, and cache non-authority"
      contains: "LiveCatalogAdapterTests"
    - path: "SiriusMac/Catalog/ListeningView.swift"
      provides: "Minimal native entitled channel browser with explicit freshness and failure state"
      contains: "ListeningView"
  key_links:
    - from: "SiriusXMClient.catalog"
      to: "LiveListeningAdapter.refreshCatalog"
      via: "the current authorized session lease and exact empirically supported catalog operation"
      pattern: "refreshCatalog"
    - from: "ListeningPresentationModel.refresh"
      to: "SiriusXMClient.catalog"
      via: "an injected Sendable listening-flow protocol and generation check"
      pattern: "catalog"
    - from: "ListeningView"
      to: "ListeningPresentationModel"
      via: "semantic actions and state only; no transport or request construction in SwiftUI"
      pattern: "ListeningPresentationModel"
  flagged_assumptions:
    - id: "SP-EDGE-CAT-03-UNCLASSIFIED"
      statement: "A new catalog, entitlement, or authorization failure shape outside the closed taxonomy remains unsupported and requires compatibility review; it cannot be inferred from cached data."
      flagged: true
  prohibitions:
    - statement: "The decided capability matrix must never silently expand into Xtra, replay, on-demand, SiriusXM-wide search, recording/download, arbitrary provider operations, or an alternate playback engine."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Deliver an entitled, deterministic, freshness-aware native channel lineup through the fixed live contract established by the checkpoint.

Purpose: Give the subscriber a truthful selection surface while keeping provider schemas and tune authority inside the reusable client.
Output: Public semantic catalog models/API, strict adapter tests, last-valid stale behavior, and minimal native channel browser.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-RESEARCH.md
@.planning/phases/02-authorized-live-listening/02-PATTERNS.md
@.planning/phases/02-authorized-live-listening/COVERAGE.md
@.planning/phases/02-authorized-live-listening/02-LIVE-CONTRACT.md
@.planning/phases/02-authorized-live-listening/02-02-SUMMARY.md
@.planning/phases/02-authorized-live-listening/02-03-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
@Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
@Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift
@SiriusMac/Authentication/AuthenticationView.swift

<interfaces>
- Plan 02-01 provides provider-agnostic semantic/Wave 0 seams; Plan 02-02 records the supported live facts; Plan 02-03 encodes the exact fixed catalog operation/decoder and safe handoff boundary from those facts.
- Public Phase 01 models are immutable `Sendable`/`Equatable` semantic values and keep integration-wire details internal.
- `AuthenticationView` owns the production `AuthenticationComposition`; the same composed client must feed listening after `.entitled` rather than creating a second client/session.
- The app test target is a manual PBX source graph rooted at E4/E1; every new app/test file needs exactly one reference/group/source membership.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `LiveListeningAdapter.swift` — maps the already-fixed supported catalog decoder into D-01 filtering, identity, ordering, and freshness semantics; unsupported candidates remain absent.
- `SiriusXMClient.swift` — replaces the catalog's current `.unavailable` result with an async current-session semantic refresh while preserving the actor boundary.
- `AuthenticationView.swift` — transitions confirmed entitled composition to one injected listening model/view and preserves sign-out.
- `project.pbxproj` — registers catalog/listening sources and their XCTest once without altering signing/build settings.

### New files and symbols

- `LiveListeningModels.swift` — `LiveChannelID`, `LiveChannel`, `ChannelEntitlement`, `ChannelArtworkReference`, `CatalogFreshness`, `CatalogFailure`, `LiveCatalogSnapshot`, and expanded `CatalogAvailability`.
- `LiveCatalogAdapterTests.swift` — expands the pre-checkpoint Wave 0 suite with complete CAT-01 through CAT-03 semantic behavior.
- `ListeningPresentationModel.swift` — `@MainActor @Observable ListeningPresentationModel`, `ListeningFlow`, and explicit browse/refresh/selection state.
- `ListeningView.swift` — minimal native channel list/grouping/freshness/failure UI.
- `ListeningCompositionTests.swift` — app-level one-client composition, no-cache-authorization, selection, and confirmed-state regressions.

### Public API changes

- `SiriusXMClient.catalog()` becomes `async -> CatalogAvailability` and returns semantic snapshots/failures. This is a `costly` first-class package contract because later app/library consumers depend on it; provider schemas and endpoints remain private.

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-04-01: Publish the strict entitled catalog snapshot</name>
  <precondition>`02-LIVE-CONTRACT.md` says `Gate Result: SUPPORTED`, `COVERAGE.md` marks catalog refresh/entity filtering `SUPPORTED`, and Plan 02-03 encoded the matching fixed request/decoder; otherwise halt with the typed unsupported result.</precondition>
  <reversibility rating="costly">The semantic channel/catalog types and async API become the reusable package contract consumed by the app and later library users.</reversibility>
  <read_first>
    - `02-LIVE-CONTRACT.md`, `COVERAGE.md`, and `02-03-SUMMARY.md` — use only the supported semantic operation/shape and already-fixed decoder; do not reproduce live evidence or add a provider operation.
    - `Public/AuthenticationModels.swift` — follow immutable Sendable/Equatable semantic style and redacted opaque-reference behavior.
    - `Public/SiriusXMClient.swift` — preserve actor/session authority and convert only the current catalog implementation.
    - `InternalAdapters/LiveListeningAdapter.swift` — promote the checkpoint-proven shape behind strict decoding; retain unsupported terminal classification.
    - `Tests/SiriusXMClientTests/SanitizedNativeResponseFixtures.swift` — use invented `fixture_marker` payloads only; do not copy live bodies.
    - `02-CONTEXT.md` D-01 through D-03 — exact filter, ordering, freshness, and cache non-authority decisions.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/LiveListeningAdapter.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift</files>
  <behavior>
    - Test 1: per D-01, only positively classified entitled standard/app-only channel-linear fixtures survive; Xtra/replay/on-demand/ambiguous/unentitled fixtures are absent.
    - Test 2: per D-02, identity is required and authoritative while number/name/description/category/artwork are independently optional; a valid empty collection yields an empty snapshot.
    - Test 3: missing/null/malformed collection, blank identity, conflicting duplicate identity, fractional/overflow channel number, unknown entitlement/entity classification, redirect/control/status/content-type drift all fail closed.
    - Test 4: exact duplicate identities collapse; deterministic grouping/order is category label, numeric channel number, available name, then stable identity, with the final identity tie-break preserving stable output.
    - Test 5: decomposed/composed Unicode presentation strings remain valid Swift strings without byte-count truncation; identity matching uses the adapter's documented opaque string semantics.
    - Test 6: per D-03, a failed refresh with a prior snapshot returns that snapshot as stale plus the current failure; no prior snapshot returns failure only, and neither result authorizes tune.
  </behavior>
  <action>Write `LiveCatalogAdapterTests.swift` first using invented/sanitized fixtures for all six CAT-02 probe edges plus D-01/D-03. Define `LiveChannelID` as a nonempty opaque semantic identity, `LiveChannel` with independently optional presentation fields, closed entitlement/failure enums, and `LiveCatalogSnapshot` with refresh date and explicit fresh/stale state. Keep `ChannelArtworkReference` opaque/redacted and non-Codable; artwork fetching remains Plan 02-06.

Consume only Plan 02-03's fixed supported catalog operation and strict decoder; do not add, infer, or modify provider request facts unless the canonical artifact and contract tests are updated together. Filter per D-01. Treat a structurally valid collection with zero entitled linear survivors as a valid empty lineup; missing/null/malformed provider collection is unsupported. For D-02, collapse exact duplicate records, reject conflicting records sharing identity, accept only integral in-range channel numbers, retain decoded Unicode presentation, and sort deterministically by category, number, name, then identity. Do not impose an undocumented provider string-length meaning; enforce only transport/body bounds needed for safe decoding.

Make `SiriusXMClient.catalog()` async, require a current session lease and entitlement, execute one refresh with no automatic retry, then revalidate generation before publishing. Retain the last valid semantic snapshot inside actor memory. On later failure return it with stale freshness and the new closed failure per D-03; never let snapshot presence create tune/resolution authority. Extend only closed safe diagnostic operation/outcome cases needed for catalog classification.</action>
  <acceptance_criteria>
    - Every CAT-02 boundary/adjacency/empty/encoding/ordering/precision behavior has a named passing fixture test.
    - Only entitled standard/app-only channel-linear fixtures reach `LiveCatalogSnapshot`.
    - Unknown/conflicting/malformed/provider-control input fails closed without raw detail.
    - Last-valid data is explicitly stale after failure and no public type carries provider URL/body/header material.
    - `catalog()` consults current actor session state and makes at most one request per explicit refresh.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter LiveCatalogAdapterTests</automated>
  </verify>
  <done>The reusable client returns a deterministic, semantic entitled lineup with honest stale/failure behavior and no cache-based authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-04-02: Browse and select the semantic lineup natively</name>
  <reversibility rating="reversible">The Phase 02 single-window browse presentation is app-internal and Phase 3 may replace its layout while retaining the model/client contracts.</reversibility>
  <read_first>
    - `AuthenticationView.swift` and `AuthenticationPresentationModel.swift` — transition only from confirmed `.entitled`, retain sign-out, and reuse the same composed client.
    - `AuthenticationView.swift` `AuthenticationComposition` — expose the existing client; do not create another session authority.
    - `AuthenticationPresentationModelTests.swift` — follow main-actor task/generation test patterns.
    - `SiriusMac.xcodeproj/project.pbxproj` — register each new source/test once in the root-selected targets.
    - `Public/LiveListeningModels.swift` — render only semantic snapshots and stable IDs.
  </read_first>
  <files>SiriusMac/Catalog/ListeningPresentationModel.swift, SiriusMac/Catalog/ListeningView.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMacTests/ListeningCompositionTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: entitled production-like composition injects the same SiriusXMClient into one listening model; non-entitled states expose no browse surface.
    - Test 2: refresh is single-flight, stale completion from an older generation cannot replace a newer snapshot, and user refresh performs exactly one client call.
    - Test 3: rows follow snapshot order, show available number/name/category plus artwork fallback and stable identity fallback, and expose fresh/stale/empty/failure state explicitly.
    - Test 4: selecting a row stores only `LiveChannelID`; it does not call tune/resolve, infer entitlement, or mark playback active.
    - Test 5: stale snapshot rows remain browsable but the view/model exposes their stale state and contains no path that treats selection as authorization.
  </behavior>
  <action>Create a small `ListeningFlow: Sendable` protocol for `catalog() async -> CatalogAvailability` and make the existing composed client conform at the app boundary. Build `ListeningPresentationModel` as `@MainActor @Observable` with explicit idle/loading/available/stale/empty/failed state, selected `LiveChannelID?`, one refresh task/generation, and cancel/reset hooks for sign-out. Publish only current-generation results.

Create `ListeningView` with native SwiftUI grouped/list rows using the deterministic client order, explicit freshness label, refresh button, empty/failure copy, selected row state, available semantic fields, and stable identity fallback. Actions call the model only. Per D-03, selection changes local stable identity but cannot call stream resolution or imply playable state. Update `AuthenticationView` to retain the existing auth UI until `.entitled`, then render one listening view using the same composed client and keep Sign Out/Clear Local Session behavior available.

Add `ListeningCompositionTests` before implementation for same-client composition, non-entitled reachability, single-flight/stale completion, selection-with-zero-tune, and stale browse state. Register all new sources/tests exactly once in the app/test targets; preserve the E4/E1 graph, package dependency, deployment target, and signing settings.</action>
  <acceptance_criteria>
    - A confirmed entitled subscriber can explicitly refresh, browse, and select the semantic lineup in the native app.
    - Non-entitled states cannot construct the listening surface or call catalog.
    - Selection performs zero tune/resolution work and stale catalog is visibly stale.
    - Older refresh completions cannot replace current presentation state.
    - The focused app test and full package suite pass with no network.
  </acceptance_criteria>
  <verify>
    <automated>plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/ListeningCompositionTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient</automated>
  </verify>
  <done>An entitled subscriber can browse and select a deterministic, freshness-aware native lineup without bypassing current tune authorization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider catalog JSON → semantic snapshot | Untrusted entity classifications and presentation fields enter strict filtering/decoding. |
| Last-valid snapshot → native browse UI | Cached semantic data is useful for browsing but has no playback authority. |
| Row selection → later tune flow | Stable identity crosses into selection without claiming authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure | catalog diagnostics/models | high | mitigate | Semantic allow-listed models and diagnostics only; no raw body/request/resource fields. |
| T-02-02 | Information Disclosure / Tampering | catalog request | high | mitigate | Checkpoint-supported fixed operation, exact host policy, cache bypass, and redirect cancellation. |
| T-02-03 | Denial of Service | refresh | medium | mitigate | Explicit single-flight refresh, one request, cancellation/generation check, no automatic retry. |
| T-02-04 | Tampering | provider JSON/artwork reference | medium | mitigate | Body bounds, strict decoder, positive entity/entitlement filtering, opaque artwork reference, and conflict rejection. |
| T-02-05 | Elevation of Privilege | stale snapshot/selection | high | mitigate | Catalog API requires active entitlement; stale/selected identity cannot invoke or satisfy tune authorization. |
| T-02-06 | Tampering | obsolete refresh | high | mitigate | Model/client generations are checked after awaits before snapshot or selection-adjacent state publication. |
</threat_model>

<verification>
- Package tests cover all CAT-01 through CAT-03 filters and all six CAT-02 probe edges.
- App tests cover same-client entitled composition, explicit refresh, stale completion, selection, and zero cache authorization.
- Full package/app suites remain green with invented data only.
</verification>

<success_criteria>
- An entitled subscriber can refresh and browse only the decided linear lineup with stable identity and explicit freshness.
- Catalog or entitlement failure remains visible and cannot make a stale channel playable.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-04-SUMMARY.md` when done.
</output>
