---
phase: 02-authorized-live-listening
plan: "13"
type: execute
wave: 12
depends_on: ["02-12"]
files_modified:
  - SiriusMac/Authentication/ClosedAuthenticationOracle.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/RestorableAuthenticationCredentialSource.swift
  - SiriusMac/Authentication/WebCredentialSelectionPolicy.swift
  - SiriusMac/Authentication/WebAuthenticationBridge.swift
  - SiriusMacTests/AuthenticationPresentationModelTests.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
  - SiriusMacTests/WebAuthenticationBridgeTests.swift
  - script/test_offline_auth_matrix.sh
  - script/tests/OfflineAuthenticationMatrixTests.swift
autonomous: true
gap_closure: true
requirements: [CAT-03, PLAY-04]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The UI and closed telemetry distinguish web credential selection, native profile verification, entitlement verification, persistence, and restore without showing dynamic cookie names or material."
    - "Missing, malformed, unavailable, or valid synthetic local credentials produce exact fixed outcomes; a valid restored credential completes with a restore-completed event and zero WebView activity."
    - "A fresh explicit sign-in retires and rotates the prior nonpersistent WebView session before loading the player entry, so retry cannot reuse the prior token or cookie store."
    - "Web credential missing, malformed, ambiguous, transferred, and already-consumed outcomes remain single-consumption and fixed-label only."
    - "The local/web/native/persistence/restore portions of the synthetic matrix turn green in a standalone no-host harness; Xcode test hosts remain prohibited until 02-14 also closes cleanup ordering and 02-15 closes launcher invariants."
  artifacts:
    - path: "SiriusMac/Authentication/ClosedAuthenticationOracle.swift"
      provides: "Fixed cross-stage state and presentation vocabulary"
      contains: "ClosedAuthenticationStage"
    - path: "SiriusMac/Authentication/WebCredentialSelectionPolicy.swift"
      provides: "Pure injected-cookie selection and payload classifier"
      contains: "WebCredentialSelectionPolicy"
    - path: "script/test_offline_auth_matrix.sh"
      provides: "No-host complete synthetic matrix gate"
      contains: "OfflineAuthenticationMatrixTests"
  key_links:
    - from: "RestorableAuthenticationCredentialSource"
      to: "ClosedAuthenticationOracle"
      via: "fixed local credential availability and restore-completed events"
      pattern: "restoreCompleted"
    - from: "WebAuthenticationBridge"
      to: "WebCredentialSelectionPolicy"
      via: "one injected cookie snapshot reduced to a closed selection result"
      pattern: "WebCredentialSelectionPolicy"
    - from: "AuthenticationPresentationModel"
      to: "AuthenticationPresentationCopy"
      via: "exact stage-specific terminal state and fixed status label"
      pattern: "ClosedAuthenticationStage"
  prohibitions:
    - statement: "No dynamic cookie-name inventory, cookie value, credential, URL, header, body, account material, raw error, or secret-derived value may be logged, rendered, persisted, or printed by the matrix."
      status: unverified
      flagged: true
    - statement: "No Xcode test host may run until the standalone complete synthetic matrix has exited successfully in the same execution."
      status: unverified
      flagged: true
    - statement: "A fresh explicit sign-in may not reuse the previous nonpersistent WebView, website data store, volatile handoff, or selected credential."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Wire the package's closed authentication oracle into truthful native presentation and prove the complete local/Web/native/persistence/restore matrix without touching the live account.

Purpose: The visibly signed-in WebView and rejected native transaction are different states. This plan makes each boundary decisive, removes secret-adjacent name inventory, and guarantees that a later authorized sign-in uses a genuinely fresh nonpersistent WebView.
Output: Fixed stage presentation, local restore telemetry, pure web credential selection, fresh WebView rotation, and a standalone offline matrix gate.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/debug/token-validity-before-resignin.md
@.planning/debug/session-lost-on-relaunch.md
@.planning/debug/live-auth-unsupported-no-logs.md
@.planning/phases/02-authorized-live-listening/02-12-SUMMARY.md
@SiriusMac/Authentication/AuthenticationPresentationModel.swift
@SiriusMac/Authentication/RestorableAuthenticationCredentialSource.swift
@SiriusMac/Authentication/WebAuthenticationBridge.swift
@SiriusMac/Authentication/FirstPartyTokenCookiePolicy.swift

<interfaces>
- `RestorableAuthenticationCredentialSource.prepareForAutomaticRestore()` already has four semantic results and never falls through to WebView for missing or unusable local material.
- `WebAuthenticationBridge.Result` already closes web selection into transferred, missing, ambiguous, malformed, cancelled, and consumed; keep those semantics and move payload selection into a pure injected policy.
- `AuthenticationPresentationModel` owns the visible stage and currently has an unused `diagnostics` array. Replace that disconnected surface with the closed oracle; do not add a free-form message sink.
- `WebAuthenticationWebsiteSession.installFreshNonpersistentSession()` rotates the configuration and WebView generation. Fresh sign-in must bulk-retire the old app-owned nonpersistent session before loading the next player entry.
</interfaces>
</context>

## Artifacts this phase produces

- A Foundation-only closed authentication oracle shared by standalone tests and native presentation.
- A no-host test command that uses only injected local readers, cookie snapshots, native responses, and stores.
- Fixed local-load, web-selection, profile, entitlement, persistence, and restore labels.
- A fresh-attempt WebView rotation rule with no dynamic cookie-name diagnostics.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-13-01: Render one complete synthetic transaction from fixed stage inputs</name>
  <reversibility rating="costly">The closed stage vocabulary becomes the app's diagnostic and presentation contract, but contains no provider-specific material.</reversibility>
  <read_first>
    - `02-12-SUMMARY.md` — consume the exact public unauthorized/forbidden and persistence-failure semantics delivered by the package.
    - `AuthenticationPresentationModel.swift` state/copy switches — profile and entitlement rejection currently collapse to one title and the diagnostics array is disconnected.
    - `token-validity-before-resignin.md` — the future oracle must answer which boundary failed without claiming token invalidity.
    - `AuthenticationPresentationModelTests.swift` — preserve fixed-copy and no-background-retry assertions.
  </read_first>
  <files>SiriusMac/Authentication/ClosedAuthenticationOracle.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift, script/test_offline_auth_matrix.sh, script/tests/OfflineAuthenticationMatrixTests.swift</files>
  <behavior>
    - Web transferred then profile unauthorized/forbidden renders a fixed profile-boundary terminal label.
    - Profile completed then entitlement unauthorized/forbidden renders a fixed entitlement-boundary terminal label.
    - Both native stages completed then save failed renders a fixed non-durable label and never ordinary Ready.
    - Both native stages and save completed renders durable Ready; restored success additionally renders restore completed.
    - Every stage/outcome label survives a secret-canary scan over all rendered and logged strings.
  </behavior>
  <action>Create a Foundation-only `ClosedAuthenticationOracle` whose inputs and outputs are enums, not strings or transport objects. Drive `AuthenticationPresentationModel` and `AuthenticationPresentationCopy` from that oracle so profile rejection, entitlement rejection, persistence failure, durable readiness, and restore completion have distinct fixed titles/messages/status labels. Remove the disconnected mutable diagnostic array and `SafeAuthenticationDiagnostic` if no production path consumes them; do not replace them with arbitrary strings. Create the standalone Swift matrix driver and shell wrapper so it compiles/runs only the pure oracle plus package semantic model sources. The wrapper must use a fresh temporary output path, print only test names/pass-fail, and exit before any Xcode test or app-host command.</action>
  <acceptance_criteria>
    - Every bounded-UAT oracle row maps to exactly one fixed presentation/telemetry label.
    - Persistence failure cannot satisfy the UI's `isReady` or `canSignOut` durable-ready conditions.
    - The standalone oracle matrix exits green without Xcode, app launch, Keychain, WebKit, or network access.
    - No free-form provider value can enter the oracle API.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh --oracle-only</automated>
  </verify>
  <done>One synthetic transaction is explainable from web handoff through durable readiness using fixed secret-free states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-13-02: Cover local credential availability and zero-WebView restore</name>
  <reversibility rating="reversible">Local availability telemetry is an app-internal closed adapter around the existing Keychain loader.</reversibility>
  <read_first>
    - `RestorableAuthenticationCredentialSource.swift` preparation and attempt-origin state machine.
    - `KeychainCredentialStore.LoadOutcome` in `KeychainCredentialStore.swift` — consume only semantic missing/invalid/unavailable/credential cases.
    - `SelectedAuthenticationCompositionTests.swift` automatic restore tests — preserve zero cookie reads and zero sign-in loads.
    - Locked user intent — no real Keychain lookup; injected synthetic loaders only.
  </read_first>
  <files>SiriusMac/Authentication/RestorableAuthenticationCredentialSource.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift, script/tests/OfflineAuthenticationMatrixTests.swift</files>
  <behavior>
    - Missing local material emits local-credential-missing and performs zero WebView/native/store work.
    - Invalid synthetic bytes emit local-credential-invalid; reader unavailable emits local-credential-unavailable; neither falls through to WebView or provider work.
    - Valid synthetic stored material is consumed once through profile, entitlement, and save; success emits restore-completed and performs zero WebView activity.
    - A restored native rejection remains stage-specific and schedules no retry or cleanup action.
  </behavior>
  <action>Inject a closed lifecycle telemetry recorder into `RestorableAuthenticationCredentialSource` and record only fixed availability/restore events. Feed those semantic results into the oracle/presentation path. Extend the standalone matrix with injected reader outcomes and exact collaborator call counts. Keep `KeychainCredentialStore` out of every test and do not add a material-returning accessor, metadata probe, OSStatus field, or fallback from a terminal local result to WebView. Preserve one automatic restore attempt per presentation model.</action>
  <acceptance_criteria>
    - All four local credential variants have exact stage labels and exact zero/unity call counts.
    - Restore success records restore completed and zero WebView loads/cookie reads.
    - Terminal restore failure performs no automatic retry, sign-out, clear-local-session, or provider variation.
    - The full standalone matrix remains secret-canary clean.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh --local-only</automated>
  </verify>
  <done>Local restore behavior is decisively classified without reading a real Keychain item or opening the WebView.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-13-03: Make web selection fixed-label, single-consumption, and genuinely fresh</name>
  <reversibility rating="costly">The owner-operated WebView remains the sole sign-in path, but its retry lifecycle and telemetry become stricter.</reversibility>
  <read_first>
    - `WebAuthenticationBridge.swift` bridge diagnostics, `beginUserOperatedSignIn`, `useLoggedInSession`, and website-session rotation.
    - `FirstPartyTokenCookiePolicy.swift` — retain exact name/domain/path/expiry matching and lookalike rejection.
    - `WebAuthenticationBridgeTests.swift` — replace dynamic inventory assertions with fixed selection outcomes and add pre-load rotation ordering.
    - Locked user intent — a new attempt must not reuse the existing nonpersistent WebView or token.
  </read_first>
  <files>SiriusMac/Authentication/WebCredentialSelectionPolicy.swift, SiriusMac/Authentication/WebAuthenticationBridge.swift, SiriusMacTests/WebAuthenticationBridgeTests.swift, script/tests/OfflineAuthenticationMatrixTests.swift</files>
  <behavior>
    - Missing, malformed, ambiguous, cancelled, transferred, and already-consumed web inputs produce only fixed labels; exactly one selected credential is consumed once.
    - No cookie-name inventory callback or dynamic name string exists.
    - Beginning an explicit sign-in discards volatile handoff, stops and bulk-retires the old nonpersistent session, installs a new generation, then loads the player entry exactly once.
    - If retirement fails, no new player load or credential selection is allowed and the fixed reset-failed result is terminal.
  </behavior>
  <action>Extract cookie snapshot and payload selection into a pure injected `WebCredentialSelectionPolicy` that returns a closed result plus an opaque `AuthenticationCredential` only for the exact single valid candidate. Make the bridge consume that result once. Delete dynamic first-party cookie-name recording and retain only fixed enum telemetry. Change explicit sign-in preparation to retire/rotate the old app-owned nonpersistent website session before loading the player entry; do not merely discard the in-memory handoff. Fail closed when retirement fails. Extend the standalone matrix with fake cookie snapshots and author the app-host regressions for later execution. Do not run those Xcode tests in this plan; cleanup ordering and launcher invariants must also be green first.</action>
  <acceptance_criteria>
    - The required web credential matrix and canary checks pass in the standalone harness.
    - Retry uses a new website-session generation and cannot reuse the prior cookie store or handoff.
    - Dynamic cookie-name telemetry is absent while fixed rejection reasons remain.
    - Focused app-host regressions are present but remain unexecuted until Plans 02-14 and 02-15 satisfy the remaining offline gates.
  </acceptance_criteria>
  <verify>
    <automated>bash script/test_offline_auth_matrix.sh</automated>
  </verify>
  <done>The complete synthetic authentication matrix is green before app-host verification, and any later owner sign-in begins from one freshly rotated nonpersistent WebView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Keychain semantic loader → restore oracle | Availability is observable; secret bytes and OSStatus are not. |
| WebView cookie store → volatile credential handoff | Untrusted cookie snapshots must reduce to one closed result and at most one opaque credential. |
| Closed oracle → native UI/logs | Only fixed labels may cross; provider material cannot be rendered. |
| Old WebView session → new explicit attempt | Old cookies/token/handoff must be retired before a new player entry loads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-13-01 | Information Disclosure | bridge telemetry | high | mitigate | Remove dynamic cookie-name inventory; enum-only labels plus whole-surface canary scan. |
| T-02-13-02 | Replay | explicit WebView retry | high | mitigate | Bulk-retire and rotate the old nonpersistent session before the new load; fail closed on reset failure. |
| T-02-13-03 | Spoofing | presentation readiness | high | mitigate | Closed oracle requires completed profile, entitlement, and persistence before durable Ready. |
| T-02-13-04 | Elevation of Privilege | offline matrix | high | mitigate | Inject all readers/cookies/responses/stores and assert zero fallback/provider/WebView calls. |
| T-02-13-05 | Repudiation | terminal stage | medium | mitigate | Fixed operation/outcome history identifies the exact stopped boundary without raw evidence. |
</threat_model>

<verification>
- Do not run an Xcode test host in this plan; 02-14 performs app-host verification only after the cleanup and launcher matrix is also green.
- Tests may compile production policies but must use only synthetic credentials/cookies/responses and in-memory stores.
- Confirm no new provider operations and no `COVERAGE.md` changes.
</verification>

<success_criteria>
- The local, web, native, persistence, and restore matrix is complete and green before another live attempt.
- A visibly signed-in web UI can be distinguished from a missing/malformed web credential without Web Inspector or dynamic cookie output.
- A later explicit attempt is genuinely fresh and remains single-consumption.
- Native UI can tell the owner exactly which closed stage stopped.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-13-SUMMARY.md` when done.
</output>
