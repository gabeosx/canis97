---
phase: 02-authorized-live-listening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift
  - SiriusMac/Catalog/ListeningPresentationModel.swift
  - SiriusMac/Listening/PlaybackCoordinator.swift
  - SiriusMac/Metadata/MetadataPresentationModel.swift
  - SiriusMacTests/ListeningCompositionTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, PLAY-01, PLAY-02, PLAY-03, PLAY-04, META-01, META-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Validation Wave 0 exists before any Phase 02 live-provider work and compiles all four test files named by 02-VALIDATION.md."
    - "The pre-checkpoint production shell is provider-agnostic: it contains semantic protocols, closed states, fake collaborators, and owner-visible unavailable/awaiting-contract presentation, but no SiriusXM content endpoint, provider field name, live request case, key/resource detail, or real provider call."
    - "Per D-03 and D-06, invented stale catalog data cannot authorize playback and fake command outcomes become visible only after collaborator confirmation."
    - "Per D-05 and PLAY-02, the app composition owns one provider-agnostic playback coordinator seam that later receives the supported media handoff without creating a second player."
    - "Per D-08 and D-10, deterministic fakes can drive bounded recovery and metadata state independently, including cancellation and audio-isolation assertions, before any provider contract is known."
  artifacts:
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift"
      provides: "Invented catalog semantic/filter/freshness contract shell for CAT-01 through CAT-03"
      contains: "LiveCatalogAdapterTests"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift"
      provides: "Fake resolver/player/command/recovery contract shell for PLAY-01 through PLAY-04"
      contains: "LivePlaybackCoordinatorTests"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift"
      provides: "Injected-clock semantic metadata lifecycle shell for META-01 and META-02"
      contains: "MetadataRefreshCoordinatorTests"
    - path: "SiriusMacTests/ListeningCompositionTests.swift"
      provides: "One-coordinator composition and provider-work reachability contract"
      contains: "ListeningCompositionTests"
  key_links:
    - from: "ListeningPresentationModel"
      to: "PlaybackCoordinator"
      via: "stable semantic channel identity and injected provider-agnostic command protocol only"
      pattern: "LiveChannelID"
    - from: "MetadataPresentationModel"
      to: "ListeningPresentationModel"
      via: "independent selected-channel generation with no player mutation capability"
      pattern: "MetadataPresentationModel"
    - from: "ListeningCompositionTests"
      to: "PlaybackCoordinator"
      via: "one composition-owned instance and fake runtime identity checks"
      pattern: "PlaybackCoordinator"
  prohibitions:
    - statement: "Pre-checkpoint sources and fixtures must contain no guessed provider operation, endpoint path, host, schema field, media key/resource detail, or authenticated content request."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** find my entitled linear channels and reliably control one live stream, **so that** I can listen on my Mac with truthful playback and metadata state.

<objective>
Create the deterministic Validation Wave 0 and reversible semantic shell required before the first owner-visible provider checkpoint.

Purpose: Prove the package/app test seams, single-coordinator composition, closed state vocabulary, cancellation hooks, and metadata isolation without guessing any SiriusXM content contract.
Output: All four required Wave 0 test files plus provider-agnostic semantic/app seams that compile and remain incapable of live content work.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-authorized-live-listening/02-CONTEXT.md
@.planning/phases/02-authorized-live-listening/02-RESEARCH.md
@.planning/phases/02-authorized-live-listening/02-VALIDATION.md
@.planning/phases/02-authorized-live-listening/02-PATTERNS.md
@.planning/phases/02-authorized-live-listening/COVERAGE.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
@SiriusMac/Authentication/AuthenticationPresentationModel.swift
@SiriusMacTests/AuthenticationPresentationModelTests.swift

<interfaces>
- Phase 01 already owns authentication, entitlement, the one actor-owned client session, cleanup, and secret-safe diagnostics; this plan adds no content operation to that authority.
- `AuthenticationModels.swift` establishes immutable `Sendable`/`Equatable` semantic public types and closed unavailable/unsupported/cancelled results.
- App presentation models use `@MainActor`, injected `Sendable` flows, task identity, and stale-completion rejection.
- `COVERAGE.md` keeps every provider-dependent content capability pending until Plan 02-02; no production or test file in this plan may convert a pending row into an assumed contract.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `SiriusMac.xcodeproj/project.pbxproj` — registers the provider-agnostic app/test shells exactly once while preserving the selected E4/E1 graph and all build/signing settings.

### New files and symbols

- `LiveListeningModels.swift` — reversible semantic IDs, freshness/state/failure enums, and provider-agnostic collaborator protocols with no provider wire representation.
- `ListeningPresentationModel.swift`, `PlaybackCoordinator.swift`, and `MetadataPresentationModel.swift` — app-internal state shells driven only by injected fakes until the gate succeeds.
- The four files required by `02-VALIDATION.md` — contract-first suites using invented values and provider-agnostic collaborators.

### Public API changes

- No provider operation or resolved-media representation is added. Semantic types are deliberately reversible until Plan 02-03 resolves the supported live contract and handoff.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 02-01-01: Trace one invented channel through the provider-agnostic app composition</name>
  <reversibility rating="reversible">The pre-gate protocols and closed states are internal semantic seams that Plan 02-03 can bind to the observed contract without migrating provider data.</reversibility>
  <read_first>
    - `02-CONTEXT.md` D-03 through D-06, D-09, and D-10 — encode cache non-authority, one coordinator, confirmed state, distinct failures, and metadata isolation semantically.
    - `Public/AuthenticationModels.swift` — follow semantic Sendable/Equatable types and closed result style without provider fields.
    - `AuthenticationPresentationModel.swift` — reuse main-actor generation and injected-flow patterns.
    - `AuthenticationPresentationModelTests.swift` — reuse deterministic blocking fakes and stale-completion assertions.
    - `COVERAGE.md` — leave every pending live row pending and implement no provider operation.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/LiveListeningModels.swift, SiriusMac/Catalog/ListeningPresentationModel.swift, SiriusMac/Listening/PlaybackCoordinator.swift, SiriusMacTests/ListeningCompositionTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <behavior>
    - Test 1: an invented stable channel identity can be selected and routed through one composition-owned coordinator fake without constructing provider requests or creating a player.
    - Test 2: an invented stale catalog snapshot remains browsable but produces zero tune calls until a distinct current-authorization collaborator confirms resolution.
    - Test 3: tune/pause/resume/stop state changes publish only after the injected fake confirms them; concurrent or superseded completions cannot mutate current state.
    - Test 4: the production pre-gate composition exposes an awaiting-live-contract/unavailable state and contains no automatic task that performs content work.
    - Test 5: project/static scans find only the two established Phase 01 request cases in `SiriusXMRequestContract` and no Phase 02 content request in this plan's files.
  </behavior>
  <action>Start with `ListeningCompositionTests.swift`. Define only provider-neutral types such as stable `LiveChannelID`, catalog freshness, selected-channel state, confirmed playback state, and closed failure domains needed by the four phase requirements. Create `ListeningPresentationModel` and `PlaybackCoordinator` as main-actor shells behind injected protocols whose live implementations are absent; production therefore reports awaiting compatibility evidence and cannot issue catalog, tune, resource, key, metadata, or artwork work. Route one invented selected identity through the fake composition, assert stale catalog non-authority per D-03, serialize fake commands per D-06, and prove one coordinator instance per D-05. Register sources/tests once without changing project settings. Do not edit the provider request contract, transport, host policy, client actor, or live adapters.</action>
  <acceptance_criteria>
    - The app composition and test target compile with one provider-agnostic coordinator seam and no content implementation.
    - Invented selection, stale-cache denial, confirmed-state sequencing, and supersession pass deterministically.
    - The production shell cannot reach live provider content before Plan 02-02.
    - No provider path, host, field name, resource form, key detail, or named content request case is present.
  </acceptance_criteria>
  <verify>
    <automated>plutil -lint SiriusMac.xcodeproj/project.pbxproj &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/ListeningCompositionTests &amp;&amp; test "$(awk '/^enum SiriusXMRequestContract:/ { inside=1; next } inside &amp;&amp; /static let host/ { exit } inside' Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift | rg '^[[:space:]]*case[[:space:]]+' | wc -l | tr -d ' ')" -eq 2</automated>
  </verify>
  <done>One invented channel exercises the future app composition end-to-end through reversible semantic seams while the production graph remains incapable of Phase 02 provider activity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-01-02: Create every deterministic Wave 0 contract suite</name>
  <reversibility rating="reversible">These contract-first suites use invented inputs and fake collaborators; supported semantic evidence can refine them without preserving provider evidence.</reversibility>
  <read_first>
    - `02-VALIDATION.md` Wave 0 Requirements and Per-Task Verification Map — create every named file and cover all nine requirement contracts.
    - `02-CONTEXT.md` D-01 through D-12 — express each locked behavior through invented values and provider-neutral collaborator outcomes.
    - `SessionCoordinatorTests.swift` — reuse actor blockers, exact call counts, cancellation, and out-of-order completion techniques.
    - `SanitizedNativeResponseFixtures.swift` — use only artificial fixture markers; do not add provider response shapes.
  </read_first>
  <files>Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift, SiriusMac/Metadata/MetadataPresentationModel.swift</files>
  <behavior>
    - Test 1: catalog suite encodes D-01 through D-03 as semantic filtering/freshness/cache-authority contracts using invented entity classifications rather than provider keys.
    - Test 2: playback suite encodes D-04 through D-09 with fake resolver/player/event collaborators, confirmed states, command supersession, exact finite budgets, and closed failures.
    - Test 3: metadata suite encodes D-10 through D-12 with an injected clock, independent text/artwork states, channel fallback, stale/unavailable transitions, and zero audio mutation.
    - Test 4: all fixtures and fake results are provider-agnostic and contain no request path, host, schema key, manifest/resource/key value, or live account material.
  </behavior>
  <action>Create the three package test files named by `02-VALIDATION.md` and complete the already-created `ListeningCompositionTests.swift` coverage from Task 1. The suites may begin as red semantic contracts against the provider-neutral types and fakes, then implement only enough semantic state-machine behavior in `MetadataPresentationModel.swift` and the Task 1 shells to make the deterministic contracts compile and pass. Catalog tests express positive semantic classification inputs without inventing SiriusXM JSON keys. Playback tests use fake resolution and player events, including same-channel recovery and terminal outcomes, without an `AVPlayerItem` or media resource. Metadata tests use fake text/artwork results and injected time with no request/response model. Confirm every Phase 02 test target runs offline and cannot access the live client transport.</action>
  <acceptance_criteria>
    - All four Wave 0 files exist before the live checkpoint and are assigned to their correct test targets.
    - CAT-01 through CAT-03, PLAY-01 through PLAY-04, and META-01 through META-02 each have at least one named deterministic contract.
    - Invented fixtures remain provider-neutral and secret-safe.
    - Package and app tests pass with networking unavailable and no audio device dependency.
  </acceptance_criteria>
  <verify>
    <automated>test -f Packages/SiriusXMClient/Tests/SiriusXMClientTests/LiveCatalogAdapterTests.swift &amp;&amp; test -f Packages/SiriusXMClient/Tests/SiriusXMClientTests/LivePlaybackCoordinatorTests.swift &amp;&amp; test -f Packages/SiriusXMClient/Tests/SiriusXMClientTests/MetadataRefreshCoordinatorTests.swift &amp;&amp; test -f SiriusMacTests/ListeningCompositionTests.swift &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS'</automated>
  </verify>
  <done>Validation Wave 0 covers all nine phase requirements through offline invented fixtures and fake collaborators before any provider-dependent runtime path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Invented fixtures → semantic state | Artificial values must not become an implied provider schema. |
| Cached semantic catalog → playback command | Browse state crosses to selection without granting current authorization. |
| Fake async completions → app presentation | Out-of-order results must pass current-generation checks before state publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Information Disclosure | test fixtures/models | high | mitigate | Invented values only, no provider schema/resource/token representation, and closed descriptions. |
| T-02-02 | Tampering | semantic shell | medium | mitigate | Reversible protocols, closed enums, and no provider decoder or request case before the checkpoint. |
| T-02-03 | Elevation of Privilege | stale catalog/selection | high | mitigate | Explicit no-authority semantics and zero resolver calls without current authorization fake confirmation. |
| T-02-04 | Denial of Service | fake recovery contracts | medium | mitigate | Finite injected budgets, cancellation, supersession, and exact call-count assertions. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Required outcome | Owning plan(s) | Status |
|---|---|---|---|---|
| GOAL | Phase 02 | Entitled linear browse plus one reliable live stream with truthful metadata | 02-04 through 02-07 | COVERED |
| REQ | CAT-01, CAT-02, CAT-03 | Entitled semantic catalog, stable presentation, visible failure/cache non-authority | 02-01, 02-04 | COVERED |
| REQ | PLAY-01, PLAY-02, PLAY-03, PLAY-04 | One confirmed controllable player, bounded recovery, distinct failures | 02-01, 02-05, 02-06 | COVERED |
| REQ | META-01, META-02 | Current text/artwork with independent freshness lifecycle | 02-01, 02-07 | COVERED |
| RESEARCH | Open Questions 1-3 | Empirical provider contract, AVFoundation support, and safe handoff resolved before implementation | 02-02, 02-03 | COVERED |
| RESEARCH | Wave 0 gaps | Four deterministic test files exist before the live checkpoint | 02-01 | COVERED |
| CONTEXT | D-01 through D-03 | Linear-only ordered/fresh catalog with cache non-authority | 02-01, 02-04 | COVERED |
| CONTEXT | D-04 through D-06 | Live-edge controls, one coordinator, serialized confirmed state | 02-01, 02-05 | COVERED |
| CONTEXT | D-07 through D-09 | Same-channel bounded recovery and distinct failures | 02-01, 02-06 | COVERED |
| CONTEXT | D-10 through D-12 | Independent stale/unavailable metadata with channel fallback | 02-01, 02-07 | COVERED |

## Spec-less Edge Projection Audit

| # | Requirement | Projected edge | Owning plan evidence | Status |
|---:|---|---|---|---|
| 1 | CAT-01 | Unclassified entity behavior | 02-04 flagged assumption and strict positive D-01 filter | COVERED |
| 2 | CAT-02 | Boundary | 02-04 identity/number/body bounds and one-step-beyond tests | COVERED |
| 3 | CAT-02 | Adjacency | 02-04 exact/conflicting duplicate identity behavior | COVERED |
| 4 | CAT-02 | Empty | 02-04 valid-empty versus null/malformed collection behavior | COVERED |
| 5 | CAT-02 | Encoding | 02-04 Unicode and opaque identity semantics | COVERED |
| 6 | CAT-02 | Ordering | 02-04 deterministic tie-breaking through stable identity | COVERED |
| 7 | CAT-02 | Precision | 02-04 integral range/overflow/fractional rejection | COVERED |
| 8 | CAT-03 | Unclassified failure/authority state | 02-04 flagged assumption and stale-cache non-authority | COVERED |
| 9 | PLAY-01 | Unclassified command behavior | 02-05 flagged assumption limiting commands to tune/start/pause/live-edge-resume/stop | COVERED |
| 10 | PLAY-02 | Unclassified control surface | 02-05 flagged assumption requiring the one existing coordinator | COVERED |
| 11 | PLAY-03 | Idempotency | 02-06 duplicate-signal coalescing, repeated-stop, and finite incident tests | COVERED |
| 12 | PLAY-03 | Concurrency | 02-06 cancellation/supersession/out-of-order recovery tests | COVERED |
| 13 | PLAY-04 | Concurrency | 02-05/02-06 obsolete failure/state publication tests | COVERED |
| 14 | META-01 | Empty | 02-07 empty/null unavailable fallback behavior | COVERED |
| 15 | META-01 | Encoding | 02-07 Unicode multi-scalar preservation behavior | COVERED |
| 16 | META-02 | Unclassified freshness state | 02-07 flagged assumption and closed fresh/stale/unavailable contract | COVERED |

<verification>
- Both deterministic suites pass before Plan 02-02 may launch any owner-visible live activity.
- Static checks prove the Phase 01 request enum remains unchanged and the new Wave 0 files contain no provider contract guess.
- Every `02-VALIDATION.md` Wave 0 file exists and is assigned to a concrete plan.
</verification>

<success_criteria>
- The first Phase 02 provider-dependent action is deferred to Plan 02-02.
- All nine requirements have deterministic pre-live test seams and closed provider-agnostic state contracts.
- One app composition-owned coordinator seam, cache non-authority, bounded recovery, and metadata isolation are testable without provider access.
</success_criteria>

<output>
Create `.planning/phases/02-authorized-live-listening/02-01-SUMMARY.md` when done.
</output>
