---
phase: 03-native-mac-listening-experience
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - SiriusMac/Player/CompactPlayerPresentation.swift
  - SiriusMac/Player/CompactPlayerView.swift
  - SiriusMac/Authentication/AuthenticationView.swift
  - SiriusMacTests/CompactPlayerPresentationTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - UI-01
  - UI-03
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The compact player consumes a closed renderer-independent semantic presentation and app-owned action closures; a renderer cannot own playback, persistence, system metadata, keyboard, or accessibility semantics per D-07 and D-16."
    - "When no channel is confirmed, the compact player shows `Nothing Playing`, `Open Library`, and no fabricated channel or metadata."
    - "During tune/refresh, prior confirmed channel/playback remains visible; without prior state the view shows native progress and never claims playback."
    - "A compact error keeps healthy confirmed state visible and offers only the valid `Try Again`, `Sign In Again`, or `Refresh Library` action."
    - "The populated compact order is channel identity/artwork, metadata, confirmed status, favorite state, transport controls, and library/topmost actions."
    - "Primary and secondary metadata each truncate after two lines so status and transport remain inside the fixed 400 × 288 pt content area."
    - statement: "A UI-state test verifies compact metadata truncation, complete VoiceOver and tooltip values, and that unusually long text never displaces controls."
      verification: backstop
    - "Confirmed track title and artist are semantic source values with channel/artwork context and clean channel fallback; rendered/skin-authored text cannot become Now Playing data per D-14."
    - "Favorite and transport state on compact and library surfaces are projections of the same LibraryStore, queue, metadata, and PlaybackCoordinator owned by ListeningSessionController."
  assumptions:
    - requirement_id: UI-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "Planner assumption: the approved fixed 400 × 288 semantic-slot, state, overflow, and accessibility contract fully bounds the detector's otherwise unclassified UI-01 edge; verification must abstain without rendered evidence."
    - requirement_id: UI-03
      category: unclassified
      status: unresolved
      flagged: true
      statement: "Planner assumption: shared object-identity tests plus cross-surface confirmed-state tests fully bound the detector's otherwise unclassified UI-03 edge; verification must abstain without native scene lifecycle evidence."
  artifacts:
    - path: "SiriusMac/Player/CompactPlayerPresentation.swift"
      provides: "Closed semantic slots, style roles, and app-owned action availability"
      contains: "struct CompactPlayerPresentation"
    - path: "SiriusMac/Player/CompactPlayerView.swift"
      provides: "Native fallback renderer and semantic controls"
      contains: "struct CompactPlayerView"
    - path: "SiriusMacTests/CompactPlayerPresentationTests.swift"
      provides: "State mapping, renderer isolation, layout, and long-text backstop evidence"
      contains: "CompactPlayerPresentationTests"
  key_links:
    - from: "SiriusMac/Authentication/AuthenticationView.swift"
      to: "SiriusMac/Player/CompactPlayerView.swift"
      via: "authenticated compact role renders the native fallback with the app-owned controller"
      pattern: "CompactPlayerView"
    - from: "SiriusMac/Player/CompactPlayerView.swift"
      to: "SiriusMac/App/ListeningSessionController.swift"
      via: "semantic presentation input plus action closures only"
      pattern: "presentation"
    - from: "SiriusMac/Player/CompactPlayerPresentation.swift"
      to: "SiriusMac/Metadata/MetadataPresentationModel.swift"
      via: "confirmed semantic metadata mapping with channel fallback"
      pattern: "LiveMetadataState"
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** use a cohesive native macOS player and library, **so that** one shared playback session continues correctly across app and system control surfaces.

<objective>
Replace the tracer compact surface with the approved fixed native player and establish the costly, skin-ready semantic-slot/style seam Phase 4 will consume.

Purpose: The compact player must be delightful and state-complete now while ensuring later appearance changes cannot acquire behavioral, accessibility, persistence, or system-media authority.
Output: Closed presentation values/style roles, a native fallback renderer, shared favorite/transport wiring, and UI-state tests.

Decision coverage: D-03, D-07, D-09, D-14, and D-16 are implemented here; D-07 retains its user-recorded costly rating and is not re-asked as a one-way decision.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md
@.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md
@.planning/phases/03-native-mac-listening-experience/03-VALIDATION.md
@.planning/phases/03-native-mac-listening-experience/03-PATTERNS.md
@.planning/phases/03-native-mac-listening-experience/03-03-SUMMARY.md

<interfaces>
- `ListeningSessionController` owns confirmed playback, active metadata, `LibraryStore`, `PlaybackQueue`, and semantic commands.
- `LiveMetadataState` provides confirmed/freshness-aware text and bounded artwork bytes; no provider URL is available to the view.
- `LibraryStore` exposes desired-state favorite operations keyed by `LiveChannelID`.
- `QueueDirectionAvailability` exposes Previous/Next enabled state; queue and coordinator remain outside the renderer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the confirmed compact happy path through semantic slots</name>
  <reversibility rating="costly">D-07 establishes the seam consumed by the compact UI, Phase 4 appearance renderers, system metadata, and accessibility; undoing it later would touch every presentation consumer.</reversibility>
  <files>SiriusMac/Player/CompactPlayerPresentation.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMacTests/CompactPlayerPresentationTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <read_first>
    - `SiriusMac/Player/CompactPlayerPresentation.swift` (create; inspect if present from an interrupted run)
    - `SiriusMac/Player/CompactPlayerView.swift` (create; inspect if present from an interrupted run)
    - `SiriusMac/Authentication/AuthenticationView.swift`
    - `SiriusMac/Catalog/ListeningView.swift:82-218`
    - `SiriusMac/Metadata/MetadataPresentationModel.swift`
    - `SiriusMac/App/ListeningSessionController.swift`
    - `SiriusMac.xcodeproj/project.pbxproj`
    - `.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md` Native presentation seam, Compact player, Typography, and Color
  </read_first>
  <behavior>
    - Test 1: confirmed playing maps stable channel identity, bounded artwork, primary/secondary metadata, playing status, favorite state, and transport availability into closed slots.
    - Test 2: paused/stopped/pending/error states map only confirmed labels/actions and never optimistic playback.
    - Test 3: native style tokens equal the approved semantic palette/spacing/typography roles and carry no behavior closures.
    - Test 4: the view sends previous/play-pause/next/favorite/library actions to injected app-owned closures and owns no coordinator/store/player.
    - Test 5: the authenticated compact scene renders `CompactPlayerView`; authentication and library roles remain separate.
  </behavior>
  <action>
Create a closed Sendable/equatable semantic model containing player background role, channel identity, optional bounded artwork, primary and secondary metadata values, confirmed status/freshness, favorite state, previous/play-pause/next availability, and typed recovery action. Keep action execution out of the value. Define semantic style roles and the native fallback tokens from UI-SPEC: 400 × 288 content, declared spacing, four text roles, dominant `#111111`, secondary `#262626`, accent `#C6FF00`, destructive `#FF453A`; accent is never the sole state signal. The model may project only validated Phase 2 semantic values and app-local stable-ID state.

Create `CompactPlayerView` as a native SwiftUI fallback renderer with native Buttons, 32 × 32 transport hit targets, SF Symbols, channel/artwork visual anchor, exactly two metadata lines each, confirmed status, favorite, Previous/Play-Pause/Next, Show Library, and Always on Top menu entry hook. It receives the presentation value plus typed app-owned closures and must not import AVFoundation, MediaPlayer, SwiftData models, client/session/transport types, or define skin manifests. Replace the authenticated compact tracer content in `AuthenticationView` with this view while preserving shared controller identity. Add source-contract and behavior tests first; register files once.
  </action>
  <acceptance_criteria>
    - `CompactPlayerPresentation` is renderer-independent and contains no playback/persistence/system/accessibility implementation object.
    - `CompactPlayerView` renders the approved 400 × 288 happy-path hierarchy with native controls and sends commands only through injected closures.
    - Play/Pause label/icon follows confirmed state and no seek, scrub, duration, replay, or Stop control exists.
    - Favorite and queue availability use the same store/queue projections as the library.
    - Native tokens preserve semantic roles without defining the Phase 4 package/schema surface.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/CompactPlayerPresentationTests</automated>
  </verify>
  <done>The authenticated compact scene renders a complete confirmed listening path through a closed semantic presentation and native fallback renderer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete compact empty, pending, failure, fallback, and overflow states</name>
  <files>SiriusMac/Player/CompactPlayerPresentation.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMacTests/CompactPlayerPresentationTests.swift</files>
  <read_first>
    - `SiriusMac/Player/CompactPlayerPresentation.swift`
    - `SiriusMac/Player/CompactPlayerView.swift`
    - `SiriusMac/Authentication/AuthenticationView.swift`
    - `SiriusMacTests/CompactPlayerPresentationTests.swift`
    - `SiriusMac/Metadata/MetadataPresentationModel.swift`
    - `.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md` Copywriting, Interaction, and Compact UI Considerations
  </read_first>
  <behavior>
    - Test 1: no confirmed channel produces exact `Nothing Playing`/`Open Library` copy and no fabricated metadata.
    - Test 2: pending work retains prior confirmed state or shows native progress when none exists.
    - Test 3: each Phase 2 failure class exposes only its valid Try Again, Sign In Again, or Refresh Library recovery.
    - Test 4: missing track/artist/artwork falls back independently to current program then channel identity/photo placeholder without invented values.
    - Test 5: very long Unicode metadata remains complete in tooltip/accessibility values, visually caps at two lines, and leaves status/transport layout metrics unchanged.
    - Test 6: appearance values cannot provide action identifiers, labels, Now Playing text, or accessibility semantics.
  </behavior>
  <action>
Implement every compact UI Consideration in the frontmatter. Map empty, pending/loading, recoverable/terminal failure, populated, missing-artwork, stale, unavailable, overflow, and long-text states from the same confirmed projection. Use exact approved copy, the `photo` artwork fallback, fixed semantic hierarchy, `.lineLimit(2)` for both metadata values, complete tooltips/accessibility values, native focus treatment, and no positional/scale/looping animation. Preserve the prior healthy confirmed state during unrelated pending or metadata/artwork failure. Add the structured long-text backstop as an executable view-state/source contract; if rendered displacement cannot be asserted automatically, leave the backstop unresolved for Plan 03-08 rather than claiming evidence.

Add deterministic SwiftUI preview fixtures for every compact state above, including unusually long Unicode metadata and unavailable queue directions. The previews consume closed semantic values and no live authentication, network, Keychain, WebKit, player, MediaPlayer, or persistence collaborator; Plan 03-08 uses them for exact rendered-state inspection.

Enforce D-14 and D-16 at the type boundary: metadata fields originate from validated app semantic state, not style/renderer text; style inputs can alter only bounded visual roles/layout values and cannot remove native controls, labels, focus, contrast fallback, actions, or state meanings.
  </action>
  <acceptance_criteria>
    - Empty, pending, failure, populated, missing-data, stale/unavailable, and long-text cases use exact approved semantics/copy.
    - Healthy confirmed playback remains visible through unrelated metadata/artwork/catalog failures.
    - Complete values reach tooltip/VoiceOver while compact metadata is visually capped at two lines and transport remains in bounds.
    - Renderer/style values cannot supply playback actions, accessibility semantics, or system metadata.
    - Every compact UI Consideration is backed by a test or the structured backstop remains explicitly abstained for manual verification.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/CompactPlayerPresentationTests</automated>
  </verify>
  <done>The compact player is state-complete, truthful, accessible by construction, and visually replaceable without transferring behavior to a renderer.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates:

- `SiriusMac/Player/CompactPlayerPresentation.swift`
  - `CompactPlayerPresentation`
  - `PlayerSemanticSlot`
  - `PlayerSemanticStyleRole`
  - `NativeCompactPlayerStyle`
  - `CompactRecoveryAction`
- `SiriusMac/Player/CompactPlayerView.swift`
  - `CompactPlayerView`
  - deterministic semantic `#Preview` fixtures for the approved compact state matrix
- `SiriusMacTests/CompactPlayerPresentationTests.swift`
  - `CompactPlayerPresentationTests`
  - `CompactPlayerViewStateTests`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session semantic state → renderer | Presentation data crosses into a replaceable visual layer. |
| Renderer controls → session commands | Native controls may request behavior but cannot implement/authorize it. |
| Appearance values → accessibility/system text | Future skin-facing values must never redefine semantic labels or Now Playing content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | CompactPlayerPresentation | high | mitigate | Project closed semantic values and bounded artwork bytes only; exclude credential/session/resource/URL/raw-provider/diagnostic types by source-contract tests. |
| T-03-02 | Elevation of Privilege | renderer action surface | high | mitigate | Renderer receives typed closures whose implementations remain on ListeningSessionController/PlaybackCoordinator; appearance values carry no executable behavior. |
| T-03-03 | Denial of Service | compact state rendering | high | mitigate | Fixed line limits, bounded artwork, finite state mapping, and no looping animation prevent unbounded layout/render work. |
| T-03-04 | Tampering | skin-ready semantic/style seam | high | mitigate | Native action identifiers, labels, focus, contrast, state meanings, and Now Playing source are outside style data and enforced by tests. |
</threat_model>

<verification>
Run focused presentation/view-state tests and the full Xcode suite after each task. The structured long-text backstop cannot be marked verified from source presence alone; it needs explicit layout/state evidence or remains for the end-of-phase rendered check.
</verification>

<success_criteria>
- The compact player satisfies the fixed hierarchy, dimensions, controls, and all six UI Considerations.
- Confirmed semantic state is the only source for labels, metadata, favorite, and transport state.
- Phase 4 can supply bounded appearance through semantic slots without owning behavior.
- Native accessibility/focus/contrast/state semantics cannot be removed or redefined by appearance data.
- Compact and library surfaces remain projections of one session.
</success_criteria>

<output>
Create `.planning/phases/03-native-mac-listening-experience/03-04-SUMMARY.md` when done.
</output>
