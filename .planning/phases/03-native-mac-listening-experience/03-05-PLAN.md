---
phase: 03-native-mac-listening-experience
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - SiriusMac/Windows/CompactWindowController.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Player/CompactPlayerView.swift
  - SiriusMac/Library/LibraryStore.swift
  - SiriusMacTests/ListeningSessionControllerTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - MAC-01
  - UI-01
  - UI-03
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After an entitled transition, both compact and library singleton windows open once per D-01; subsequent Command-L/show requests focus the existing library."
    - "Closing the library destroys only that window presentation and healthy audio/session state continues; reopening reconnects to the existing session per D-02."
    - "Closing the compact window requests normal application termination, and normal shutdown invalidates playback exactly once."
    - "The compact content area is fixed and non-resizable at 400 × 288 pt, has no full-screen behavior, restores a valid on-screen frame or centers on the active display per D-03."
    - "The library window defaults to 980 × 700 pt, is resizable down to 760 × 540 pt, and restores its own valid on-screen frame."
    - "Always on Top is off on first launch, user-controlled, persisted through the LibraryStore preference facade, and changes only the compact NSWindow level per D-04."
    - "Backgrounding, closing the library, changing window level, and frame restoration neither pause healthy audio nor create a player or custom output route."
  assumptions:
    - requirement_id: MAC-01
      category: unclassified
      status: unresolved
      flagged: true
      statement: "Planner assumption: the approved compact-close/library-close/background lifecycle matrix fully bounds the detector's otherwise unclassified MAC-01 edge; verification must abstain until the native running-app checkpoint supplies evidence."
  artifacts:
    - path: "SiriusMac/Windows/CompactWindowController.swift"
      provides: "Narrow AppKit window attachment, frame, level, close, and lifecycle behavior"
      contains: "final class CompactWindowController"
    - path: "SiriusMacTests/ListeningSessionControllerTests.swift"
      provides: "Injected window/lifecycle policy tests without opening production windows"
      contains: "WindowLifecyclePolicyTests"
  key_links:
    - from: "SiriusMac/SiriusMacApp.swift"
      to: "SiriusMac/Windows/CompactWindowController.swift"
      via: "role-scoped NSViewRepresentable attachment on exact singleton window roots"
      pattern: "CompactWindowController"
    - from: "SiriusMac/Windows/CompactWindowController.swift"
      to: "SiriusMac/Library/LibraryStore.swift"
      via: "Always on Top desired state read through preference facade"
      pattern: "alwaysOnTop"
    - from: "SiriusMac/SiriusMacApp.swift"
      to: "SiriusMac/App/ListeningSessionController.swift"
      via: "normal application termination invokes session shutdown exactly once"
      pattern: "shutdown"
---

## Phase Goal

**As a** SiriusXM subscriber, **I want to** use a cohesive native macOS player and library, **so that** one shared playback session continues correctly across app and system control surfaces.

<objective>
Finish the two native window roles and lifecycle: fixed primary compact player, resizable non-destructive library, singleton reopen/focus, safe frame restoration, and remembered Always on Top.

Purpose: macOS window behavior must preserve the already-proven listening owner and make normal application termination the only compact-close shutdown path.
Output: A narrow injectable AppKit adapter, exact scene sizing/lifecycle wiring, local preference integration, and automated policy tests ready for final native verification.

Decision coverage: D-01, D-02, D-03, and D-04 are fully implemented here.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md
@.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md
@.planning/phases/03-native-mac-listening-experience/03-VALIDATION.md
@.planning/phases/03-native-mac-listening-experience/03-RESEARCH.md
@.planning/phases/03-native-mac-listening-experience/03-04-SUMMARY.md

<interfaces>
- The Plan 03-01 `SiriusMacApp` production branch defines unique compact/authentication and library `Window` ids backed by one controller.
- `ListeningSessionController.shutdown()` must invalidate playback/session-owned observers once and must not erase credentials unless an explicit authentication cleanup action requested it.
- `LibraryStore` is the only facade for small local preferences; Always on Top defaults false.
- `CompactPlayerView` exposes app-owned Show Library and Always on Top controls but has no NSWindow ownership.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce compact-primary and library-nondestructive native lifecycle</name>
  <files>SiriusMac/Windows/CompactWindowController.swift, SiriusMac/SiriusMacApp.swift, SiriusMacTests/ListeningSessionControllerTests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <read_first>
    - `SiriusMac/Windows/CompactWindowController.swift` (create; inspect if present from an interrupted run)
    - `SiriusMac/SiriusMacApp.swift`
    - `SiriusMac/App/ListeningSessionController.swift`
    - `SiriusMacTests/ListeningSessionControllerTests.swift`
    - `SiriusMac.xcodeproj/project.pbxproj`
    - `.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md` D-01 through D-03
    - `.planning/phases/03-native-mac-listening-experience/03-RESEARCH.md` Open Questions 1 and 2
  </read_first>
  <behavior>
    - Test 1: compact attachment applies exact fixed content size, disables resizing/full screen, and uses a compact-only frame autosave name.
    - Test 2: library attachment preserves resizability/minimum size and uses a different frame autosave name.
    - Test 3: invalid/off-screen saved frame is rejected and the window centers on the active display; a valid on-screen frame is restored.
    - Test 4: exact compact-window close requests application termination once; library close never calls termination or session shutdown.
    - Test 5: application termination invokes session shutdown/playback invalidation once, while library reopen reuses the controller.
  </behavior>
  <action>
Create a narrow, injected AppKit bridge in `CompactWindowController.swift` with explicit compact/library roles. Attach to the exact `NSWindow` using a representable, apply role-specific frame autosave names, validate a restored frame intersects a current screen, and center on the active screen when not valid. For compact role set content size 400 × 288, remove resizable/full-screen behavior, and observe the exact window's `willCloseNotification` to request `NSApplication.terminate(nil)` through an injected terminator. Use the scoped notification approach instead of replacing SwiftUI's `NSWindowDelegate`. For library role set default/minimum sizing through SwiftUI/AppKit but do not terminate or alter playback on close.

Wire the adapter into the unique scene roots. Gate the initial library `openWindow(id:)` on the first entitled transition, and make every later open call target the same id/focus path. Route `NSApplication.willTerminateNotification` to idempotent `ListeningSessionController.shutdown()` so normal quit ends playback without conflating window close with credential erasure. Write tests against injected window/termination facades before live AppKit wiring and add the source/test membership exactly once. This is the planner's reversible choice for the research close-hook question.
  </action>
  <acceptance_criteria>
    - Compact and library roles have exact, separate size/frame/close behavior and frame autosave identities.
    - Compact close requests normal termination once without overwriting SwiftUI's window delegate.
    - Library close performs no shutdown/playback mutation; reopen targets the existing scene/controller.
    - App termination invalidates playback once and does not implicitly erase stored credentials.
    - Tests exercise policy through injected facades without opening a production window or launching audio.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WindowLifecyclePolicyTests</automated>
  </verify>
  <done>The compact window is the primary lifetime window and the library is safely closeable/reopenable without changing the one listening session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore valid frames and remember Always on Top without touching playback</name>
  <files>SiriusMac/Windows/CompactWindowController.swift, SiriusMac/SiriusMacApp.swift, SiriusMac/Player/CompactPlayerView.swift, SiriusMac/Library/LibraryStore.swift, SiriusMacTests/ListeningSessionControllerTests.swift</files>
  <read_first>
    - `SiriusMac/Windows/CompactWindowController.swift`
    - `SiriusMac/SiriusMacApp.swift`
    - `SiriusMac/Player/CompactPlayerView.swift`
    - `SiriusMac/Library/LibraryStore.swift`
    - `SiriusMacTests/ListeningSessionControllerTests.swift`
    - `.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md` Compact and Library window contracts
    - `.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md` D-04
  </read_first>
  <behavior>
    - Test 1: fresh preference state is false and maps the compact window to `.normal` level.
    - Test 2: user desired-state toggle persists and maps only the compact window to `.floating`; replaying the same desired state is idempotent.
    - Test 3: library level, playback state, queue, and session identity are unchanged by the toggle.
    - Test 4: compact and library frame restoration remain separate and off-screen saved frames are not applied.
    - Test 5: app menu and compact menu expose one checked state backed by the same preference.
  </behavior>
  <action>
Extend the `LibraryStore` preference projection with `alwaysOnTop` defaulting false and desired-state persistence; keep it within the existing non-secret facade. Bind the compact menu/control and checked application command to that single value. Update only the attached compact `NSWindow.level` between `.normal` and `.floating`; do not order windows repeatedly, change activation policy, create another controller, or send playback commands. Keep compact/library frame restoration keys separate. Assert idempotency, default state, menu parity, and zero playback/session mutations in tests. This implements D-04 exactly.
  </action>
  <acceptance_criteria>
    - A first run is not floating and the checked state is false.
    - Changing Always on Top updates/persists one desired state and changes only the exact compact window level.
    - Compact menu and application menu show the same checked state.
    - Library window level, playback state, queue, controller identity, and audio ownership are unchanged.
    - Frame restoration never places either window wholly off-screen and never shares role keys.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WindowLifecyclePolicyTests</automated>
  </verify>
  <done>Both native windows restore safely and the compact window's remembered topmost preference affects only its window level.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates:

- `SiriusMac/Windows/CompactWindowController.swift`
  - `CompactWindowController`
  - `WindowRole`
  - `WindowLifecycleAttaching`
  - `ApplicationTerminating`
  - `WindowAttachmentView`
- New test symbols in `SiriusMacTests/ListeningSessionControllerTests.swift`
  - `WindowLifecyclePolicyTests`
  - injected window, screen, and terminator fakes

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SwiftUI scene → AppKit NSWindow | A narrow bridge mutates native window policy. |
| Stored frame/preference → live window | Local state controls placement and floating level. |
| Window close → app/session lifecycle | Only compact close may request normal termination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | frame/topmost preferences | high | mitigate | Persist role names, frame autosave data, and a Boolean only through the non-secret facade; no playback/session/resource value enters the bridge. |
| T-03-02 | Elevation of Privilege | stale/removed channel tuning | high | mitigate | Window activation carries no tuning authority; all channel commands remain in the queue/coordinator path from Plan 03-03. |
| T-03-03 | Denial of Service | close/reopen/window attachment | high | mitigate | Role-scoped notification observers, idempotent termination/shutdown, singleton Window ids, and construction-count tests prevent duplicate windows/players. |
| T-03-04 | Tampering | window role and appearance seam | high | mitigate | Closed role enum controls frame/level/close policy; renderer/style data cannot select roles or mutate lifecycle semantics. |

## ASVS L1 Control Mapping

Common executable evidence: `xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS'`; the rows below identify the plan-local assertions that command must pass.

| Control | Applicability / plan-local trust boundary | Mitigation | Executable verification evidence |
|---------|-------------------------------------------|------------|----------------------------------|
| V2 Authentication | Not applicable — window policy neither authenticates nor changes the existing user-operated authentication flow. | Compact/library activation receives the established app controller and cannot create authentication state. | `WindowLifecyclePolicyTests` construction-count assertions prove open/reopen paths reuse the controller. |
| V3 Session Management | Applicable — window close/termination → app session lifecycle | Library close cannot retire session state; compact close requests normal termination, whose idempotent shutdown invalidates playback once without implicit credential erasure. | `xcodebuild test -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WindowLifecyclePolicyTests` exercises exact close and shutdown counts. |
| V4 Access Control | Not applicable — window activation, placement, and level carry no channel or playback authority. | All listening actions remain in the controller/queue/coordinator path created by earlier plans. | The focused policy suite asserts window operations produce zero playback, queue, or session-identity mutations. |
| V5 Input Validation | Applicable — stored frame/preference → live `NSWindow` | Accept a closed role and Boolean desired state; apply saved frames only when they intersect a current screen, with role-separated keys and deterministic fallback centering. | The focused policy suite covers off-screen rejection, valid restoration, role separation, idempotent level changes, and first-run defaults. |
</threat_model>

<verification>
Run focused policy tests plus the full Xcode suite after each task. Automated tests prove injected policy; the final Plan 03-08 checkpoint supplies required native evidence for actual close, focus, frame, background, and audio behavior.
</verification>

<success_criteria>
- The compact window is fixed, primary, safely restored, and normally terminates the app on close.
- The library is resizable, independently restored, nondestructive, and singleton-reopenable.
- Background/library lifecycle does not interrupt healthy audio or allocate another owner.
- Always on Top defaults off, persists, and affects only compact window level.
- Window bridge code remains narrow and behavior is testable without production AppKit globals.
</success_criteria>

<output>
Create `.planning/phases/03-native-mac-listening-experience/03-05-SUMMARY.md` when done.
</output>
