---
phase: 03-native-mac-listening-experience
plan: "09"
type: execute
wave: 9
depends_on: ["03-08"]
files_modified:
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Windows/CompactWindowController.swift
  - SiriusMac/Windows/NativeListDoubleActionBridge.swift
  - SiriusMac/Catalog/ListeningView.swift
  - SiriusMac/Testing/UITestHarness.swift
  - SiriusMacTests/ListeningSessionControllerTests.swift
  - SiriusMacUITests/SiriusMacUITests.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme
autonomous: true
gap_closure: true
gap_ids: [G-03-7, G-03-17]
requirements: [MAC-01, UI-01, UI-02, UI-03, ACCS-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "The authenticated compact surface always occupies an exact 400 x 288 content area without retaining the larger authentication-window frame, while a safe saved origin is still restored."
    - "A single library-row click moves native browse selection immediately and never tunes; double-click or Return tunes exactly once through the shared controller."
    - "Launched-app macOS UI automation proves compact geometry and real native row selection/activation instead of relying on source-string or model-only contracts."
    - "The UI-test launch path is explicit, DEBUG-only, deterministic, offline, and incapable of reading Keychain material, opening WebKit authentication, contacting SiriusXM, resolving media, or persisting test fixtures."
  artifacts:
    - path: "SiriusMac/Windows/CompactWindowController.swift"
      provides: "Exact compact content sizing with safe-origin restoration"
      contains: "setContentSize"
    - path: "SiriusMac/Catalog/ListeningView.swift"
      provides: "Immediate native selection plus non-competing double-click activation"
    - path: "SiriusMac/Testing/UITestHarness.swift"
      provides: "DEBUG-only offline semantic UI fixture composition"
    - path: "SiriusMacUITests/SiriusMacUITests.swift"
      provides: "Launched-app geometry, selection, and exactly-once activation regressions"
    - path: "SiriusMac.xcodeproj/project.pbxproj"
      provides: "SiriusMacUITests target and scheme membership"
  key_links:
    - from: "SiriusMacApp"
      to: "UITestHarness"
      via: "Explicit DEBUG-only process environment gate before production session composition"
      pattern: "SIRIUS_MAC_UI_TEST_MODE"
    - from: "CompactWindowController"
      to: "NSWindow"
      via: "Restore origin, then enforce exact compact content size"
      pattern: "setContentSize"
    - from: "LibraryView"
      to: "ListeningPresentationModel.selectedChannelID"
      via: "Native List selection binding unaffected by activation recognition"
      pattern: "selection"
    - from: "Library double action"
      to: "ListeningSessionController.tune"
      via: "Clicked/selected stable channel identity and captured visible origin IDs"
      pattern: "tune"
  prohibitions:
    - statement: "UI automation must not authenticate, read or erase Keychain state, load WebKit, contact SiriusXM, resolve a stream, play audio, or reuse a subscriber's persisted library data."
      status: unverified
      flagged: true
    - statement: "Fixing double-click must not replace native List selection with a custom visual selection state or make a single click tune."
      status: unverified
      flagged: true
---

<objective>
Fix the compact-window sizing and library-selection regressions found in Phase 3 UAT, and replace the missing interaction coverage with deterministic launched-app macOS UI tests.

Purpose: Restore the intended compact native form and immediate browse-selection behavior while making both failures reproducible without subscriber credentials or repeated manual UAT.
Output: Exact compact geometry, non-competing native selection/activation, a safe offline UI-test harness, and XCUITest regression coverage.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md
@.planning/phases/03-native-mac-listening-experience/03-UI-SPEC.md
@.planning/phases/03-native-mac-listening-experience/03-UAT.md
@.planning/debug/compact-window-excess-chrome.md
@.planning/debug/library-selection-gesture-stall.md
@SiriusMac/SiriusMacApp.swift
@SiriusMac/Windows/CompactWindowController.swift
@SiriusMac/Catalog/ListeningView.swift
@SiriusMacTests/ListeningSessionControllerTests.swift

<interfaces>
- `CompactPlayerView` owns a fixed 400 x 288 semantic canvas; `CompactWindowController` is the only allowed AppKit window-policy bridge.
- The compact scene initially hosts a 760 x 620 authentication view, so the authenticated transition must resize the existing `NSWindow` rather than relying on scene defaults.
- `LibraryView` uses a native `List` whose selection binding synchronously calls `ListeningPresentationModel.select`; browse selection must stay independent from confirmed playback.
- Double-click and Return tune through `ListeningSessionController.tune(channelID:originIDs:)`; cached row identity never grants playback authority.
- The existing app target has a unit-test host but no UI-test target. Any launched-app fixture path must be explicit, DEBUG-only, offline, and constructed before production authentication/session objects.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 03-09-01: Establish a credential-free launched-app UI automation path</name>
  <reversibility rating="reversible">The harness and target are debug/test-only additions with no production data format or public API change.</reversibility>
  <read_first>
    - `SiriusMac/SiriusMacApp.swift` — preserve the inert unit-test-host guard and ensure the UI-test branch is chosen before `ListeningSessionController()` is constructed.
    - `SiriusMac/Catalog/ListeningView.swift` and `SiriusMac/Player/CompactPlayerView.swift` — compose the real production views with synthetic semantic state rather than duplicating their rendering.
    - `SiriusMac.xcodeproj/project.pbxproj` and `SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme` — follow existing target IDs/build phases and add only the UI-test target/product/scheme entries required by Xcode.
    - `SiriusMac/Authentication/AuthenticationView.swift` (`AuthenticationComposition`) and `SiriusMac/Library/LibraryStore.swift` — use injected/in-memory collaborators only; do not create production Keychain, WebKit, URLSession, SwiftData disk, or AVFoundation collaborators.
  </read_first>
  <files>SiriusMac/SiriusMacApp.swift, SiriusMac/Testing/UITestHarness.swift, SiriusMacUITests/SiriusMacUITests.swift, SiriusMac.xcodeproj/project.pbxproj, SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme</files>
  <behavior>
    - `SIRIUS_MAC_UI_TEST_MODE=1` is honored only in DEBUG builds and selects the offline fixture roots before production session construction.
    - The compact and library windows render the real production views with two or more synthetic entitled channels and an in-memory local store.
    - The harness exposes stable accessibility identifiers/values for the compact canvas, row selection, and tune count/confirmed channel without exposing a general runtime diagnostics surface.
    - Launch and teardown perform zero provider, Keychain, WebKit, persisted SwiftData, stream-resolution, AVFoundation, or audio operations.
  </behavior>
  <action>Add a macOS UI-test target and a narrow DEBUG-only offline launch branch. Build the fixture from in-memory semantic collaborators and fixed non-sensitive channel data, open the same compact and library scenes, and add only the accessibility values needed to assert real behavior. The production branch must remain byte-for-byte unreachable from the UI-test environment gate: check the gate before constructing the normal session controller. Add a first XCUITest smoke case proving both windows appear and teardown terminates the launched app.</action>
  <acceptance_criteria>
    - The UI-test target launches the app without presenting authentication and without reading existing subscriber state.
    - Both real view implementations are present and queryable by stable accessibility identifiers.
    - The fixture has no network/media/auth/persistence side effects and disappears completely after teardown.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath /tmp/sirius-mac-03-09-ui-build-only CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>A deterministic launched-app test can exercise the real compact and library views without subscriber state or external services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-02: Enforce exact compact content geometry after authentication and restoration</name>
  <reversibility rating="reversible">The change is contained to private window policy and can be reverted without migrating preferences or UI state.</reversibility>
  <read_first>
    - `SiriusMac/Windows/CompactWindowController.swift` — inspect `configure`, `restoreFrameOrCenter`, and the distinction between content and outer frame.
    - `SiriusMac/SiriusMacApp.swift` — preserve the large authentication state while resizing only when the compact listening slice attaches.
    - `SiriusMacTests/ListeningSessionControllerTests.swift` — extend policy tests with a configured-window seam or real in-process `NSWindow` assertion.
    - `.planning/debug/compact-window-excess-chrome.md` — implement the diagnosed restore-origin/exact-size direction.
  </read_first>
  <files>SiriusMac/Windows/CompactWindowController.swift, SiriusMacTests/ListeningSessionControllerTests.swift, SiriusMacUITests/SiriusMacUITests.swift</files>
  <behavior>
    - Attaching the compact role to a 760 x 620 current window immediately produces a 400 x 288 content rect.
    - A valid saved compact origin is retained, but saved width/height never override the fixed content size.
    - Invalid/off-screen origins still center safely; library sizing and restoration remain unchanged.
    - The launched-app test compares the compact canvas and window content geometry closely enough to catch the reported excess border.
  </behavior>
  <action>Refactor restoration so compact and library roles have explicit behavior: library may restore a valid full frame, while compact restores only a valid origin and always calls `setContentSize(policy.defaultContentSize)` after style-mask configuration. Do not remove normal titlebar chrome unless the UI spec requires it; eliminate only the unintended surplus content area. Add an AppKit regression starting from an oversized window/saved frame and extend XCUITest to assert the compact canvas fills the content region at 400 x 288.</action>
  <acceptance_criteria>
    - The authenticated compact window cannot remain at the authentication scene's 760 x 620 content size.
    - Autosave preserves position but cannot reintroduce a large compact frame.
    - Library window resizing/restoration and compact close/always-on-top semantics remain intact.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath /tmp/sirius-mac-03-09-window-build-only CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>The compact player opens at its deliberate fixed size with no authentication-sized border.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-09-03: Separate immediate native selection from double-click activation</name>
  <reversibility rating="costly">The bridge is private but touches native List/AppKit event handling and must preserve keyboard, VoiceOver, context-menu, and exactly-once tune semantics.</reversibility>
  <read_first>
    - `SiriusMac/Catalog/ListeningView.swift` — preserve the native List selection binding, stable tags, visible collection ordering, Return path, favorite button, and context menu.
    - `.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md` D-06 and `03-UI-SPEC.md` channel-library interaction — single-click selects only; double-click/Return tune.
    - `.planning/debug/library-selection-gesture-stall.md` — remove the competing row-level two-click recognizer diagnosed there.
    - `SiriusMacTests/ListeningSessionControllerTests.swift` — reuse controller tune-count/origin-order fakes where possible.
  </read_first>
  <files>SiriusMac/Catalog/ListeningView.swift, SiriusMac/Windows/NativeListDoubleActionBridge.swift, SiriusMacUITests/SiriusMacUITests.swift, SiriusMac.xcodeproj/project.pbxproj</files>
  <behavior>
    - One click on a different row updates the native selection highlight and accessibility value promptly and leaves tune count unchanged.
    - Double-click uses the clicked/selected stable ID, captures the visible tab's origin order, and tunes exactly once.
    - Return tunes the selected row exactly once; arrow movement only changes selection; favorite buttons/context menus remain usable.
    - Pending tune disables activation without delaying or clearing browse selection.
  </behavior>
  <action>Remove the row-level `.onTapGesture(count: 2)`. Add one narrow AppKit double-action bridge at the list level (current-macOS only) that observes the underlying native table/outline view's clicked row and dispatches a stable semantic activation without installing a competing single/double tap recognizer on each row. Keep `List(selection:)` as the sole owner of native selection and expose “Selected” in the row's semantic accessibility value. Add XCUITest cases that click two rows and assert prompt selection/no tune, then double-click and press Return to assert exactly one tune per activation and the correct origin collection.</action>
  <acceptance_criteria>
    - Native highlight follows every single click without a double-click-delay pause.
    - Single click and arrow navigation never authorize playback.
    - Double-click and Return each route one accepted tune through the shared controller.
    - The favorite control, context menu, search focus, VoiceOver row semantics, and Reduce Motion behavior do not regress.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath /tmp/sirius-mac-03-09-selection-build-only CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual</automated>
  </verify>
  <done>Library selection feels native and immediate, activation remains explicit, and both behaviors are covered by launched-app automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI-test process environment → app composition | A test flag must select only synthetic offline collaborators before production auth/session objects exist. |
| Native row gestures → playback authority | Selection must not become tune authorization; activation must use current entitled semantic IDs. |
| Autosaved window data → compact geometry | UserDefaults frame data may influence origin but cannot expand the fixed compact canvas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09-01 | Information Disclosure | UI-test launch mode | high | mitigate | DEBUG-only gate chosen before Keychain/WebKit/session construction; fixed synthetic data only. |
| T-03-09-02 | Spoofing | selected row vs tune target | high | mitigate | Resolve clicked/selected stable ID against the current visible entitled collection and route through controller authorization. |
| T-03-09-03 | Denial of Service | competing click recognizers | medium | mitigate | Native List owns selection; one table-level double action handles activation. |
| T-03-09-04 | Tampering | saved compact frame | low | mitigate | Restore compact origin only and enforce exact policy content size. |
</threat_model>

<verification>
- Build the offline UI-test target only with manual ad-hoc signing: `xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -derivedDataPath /tmp/sirius-mac-03-09-build-only CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual`.
- Local launched-app UI-test execution is blocked after the 2026-08-22 loginwindow safety incident. Do not run `xcodebuild test`, `test-without-building`, `xctest`, or any runner/app executable until a separately authorized safety review establishes a safe execution environment.
- Prove single-click selection and double-click/Return activation through real launched-app native controls, not source scans.
- Run focused AppKit window tests before the launched geometry test, then the full existing app suite.
- Ensure UI-test teardown leaves no SiriusMac process and no test fixture persists across launches.
</verification>

<success_criteria>
- G-03-17 is closed by exact content sizing plus launched window geometry automation.
- G-03-7 is closed by immediate native selection plus exactly-once activation automation.
- The automation path is safe to run repeatedly and cannot touch subscriber state or SiriusXM.
- Existing Phase 3 semantic, playback, media, and accessibility tests remain green.
</success_criteria>

<output>
Create `.planning/phases/03-native-mac-listening-experience/03-09-SUMMARY.md` when done.
</output>
