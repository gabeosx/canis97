---
phase: 00-authentication-feasibility-gate
plan: "14"
type: execute
wave: 10
depends_on: ["00-13"]
files_modified:
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift
  - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
  - .planning/phases/00-authentication-feasibility-gate/COVERAGE.md
  - .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
  - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
  - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
  - .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
  - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
autonomous: false
gap_closure: true
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04, FEAS-05]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After the owner signs in inside the nonpersistent SiriusXM WebView, one explicit button transfers exactly one current first-party player session into one ephemeral native verification request (FEAS-01)."
    - "The browser-return path is the sole selected path; native-direct stays unavailable and no fallback selector or alternate live branch is introduced (FEAS-02, D-03, D-09 through D-12)."
    - "Exactly two separately initiated runs reach a closed authenticated-and-entitled result, verified first-party sign-out, and verified local cleanup, with an owner-confirmed cooldown between them (FEAS-03)."
    - "The app, not the owner, records each run's closed result and derives the final bundle; the owner is never asked to recognize token renewal, expiry, cookies, requests, or hidden browser state."
    - "Protected, rate-limited, rejected, malformed, off-provenance, ambiguous, or cleanup-failed outcomes latch immediately and cannot retry, advance the ledger, select native-direct, or produce GO (FEAS-04)."
    - "Only a byte-canonical two-run browser-return bundle produces `GO browser-return`; every missing, partial, malformed, or terminal bundle remains blocked (FEAS-05)."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      provides: "Single-attempt runtime plus ordered two-run semantic ledger and verified cleanup"
      contains: "LiveBrowserRuntime"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      provides: "Requirement-shaped owner-result schema and GO-only decision derivation"
      exports: ["OwnerResult", "DecisionGate", "PhaseOneGate"]
    - path: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      provides: "Runtime-generated, sanitized two-run browser proof"
      contains: "Schema: browser-probe-v3"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      provides: "Canonical Phase 0 decision and Phase 1 continuation gate"
      contains: "Feasibility decision: GO browser-return"
  key_links:
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift"
      via: "owner-triggered first-party AUTH_TOKEN extraction, single-consumption access token, and ephemeral native profile verification"
      pattern: "extractFirstPartySession|NativeWebSessionVerifier"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      via: "atomic write of closed run/cooldown/sign-out/cleanup facts after every state transition"
      pattern: "browser-probe-v3|run-1|run-2"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      via: "auth-feasibility finalizer parses, rederives, atomically installs, and byte-validates the canonical quartet"
      pattern: "finalize-phase|require-phase-one-go"
  prohibitions:
    - statement: "Session tokens, cookie values, authorization headers, raw authenticated responses, account identifiers, profile values, and WebKit storage contents must never be persisted or logged."
      status: unverified
      flagged: true
    - statement: "The harness must never bypass or work around CAPTCHA, MFA, DRM, subscription, device, geographic, anti-bot, rate-limit, or other protected controls."
      status: unverified
      flagged: true
    - statement: "GO must never be inferred from owner memory, a hidden-state observation, a 2xx response from the wrong endpoint, an incomplete run, or a handwritten artifact."
      status: unverified
      flagged: true
    - statement: "Phase 0 must not add Keychain persistence, catalog behavior, general stream resolution, production playback, app UI, or a production SiriusXMClient API."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** close the browser-return feasibility gate from automatically recorded two-run evidence, **so that** Phase 1 begins only after the selected WebView-to-native session handoff is repeatable and cleans up safely.

<objective>
Convert the already verified WebView session bridge into the smallest complete, instrumented Phase 0 decision path.

Purpose: Preserve the successful result of commits `0630fd5` and `49c5a95`, repair the still-disconnected runtime-to-canonical-evidence link, and stop asking the account owner to interpret token renewal or other hidden state.
Output: A two-run browser-return ledger, automatic sanitized evidence capture, requirement-shaped finalization, and one final owner checkpoint with an app-reported binary outcome.

<acceptance_criteria>
- The existing `web-session-bridge-v1 / verified` artifact remains evidence that extraction works, but it is not silently promoted to a complete FEAS-03 run because it lacks a runtime-recorded sign-out and cleanup proof.
- All automated tests and launch gates pass before the owner sees the WebView.
- The owner performs no diagnostic interpretation: each run ends in an explicit app state of `run-complete`, `run-incomplete`, or `terminal-stop`.
- No human-visible or durable renewal field is required for GO; renewal becomes an internal production concern only if the selected path later needs it.
- The Phase 1 gate succeeds only after two newly instrumented complete runs and a canonical `GO browser-return` bundle.
</acceptance_criteria>
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-VERIFICATION.md
@.planning/phases/00-authentication-feasibility-gate/00-WEB-PAGE-STATUS.md
@.planning/phases/00-authentication-feasibility-gate/00-WEB-SESSION-BRIDGE.md
@.planning/phases/00-authentication-feasibility-gate/COVERAGE.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift

<interfaces>
- `WebLoginSession.extractFirstPartySession()` currently reads only current first-party `AUTH_TOKEN` cookies and returns `WebSessionExtraction` without writing the token.
- `NativeWebSessionVerifier.verify(_:)` consumes `VolatileWebSession` once and sends `GET https://api.edge-gateway.siriusxm.com/profile/v4/profiles/me` through an ephemeral `URLSession`.
- `WebSessionBridgeResult.canonicalText` is a closed, non-secret artifact surface; a result must never contain the token or response body.
- `CleanupCoordinator.cleanUp()` coalesces concurrent callers, performs fixed ordered teardown, and caches `.verified` or `.failed`.
- `PhaseOneGate.require(_:)` is the sole downstream authorization boundary and must reparse/rederive the complete quartet.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: sole selected authentication path.
Decision: no-change — the word `alternate` in the roadmap remains a prohibition on path switching. This plan keeps one browser-return path and does not introduce a plural path model.
</assumption_delta_decision>

<gap_reconciliation>
- CLOSED before this plan: exact query-free main-frame callback classification, WebKit rendering/subframe policy, owner-ended renewal latching, incomplete playback latching, concrete awaited cleanup, cleanup-call coalescing, nonzero launcher failure, and native session extraction/verification.
- SUPERSEDED by the user's clarified target: a custom app-bound callback is not the selected session handoff. The selected browser-return is the explicit owner-triggered extraction of the current first-party player session from the nonpersistent WebView after login.
- REMAINING: successful session import is not connected to an ordered two-run ledger, sign-out/cleanup verification, canonical browser proof, or a final GO bundle.
- ROOT CAUSE: changed-assumption plus missing wiring. The historical plans optimized for callback and human renewal evidence; the verified implementation established a different safe handoff and the artifact pipeline was never updated to consume it.
</gap_reconciliation>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 00-14-01: Wire one WebView session import through native verification, sign-out, cleanup, and sanitized run evidence</name>
  <read_first>
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift
    - .planning/phases/00-authentication-feasibility-gate/00-WEB-SESSION-BRIDGE.md
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift</files>
  <behavior>
    - A synthetic current first-party `AUTH_TOKEN` passes once from WebKit extraction to the exact profile endpoint, produces one authenticated-and-entitled semantic result, is consumed, and cannot be reused.
    - Missing, expired, duplicate, off-domain, malformed, or whitespace-bearing session material stops before a request or returns one closed incomplete/terminal result.
    - Only the exact HTTPS host/path plus a successful provider response classify the selected session as authenticated-and-entitled; 401, 403, 429, 5xx, redirects, unexpected statuses, transport failure, cancellation, and malformed success are distinct fail-closed outcomes.
    - The owner signs out in the first-party WebView. The harness checks only whether the named current first-party auth session is absent; it never reads or records the value during sign-out verification.
    - A run becomes complete only after native verification, auth-session absence, awaited cleanup, and local volatile-state absence all succeed in order.
    - Every transition atomically rewrites `00-BROWSER-PROBE.md` from closed enums; interruption leaves a canonical incomplete file rather than a partial success.
  </behavior>
  <action>Implement the production-quality tracer for FEAS-01, FEAS-03, FEAS-04, D-03, D-08 through D-11, and D-13 through D-16. Keep extraction exactly owner-triggered, first-party-domain-scoped, name-scoped, current, single-candidate, in-memory, and single-consumption. Extend the native verifier so the selected profile bootstrap returns an `authenticated-and-entitled` semantic only for its exact success contract; it must ignore the body for persistence/logging and fail closed when the success shape cannot support that semantic. Add a sign-out verifier that reports only present/absent/ambiguous for the named first-party auth session and never exposes its value. Extend `LiveBrowserRuntime` with one-attempt state that orders import, owner WebView sign-out, sign-out verification, `CleanupCoordinator.cleanUp()`, and local absence. Update the launcher with explicit `Use Logged-In Session` and `Verify Sign-Out & Finish Run` actions, disable actions while work is in flight, and show only closed result text. Atomically generate `browser-probe-v3` after each transition. Do not implement callback waiting, renewal observation, token expiry display, tune/key work, AVPlayer work, Keychain, retries, polling, or alternate authentication. Write tests first using only synthetic cookies, responses, and semantic states; no test may contact SiriusXM or launch provider UI.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter WebSessionBridgeTests &amp;&amp; CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter BrowserReturnContractTests</automated>
  </verify>
  <acceptance_criteria>
    - One synthetic owner-triggered login handoff reaches a sanitized complete-run record through every runtime layer.
    - No token, cookie value, authorization header, or response body appears in the probe text, UI status, test failure, or log surface.
    - Sign-out or cleanup failure cannot produce a complete run and cannot be retried inside the same attempt.
  </acceptance_criteria>
  <done>A single selected-path run is measured end to end and recorded automatically as complete, incomplete, or terminal without asking the owner to interpret hidden state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-14-02: Expand the tracer to the exact two-run FEAS ledger and canonical GO derivation</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-VERIFICATION.md
    - .planning/phases/00-authentication-feasibility-gate/COVERAGE.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift, .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md, .planning/phases/00-authentication-feasibility-gate/COVERAGE.md, .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md, .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md</files>
  <behavior>
    - The ledger accepts exactly `run-1`, owner-confirmed cooldown, then `run-2`; empty, duplicate, reordered, merged, third, mixed-path, or partially complete input is invalid.
    - Each accepted run contains only selected path, authenticated-and-entitled, signed-out, and cleanup-verified semantic fields.
    - The former tune/key, audible-playback, and renewal fields cannot gate FEAS-03 or appear in the owner checkpoint. Their production behavior remains owned by later phases.
    - `auth-feasibility finalize-phase` derives all four canonical artifacts from the validated browser probe in one staging directory, validates and byte-compares them, atomically installs them, then runs `require-phase-one-go`.
    - `GO browser-return` is the only successful result for the selected branch. Partial input stays incomplete; locked protected/ambiguous outcomes derive canonical `NO-GO unsupported`; malformed or contradictory input fails validation without overwriting the last canonical blocked bundle.
  </behavior>
  <action>Implement the two-run expansion directly from FEAS-01 through FEAS-05 and the user's later scope correction. Append authoritative follow-up decisions D-18 through D-23 to `00-CONTEXT.md`: D-18 defines browser-return as explicit owner-triggered extraction of the current first-party player session from the nonpersistent WebView; D-19 makes the exact FEAS-03 authentication/entitlement/sign-out/cleanup/cooldown predicate the Phase 0 finish line; D-20 supersedes the human renewal-observation clause in D-06/D-07 and makes renewal an internal automated concern only if later production code needs it; D-21 returns tune/key and playback implementation to their roadmap phases rather than this feasibility closure; D-22 treats `00-WEB-SESSION-BRIDGE.md` as verified architecture evidence but not a complete run; D-23 prohibits another owner attempt until the binary instrumented path and all synthetic tests pass. Update `OwnerResult` and the browser probe to a new strict schema rather than weakening or silently accepting revision 2; retain revision-2 artifacts as historical blocked input. Update `DecisionGate`, `FinalizationGate`, `ArtifactBundle`, `PhaseOneGate`, and the runner finalizer for exact two-run browser-return derivation. Update `COVERAGE.md` to only the bounded Phase 0 surface: player WebView, exact session extraction, ephemeral profile verification, sign-out/cleanup, semantic ledger, and decision gate; record explicit opt-outs for renewal, tune/key, general playback, catalog, Keychain, and production client work. Rewrite the owner runbook around app-reported binary states and remove every instruction to observe renewal. Extend supersession authority through Plan 00-14 without changing historical plans or summaries. Per D-01 through D-05 and D-08 through D-17, keep the single-path, owner-control, terminal-stop, least-data, and canonical-artifact invariants; where D-05 through D-07 and D-12 specified the older expanded finish line, record the later user decisions as the explicit superseding authority instead of silently pretending those observations occurred.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter DecisionGateTests &amp;&amp; CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter FinalizationGateTests &amp;&amp; ! rg -n 'Renewal pending|Renewal verified|renewal-still-pending' .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - Two complete ordered browser-return runs plus cooldown are necessary and sufficient for GO; all 0/1/3+, empty, duplicate, reordered, mixed, partial, and terminal cases are covered.
    - The finalizer cannot copy, hand-edit, or infer a GO and cannot accept the existing single bridge artifact as a complete run.
    - The runbook contains no renewal-observation step and asks the owner to report no hidden state.
    - COVERAGE.md contains only the bounded feasibility APIs and reasoned later-phase opt-outs.
  </acceptance_criteria>
  <done>The core and CLI can derive the exact Phase 0 decision from runtime facts, with no manual renewal condition and no Phase 1 product work pulled forward.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 00-14-03: Run the fully instrumented two-run browser proof and accept only its binary result</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
    - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - script/build_and_run.sh
  </read_first>
  <files>.planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md, .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md, .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md</files>
  <action>Before pausing for the owner, run the full synthetic suite, validate the exact toolchain/contract/approval gate, build the real `.app` bundle through `script/build_and_run.sh --verify`, and assert the harness process is running. The application must own the protocol and artifact writes. The owner performs only visible first-party operations: sign in, click `Use Logged-In Session`, wait for the app's binary authenticated-and-entitled result, sign out in the SiriusXM WebView, click `Verify Sign-Out &amp; Finish Run`, choose a cooldown, explicitly start run 2, and repeat. The app records all semantic evidence automatically. Do not ask the owner whether renewal occurred, whether a token changed, what a response contained, or whether hidden cleanup happened. On the first incomplete or terminal result, do not retry or switch paths. After the checkpoint resumes, run `auth-feasibility finalize-phase` over the runtime-generated probe, validate the installed quartet and supersession, and run `require-phase-one-go`; inspect only schema keys and fixed validator output.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-bundle .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md .planning/phases/00-authentication-feasibility-gate/00-DECISION.md &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility require-phase-one-go .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md .planning/phases/00-authentication-feasibility-gate/00-DECISION.md</automated>
  </verify>
  <what-built>A complete two-run browser-return harness that verifies the native session, first-party sign-out, local cleanup, cooldown ordering, sanitized evidence, and final GO gate without exposing tokens or asking for hidden-state interpretation.</what-built>
  <how-to-verify>
    1. In the opened SiriusXM WebView, sign in normally.
    2. Click `Use Logged-In Session`; continue only if the app reports `Authenticated and entitled`.
    3. Sign out using the SiriusXM surface, then click `Verify Sign-Out &amp; Finish Run`; continue only if the app reports `Run 1 complete`.
    4. After a cooldown you choose, click `Confirm Cooldown &amp; Start Run 2` and repeat the same visible steps.
    5. The only successful finish is the app reporting `Phase 0 GO: browser-return`. Any other fixed result is a failure/incomplete result and must not be interpreted or retried.
  </how-to-verify>
  <resume-signal>Type `browser-proof-complete` only after the app reports `Phase 0 GO: browser-return`; otherwise type `browser-proof-failed` with no account, token, response, or provider details.</resume-signal>
  <acceptance_criteria>
    - The app, probe, owner result, and decision agree on exactly two complete browser-return runs.
    - The owner supplies no renewal judgment and no secret/provider detail.
    - `require-phase-one-go` passes only for the freshly derived GO quartet; any other outcome leaves Phase 1 blocked.
  </acceptance_criteria>
  <done>The account owner receives one measurable pass/fail result from the fully instrumented path, and Phase 1 continuation matches the canonical bundle.</done>
</task>

</tasks>

## Artifacts this phase produces

- `WebSessionBridgeResult`/native verifier semantics that distinguish authenticated-and-entitled, incomplete, and terminal outcomes without durable response data.
- A sign-out presence classifier limited to the named current first-party auth session.
- An ordered, one-attempt `browser-probe-v3` two-run ledger with owner-confirmed cooldown and verified cleanup.
- A strict new `OwnerResult` schema shaped to FEAS-03 rather than the superseded manual renewal/playback experiment.
- Runtime-generated `.planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md`.
- Fresh byte-canonical `00-EVIDENCE.md`, `00-SELECTION.md`, `00-OWNER-RESULT.md`, and `00-DECISION.md` derived from that probe.
- Updated Phase 0 context, API coverage, runbook, and supersession records that preserve the single-path scope fence.

## Multi-Source Coverage Audit

| Source | ID | Required outcome | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 0 | Exactly one safe path yields an evidence-backed decision before Phase 1 | 01-03 | COVERED | Runtime facts feed the final gate. |
| REQ | FEAS-01 | Determine the clean WebView-to-native browser-return without unrelated browser-state inspection | 01 | COVERED | Exact owner-triggered first-party session extraction only. |
| REQ | FEAS-02 | Native-direct only after browser rule-out and never as fallback | 01-02 | COVERED | Verified browser-return remains sole path; native stays not-applicable. |
| REQ | FEAS-03 | Two initiated authenticated-and-entitled runs with sign-out, cleanup, and cooldown | 01-03 | COVERED | Exact two-run ledger plus binary owner checkpoint. |
| REQ | FEAS-04 | Protected/ambiguous results stop and retain no secret evidence | 01-03 | COVERED | Closed classifiers, single attempt, semantic artifacts. |
| REQ | FEAS-05 | One canonical decision gates Phase 1 | 02-03 | COVERED | Fresh derivation plus independent GO gate. |
| RESEARCH | one terminal state machine | No retry, polling, concurrency, or path switch | 01-02 | COVERED | Existing latches retained and run ledger added. |
| RESEARCH | least-data boundary | Durable output contains only closed semantics | 01-03 | COVERED | Positive allowlist and atomic writes. |
| RESEARCH | human protocol | Owner alone operates first-party login/sign-out | 03 | COVERED | Automation handles all hidden verification. |
| RESEARCH | no package install | OS/toolchain frameworks only | 01-03 | COVERED | No dependency change. |
| CONTEXT | D-01, D-02 | Empirical owner path, not documentation-only inference | 01-03 | COVERED | Existing verified bridge is the tracer evidence. |
| CONTEXT | D-03, D-09-D-12 | Browser first and sole path; no fallback | 01-02 | COVERED | Native remains not-applicable. |
| CONTEXT | D-04, D-08 | Two owner runs, one in flight, owner cooldown | 02-03 | COVERED | Enforced structurally. |
| CONTEXT | D-05-D-07, D-12 | Older expanded playback/renewal finish line | 02 | COVERED | Later user correction is recorded explicitly as D-19-D-21; no evidence is fabricated. |
| CONTEXT | D-13-D-17 | Owner control, terminal stop, semantic evidence, cleanup | 01-03 | COVERED | Preserved unchanged. |
| CONTEXT | D-18-D-23 | WebView extraction and measurable closure correction | 01-03 | COVERED | Authored by Task 02 and implemented end to end. |

Deferred Phase 1+ production client, Keychain, catalog, stream resolution, general playback, desktop UI, skins, and release work remain excluded. No source item is silently omitted.

## Spec-less Edge Coverage Resolution

| Probe candidate | Resolution | Status |
|---|---|---|
| FEAS-01 boundary | Missing/expired/duplicate/off-domain/malformed session and non-first-party sign-out states fail closed | RESOLVED |
| FEAS-01 precision | Exact cookie name/domain/currentness, one owner click, one consumption, exact endpoint | RESOLVED |
| FEAS-02 unclassified | Any state outside selected browser-return remains unavailable and cannot select native | FLAGGED-UNVERIFIED until tests pass |
| FEAS-03 boundary | 0/1/2/3+ run cases; only exactly two pass | RESOLVED |
| FEAS-03 adjacency | Run 1, cooldown confirmation, and Run 2 remain distinct state transitions | RESOLVED |
| FEAS-03 empty | Missing run/cooldown/sign-out/cleanup fields fail canonical parsing | RESOLVED |
| FEAS-03 ordering | Run labels and cooldown order are exact and immutable | RESOLVED |
| FEAS-03 precision | Cooldown is a closed owner confirmation, never automated duration arithmetic | RESOLVED |
| FEAS-04 boundary | First protected/ambiguous/incomplete outcome latches and closes | RESOLVED |
| FEAS-04 precision | Exact status/transport/session/cleanup classifiers; no threshold guessing | RESOLVED |
| FEAS-05 unclassified | Only the three locked decisions parse; incomplete never aliases a decision | FLAGGED-UNVERIFIED until finalizer tests pass |

All 11 deterministic candidates are carried explicitly: 9 resolved checks and 2 flagged-unverified checks. No false positive is silently dropped.

<threat_model>
## ASVS L1 Enforcement

Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Nonpersistent WKWebView → native verifier | A user-authorized first-party session crosses into native memory exactly once. |
| Native SiriusXM response → semantic run state | Remote status/shape is untrusted until exact closed classification. |
| Runtime state → planning artifacts | Only allow-listed semantics may cross to disk. |
| Canonical quartet → Phase 1 | Untrusted files must be reparsed, rederived, and byte-validated before continuation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-14-01 | Information Disclosure | WebSessionBridge / diagnostics | critical | mitigate | Exact named first-party extraction, single-consumption token, ephemeral request, positive-list artifact/UI types, canary tests, and no raw response logging. |
| T-00-14-02 | Spoofing | Native profile verification | high | mitigate | Fixed HTTPS host/path, no redirects or alternate endpoint, exact success classification, and fail-closed unexpected status/shape tests. |
| T-00-14-03 | Tampering | Browser probe and canonical quartet | critical | mitigate | Closed schema, ordered run labels, canonical encoding, atomic staging/install, byte comparison, and rederivation at PhaseOneGate. |
| T-00-14-04 | Elevation of Privilege | DecisionGate | critical | mitigate | Exactly two complete same-path runs plus cooldown/sign-out/cleanup; incomplete, terminal, malformed, and handwritten states cannot produce GO. |
| T-00-14-05 | Denial of Service | Owner actions / remote boundary | medium | mitigate | One in-flight action, latching terminal state, no automatic retry/polling, and explicit incomplete result for availability failure. |
| T-00-14-06 | Repudiation | Owner checkpoint | medium | mitigate | App records opaque run labels and closed outcomes at transition time; no reliance on later memory or free-form testimony. |
| T-00-14-SC | Tampering | Package supply chain | high | mitigate | No package installation or dependency change; Apple frameworks and existing SwiftPM targets only. |
</threat_model>

<verification>
- Run the complete 49-test baseline first, then the new focused session, two-run, finalization, and redaction tests; the resulting count may increase, so gates assert pass/fail rather than a recalled number.
- Build the `.app` bundle through `script/build_and_run.sh --verify`; do not use raw `swift run` for the GUI.
- Confirm `00-WEB-SESSION-BRIDGE.md` remains secret-free and blocked until the two-run finalizer succeeds.
- Validate the runtime-generated browser probe, canonical quartet, supersession, and independent Phase 1 gate without printing artifact values.
- Re-run Phase 0 verification after the plan completes; the old `00-VERIFICATION.md` remains historical evidence and must not be hand-edited into a pass.
</verification>

<success_criteria>
- FEAS-01 through FEAS-05 are satisfied by one selected, instrumented browser-return path.
- Session extraction, native verification, sign-out, cleanup, cooldown, and two-run ordering have automated failure-path coverage.
- The owner performs visible first-party actions only and receives one binary app result; renewal is not a human acceptance condition.
- A complete runtime probe derives `GO browser-return` and passes `require-phase-one-go`; every other outcome remains safely blocked.
- No production app/client, persistent secret handling, catalog, general playback, or release work is introduced.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-14-SUMMARY.md` when done.
</output>
