---
phase: 00-authentication-feasibility-gate
plan: "14"
type: execute
wave: 10
depends_on: ["00-13"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
  - .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
  - .planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/EntitlementContractTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift
autonomous: true
gap_closure: true
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04]
estimate:
  tokens: 22000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "FEAS-01 permits only an owner-triggered, named, current, first-party AUTH_TOKEN extraction from the app-owned nonpersistent WKWebView into single-consumption memory; broad cookie/storage enumeration, developer-tools extraction, shared-browser access, persistence, and logging remain prohibited."
    - "A successful GET /profile/v4/profiles/me proves authentication only and can never by itself produce the entitled semantic required by FEAS-03."
    - "Entitlement is complete only when a separately validated public-first-party contract names an exact HTTPS method/host/path and an allow-listed response predicate; absent or ambiguous public evidence closes unsupported before another owner run."
    - "One supported synthetic run traverses session extraction, authenticated classification, entitled classification, first-party sign-out absence, awaited cleanup, and local volatile-state absence without retaining a secret or raw response."
    - "Protected, challenged, rate-limited, malformed, off-provenance, contradictory, or cleanup-failed outcomes latch and cannot retry, switch authentication paths, or become complete."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md"
      provides: "Canonical supported-or-unsupported public entitlement predicate without session data"
      contains: "Schema: entitlement-contract-v1"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift"
      provides: "Strict parser and validator for the entitlement contract"
      exports: ["EntitlementContract", "EntitlementContractStatus"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift"
      provides: "Separate authentication and entitlement verifiers using one volatile session"
      contains: "NativeEntitlementVerifier"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      provides: "One-attempt complete/incomplete/terminal runtime result"
      contains: "InstrumentedBrowserRun"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md"
      to: "NativeEntitlementVerifier"
      via: "canonical parse before any entitlement request"
      pattern: "validate-entitlement-contract|EntitlementContract"
    - from: "NativeWebSessionVerifier"
      to: "NativeEntitlementVerifier"
      via: "one in-memory access token, with authentication and entitlement classified separately"
      pattern: "authenticated|entitled"
    - from: "InstrumentedBrowserRun"
      to: "CleanupCoordinator"
      via: "sign-out absence and awaited cleanup are required before run-complete"
      pattern: "signedOut|cleanupVerified"
  prohibitions:
    - statement: "A profile HTTP 2xx must never be relabeled as subscription entitlement."
      status: unverified
      flagged: true
    - statement: "Tokens, cookie values, authorization headers, raw authenticated bodies, account identifiers, and profile values must never be persisted or logged."
      status: unverified
      flagged: true
    - statement: "The harness must never bypass CAPTCHA, MFA, DRM, subscription, device, geographic, anti-bot, or rate-limit controls."
      status: unverified
      flagged: true
    - statement: "Phase 0 must not add Keychain, catalog browsing, general stream resolution, playback, or a production client API."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** prove authentication and entitlement as separate, closed semantics after an owner completes WebView login, **so that** a later two-run GO decision cannot be based on authentication alone.

<objective>
Reconcile the approved WebView extraction boundary with Phase 0's authorities, establish an evidence-backed entitlement predicate, and complete one production-quality synthetic runtime tracer.

Purpose: Preserve the verified WebView-to-native authentication handoff while correcting the unsupported inference that profile success also proves entitlement.
Output: Reconciled requirements/research/validation, a canonical entitlement contract, strict synthetic classifiers, and an instrumented one-run runtime ready for the final evidence plan.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 00-14-01: Reconcile extraction authority and lock a truthful entitlement contract</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
    - .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-WEB-SESSION-BRIDGE.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md, .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md, .planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/EntitlementContractTests.swift</files>
  <action>Replace FEAS-01's absolute no-cookie wording with the narrow user-approved boundary: the owner clicks an explicit control after signing in inside this app's nonpersistent WKWebView; code may select only the single current first-party cookie named `AUTH_TOKEN`, decode only `session.accessToken`, hold it in a non-Codable single-consumption object, and send it only to an exact SiriusXM HTTPS verifier. Explicitly retain prohibitions on broad cookie/storage enumeration, arbitrary JavaScript extraction, developer tools, Chrome/shared-browser state, persistence, diagnostics, fixtures, and raw artifacts. Add an authoritative correction section to 00-RESEARCH.md that supersedes only the conflicting no-WebKit-state language and leaves access-control protections intact. Update 00-VALIDATION.md before source implementation: add rows for 00-14-01, 00-14-02, 00-15-01, 00-16-01, and 00-16-02; replace the old blanket no-state assertion with tests for the narrow boundary; remove renewal/playback from the new closure predicate while retaining historical rows as superseded context.

Create `EntitlementContract` and `validate-entitlement-contract`. The canonical `entitlement-contract-v1` artifact has exactly two statuses. `supported` requires method, HTTPS host, path, public first-party provenance URL, retrieval date, and an exact allow-listed JSON success predicate plus explicit denial/malformed rules. `unsupported` requires reason `no-public-bounded-entitlement-predicate` and contains no method, endpoint, or predicate. Resolve the artifact only from anonymously accessible SiriusXM player HTML/JavaScript or published first-party material; do not use an authenticated response, cookie, token, account, proxy capture, developer tools, or guessed endpoint. The validator permits only `api.edge-gateway.siriusxm.com`, forbids redirects and query secrets, rejects duplicate/unknown fields, and requires byte-canonical text. Record `supported` only when public evidence identifies both the request and exact subscription/entitlement field semantics; otherwise record `unsupported`. Add tests first for canonical supported/unsupported fixtures, missing provenance, profile-only evidence, 2xx-without-predicate, wrong host/path, redirects, malformed/unknown JSON fields, denial, and sensitive-value canaries.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter EntitlementContractTests &amp;&amp; CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-entitlement-contract .planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md &amp;&amp; rg -n '00-14-01|00-14-02|00-15-01|00-16-01|00-16-02' .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - FEAS-01, research, and validation describe the same narrow extraction boundary and retained prohibitions.
    - The profile endpoint is explicitly authentication-only in source, tests, and the entitlement contract.
    - A supported contract has exact public provenance and a machine-testable entitlement predicate; absence or ambiguity produces canonical unsupported.
    - No owner login or provider-authenticated request occurs during this task.
  </acceptance_criteria>
  <done>The repository has one noncontradictory extraction authority and one validated, honest entitlement-contract result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-14-02: Implement the one-attempt authentication, entitlement, sign-out, and cleanup tracer</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarnessLauncher/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift</files>
  <action>Refactor `WebSessionBridgeResult.verified` into closed authentication-only results and make `NativeWebSessionVerifier` classify `/profile/v4/profiles/me` as authenticated, rejected, protected-control, rate-limited, unavailable, malformed, or cancelled. Preserve the consumed bearer only in a scoped `VolatileWebSession` long enough to run authentication followed by entitlement; erase it after either terminal result. Add `NativeEntitlementVerifier`, which can be constructed only from a validated supported `EntitlementContract`, sends one request to its exact method/host/path with redirects disabled, and parses only allow-listed entitlement fields into entitled/not-entitled/malformed. A canonical unsupported entitlement contract prevents construction, provider activity, and owner-run UI.

Add a WebKit sign-out presence check limited to the exact current first-party `AUTH_TOKEN` name and domain. It returns present/absent/ambiguous without exposing the value; only absent proceeds. Add `InstrumentedBrowserRun` state ordering owner-triggered extraction, authentication, entitlement, owner sign-out, cookie absence, awaited `CleanupCoordinator.cleanUp()`, and local volatile-state absence. Every incomplete or terminal result latches; buttons are disabled while a step is in flight; there is no retry, callback wait, renewal observer, tune/key request, AVPlayer, Keychain, or alternate path. The launcher exposes only `Use Logged-In Session` and `Verify Sign-Out &amp; Finish Run`, with closed owner-visible status. Write synthetic tests for exact order, token consumption, unsupported entitlement, every HTTP class, wrong/malformed predicate, sign-out present/ambiguous, cleanup failure, concurrent clicks, cancellation, and secret canaries.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter WebSessionBridgeTests &amp;&amp; CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter BrowserReturnContractTests &amp;&amp; ! rg -n 'authenticated-and-entitled|case verified' Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic authentication success alone cannot complete a run.
    - Only the exact supported entitlement predicate yields entitled; unsupported, denied, malformed, or ambiguous results stop closed.
    - Sign-out and cleanup are concrete, awaited, idempotent, and required for run-complete.
    - No token or raw response is representable in owner-visible text, canonical text, logs, fixtures, or test diagnostics.
  </acceptance_criteria>
  <done>One synthetic run truthfully measures the full FEAS-03 semantic path without human hidden-state interpretation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `00-ENTITLEMENT-CONTRACT.md` with canonical supported-or-unsupported public evidence.
- `EntitlementContract`, `EntitlementContractStatus`, and `validate-entitlement-contract`.
- Authentication-only `NativeWebSessionVerifier` results.
- `NativeEntitlementVerifier` with exact allow-listed response semantics.
- Named-cookie sign-out presence classification.
- `InstrumentedBrowserRun` one-attempt state machine and synthetic coverage.

<threat_model>
ASVS level: 1. Blocking threshold: high.

- Critical disclosure: token/raw response escapes memory. Mitigate with non-Codable single-consumption storage, allow-listed semantic outputs, and canary tests.
- High spoofing: profile success is promoted to entitlement. Mitigate with separate verifiers and an exact public entitlement contract.
- High SSRF/exfiltration: a tampered contract changes destination. Mitigate with canonical parsing, HTTPS, exact host allowlist, no redirects, and no query secrets.
- High elevation: incomplete or protected results expose the owner run or later GO path. Mitigate with constructor gating and latched terminal states.
- Medium race: repeated button actions duplicate requests or cleanup. Mitigate with one in-flight action and idempotent awaited cleanup.
</threat_model>

<verification>
- Exact Xcode 26.6 package tests pass with pinned cache paths.
- Entitlement contract validation passes for either canonical supported or canonical unsupported status.
- Source and tests distinguish authenticated from entitled.
- The updated validation matrix maps both Plan 14 tasks, Plan 15 finalization, and both Plan 16 execution boundaries.
</verification>

<success_criteria>
- Planning authorities no longer contradict the approved WebView extraction boundary.
- Entitlement has a defensible public predicate or a fail-closed unsupported result.
- A complete synthetic run requires authentication, entitlement, sign-out, cleanup, and local absence in order.
- No owner action has been requested yet.
</success_criteria>
