---
phase: 00-authentication-feasibility-gate
plan: "06"
type: execute
wave: 2
depends_on: ["00-05"]
files_modified:
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/PublicAuthContractTests.swift
autonomous: true
requirements: [FEAS-01, FEAS-02, FEAS-04, FEAS-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Browser experiment readiness requires a public first-party entry surface, allow-listed ordinary navigation/provenance bounds, explicit stop bounds, and sanitized D-02 preliminary expectations; it does not require complete third-party callback documentation (FEAS-01, D-01, D-02)."
    - "Every construction predicate has a closed established, open, or unsafe state; an open documentation question remains an auditable gap and cannot by itself produce `NO-GO unsupported` or prevent the bounded owner-operated experiment."
    - "Native-direct qualification records an honest purpose-scoped contract derived from allowable sanitized evidence and cannot select native without later strict WebKit-specific rule-out plus separate approval (FEAS-02, D-03, D-09 through D-12)."
    - "Sanitized preliminary findings may define expected semantic event shapes and safe bounds, but raw capture values and authenticated browser state are never representable or reused."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift"
      provides: "Strict safe-construction, empirical-return, and native-purpose contract schema"
      exports: ["AuthExperimentContract", "BrowserExperimentEvidence", "NativePurposeContractEvidence"]
    - path: "Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/PublicAuthContractTests.swift"
      provides: "Boundary, provenance, canonicalization, and candidate-gating coverage"
  key_links:
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift"
      via: "Browser experiment construction consumes canonical safe bounds and owner approval rather than a documentation-completeness verdict"
      pattern: "AuthExperimentContract|CandidateSelection"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      via: "Fixed construction-validation, readiness, approval-recording, and approval-validation commands"
      pattern: "validate-auth-experiment-contract|derive-experiment-readiness|record-experiment-approval|validate-experiment-approval"
  prohibitions:
    - statement: "Experiment-contract qualification must never enumerate authenticated browser state, import raw capture data, or infer provider operations."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Define and enforce the safe construction, provenance, and bounded empirical contract that gates the browser experiment and conditional native candidate.

Purpose: Use public first-party provenance and sanitized preliminary evidence to bound a real WKWebView experiment without converting missing third-party documentation into infeasibility.
Output: A strict auth-experiment contract schema, exhaustive tests, candidate-selection integration, and fixed `auth-feasibility` commands for readiness and approval binding.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-05-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift

<interfaces>
- Decision raw values remain exactly `GO browser-return`, `GO native-direct`, and `NO-GO unsupported`.
- Experiment records may contain only public first-party entry/provenance references, closed semantic expectation states, a review revision, sanitized D-02 provenance, and a deterministic digest.
- Native purpose-contract qualification is eligibility data for a later strict-rule-out branch; it is never a live candidate by itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-06-01: Define the closed safe-construction and empirical contract schema</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/PublicAuthContractTests.swift</files>
  <behavior>
    - Browser construction evidence separately represents the public first-party entry surface, ordinary first-party navigation/provenance allowlist, app-bound return event shapes, semantic state-transition allowlist, experimental stop bounds, and downstream authentication, entitlement, renewal, tune/key, and sign-out expectations.
    - Each browser item records whether its provenance is public-first-party, sanitized-preliminary per D-02, empirically observed during the bounded run, or open; an open third-party-documentation item is non-dispositive.
    - Native evidence separately represents an honest purpose-scoped client identity plus authentication, result, entitlement, renewal, tune/key authorization, and sign-out operations derived only from allowable sanitized evidence.
    - Free text, secret-bearing values, raw/private capture content, duplicate or conflicting references, browser-state fields, unknown fields, and noncanonical bytes fail validation.
  </behavior>
  <action>Per FEAS-01, FEAS-02, D-01, D-02, D-03, and D-09 through D-12, implement `AuthExperimentContract`, `BrowserExperimentEvidence`, and `NativePurposeContractEvidence` as strict canonical value types in the existing source file. Encode every safe-construction, expected-semantic, and honest-native item named in the behavior, require public first-party HTTPS provenance for the entry surface and ordinary navigation boundary, permit sanitized D-02 provenance for preliminary semantic expectations, and calculate a deterministic digest over canonical bytes. Treat missing public third-party callback documentation as an `open` audit fact that neither establishes success nor derives unsupported. Reject raw capture content, copied values, guessed descriptors, arbitrary strings, secret-bearing fields, browser-state fields, duplicate/conflicting references, and unknown schema keys. Write `PublicAuthContractTests` first for ready construction with an open documentation item, incomplete construction, unsafe construction, duplicate, conflicting, non-first-party entry, sanitized-preliminary provenance, noncanonical, and unrecognized inputs.</action>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility --filter PublicAuthContractTests</automated>
  </verify>
  <done>The core can canonically classify safe browser construction, empirical evidence gaps, and a conditional native purpose contract without representing raw observations, provider secrets, or guessed operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-06-02: Gate experiment readiness and approval commands on safe bounds</name>
  <read_first>
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/PublicAuthContractTests.swift</files>
  <behavior>
    - Safe first-party entry/provenance, closed stop bounds, and sanitized expectations produce browser-experiment-ready even when third-party callback documentation remains open.
    - Missing required construction input produces browser-experiment-incomplete; unsafe or malformed input fails validation, and neither condition derives `NO-GO unsupported` solely from missing documentation.
    - Native purpose qualification is a closed fact only and never selects or launches native by itself.
    - Fixed `auth-feasibility` commands validate the contract, derive experiment readiness, record digest-bound approval, and validate that approval without echoing rejected input.
  </behavior>
  <action>Wire the schema into `CandidateSelection` and the existing `auth-feasibility` executable with commands `validate-auth-experiment-contract`, `derive-experiment-readiness`, `record-experiment-approval`, and `validate-experiment-approval`. Per D-01 through D-03 and D-09 through D-12, permit browser experiment readiness only for canonical safe construction bounds, regardless of whether a public third-party callback document exists, and retain native purpose qualification solely for the later proven-rule-out conjunction. For missing required construction input, emit fixed `browser-experiment-incomplete`; for unsafe, malformed, raw, or digest-mismatched input, fail closed without a constructor or terminal feasibility claim. Extend `PublicAuthContractTests` so Apple API capability alone cannot establish SiriusXM success, documentation absence cannot block an otherwise safe bounded experiment, native qualification alone never selects a runtime, and flipping any safety predicate blocks construction.</action>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility --filter PublicAuthContractTests</automated>
  </verify>
  <done>Experiment readiness and owner-approval tooling consume exact canonical safe-construction evidence, preserve open documentation questions honestly, and cannot select native or infer provider behavior.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Public/sanitized provenance → contract schema | Public content and sanitized preliminary findings are untrusted until provenance and every safety predicate validate. |
| Contract schema → experiment readiness | Only exact canonical safe construction may create a browser experiment; native remains conditional evidence. |
| Contract digest → owner approval | Approval commands bind exact canonical bytes and never a summary or private observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-06-01 | Spoofing | Construction provenance | critical | mitigate | First-party HTTPS entry/provenance, explicit sanitized-preliminary labels, canonical digest, exhaustive negative tests. |
| T-00-06-02 | Tampering | PublicAuthContract | critical | mitigate | Strict schema, deterministic encoding, unknown-field rejection, canonical byte comparison. |
| T-00-06-03 | Elevation of Privilege | CandidateSelection | critical | mitigate | All safety predicates required; documentation completeness is non-dispositive; native qualification cannot select a runtime. |
| T-00-06-04 | Information Disclosure | Contract/CLI APIs | high | mitigate | Positive allowlists, fixed diagnostics, and no raw provider or browser values in representable input. |
| T-00-06-SC | Tampering | Dependencies | high | mitigate | No installation; Foundation and existing SwiftPM targets only. |
</threat_model>

<verification>
- Run PublicAuthContractTests and the complete command-line package suite.
- Exercise ready-with-open-docs, missing safety input, unsafe, raw/private, non-first-party-entry, conflicting, malformed, and digest-mismatched rows.
- Confirm only exact safe construction can create an experiment and native qualification alone never can.
</verification>

<success_criteria>
- FEAS-01 and FEAS-02 have executable safe-construction, empirical-return, and conditional native-purpose contracts.
- No authenticated browser-state inspection or provider operation inference is representable.
- Missing third-party callback documentation is recorded honestly but does not block the owner-operated WKWebView experiment; missing safety bounds still block launch without fabricating a terminal decision.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-06-SUMMARY.md` when done.
</output>
