---
phase: 00-authentication-feasibility-gate
plan: "11"
type: execute
wave: 7
depends_on: ["00-10"]
files_modified:
  - Spikes/AuthenticationFeasibility/Package.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/HarnessApp.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeCredentialSession.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeDirectRuntime.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/NativeFallbackGateTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/NativeDirectPreflightTests.swift
autonomous: true
requirements: [FEAS-02, FEAS-03, FEAS-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Native-direct source can exist only when current-SDK readiness, exact WebKit-specific rule-out, native purpose-contract qualification from allowable sanitized evidence, experiment approval, and separate password-boundary approval all validate (D-09 through D-12)."
    - "When native-direct becomes eligible, the compiled live harness no longer contains the browser runtime; diagnostic evidence is retained only as closed data, never a second selectable path (D-03)."
    - "Missing safe native-purpose values remain incomplete and cannot unlock native; rejection, challenge, protected behavior, rate limit, provider ambiguity, or network failure closes unsupported (D-10, D-14)."
    - "The owner types credentials into a local secure field; the executor cannot operate or read it, and all values remain volatile for one exact purpose-scoped direct request before cleanup (D-11, D-13, D-16)."
    - "The approved native runtime reuses the unchanged two-run authentication, entitlement, playback, renewal, sign-out, and cleanup state machine (D-12, D-17)."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/NativeFallbackGateTests.swift"
      provides: "Exhaustive native eligibility and one-live-path truth table"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeCredentialSession.swift"
      provides: "Owner-only volatile credential entry boundary for the authorized branch"
      exports: ["NativeCredentialSession", "VolatileCredentials"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeDirectRuntime.swift"
      provides: "Purpose-contract-bound native composition over the shared complete proof chain"
      exports: ["NativeDirectRuntime"]
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift"
      via: "Exact native-purpose-qualified verdict is required alongside strict rule-out and both approvals"
      pattern: "native-purpose-qualified|strict-webkit-ruleout|approved"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeCredentialSession.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeDirectRuntime.swift"
      via: "Volatile credentials move once into the exact purpose-scoped direct operation and are cleared on every completion"
      pattern: "VolatileCredentials"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeDirectRuntime.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift"
      via: "Native-direct reuses the same proof protocol and cleanup proof"
      pattern: "CleanupCoordinator|ProofRunRecord"
  prohibitions:
    - statement: "Native eligibility must never weaken into a response to provider rejection, protection, ambiguity, network failure, or owner frustration."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Conditionally replace the ruled-out browser runtime with the one purpose-qualified and separately approved native-direct path.

Purpose: Preserve a single honest live implementation and require every native operation to come from allowable sanitized evidence and explicit owner approval rather than guessing or documentation absence.
Output: Exhaustive fallback gating and, only in the authorized branch, an owner-operated ephemeral native runtime.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-10-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
@.planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
@.planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/HarnessApp.swift

<interfaces>
- `validate-native-approval` validates native purpose-contract qualification, strict browser rule-out, and the separate approval artifact together.
- Native-direct may reuse the semantic proof, playback, renewal, cleanup, and run coordinator contracts without weakening them.
- A secret-free WebKit diagnostic is eligibility data only; it is not a retained live authentication implementation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-11-01: Enforce native purpose qualification and one-live-path replacement</name>
  <reversibility rating="costly">Selecting native-direct replaces the browser live runtime and changes the password exposure boundary; reversal requires restoring the prior source graph and obtaining new proof.</reversibility>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/HarnessApp.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Package.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/HarnessApp.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/NativeFallbackGateTests.swift</files>
  <behavior>
    - The only eligible row has current-sdk-ready, browser strict-webkit-ruleout, matching local diagnostic, native-purpose-qualified contract, approved experiment digest, and approved native disclosure digest.
    - Flipping any predicate and every protected/provider/network/ambiguous category rejects eligibility and validates canonical unsupported.
    - Eligible execution removes WebLoginSession and LiveBrowserRuntime from the executable source graph before native source is introduced.
    - Ineligible execution has no native runtime source/target and performs no provider request.
    - CandidateSelection exposes one selected live candidate or unsupported, never both and never a runtime transition.
  </behavior>
  <action>Per D-03 and D-09 through D-12, extend `CandidateSelection` to consume only the canonical toolchain, auth-experiment contract, experiment approval, browser result, and native approval artifacts. Encode the full eligibility conjunction above and exhaustively flip every predicate in tests. Implement `auth-feasibility validate-native-launch-gate` with fixed eligible, incomplete, and unsupported output. Missing third-party documentation is non-dispositive; missing a concrete safe purpose-contract value produces incomplete and no source, while a locked terminal or explicit rejection preserves or rederives canonical NO-GO. In every ineligible row, keep native source absent and do not modify the live browser graph solely to create an alternate. If eligible, remove browser live files from the SwiftPM executable graph before Task 00-11-02 adds native-direct; retain only the closed diagnostic result. Update `HarnessApp` to accept one constructor with no selector or runtime fallback.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --require-ready-or-closed &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-native-approval .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter NativeFallbackGateTests</automated>
  </verify>
  <done>The source graph admits browser-return, authorized native-direct, incomplete, or unsupported, with honest native purpose qualification required and never two live paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-11-02: Compose only the qualified honest native-direct runtime</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeCredentialSession.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/NativeDirectRuntime.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/NativeDirectPreflightTests.swift</files>
  <behavior>
    - Ineligible execution proves both native files are absent from disk and the source graph, records not-applicable, and makes no provider request.
    - Eligible secure entry yields one non-serializable `VolatileCredentials` value only after owner submission; the executor has no automation or observation path.
    - Credentials are used once for the exact honest-client authentication operation established by allowable sanitized evidence and cleared on response, cancellation, or error.
    - Safe success feeds the unchanged account/entitlement/tune/key/playback/renewal/sign-out/cleanup coordinator.
    - Any rejection, challenge, protection, rate limit, unexpected result/redirect, or ambiguity terminates the attempt and clears volatile state.
  </behavior>
  <action>Read only the validated conditional branch from Task 00-11-01. For ineligible state, run the absent-source and not-applicable tests and create no native source. For eligible state, implement the native runtime per D-11 through D-16. Build a local secure credential view whose fields remain internal state and yield one move-once `VolatileCredentials` value. Construct only the honest native identity and authentication operation explicitly established by the approved native purpose contract from allowable sanitized evidence and the bounded experiment; if any required value or semantic is unavailable, record incomplete, keep Phase 1 blocked, and do not issue or guess a request. Use one fresh ephemeral URLSession, clear volatile buffers on all outcomes, and reuse the full proof chain unchanged. Add no credential persistence, identity emulation, challenge handling, retry, or guessed provider behavior.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --require-ready-or-closed &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter NativeDirectPreflightTests</automated>
  </verify>
  <done>Only the fully authorized branch contains an honest purpose-contract-bound native runtime; incomplete and terminal branches remain non-live with their precise canonical status.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical evidence/approvals → source graph | Exact artifacts determine whether native source may exist. |
| Account owner → native secure field | A password enters disposable harness memory under explicit disclosure. |
| Direct authentication result → semantic proof chain | Untrusted provider data must collapse to success or terminal stop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-11-01 | Elevation of Privilege | NativeFallbackGate | critical | mitigate | Full purpose-contract/rule-out/approval conjunction, exhaustive negative table, no switching. |
| T-00-11-02 | Information Disclosure | NativeCredentialSession | critical | mitigate | Owner-only secure fields, move-once volatile value, immediate clearing, no logs/persistence/automation. |
| T-00-11-03 | Spoofing | Direct operation | high | mitigate | Exact honest-client purpose contract from allowable sanitized evidence, system TLS, no inferred identity or request behavior. |
| T-00-11-04 | Denial of Service | NativeDirectRuntime | high | mitigate | One bounded attempt, first unsafe event terminal, shared idempotent cleanup, no retry. |
| T-00-11-SC | Tampering | Dependencies | high | mitigate | No installation; only verified system frameworks and existing package core. |
</threat_model>

<verification>
- Run both focused suites and the full package suite in each conditional source-graph state.
- Prove native source absence for every incomplete, ambiguous, rejected, or terminal purpose-contract row.
- Prove exactly one live candidate symbol for the fully authorized native row.
</verification>

<success_criteria>
- Native-direct cannot be built from an unresolved safe-contract value, raw preliminary evidence, or missing documentation alone.
- An eligible fallback replaces rather than joins the browser candidate.
- The native runtime preserves owner control and the unchanged complete proof contract.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-11-SUMMARY.md` when done.
</output>
