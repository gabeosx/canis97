---
phase: 00-authentication-feasibility-gate
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/ToolchainGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ToolchainGateTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ContractTracerTests.swift
  - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
autonomous: true
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04, FEAS-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The active replacement execution is authorized only by `00-SUPERSESSION.md` status `replacement-planned`; Plans 00-01 through 00-04 and the existing fail-closed quartet remain historical rather than proof that replacement execution is complete."
    - "A dependency-free command-line tracer reaches a canonical terminal Phase 0 decision without constructing AppKit, WebKit, AVFoundation, account, or provider surfaces."
    - "The exact selected Xcode and macOS SDK are proven by tool output and framework type-checking before any GUI/live-capable target can exist; unavailable or mismatched tooling leaves the experiment incomplete and Phase 1 blocked rather than making a provider-feasibility claim."
    - "The canonical core has no API for authenticated browser-state inspection, transport inference, endpoint guessing, or secret-bearing durable evidence (FEAS-01, FEAS-02, D-02, D-16)."
    - "Exactly two distinct ordered same-path runs with owner cooldown, complete proof predicates, renewal, sign-out, and cleanup are the only GO shape (D-04 through D-08, D-17)."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md"
      provides: "Machine-auditable historical-versus-active plan authority and Phase 1 block during replacement execution"
      contains: "Status: replacement-planned"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/ToolchainGate.swift"
      provides: "Closed current-Xcode/current-SDK readiness or unavailable result"
      exports: ["ToolchainGate", "ToolchainEligibility"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      provides: "Canonical evidence-to-decision derivation and Phase 1 gate"
      exports: ["DecisionGate", "ArtifactBundle"]
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-05-PLAN.md"
      via: "Exact active-plan/status fields authorize the first replacement task while preserving the historical quartet as fail-closed only"
      pattern: "Active plan range: 00-05..00-13|Phase 1 continuation: blocked"
    - from: "Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh"
      to: ".planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md"
      via: "Exact selected-developer, Xcode-version, SDK-version, and framework-import checks write one closed eligibility artifact"
      pattern: "current-sdk-ready|toolchain-unavailable"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      via: "Unavailable tooling takes the canonical environment-pending path, while only locked terminal outcomes take the unsupported writer path"
      pattern: "environment-pending|close-unsupported|require-phase-one-go"
  flagged_assumptions:
    - id: "SP-EDGE-FEAS-02-UNCLASSIFIED"
      statement: "Any future native eligibility state outside the exhaustive browser-rule-out, native-purpose-contract, and owner-approval table requires explicit human review and remains incomplete unless it is a locked terminal outcome."
      flagged: true
  prohibitions:
    - statement: "The disposable feasibility package must never become a production client or application target."
      status: unverified
      flagged: true
    - statement: "No GUI or provider-capable source may exist before toolchain readiness, safe construction/provenance validation, and owner experiment approval."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Establish the safe terminal tracer, exact toolchain preflight, and canonical empirical proof contract that every later live-capable branch must pass.

Purpose: Make environment readiness, safe construction bounds, and empirical proof status explicit without treating missing third-party documentation as evidence of infeasibility.
Output: A dependency-free core plus `auth-feasibility` executable, current-Xcode readiness artifact, strict empirical proof schema, and canonical decision machinery.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@.planning/phases/00-authentication-feasibility-gate/00-VERIFICATION.md
@.planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md

<interfaces>
- Decision raw values remain exactly `GO browser-return`, `GO native-direct`, and `NO-GO unsupported`.
- Existing Phase 1 consumers keep `require-phase-one-go <evidence> <selection> <owner-result> <decision>`.
- Construction records carry the public first-party entry surface, allow-listed provenance/navigation bounds, sanitized D-02 preliminary expectations, closed predicate results, reviewer state, and a contract digest only; unknown or free-form provider facts are invalid.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: sole selected authentication candidate/path.
Decision: no-change — the ROADMAP prohibition on alternate methods does not create a second runtime variant. Candidate and finalization tests continue to prove exactly one selected path or unsupported.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 00-05-01: Trace exact toolchain readiness to a safe terminal decision</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
    - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - Spikes/AuthenticationFeasibility/Package.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/ToolchainGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ToolchainGateTests.swift, .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md</files>
  <behavior>
    - The script discovers the selected developer directory with system tools and never assumes a fixed installation path.
    - Ready requires the exact researched Xcode 26.6 toolchain, current macOS 26.5 SDK, macOS 26.0 deployment target support, and successful type-check imports of AppKit, WebKit, and AVFoundation.
    - Missing Xcode, Command Line Tools selection, version mismatch, unavailable SDK, or any failed import writes one canonical `environment-pending` result, leaves replacement execution incomplete, and builds no GUI target.
    - The ready branch writes one canonical `current-sdk-ready` result; neither branch prints local paths or environment details into the durable artifact.
    - `--require-ready` fails closed, while `--check-conditional` and `--require-ready-or-closed` accept only exact ready, canonical incomplete, or already-canonical terminal unsupported states for later conditional tasks.
  </behavior>
  <action>Before implementation, validate `00-SUPERSESSION.md` against its exact declared schema, status `replacement-planned`, active range `00-05..00-13`, replacement incomplete, and Phase 1 blocked; halt if any field differs, because the historical plans/quartet cannot authorize replacement completion. Per the Phase 0 scope fence and the research Environment Availability finding, keep this preflight in the command-line core with no GUI framework dependency. Implement an executable shell preflight plus pure `ToolchainGate` parser and retain or implement the `auth-feasibility` executable's canonical unsupported writer for locked terminal outcomes only. Discover the active developer directory via `xcode-select`, verify `xcodebuild -version`, verify `xcrun --sdk macosx --show-sdk-version`, and type-check a temporary import-only source through `xcrun swiftc`; remove the temporary source on every exit. Write `00-TOOLCHAIN.md` atomically with one closed status and public version labels only. On any non-ready result, record `environment-pending`, keep supersession replacement-planned and Phase 1 blocked, and prohibit later live-capable targets without deriving `NO-GO unsupported`. Implement the three closed script modes named in the behavior so later conditional tasks never interpret shell output. Use `ToolchainGateTests` to invoke the script with injected command results for every failure point plus the real environment.</action>
  <verify>
    <automated>supersession=.planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md; test -r "$supersession" &amp;&amp; rg -q '^Schema: phase-0-supersession-v1$' "$supersession" &amp;&amp; rg -q '^Status: replacement-planned$' "$supersession" &amp;&amp; rg -q '^Active plan range: 00-05\.\.00-13$' "$supersession" &amp;&amp; rg -q '^Replacement execution complete: no$' "$supersession" &amp;&amp; rg -q '^Phase 1 continuation: blocked$' "$supersession" &amp;&amp; swift test --package-path Spikes/AuthenticationFeasibility --filter ToolchainGateTests</automated>
  </verify>
  <done>The phase has a runnable end-to-end safe path from exact environment inspection to ready or canonical blocked outcome, with no protected surface reachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-05-02: Rebuild the canonical empirical proof contract</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
    - .planning/phases/00-authentication-feasibility-gate/00-VERIFICATION.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ContractTracerTests.swift</files>
  <behavior>
    - Evidence validation precedes candidate and decision derivation at every public entry point.
    - A terminal closure reason cannot coexist with a candidate, and every durable artifact must equal its canonical re-encoding byte-for-byte.
    - GO requires exactly two different ordered run labels, each complete authentication, entitlement, tune/key, audible-playback, sign-out, and cleanup evidence, owner-confirmed cooldown, and legitimate renewal in at least one run.
    - Zero, one, three or more, duplicate, merged, reordered, partially complete, or contradictory runs remain blocked; a terminal stop derives canonical unsupported.
    - `require-phase-one-go` fails before production writes for old, malformed, incomplete, or NO-GO input.
  </behavior>
  <action>Revalidate the same planning-time supersession fields before altering the canonical contract; historical revision-1 artifacts are safe blocked inputs only and must never be migrated by copying or hand editing. Per D-01, D-03 through D-08, and D-14 through D-17, revise the canonical format to revision 2 and replace the historical documentation-only premise with explicit empirical proof fields. Make evidence validation throwing and mandatory for candidate derivation, final decision derivation, and `auth-feasibility` commands. Require a real rounded Gregorian date, mutual exclusion between closure and candidate, canonical byte equality, exactly two ordered same-path complete runs, owner cooldown, verified cleanup, and renewal in at least one. Add fixed-output `auth-feasibility validate-bundle`, `auth-feasibility close-unsupported`, and unchanged-signature `auth-feasibility require-phase-one-go` commands to the existing executable product. Preserve one-candidate/no-switch invariants and ensure rejected input is never echoed.</action>
  <verify>
    <automated>supersession=.planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md; test -r "$supersession" &amp;&amp; rg -q '^Status: replacement-planned$' "$supersession" &amp;&amp; rg -q '^Active plan range: 00-05\.\.00-13$' "$supersession" &amp;&amp; rg -q '^Replacement execution complete: no$' "$supersession" &amp;&amp; rg -q '^Phase 1 continuation: blocked$' "$supersession" &amp;&amp; swift test --package-path Spikes/AuthenticationFeasibility --filter ContractTracerTests</automated>
  </verify>
  <done>The corrected canonical contract cannot derive GO from obsolete, invalid, partial, stopped, or multi-candidate evidence.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Required outcome | Owning plan(s) | Status |
|---|---|---|---|---|
| GOAL | Phase 0 | Exactly one safe path completes the two-run proof before production | 00-05..00-13 | COVERED |
| REQ | FEAS-01 | Bounded owner-operated app-bound browser experiment with no authenticated browser-state inspection | 00-05, 00-06, 00-07, 00-08, 00-10 | COVERED |
| REQ | FEAS-02 | Native-direct only after strict rule-out, allowable sanitized purpose-contract qualification, and approval | 00-06, 00-07, 00-10, 00-11, 00-12, 00-13 | COVERED |
| REQ | FEAS-03 | Two owner runs with cooldown, renewal, playback, sign-out, cleanup | 00-05, 00-09, 00-10, 00-11, 00-12, 00-13 | COVERED |
| REQ | FEAS-04 | First protected/ambiguous result stops and redacts | 00-05..00-13 | COVERED |
| REQ | FEAS-05 | One canonical decision; Phase 1 requires exact GO | 00-05, 00-07, 00-13 | COVERED |
| RESEARCH | safe construction provenance | Establish first-party entry, allow-listed navigation/return bounds, and stop policy before the bounded experiment | 00-06, 00-07, 00-08 | COVERED |
| RESEARCH | unresolved provider questions | Resolve browser handoff empirically; preserve ordinary no-return and renewal gaps as incomplete, never speculation | 00-06, 00-07, 00-10, 00-13 | COVERED |
| RESEARCH | environment availability | Verify current Xcode/SDK or close without live work | 00-05, 00-07 | COVERED |
| RESEARCH | isolated harness | Dependency-free disposable SwiftPM boundary | 00-05, 00-08, 00-11 | COVERED |
| RESEARCH | one terminal actor | One attempt, no retry/poll/timer/concurrent start, fixed terminal stops | 00-05, 00-08, 00-09, 00-11 | COVERED |
| RESEARCH | least-data boundary | Convert every runtime observation immediately to closed semantic values | 00-05, 00-08, 00-09, 00-11, 00-13 | COVERED |
| RESEARCH | human protocol | Account owner alone operates live proof outside CI | 00-10, 00-12 | COVERED |
| RESEARCH | entitlement mapping | Allowable sanitized evidence plus bounded observation establishes semantic mapping before proof acceptance; ambiguity is terminal | 00-06, 00-07, 00-08, 00-10 | COVERED |
| RESEARCH | no package install | Use only SwiftPM and OS/toolchain frameworks | 00-05..00-13 | COVERED |
| RESEARCH | downstream gate | Phase 1 consumes only a rederived canonical GO bundle | 00-05, 00-13 | COVERED |
| CONTEXT | D-01, D-02 | Missing third-party docs are non-dispositive; sanitized preliminary findings may bound but never replace the empirical experiment | 00-05, 00-06, 00-07, 00-08 | COVERED |
| CONTEXT | D-03 | Browser first; native only after strict rule-out; one runtime | 00-06, 00-08, 00-10, 00-11 | COVERED |
| CONTEXT | D-04, D-05 | Two real browser runs with the complete finish line | 00-08, 00-09, 00-10 | COVERED |
| CONTEXT | D-06, D-07 | Natural renewal in one run; missing renewal is incomplete | 00-09, 00-10, 00-12, 00-13 | COVERED |
| CONTEXT | D-08 | One attempt and owner-confirmed cooldown | 00-05, 00-10, 00-12 | COVERED |
| CONTEXT | D-09, D-10 | Strict WebKit-specific threshold; provider/control errors never unlock native | 00-10, 00-11 | COVERED |
| CONTEXT | D-11, D-12 | Separate disclosure approval; native meets same bar | 00-10, 00-11, 00-12 | COVERED |
| CONTEXT | D-13, D-14 | Owner controls live surfaces; first unsafe signal stops | 00-08..00-12 | COVERED |
| CONTEXT | D-15, D-16, D-17 | Semantic-only durable bundle; full proof and cleanup required | 00-05, 00-08, 00-09, 00-13 | COVERED |

Deferred Phase 1–5 product work remains excluded. No source item is unplanned.

## Spec-less Edge Coverage Resolution

| Probe item | Resolution / authored location | Status |
|---|---|---|
| FEAS-01 boundary | Unsafe/malformed construction records block launch; missing third-party callback documentation remains an audit gap and cannot itself derive NO-GO in 00-06/00-07 | RESOLVED explicit |
| FEAS-01 precision | Exact canonical construction bytes, deterministic digest, allow-listed in-memory return matcher, and digest-bound experiment approval in 00-06 through 00-08 | RESOLVED explicit |
| FEAS-02 unclassified | `SP-EDGE-FEAS-02-UNCLASSIFIED` above | FLAGGED assumption |
| FEAS-03 boundary | 0/1/2/3+ run cardinality; exactly 2 may pass | RESOLVED explicit |
| FEAS-03 adjacency | run-1, cooldown, and run-2 remain distinct; merged/touching input fails | RESOLVED explicit |
| FEAS-03 empty | missing/empty run, predicate, cooldown, renewal, or cleanup input fails | RESOLVED explicit |
| FEAS-03 ordering | fixed stable order; duplicates, equal order, and reordering fail | RESOLVED explicit |
| FEAS-03 precision | owner cooldown is a closed confirmation with no automated duration arithmetic | RESOLVED explicit |
| FEAS-04 boundary | the first stop event is terminal; no retry count or alternate transition exists | RESOLVED explicit |
| FEAS-04 precision | exact protected/rate/redirect/ambiguity classifications; no tolerance threshold | RESOLVED explicit |
| FEAS-05 unclassified | `SP-EDGE-FEAS-05-UNCLASSIFIED` in 00-13 | FLAGGED assumption |

No-silent-drop equality: 11 probe items = 9 resolved truths + 2 flagged assumptions.

## Prohibition Recall Audit

The bespoke scope, owner-control, no-inspection, no-path-switch, no-recording, and semantic-bundle constraints are projected under `must_haves.prohibitions` across plans 00-05 through 00-13. Generic input-validation, path, TLS, and dependency threats remain in the per-plan ASVS L1 registers. Routine empty-input, ordering, cleanup-error, and concurrency cases are covered by the explicit edge tests above rather than minted as new prohibitions.

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Local toolchain → eligibility artifact | Untrusted local configuration must not be mistaken for the exact supported SDK. |
| Construction provenance → experiment readiness | A public URL alone is insufficient; first-party entry/provenance, sanitized expectations, and stop bounds must be explicit and reviewed. |
| Canonical core → Phase 1 | Only a complete rederived GO bundle may unlock production work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-05-01 | Spoofing | Toolchain preflight | high | mitigate | Discover the selected toolchain; verify exact versions and type-check framework imports; canonical closed result. |
| T-00-05-02 | Tampering | Empirical evidence record | critical | mitigate | Strict revision-2 parsing, closed proof fields, deterministic encoding, and canonical bytes. |
| T-00-05-03 | Elevation of Privilege | Candidate selection | critical | mitigate | No constructor until all browser predicates qualify; native also requires later strict rule-out and approval. |
| T-00-05-04 | Information Disclosure | Contract/artifact APIs | high | mitigate | Positive allowlists and fixed diagnostics; no raw provider/framework values are representable. |
| T-00-05-SC | Tampering | SwiftPM manifest | high | mitigate | Zero external dependencies; package contains only system/toolchain targets. |
</threat_model>

<verification>
- Run the three focused suites plus the complete command-line-only package suite.
- Exercise exact ready and unavailable toolchain branches without assuming an installation path.
- Prove the core cannot create a candidate or GO bundle from incomplete, private, ambiguous, noncanonical, or secret-bearing evidence.
</verification>

<success_criteria>
- The phase has a production-quality safe tracer that closes without live work when prerequisites fail.
- Full Xcode/current SDK readiness and the complete empirical proof contract are explicit executable gates.
- The canonical proof and Phase 1 contract preserve all FEAS requirements and D-01 through D-17.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-05-SUMMARY.md` when done.
</output>
