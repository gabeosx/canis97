---
phase: 00-authentication-feasibility-gate
plan: "02"
type: execute
wave: 2
depends_on: [00-01]
files_modified:
  - .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
  - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
autonomous: false
requirements: [FEAS-01, FEAS-02, FEAS-04, FEAS-05]
user_setup:
  - service: siriusxm-public-documentation
    why: "The account owner must review public first-party evidence and return only a closed eligibility classification plus public reference URLs; no authenticated browser/account state may be shared."
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Browser-return is qualified only when one or more public first-party references establish every required callback, verification, honest-client, entitlement, sign-out, and control-handling predicate exactly; visible site behavior is not evidence (FEAS-01)."
    - "If any browser predicate is missing, ambiguous, conflicting, or not first-party/public, browser-return is explicitly ruled out before native-direct is considered."
    - "Native-direct is qualified only after that rule-out and only when public first-party references establish every exact honest request, client identity, authentication, entitlement, sign-out, and control predicate (FEAS-02)."
    - "Zero candidates, multiple candidates, native-before-browser-ruleout, and every incomplete evidence set select `unsupported`; the result never retains an alternate path."
    - "Evidence review creates no session, attempt, browser automation, authenticated observation, endpoint guess, or account interaction."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md"
      provides: "Sanitized closed evidence checklist and public first-party references only"
      contains: "Evidence completeness"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-SELECTION.md"
      provides: "Exactly one browser-return, native-direct, or unsupported selection"
      contains: "Selected candidate"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift"
      via: "CLI validates every closed predicate before selection"
      pattern: "complete|ruled-out|unsupported"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-SELECTION.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-03-PLAN.md"
      via: "Only one exact supported selection permits a candidate; unsupported skips all live work"
      pattern: "Selected candidate: (browser-return|native-direct|unsupported)"
  prohibitions:
    - statement: "Missing provider facts must never be inferred from authenticated browser/account/session material or endpoint/header guessing."
      status: unverified
      flagged: true
    - statement: "A second sign-in method or fallback must never be retained after one candidate is selected."
      status: unverified
      flagged: true
---

<objective>
Resolve the ordered public-evidence gate into exactly one candidate or the valid unsupported outcome, without any live authentication.

Purpose: Prevent provider capability guesses from reaching a session boundary and give later plans a mechanical single-path input.
Output: Sanitized `00-EVIDENCE.md` and an exactly validated `00-SELECTION.md`.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@.planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/00-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 00-02-01: Classify browser-first public evidence with the account owner</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
    - .planning/phases/00-authentication-feasibility-gate/00-01-SUMMARY.md
  </read_first>
  <files>None; Task 00-02-02 records the allow-listed result.</files>
  <action>Run the automated offline suite before requesting human input. Ask the account owner to review public first-party SiriusXM sources only. Browser-return qualifies only if those sources define an authorization initiation, fixed app-bound return, return verification/exchange, honest permitted app identity, explicit authenticated predicate, explicit entitlement predicate, clean sign-out/cleanup, and protected-control behavior with no missing or ambiguous fact. If any browser item fails, the owner must classify browser-return as ruled out before reviewing native-direct. Native-direct then qualifies only if public first-party sources define the exact direct request/host contract, honest permitted app identity, explicit authenticated and entitlement predicates, clean sign-out/cleanup, and protected-control behavior. Do not open or inspect an authenticated browser, initiate a sign-in, use developer tools, collect a redirect, guess a field, or consult non-public session material. The owner returns exactly one allow-listed classification plus public first-party reference URLs: `browser-return-complete`; `browser-ruled-out/native-direct-complete`; or `unsupported-no-complete-public-contract`. Missing, multiple, conflicting, or malformed input is unsupported.</action>
  <instructions>
    1. Review only publicly accessible first-party SiriusXM documentation; do not sign in or inspect an authenticated state.
    2. Apply every browser-return predicate first. If any is absent, inexact, ambiguous, conflicting, or not first-party/public, rule browser-return out.
    3. Review native-direct predicates only after that explicit browser rule-out. If any native predicate fails, use the unsupported classification.
    4. Return one allow-listed classification plus only the public first-party reference URLs used; disclose no callback/provider/account/session value.
  </instructions>
  <verification>The executor accepts one allow-listed classification and public first-party URLs only, maps all other input to unsupported, and cannot inspect a browser/account to strengthen or challenge the owner's classification.</verification>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility</automated>
    <manual>The account owner supplies exactly one allow-listed classification and only public first-party reference URLs. They provide no cookie, token, account identifier, callback value, screenshot, developer-tools material, raw request/response, authenticated observation, or precise timing.</manual>
  </verify>
  <done>The account owner has returned one browser-first eligibility classification from public first-party evidence without any live/provider session activity.</done>
  <resume-signal>Reply with exactly one classification—`browser-return-complete`, `browser-ruled-out/native-direct-complete`, or `unsupported-no-complete-public-contract`—followed only by the public first-party reference URL(s) used.</resume-signal>
</task>

<task type="auto">
  <name>Task 00-02-02: Record and validate the sole evidence-selected path</name>
  <reversibility rating="costly">This result controls whether a disposable candidate may exist and which sole path the account owner may later operate.</reversibility>
  <files>.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md</files>
  <action>Translate the single checkpoint classification into `00-EVIDENCE.md` only, using the exact closed predicate shapes and safe fields already specified. Run strict validation and generate `00-SELECTION.md` solely with `derive-selection`; never hand-edit selection. Re-derive to a scoped temporary, compare byte-for-byte, validate, and check cardinality. If evidence or selection is missing, unreadable, malformed, duplicate, conflicting, non-public, non-canonical, zero/multiple, native-before-ruleout, or otherwise fails any persisted-artifact gate, invoke `close-unsupported --reason invalid-artifact` for all four Phase 0 artifact paths without passing or copying raw contents. Then validate evidence/selection/owner/decision, freshly derive selection/decision to scoped temporaries, and byte-compare the complete canonical unsupported chain. Only inability to write or validate that safe closure is an error. Do not request another checkpoint or preserve the invalid material.</action>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; closed=0; if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" || ! cmp -s "$derived_selection" "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason invalid-artifact --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; { [ "$closed" -eq 0 ] || { swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file"; }; } &amp;&amp; [ "$(rg -c '^Selected candidate:' "$selection")" -eq 1 ] &amp;&amp; [ "$(rg -c '^Live attempt permitted:' "$selection")" -eq 1 ]</automated>
  </verify>
  <done>One validated selection exists; unsupported is final for live work, while a qualified result names one sole owner-operated candidate and excludes the other.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Public sources → owner evidence classification | Only explicit first-party public contracts qualify; absence and ambiguity block. |
| Owner classification → repository artifacts | Only one closed token and public reference URLs cross; authenticated/account/session material does not. |
| Evidence artifact → candidate selection | Invalid cardinality, ordering, completeness, or source type maps to unsupported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-01 | Information Disclosure | Human evidence handoff | high | mitigate | Closed token plus public URLs only; no live browser/account/session material. |
| T-00-02 | Spoofing / Tampering | Evidence qualification | critical | mitigate | Complete exact first-party predicate checklist; browser-first order; invalid/ambiguous evidence unsupported. |
| T-00-04 | Elevation of Privilege | Evidence review | high | mitigate | No sign-in, callback observation, endpoint/header guessing, or protected-surface probing. |
| T-00-05 | Tampering | Selection artifact | high | mitigate | Exactly one CLI-validated selection and a matching owner-only/prohibited live disposition. |
| T-00-SC | Tampering | Tool/package use | high | mitigate | No install task or external package; use the Wave 1 local harness only. |
</threat_model>

<verification>
- Evidence review uses public first-party sources only and initiates no session.
- The evidence file contains every required closed predicate and no raw/provider/account value.
- The selection validator accepts exactly one supported path or unsupported; it cannot retain a fallback.
</verification>

<success_criteria>
- Browser-first ordering is explicit and mechanically validated.
- One qualified candidate or unsupported is recorded, with zero/multiple/ambiguous input blocked.
- Unsupported skips all subsequent live work and remains a valid Phase 0 completion path.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-02-SUMMARY.md` when done.
</output>
