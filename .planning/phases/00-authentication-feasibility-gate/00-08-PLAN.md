---
phase: 00-authentication-feasibility-gate
plan: "08"
type: execute
wave: 4
depends_on: ["00-07"]
files_modified:
  - Spikes/AuthenticationFeasibility/Package.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift
autonomous: true
requirements: [FEAS-01, FEAS-03, FEAS-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The GUI/browser target exists only when exact current-SDK readiness, canonical safe-construction evidence, and digest-bound owner approval all validate; open third-party callback documentation does not disqualify an otherwise safe experiment."
    - "The account owner alone operates a purpose-scoped WKWebView; automation observes only allow-listed app-bound navigation/return events and sanitized semantic transitions needed to determine whether a clean return occurs (FEAS-01, D-03, D-04, D-13)."
    - "The runtime has no API that enumerates authenticated browser state; only material delivered explicitly through the matched app-bound return may pass in memory to the semantic client and is immediately collapsed to closed outcomes (D-02, D-14 through D-16)."
    - "Any protected, challenged, rate-limited, redirected, suspicious, unknown, or ambiguous result is terminal and cannot produce retry, fallback, or a second attempt (D-10, D-14)."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift"
      provides: "Owner-operated purpose-scoped WKWebView using only the approved bounded observation contract"
      exports: ["WebLoginSession", "AppBoundReturnResult"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift"
      provides: "Explicit in-memory return handoff collapsed to semantic proof events"
      exports: ["SemanticProofClient", "EphemeralSiriusXMProbeClient"]
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift"
      via: "Exact experiment-ready-plus-approved validation is checked before the source/target constructor is created"
      pattern: "validate-experiment-approval"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift"
      via: "Only the matched app-bound return explicitly delivers volatile material to the semantic client and becomes closed semantic events"
      pattern: "AppBoundReturnResult|SafeProbeEvent"
  prohibitions:
    - statement: "The browser experiment must never become an authenticated-browser observation or extraction tool."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Build the bounded browser-return experiment and semantic handoff from the approved safe-construction contract and current macOS SDK.

Purpose: Test the D-02/D-03 empirical WKWebView path without browser-state extraction, then preserve only a clean explicit return, incomplete no-return, or terminal semantic outcome.
Output: Conditional app-bound browser target, owner-operated WebKit experiment, explicit-return semantic client, and closed incomplete/terminal behavior.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-07-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
@.planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RunProtocol.swift

<interfaces>
- `validate-experiment-approval` is the only browser-constructor precondition after current-SDK readiness.
- `AppBoundReturnResult` is volatile and non-serializable; `SafeProbeEvent` is the only evidence boundary.
- Current SDK APIs may be used only after `verify-current-xcode.sh --require-ready` succeeds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-08-01: Conditionally construct the owner-operated app-bound browser</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/PublicAuthContract.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Package.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift</files>
  <behavior>
    - Environment-pending, construction-incomplete, unapproved, malformed, or digest-mismatched input leaves the GUI target and browser source absent and keeps Phase 1 blocked; only an explicit terminal row validates canonical NO-GO.
    - Ready execution creates WKWebView only after an explicit owner start and observes only the allow-listed first-party navigation/provenance and app-bound return shapes in the approved experiment contract.
    - The WKWebView uses `WKWebsiteDataStore.nonPersistent()` and never queries that store's contents.
    - The owner operates every provider page; the harness exposes no form automation, script injection, screenshots, accessibility scraping, cookie/storage/profile enumeration, developer-tools bridge, or browser-state query surface.
  </behavior>
  <action>Run the toolchain, auth-experiment-contract, and approval validators before modifying the conditional source graph. For any non-ready row, keep `AuthFeasibilityHarness` and `WebLoginSession` absent from the manifest/source graph, exercise incomplete and terminal cases in `BrowserReturnContractTests`, and make no provider request. For the exact ready row, add the macOS 26 GUI target and implement the purpose-scoped WKWebView entry per D-01 through D-04, D-13, D-14, and D-16. Use WKWebView because D-03 is locked. Configure `WKWebsiteDataStore.nonPersistent()`, load only the validated public first-party entry surface, allow ordinary navigation only within the approved provenance boundary, and observe only allow-listed navigation/return events and semantic transitions. Match the approved app-bound return shape entirely in memory. Do not enumerate, read, copy, export, or infer cookies, WebKit storage, profiles, credentials, developer-tools data, raw token/session material, or other authenticated browser state. Missing public third-party callback documentation remains an empirical question and must not suppress construction of this bounded path.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --check-conditional .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter BrowserReturnContractTests</automated>
  </verify>
  <done>The source graph contains no browser target for every blocked branch, or one approved owner-operated bounded app-bound browser with no authenticated-state extraction surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-08-02: Collapse the observed explicit return to closed proof semantics</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebLoginSession.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RunProtocol.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserReturnContractTests.swift</files>
  <behavior>
    - Only material delivered explicitly by the matched app-bound return may enter the purpose-scoped semantic exchange in memory; no browser store, profile, prior raw capture, or unrelated navigation can supply it.
    - Clean return, explicit authentication, and explicit entitlement collapse immediately to `SafeProbeEvent`; runtime return, response, account, and navigation values are volatile and non-serializable.
    - A bounded ordinary run with no clean app-bound return emits `no-clean-return` with sanitized provenance and remains incomplete unless the later D-09 three-part rule-out validates.
    - Unknown result, unexpected or off-provenance navigation, challenge, protection, rejection, redirect mismatch, rate limit, bot/access-control signal, or ambiguity emits one terminal closed event.
    - Completion, incomplete no-return, cancellation, sign-out, and terminal stop clear all browser and ephemeral-session values without retry, fallback, or a second attempt.
  </behavior>
  <action>Implement `LiveBrowserRuntime` and `SemanticProofClient` over the Plan 00-08-01 constructor. Per FEAS-01, FEAS-04, D-01 through D-03, and D-13 through D-16, accept only volatile material explicitly delivered through the matched app-bound return and consume it once in the purpose-scoped client-owned ephemeral URLSession; never derive a handoff from browser state. Bind the allowed semantic exchange to the canonical experiment contract derived from public provenance and sanitized D-02 expectations, and stop if any required operation would have to be guessed. Convert clean return, safe success, explicit entitlement, no-clean-return, and every terminal category to `SafeProbeEvent`; persist only semantic outcomes and sanitized provenance classes. Make blocked branches incapable of constructing either type and extend `BrowserReturnContractTests` for explicit-return delivery, no-clean-return, unexpected/off-provenance navigation, protected, ambiguous, cancellation, and teardown outcomes.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --check-conditional .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter BrowserReturnContractTests</automated>
  </verify>
  <done>The observed app-bound return feeds only closed volatile proof semantics; ordinary no-return remains honest and incomplete, while every unsafe or unknown state terminates without retained browser data.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved experiment contract → source graph | Only exact canonical safe construction may create the GUI/live target. |
| Account owner → WKWebView | Human operates provider content; app observes only allow-listed app-bound navigation/return events and semantic transitions. |
| Volatile provider/AV values → semantic coordinator | Framework values must collapse to closed events and be absent before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-08-01 | Elevation of Privilege | Conditional source graph | critical | mitigate | Exact toolchain + safe construction + digest approval gate; blocked-branch tests prove target/source absence. |
| T-00-08-02 | Information Disclosure | WebLoginSession | critical | mitigate | Allow-listed app-bound events only, no browser-state enumeration, volatile non-serializable return values, unconditional teardown. |
| T-00-08-03 | Spoofing | SemanticProofClient | high | mitigate | First-party provenance plus allowable sanitized contract, system TLS, exact return matching, no inferred identity or operation. |
| T-00-08-04 | Denial of Service | Live runtime | high | mitigate | One in-flight attempt, first unsafe event terminal, cancellation and idempotent cleanup, no retry. |
| T-00-08-SC | Tampering | Dependencies | high | mitigate | No installation; conditional target uses only verified current system frameworks. |
</threat_model>

<verification>
- Run all focused suites in synthetic ready, incomplete, and terminal configurations.
- Confirm blocked source graphs have no GUI/live constructor.
- Confirm the ready runtime accepts handoff material only from the matched app-bound return, never from browser state, and exposes only closed semantic events.
</verification>

<success_criteria>
- FEAS-01 is implemented as a bounded empirical app-bound return experiment with no authenticated-browser inspection or extraction.
- The browser-return construction exists only for the safe approved branch and remains owner-operated, sequential, bounded, and ephemeral even when third-party callback documentation is open.
- The fixed return and semantic handoff preserve FEAS-01, FEAS-04, D-03, D-04, and D-13 through D-16 without authenticated browser-state inspection.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-08-SUMMARY.md` when done.
</output>
