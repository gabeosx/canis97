---
phase: 00-authentication-feasibility-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Spikes/AuthenticationFeasibility/Package.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ContractTracerTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/CandidateSelectionTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/StopConditionTests.swift
autonomous: true
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04, FEAS-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A dependency-free SwiftPM tracer can validate a synthetic evidence record and emit a sanitized `NO-GO unsupported` decision end to end without network, browser, account, credential, or provider inputs."
    - "Every required evidence field is present and exact before a candidate can be selected; one missing, conflicting, or ambiguous field blocks before any session can be created (FEAS-01)."
    - "Zero or multiple candidates, native-direct before an explicit browser-return rule-out, and any retained alternate path are invalid; at most one path can proceed (FEAS-02)."
    - "Exactly two distinct, ordered, same-path proof records with authenticated, entitled, clean sign-out, and owner-confirmed cooldown are required for GO; 0, 1, more than 2, duplicate, out-of-order, mixed-path, or incomplete records block (FEAS-03)."
    - "One stop, unknown, or ambiguous signal is terminal; there is no tolerated count, retry threshold, or fallback (FEAS-04)."
    - "The artifact contains exactly one allow-listed decision and a continuation value consistent with it; missing, duplicate, malformed, or conflicting fields block Phase 1 (FEAS-05)."
    - "`00-SELECTION.md` is a deterministic derivation of a strictly validated `00-EVIDENCE.md`, and `00-DECISION.md` is a deterministic derivation of validated evidence, selection, and owner result; a hand-authored self-consistent selection or GO cannot pass equivalence checks."
    - "Any missing, unreadable, malformed, duplicate, or conflicting persisted Phase 0 artifact can be closed without reading its contents by one canonical idempotent unsupported-bundle command; safe closure failure is the only artifact-state error that aborts Phase 0."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Package.swift"
      provides: "Disposable SwiftPM package with no external dependency or product-target coupling"
      contains: "AuthFeasibilityCore"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      provides: "Pure fail-closed proof and decision state machine"
      contains: "NO-GO unsupported"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      provides: "Explicit local validation CLI with no default live behavior"
      contains: "validate-decision"
  key_links:
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      via: "CLI parses only allow-listed planning artifacts and delegates all decisions to the pure core"
      pattern: "DecisionGate"
    - from: "Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ContractTracerTests.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      via: "Synthetic end-to-end invocation produces a valid blocked decision without provider access"
      pattern: "NO-GO unsupported"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift"
      via: "Strict validate-evidence precedes pure derive-selection; selection is compared byte-for-byte with fresh derivation"
      pattern: "validate-evidence|derive-selection"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      via: "Strict validate-owner-result plus evidence/selection inputs feed pure derive-decision; decision is compared with fresh derivation"
      pattern: "validate-owner-result|derive-decision"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md"
      via: "close-unsupported stages all four canonical files, replaces them in documented order, and relies on strict full-chain validation/fresh derivation/byte comparison before any decision text is authoritative"
      pattern: "close-unsupported"
  prohibitions:
    - statement: "The disposable POC must never silently become the production SiriusXM client or be imported by a product target."
      status: unverified
      flagged: true
    - statement: "Missing provider facts must never be inferred from authenticated browser/account/session material or endpoint/header guessing."
      status: unverified
      flagged: true
    - statement: "A second sign-in method, fallback, retry, or bypass must never be retained after path selection or a stop condition."
      status: unverified
      flagged: true
    - statement: "Diagnostics and decision artifacts must never preserve live credentials, tokens, cookies, account identifiers, callback URLs, raw provider values, or precise timings."
      status: unverified
      flagged: true
---

<objective>
Build the production-quality offline tracer for the Phase 0 decision contract before any provider-facing work exists.

Purpose: Make every edge condition and safety rule executable with synthetic inputs, so missing evidence and unsupported authentication are safe terminal outcomes rather than prompts to probe.
Output: An isolated SwiftPM core, explicit local CLI, and complete synthetic test suite under `Spikes/AuthenticationFeasibility`.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@.planning/phases/00-authentication-feasibility-gate/00-PATTERNS.md
@.planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 00-01-01: Prove a synthetic blocked decision end to end</name>
  <files>Spikes/AuthenticationFeasibility/Package.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EvidenceContract.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/ContractTracerTests.swift</files>
  <behavior>
    - Test 1: an incomplete or absent public-evidence record reaches `NO-GO unsupported` with `Phase 1 continuation: blocked` through the CLI and creates no provider-facing object.
    - Test 2: `validate-evidence` requires every browser-return predicate to be present, exact, first-party/public, and mutually consistent before eligibility; missing, duplicate, malformed, conflicting, non-public, credentialed/query-bearing, or non-first-party references block.
    - Test 3: the artifact parser accepts exactly one of `GO browser-return`, `GO native-direct`, or `NO-GO unsupported` and rejects missing, duplicated, malformed, extra, or decision/continuation-conflicting fields.
    - Test 4: serialized evidence and decisions accept only rounded date, harness revision, public reference URLs, opaque run labels, closed semantic outcomes, cooldown confirmation, cleanup confirmation, decision, and continuation; synthetic secret/callback/raw-value canaries cannot serialize.
    - Test 5: invoking the executable with no explicit validation or synthesis subcommand performs no work and cannot initiate a live attempt.
  </behavior>
  <action>Create a standalone Swift 6.3 package at `Spikes/AuthenticationFeasibility` with no external dependencies and no production reference. Start with a failing executable-level synthetic test, then implement closed evidence/selection/owner-result/decision values plus `validate-evidence`, `derive-selection`, `validate-selection`, `validate-owner-result`, `derive-decision`, `validate-decision`, and `close-unsupported --reason ... --evidence ... --selection ... --owner-result ... --decision ...`. Derivations use canonical ordering/newlines. Validators require every field once, reject unknown/free-text/conflicting fields, never echo invalid content, and allow public references only as clean HTTPS first-party SiriusXM URLs. `close-unsupported` accepts only the closed enum `invalid-artifact`, `unsupported-selection`, `browser-tooling-unavailable`, or `candidate-preflight-failed`; it must not read, parse, copy, or log any target's prior content. It stages canonical sanitized evidence, deterministically derived unsupported selection, zero-live owner result, and deterministically derived NO-GO/blocked decision in scoped sibling temporaries, validates the staged chain, then replaces evidence, selection, owner result, and decision in that documented order. Any staging or individual rename failure makes the cross-artifact generation invalid. A stale raw GO/unlocked decision file may physically remain, but no validated artifact bundle remains GO/unlocked because every Phase 0 and Phase 1 consumer must strict-validate all four files, freshly derive selection and decision, and byte-compare the complete evidence→selection→owner→decision chain before treating decision text as authoritative. Closure is idempotently retryable, cannot create a candidate/session/checkpoint or invoke live work, and a successful retry converges to the canonical blocked bundle. Tests inject missing/unreadable/malformed/duplicate/conflicting inputs, each of the four rename failures, and staging failures, proving both Phase 0 and Phase 1 gate predicates reject every partial generation before a successful retry passes as blocked. The CLI has no default live path.</action>
  <verify>
    <automated>swift package --package-path Spikes/AuthenticationFeasibility describe &amp;&amp; swift test --package-path Spikes/AuthenticationFeasibility --filter ContractTracerTests</automated>
  </verify>
  <done>The dependency-free CLI carries one synthetic incomplete-evidence case through every layer to a schema-valid blocked decision while proving zero live/provider activity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-01-02: Enforce single-path, terminal-stop, and exact-proof invariants</name>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CandidateSelection.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/CandidateSelectionTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/StopConditionTests.swift</files>
  <behavior>
    - Test 1: zero candidates, two candidates, or native-direct before an explicit browser-return rule-out produces unsupported and never creates a candidate session.
    - Test 2: browser-return is selected only from a complete browser contract; native-direct is selected only after browser-return is explicitly ruled out and its own complete honest contract is attested.
    - Test 3: exactly two distinct ordered same-path passes are accepted only when each includes authenticated, entitled, and clean sign-out plus an intervening owner-confirmed cooldown.
    - Test 4: 0, 1, 3 or more, duplicated, out-of-order, mixed-path, missing-cooldown, missing-entitlement, or missing-sign-out proof input yields a blocked NO-GO.
    - Test 5: one rejection, CAPTCHA, challenge, unavailable MFA, 403, 429, rate limit, unexpected redirect, suspected bot response, device/geographic/subscription/DRM control, cleanup failure, unknown, or ambiguity is terminal and clears transient semantic input without retry/fallback.
    - Test 6: `derive-selection` deterministically maps complete browser evidence to browser-return, explicit browser rule-out plus complete native evidence to native-direct, and missing/duplicate/malformed/conflicting/non-public, zero/multiple, or native-before-ruleout evidence to unsupported or validation failure; fresh derivation must equal the stored selection.
    - Test 7: `validate-owner-result` rejects missing/duplicate/extra/out-of-order/mixed-path/partial proof fields, while accepting the exact zero-run unsupported record, one terminal record, or two ordered same-path passes only.
    - Test 8: `derive-decision` consumes validated selection, evidence, and owner result and deterministically produces the only valid decision/continuation pair; fresh derivation must equal the stored decision, so hand-authored GO text cannot unlock Phase 1.
    - Test 9: `close-unsupported` is idempotent for every closed reason, does not read/copy/log invalid target data, yields a byte-stable canonical unsupported chain, and cannot qualify a candidate or expose a live command.
    - Test 10: missing, unreadable, malformed, duplicate, and conflicting persisted inputs all close successfully; inject failure at each of the four ordered renames and assert complete-bundle validation plus both Phase 0 and Phase 1 gate predicates reject the resulting cross-artifact generation even when stale GO/unlocked text physically remains, then assert an idempotent successful retry yields the byte-stable canonical blocked bundle and cleans scoped temporaries.
  </behavior>
  <action>Implement a pure candidate-selection latch and proof-decision state machine behind the strict derivation commands. It accepts only the closed semantic inputs defined by the context, exposes no networking type, permits one in-flight proof label, and has no timer, poll, retry count, fallback transition, or candidate selector. A terminal stop irreversibly selects the blocked result for the run ledger. Preserve the order `run-1`, owner-confirmed cooldown, `run-2`; do not sort or deduplicate malformed inputs into validity. Bind every owner result to the selected path and evidence revision. Make GO construction impossible unless the exact two-run predicate passes, and make every other valid terminal state derive `NO-GO unsupported` with blocked continuation. Tests must invoke derivation twice and prove canonical equality, plus prove that manually authored selection/decision files inconsistent with fresh derivation are rejected by comparison.</action>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility &amp;&amp; ! rg -n 'URLSession|ASWebAuthenticationSession|WKWebView|HTTPCookieStorage|User-Agent|URLCredentialStorage|http://|https://' Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore</automated>
  </verify>
  <done>The pure core makes every surfaced edge case fail closed, allows exactly one candidate, and cannot create a GO without the exact ordered two-run proof.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Required outcome | Owning plan(s) | Status |
|---|---|---|---|---|
| GOAL | Phase 0 | Safe, evidence-backed GO/NO-GO before production | 00-01..00-04 | COVERED |
| REQ | FEAS-01 | Complete exact browser evidence without browser-state inspection | 00-01, 00-02 | COVERED |
| REQ | FEAS-02 | Browser-first, then at most one honest native candidate | 00-01..00-03 | COVERED |
| REQ | FEAS-03 | Exactly two owner-run authenticated/entitled/sign-out proofs | 00-01, 00-04 | COVERED |
| REQ | FEAS-04 | A single protected/unknown/ambiguous signal stops permanently | 00-01, 00-03, 00-04 | COVERED |
| REQ | FEAS-05 | Exactly one decision and consistent Phase 1 continuation | 00-01, 00-02, 00-04 | COVERED |
| RESEARCH | R-OFFLINE | Synthetic-only safety harness before live work | 00-01 | COVERED |
| RESEARCH | R-EVIDENCE | Public first-party contract is required for eligibility | 00-02 | COVERED |
| RESEARCH | R-CANDIDATE | Conditional system browser or ephemeral native boundary | 00-03 | COVERED |
| RESEARCH | R-HUMAN | Owner-only two-run protocol outside tests/CI | 00-04 | COVERED |
| CONTEXT | ordered-single-path | Browser first, native only after explicit rule-out, no dual path | 00-01..00-03 | COVERED |
| CONTEXT | immediate-stop | No bypass, retry, poll, concurrency, or alternate method | 00-01, 00-03, 00-04 | COVERED |
| CONTEXT | downstream-gate | GO alone unlocks Phase 1; NO-GO ends implementation | 00-04 | COVERED |
| CONTEXT | scope-fence | Spike never becomes app/client/Keychain/catalog/playback/release work | 00-01..00-04 | COVERED |

Deferred Phase 1–5 production features are excluded by the Phase 0 scope fence and are reasoned `OPT-OUT` rows in `COVERAGE.md`; they are not planning gaps.

<threat_model>
## ASVS L1 Enforcement

The offline core applies ASVS L1 input-validation, authentication-state, session-state, and privacy controls before any candidate exists.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Synthetic fixture → feasibility core | Untrusted/malformed planning input must produce a blocked result. |
| Feasibility core → decision artifact | Only closed semantic fields may cross; raw Foundation/provider values are unrepresentable. |
| Spike → future product | No build dependency or import connects disposable POC code to production targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-01 | Information Disclosure | Evidence/artifact schema | high | mitigate | Closed fields, unknown-key rejection, secret-canary tests, no raw networking/browser types. |
| T-00-02 | Spoofing / Elevation of Privilege | Candidate selection | high | mitigate | Exact browser-first predicates; no candidate from incomplete evidence and no alternate/fallback state. |
| T-00-03 | Tampering | Proof ledger | high | mitigate | Exact ordered cardinality and same-path validation; malformed/partial records block. |
| T-00-04 | Denial of Service / Elevation of Privilege | Stop handling | high | mitigate | First stop is irreversible; no retries, timers, polling, concurrency, or threshold. |
| T-00-05 | Tampering | Phase continuation | critical | mitigate | Exact decision/continuation pair; all invalid states block Phase 1. |
| T-00-SC | Tampering | SwiftPM dependencies | high | mitigate | Manifest has zero external dependencies; no package installation occurs. |
</threat_model>

<verification>
- `swift test --package-path Spikes/AuthenticationFeasibility` passes using synthetic data only.
- The core source contains no provider-facing or browser-facing API and the package has no production target coupling.
- Every surfaced boundary, precision, cardinality, ordering, and malformed-artifact edge has an explicit test.
</verification>

<success_criteria>
- The offline tracer can emit and validate the terminal unsupported branch end to end.
- Candidate selection and GO construction are impossible from incomplete, ambiguous, multiple, mixed, or partial evidence.
- The harness is isolated, dependency-free, and incapable of live work by default.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-01-SUMMARY.md` when done.
</output>
