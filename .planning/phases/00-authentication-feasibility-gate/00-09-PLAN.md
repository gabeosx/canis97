---
phase: 00-authentication-feasibility-gate
plan: "09"
type: execute
wave: 5
depends_on: ["00-08"]
files_modified:
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/AuthorizedPlaybackProbe.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RenewalObserver.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/AuthorizedPlaybackProbeTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/RenewalObserverTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/CleanupCoordinatorTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserProofPreflightTests.swift
autonomous: true
requirements: [FEAS-01, FEAS-03, FEAS-04]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A qualified browser run proves bounded tune/key authorization and audible AVPlayer output without recording, downloading, caching, or extended listening (D-05, D-17)."
    - "Renewal counts only for a distinct provider-issued replacement obtained through the empirically established ordinary flow; missing renewal stays incomplete and never becomes GO or false NO-GO (D-06, D-07)."
    - "Every success, pending-renewal, cancellation, terminal stop, and error performs the canonically established sign-out when safely available, tears down volatile browser/network/media state, and verifies local absence before closure (D-15 through D-17)."
    - "The complete synthetic browser preflight requires fixed-order authentication, entitlement, tune/key authorization, audible proof, renewal state, sign-out, and verified cleanup before a run can serialize as complete (FEAS-03)."
    - "Environment-pending, construction-incomplete, owner-rejected, and terminal branches construct no AV or provider work and produce only the matching incomplete, not-applicable, or unsupported semantic result."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/AuthorizedPlaybackProbe.swift"
      provides: "Bounded current-SDK AVPlayer and content-key authorization proof"
      exports: ["AuthorizedPlaybackProbe", "PlaybackProof"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RenewalObserver.swift"
      provides: "Passive legitimate provider-issued renewal classification"
      exports: ["RenewalObserver", "RenewalProof"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift"
      provides: "Idempotent sign-out, teardown, and absence verification"
      exports: ["CleanupCoordinator", "CleanupProof"]
  key_links:
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/AuthorizedPlaybackProbe.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      via: "Closed tune/key and owner-audible proof events advance the fixed run protocol"
      pattern: "PlaybackProof|SafeProbeEvent"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RenewalObserver.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      via: "Only verified provider-issued replacement or renewal-pending enters the run record"
      pattern: "RenewalProof|renewal-pending"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift"
      via: "Every exit awaits idempotent cleanup and absence verification before closure"
      pattern: "CleanupProof"
  prohibitions:
    - statement: "The bounded audible proof must never become recording, downloading, caching, or extended listening."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Expand the approved empirical browser-return construction into the complete bounded playback, renewal, sign-out, and cleanup proof chain.

Purpose: Make the full FEAS-03 finish line executable and synthetically testable while preserving the non-live unsupported branch and FEAS-04 terminal-stop behavior.
Output: Authorized playback probe, legitimate renewal observer, idempotent cleanup coordinator, closed `auth-feasibility` commands, and full empirical browser preflight tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-08-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RunProtocol.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift

<interfaces>
- `SafeProbeEvent` is the only boundary from volatile browser/provider/media values into proof coordination.
- `renewal-pending` is a valid incomplete result and never a terminal GO or NO-GO decision.
- A `ProofRunRecord` may close complete only after sign-out, teardown, and verified local absence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-09-01: Prove bounded playback and legitimate renewal</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RunProtocol.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/AuthorizedPlaybackProbe.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RenewalObserver.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/AuthorizedPlaybackProbeTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/RenewalObserverTests.swift</files>
  <behavior>
    - Blocked-branch tests prove neither playback nor renewal can trigger provider work and return the matching incomplete or not-applicable semantic results.
    - Empirically qualified playback attaches the asset to the current-SDK content-key session before loading and requires both player readiness and explicit owner audible confirmation.
    - Owner cancellation, media/key failure, protected behavior, timeout, or unsupported request terminates and clears every AV object and secret-adjacent byte.
    - Renewal counts only for a distinct provider-issued replacement received through the observed ordinary flow recorded in the canonical experiment contract and verified by the same authenticated semantic check.
    - Owner-ended observation without renewal returns `renewal-pending`; it produces neither GO nor NO-GO (D-06, D-07).
  </behavior>
  <action>In blocked branches, add only synthetic incomplete/not-applicable coverage and do not construct AV or network work. In the empirically ready branch, implement the bounded AVPlayer proof and passive `RenewalObserver` per D-05 through D-07 and D-14 through D-17, using only operations established from public provenance, sanitized D-02 expectations, and the explicit app-bound return observed by Plan 00-08. Create the current-SDK AVContentKeySession, add the asset as recipient before load, satisfy only the expected authorization request in volatile memory, require owner audible confirmation, then stop. Observe renewal only on the established ordinary provider path; prohibit mutation, clock manipulation, fabricated expiry, guessed refresh calls, polling, and retry. Clear all AV/session material on every exit.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --require-ready-or-closed &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter AuthorizedPlaybackProbeTests &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter RenewalObserverTests</automated>
  </verify>
  <done>The empirically qualified runtime can prove bounded audible authorization and natural renewal, while blocked branches remain non-live and renewal absence remains incomplete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-09-02: Verify complete-chain cleanup and browser preflight</name>
  <read_first>
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RunProtocol.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/AuthorizedPlaybackProbe.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/RenewalObserver.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/CleanupCoordinator.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/CleanupCoordinatorTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/BrowserProofPreflightTests.swift</files>
  <behavior>
    - Cleanup runs after success, pending renewal, cancellation, first terminal stop, and thrown error; repeated cleanup is idempotent.
    - Qualified cleanup performs the canonically established sign-out when safely available, cancels the ephemeral client, tears down browser/playback objects, clears all volatile material, and verifies local absence before run closure.
    - Synthetic preflight reaches a complete run only after fixed-order authentication, entitlement, tune/key authorization, audible proof, renewal state, sign-out, and verified cleanup.
    - Unsupported, partial, reordered, duplicated, pending-renewal, or cleanup-failed states cannot serialize as a complete proof run.
  </behavior>
  <action>Implement `CleanupCoordinator` and integrate `LiveBrowserRuntime` per D-05, D-15, D-16, and D-17. Give every volatile collaborator an async idempotent cleanup and absence-verification hook, run them in fixed structured teardown order, and perform only the sign-out operation canonically established by allowable sanitized evidence and the bounded experiment. Add `auth-feasibility` commands `validate-browser-launch-gate`, `record-browser-not-applicable`, `validate-live-result`, `validate-native-approval`, and `record-native-not-applicable` over closed semantic artifacts; the native approval validator must require the native purpose-contract artifact in addition to rule-out and consent. Build a full synthetic browser preflight with no provider values and separate incomplete/terminal branch proofs. No command may echo rejected content.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --require-ready-or-closed &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter CleanupCoordinatorTests &amp;&amp; xcrun swift test --package-path Spikes/AuthenticationFeasibility --filter BrowserProofPreflightTests</automated>
  </verify>
  <done>The conditional browser runtime cannot close a proof run until the full empirically established chain and verified cleanup are complete.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Closed browser events → playback/renewal | Only operations established by the canonical experiment contract may create volatile AV and renewal work. |
| Provider/media values → proof record | Untrusted values collapse to closed outcomes and are cleared before persistence. |
| Runtime exit → cleanup proof | Every success, incomplete, cancellation, stop, and error must verify local absence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-09-01 | Spoofing | Playback/renewal operation | high | mitigate | Canonical empirical contract only, exact expected authorization, system frameworks, no inferred provider identity. |
| T-00-09-02 | Information Disclosure | AV/session state | critical | mitigate | Volatile non-serializable values, fixed diagnostics, immediate teardown, synthetic canary tests. |
| T-00-09-03 | Denial of Service | Renewal/live coordinator | high | mitigate | Passive observation, one in-flight attempt, first unsafe state terminal, no retry/poll/timer. |
| T-00-09-04 | Tampering | Cleanup/run record | critical | mitigate | Fixed structured teardown, idempotence, absence verification, complete-record validation after cleanup. |
| T-00-09-SC | Tampering | Dependencies | high | mitigate | No installation; only verified current system frameworks and existing SwiftPM core. |
</threat_model>

<verification>
- Run all four focused suites and the complete package suite in qualified synthetic and unsupported configurations.
- Confirm playback, renewal, sign-out, and cleanup operations derive only from allowable sanitized evidence plus the observed explicit return contract.
- Confirm every incomplete, terminal, cancellation, and error path clears volatile state and cannot serialize complete.
</verification>

<success_criteria>
- FEAS-03's full per-run finish line is executable without reducing D-04 through D-08 or D-17.
- Renewal absence remains incomplete, and protected or ambiguous results remain terminal.
- Browser live proof cannot launch until the complete synthetic preflight passes.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-09-SUMMARY.md` when done.
</output>
