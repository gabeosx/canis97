---
phase: 00-authentication-feasibility-gate
plan: "03"
type: execute
wave: 3
depends_on: [00-02]
files_modified:
  - .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
  - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
  - Spikes/AuthenticationFeasibility/Package.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/OwnerAttemptCommand.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift
  - .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
autonomous: false
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04]
user_setup:
  - service: xcode
    why: "Only if browser-return is the qualified candidate, a selected full Xcode/macOS SDK may be required to compile AuthenticationServices. If it is unavailable, do not fabricate a bridge or switch paths; keep the result unsupported until the exact branch can be built."
  - service: siriusxm
    why: "Only the account owner may later operate the sole evidence-qualified candidate with their own subscriber account; this plan performs no live attempt."
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "When selection is unsupported, no browser/native candidate source or live command exists, `00-RUNBOOK.md` says live operation is prohibited, and later proof is skipped."
    - "If browser-return is selected but a full Xcode/macOS SDK is unavailable, the four unsupported artifacts are staged and replaced in documented order; only the strictly validated, freshly derived, byte-identical complete chain is authoritative, both candidate files are absent, and no checkpoint accepts a stale browser selection."
    - "When one candidate qualifies, only its minimal branch is compiled; the unselected branch is absent, not dormant or retained as fallback (FEAS-02)."
    - "The candidate cannot start from tests, CI, the default executable path, a timer, a retry, a poll, or a second concurrent invocation; it requires an explicit owner acknowledgement for one run label (FEAS-03)."
    - "Browser-return uses only the system authentication-session boundary and the complete first-party contract; native-direct uses only an ephemeral Foundation session with honest system identity and the complete first-party contract."
    - "One protected, challenged, rejected, rate-limited, redirected, suspicious, cleanup-failed, unknown, or ambiguous state cancels/invalidate the candidate, returns one safe terminal label, and prevents another path or retry (FEAS-04)."
    - "The candidate and runbook contain no persisted credential, token, cookie, account identifier, callback URL, raw provider value, precise timing, endpoint guess, spoofed identity, or authenticated-browser inspection."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md"
      provides: "Exact owner-only one-path procedure, stop matrix, cooldown boundary, and unsupported skip branch"
      contains: "Live operation"
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/OwnerAttemptCommand.swift"
      provides: "Explicit non-default owner initiation and one-attempt latch"
      contains: "ownerAcknowledged"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-SELECTION.md"
      to: "Spikes/AuthenticationFeasibility/Package.swift"
      via: "Exactly one supported selection determines the sole compiled candidate; unsupported compiles neither"
      pattern: "browser-return|native-direct|unsupported"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/OwnerAttemptCommand.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md"
      via: "Owner invokes one exact run label after preflight; output is one closed semantic disposition"
      pattern: "run-1|run-2"
  prohibitions:
    - statement: "The disposable candidate must never be imported by a production app or SiriusXM client target."
      status: unverified
      flagged: true
    - statement: "A second sign-in method, automatic fallback, retry, poll, concurrent attempt, or control bypass must never be compiled or retained."
      status: unverified
      flagged: true
    - statement: "Live credentials, tokens, cookies, account identifiers, callback URLs, raw provider values, or precise timings must never enter diagnostics, artifacts, source fixtures, or command arguments."
      status: unverified
      flagged: true
---

<objective>
Prepare only the evidence-selected disposable owner-operated candidate and an exact safety runbook, or prepare the no-live-work closure when selection is unsupported.

Purpose: Make the potential live boundary inspectable and deterministic without performing authentication, preserving a single fail-closed path.
Output: `00-RUNBOOK.md` plus exactly one conditional candidate branch—or no candidate at all for unsupported.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@.planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/00-01-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-02-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
@.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
</context>

<tasks>

<task type="auto">
  <name>Task 00-03-01: Normalize unsupported tooling before any candidate or checkpoint</name>
  <files>.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md, .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md</files>
  <action>Strictly validate `00-EVIDENCE.md`, freshly derive selection to a scoped temporary, require byte equality with `00-SELECTION.md`, and validate selection before branching. Any missing, unreadable, malformed, duplicate, conflicting, non-canonical, or inequivalent consumed artifact invokes `close-unsupported --reason invalid-artifact` for evidence, selection, owner result, and decision without reading the bad input. A valid unsupported selection invokes the same canonical closure with `--reason unsupported-selection`. If selection is browser-return, run only the offline tooling check `xcodebuild -version`; unavailable full Xcode invokes `close-unsupported --reason browser-tooling-unavailable`. Native-direct remains selected only when the prior exact evidence contract supports it. Closure stages all four unsupported artifacts and replaces them in documented order. After closure, validate all four files, freshly derive and byte-compare selection and decision, and treat any partial/rename failure as an invalid cross-artifact generation; a physically stale decision is never authoritative. Only inability to complete or validate safe closure is an artifact-state error, and idempotent retry cannot invoke live work. A validated unsupported chain removes/omits both candidate files and writes `Live operation: prohibited`, so no checkpoint accepts a stale browser selection, candidate source, or native fallback. Do not create a candidate or present a checkpoint in this task.</action>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; runbook=.planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; closed=0; if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" || ! cmp -s "$derived_selection" "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason invalid-artifact --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi &amp;&amp; if [ "$closed" -eq 0 ]; then selected=$(sed -n 's/^Selected candidate: //p' "$selection"); case "$selected" in unsupported) swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason unsupported-selection --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1;; browser-return) if ! xcodebuild -version &gt;/dev/null; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason browser-tooling-unavailable --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi;; native-direct) true;; *) false;; esac; fi &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; { [ "$closed" -eq 0 ] || { swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file"; }; } &amp;&amp; selected=$(sed -n 's/^Selected candidate: //p' "$selection") &amp;&amp; case "$selected" in unsupported) [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift ] &amp;&amp; [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift ] &amp;&amp; rg -q '^Live operation: prohibited$' "$runbook";; browser-return) xcodebuild -version &gt;/dev/null;; native-direct) true;; *) false;; esac</automated>
  </verify>
  <done>The stored selection is a fresh deterministic evidence derivation; missing browser tooling has become validated unsupported with no candidate and no stale supported state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 00-03-02: Compile only the selected owner-operated candidate or none</name>
  <precondition>`00-EVIDENCE.md` validates and freshly derives byte-identical `00-SELECTION.md`; when browser-return remains selected, Task 00-03-01 has verified a full Xcode installation.</precondition>
  <files>Spikes/AuthenticationFeasibility/Package.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/OwnerAttemptCommand.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift, .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md</files>
  <behavior>
    - Test 1: unsupported selection creates neither candidate target/file and the runner has no attempt command; runbook marks live operation prohibited.
    - Test 2: browser-return selection compiles only the browser candidate after full-Xcode preflight; the native candidate is absent and no browser-state-reading interface exists.
    - Test 3: native-direct selection compiles only an ephemeral-session candidate after explicit browser rule-out; the browser candidate is absent and no client/user-agent/device override exists.
    - Test 4: the owner command requires an interactive acknowledgement and one of the exact labels `run-1` or `run-2`; missing, duplicate, concurrent, scheduled, CI, or test invocation is rejected before session creation.
    - Test 5: every stop label cancels/invalidate transient state once and makes subsequent activity unavailable; output exposes one semantic label and never a URL, status body, header, token, error description, or precise time.
  </behavior>
  <action>Read the evidence-derived selection and implement one mechanical branch. For `unsupported`, preserve the absent candidate target files and attempt subcommand plus the prohibited-live runbook; never recreate the pre-normalization branch. For `browser-return`, use the already verified full Xcode toolchain and create only a small `ASWebAuthenticationSession` bridge; the exact public contract is entered by the owner through a transient interactive prompt at run time, held only in process memory, and never accepted through command arguments, environment variables, files, logs, fixtures, or source constants. For `native-direct`, create only one `URLSessionConfiguration.ephemeral` request sequence with no shared storage and no identity overrides; contract inputs and credentials are entered interactively with secret values non-echoing, stay in memory, and are discarded on every exit. Do not use an automated command to invoke either live branch. Both candidates expose only closed semantic results, allow one in-flight attempt, invalidate/cancel on stop or sign-out, and cannot retry or switch method. Write the exact preflight, owner action, semantic-result vocabulary, immediate-stop table, cleanup, owner-controlled cooldown, and second-run ordering in `00-RUNBOOK.md`; do not include provider values or secrets.</action>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility &amp;&amp; swift build --package-path Spikes/AuthenticationFeasibility &amp;&amp; selected=$(sed -n 's/^Selected candidate: //p' .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md) &amp;&amp; case "$selected" in unsupported) [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift ] &amp;&amp; [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift ];; browser-return) [ -f Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift ] &amp;&amp; [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift ];; native-direct) [ -f Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityNativeCandidate/NativeCandidate.swift ] &amp;&amp; [ ! -e Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityBrowserCandidate/BrowserCandidate.swift ];; *) false;; esac</automated>
  </verify>
  <done>The spike contains exactly one compile-tested, explicitly owner-operated candidate or no candidate for unsupported, plus a no-secret runbook; no live attempt has occurred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 00-03-03: Approve the sole candidate safety boundary without authenticating</name>
  <what-built>The selected branch and `00-RUNBOOK.md` have been compiled/tested using synthetic data only. When selection is unsupported, the executor must verify the prohibited-live branch automatically and bypass this human checkpoint without presenting authentication instructions.</what-built>
  <how-to-verify>
    1. If `Selected candidate: unsupported`, confirm `Live operation: prohibited`, confirm both candidate files are absent, record `candidate-review=skipped-unsupported`, and continue without asking the owner to operate anything.
    2. Otherwise, the account owner reviews the source and runbook without entering credentials or initiating authentication.
    3. Confirm only the selected path exists, the other is absent, the command requires one explicit owner acknowledgement/run label, and tests/default/CI paths cannot invoke it.
    4. Confirm the immediate-stop list covers rejection, absent entitlement, cleanup failure, CAPTCHA, challenge/MFA, 403, 429/rate limiting, unexpected redirect, suspected bot response, device/geographic/subscription/DRM control, unknown, and ambiguity.
    5. Confirm artifacts/logs cannot retain credentials, token/cookie/account/browser/callback/raw provider values, or precise timing.
  </how-to-verify>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility &amp;&amp; swift build --package-path Spikes/AuthenticationFeasibility &amp;&amp; rg -q '^Live operation: (owner-only|prohibited)$' .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md</automated>
    <manual>For a supported selection only, approve the source/runbook boundary without opening a browser, typing a credential, initiating a session, or exposing account/provider state.</manual>
  </verify>
  <resume-signal>For a supported selection reply `candidate-safety-approved`; for unsupported the executor records `candidate-review=skipped-unsupported` and does not pause.</resume-signal>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Selection artifact → compiled spike | Exactly one validated selection controls target composition; unsupported compiles no live candidate. |
| Account owner → candidate | Sensitive/provider contract input exists only during an explicit interactive owner run and stays in process memory. |
| Candidate → semantic result | Raw callback/transport/account state is collapsed to a closed result before any output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-01 | Information Disclosure | Interactive candidate | critical | mitigate | Non-echo secret input, in-memory-only values, semantic-only output, no args/env/files/logs/raw errors. |
| T-00-02 | Spoofing | Native/browser identity | critical | mitigate | System APIs, default honest identity, exact public contract, no override/fingerprint emulation. |
| T-00-03 | Denial of Service | Attempt initiation | high | mitigate | One owner acknowledgement, one run label, one in-flight latch, no CI/test/timer/poll/retry. |
| T-00-04 | Elevation of Privilege | Protected-control handling | critical | mitigate | First stop cancels/invalidate and disables the path; no alternate candidate or continuation. |
| T-00-05 | Tampering | Conditional build | high | mitigate | Source/file cardinality test proves only the selected target exists; unsupported proves neither exists. |
| T-00-SC | Tampering | SwiftPM dependencies | high | mitigate | No external package install; Foundation/AuthenticationServices only. |
</threat_model>

<verification>
- Full offline suite and selected conditional target build pass; no automated command invokes the attempt subcommand.
- Exactly one candidate source exists for supported selection and none for unsupported.
- The owner approves the safety boundary only; they perform no authentication in this plan.
</verification>

<success_criteria>
- The compiled spike cannot retain or fall back to another method.
- Unsupported cleanly skips candidate creation and all live operations.
- A supported candidate is ready for exactly two owner-operated runs under Plan 00-04, with immediate terminal stop behavior.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-03-SUMMARY.md` when done.
</output>
