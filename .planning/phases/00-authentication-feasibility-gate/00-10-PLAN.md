---
phase: 00-authentication-feasibility-gate
plan: "10"
type: execute
wave: 6
depends_on: ["00-09"]
files_modified:
  - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
  - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
  - .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
  - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
autonomous: false
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The browser checkpoint is not presented unless current-SDK readiness, safe experiment contract, owner approval, conditional source graph, and full synthetic preflight all validate; missing third-party callback documentation is not a launch disqualifier."
    - "The account owner alone operates two real current-macOS WKWebView runs and supplies only explicit audible/cooldown confirmations; the executor never interacts with the account surface (D-04, D-08, D-13)."
    - "A complete browser proof contains exactly two distinct ordered runs, each with authentication, entitlement, tune/key authorization, audible playback, sign-out, and verified cleanup, with legitimate renewal in at least one (D-05, D-06, D-17)."
    - "Pending renewal remains incomplete; any protected, challenged, rate-limited, suspicious, unknown, or ambiguous result is terminal and does not expose native-direct (D-07, D-10, D-14)."
    - "Native-direct disclosure is shown only after exact WebKit-specific rule-out and a separately qualified honest native purpose contract derived from allowable sanitized evidence; an ordinary no-clean-return result without that proof remains incomplete and cannot expose native credentials (D-09 through D-12)."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      provides: "Canonical browser result: not-applicable, complete, renewal-pending, strict-webkit-ruleout, or terminal-stop"
      contains: "format_revision: 2"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md"
      provides: "Separate native password-boundary approval, rejection, or not-applicable state"
      contains: "disclosure_revision: 1"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      via: "Experiment-ready approved construction contract is validated before live launch"
      pattern: "validate-experiment-approval"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md"
      via: "Only strict WebKit rule-out plus native-purpose-qualified evidence reveals the disclosure decision"
      pattern: "strict-webkit-ruleout|native-purpose-qualified"
  prohibitions:
    - statement: "A safe stop must never become retry, workaround, path switch, or pressure to continue."
      status: unverified
      flagged: true
    - statement: "Strict WebKit failure must never become native password exposure without honest native purpose-contract qualification and separate owner approval."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Run the bounded browser-first experiment under account-owner control and conditionally decide whether the separately qualified native boundary may be exposed.

Purpose: Obtain empirical proof after safe-construction and environment gates, preserving ordinary no-return and renewal gaps as incomplete while closing protected outcomes immediately.
Output: A canonical complete, incomplete, strict-ruleout, or terminal browser result and a native disclosure decision only when all strict fallback predicates hold.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-09-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 00-10-01: Account owner conditionally performs the browser proof</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-09-SUMMARY.md
  </read_first>
  <files>.planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md, .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md</files>
  <action>Before presenting this checkpoint, run the toolchain, experiment-approval, conditional-source, complete-suite, and browser-preflight validators. If any environment or safe-construction prerequisite is incomplete, run `auth-feasibility record-browser-not-applicable --reason prerequisite-incomplete`, keep replacement execution open and Phase 1 blocked, and do not launch or display provider content; do not derive NO-GO merely because third-party callback documentation is open. For the exact ready branch, launch the local harness with `--live-browser` and the Phase 0 output path, then stop executor interaction when the WKWebView appears. Per D-01 through D-10 and D-13 through D-17, the owner alone completes normal provider UI, confirms audible output, requests sign-out, waits a self-chosen cooldown, and starts the second run only after a clean first run. Automation observes only the allow-listed app-bound navigation/return events and sanitized semantic state transitions defined in Plan 00-08. Preserve `renewal-pending` and an ordinary bounded `no-clean-return` without D-09 corroboration as incomplete. Any protected, challenged, rate-limited, 403/429, bot/access-control, suspicious, or ambiguous result closes immediately to terminal unsupported with no repetition or fallback. Treat fallback eligibility as possible only when the validator proves sanitized prior normal browser success, one owner-operated WebKit-specific failure, and matching secret-free local reproduction.</action>
  <verify>
    <automated>bash Spikes/AuthenticationFeasibility/Scripts/verify-current-xcode.sh --require-ready-or-closed &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-browser-launch-gate .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-live-result --candidate browser-return .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md</automated>
  </verify>
  <instructions>Incomplete and terminal branches write and validate closed semantic status without launch. The ready branch launches `AuthFeasibilityHarness --live-browser --output .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md`; the executor then stops while the owner performs the fixed run protocol.</instructions>
  <verification>Use only the validator's fixed class. Complete means two full distinct runs with renewal and cleanup; incomplete means an ordinary no-clean-return or renewal gap; strict rule-out means prior normal browser success, one WebKit-only failure, and matching secret-free local reproduction.</verification>
  <how-to-verify>
    1. Confirm every automated launch prerequisite passed before provider content appears.
    2. Operate the provider UI yourself; do not send the executor credentials, codes, screenshots, navigation details, service errors, or account data.
    3. Confirm audio only when heard, request stop/sign-out, and wait for verified cleanup.
    4. Choose and observe your cooldown, then repeat once with a distinct run label.
    5. If renewal remains pending, resume only after a later safe observation window. If a terminal stop appears, do not repeat.
  </how-to-verify>
  <resume-signal>Type `not-applicable`, `browser-complete`, `browser-incomplete`, `renewal-still-pending`, `strict-webkit-ruleout`, or `terminal-stop` exactly as shown by the validator; include no account details.</resume-signal>
  <done>The owner-controlled browser experiment has one validated complete, incomplete, strict-ruleout, or terminal semantic result, or a prerequisite-incomplete branch completed with no protected surface.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 00-10-02: Decide whether to expose the qualified native-direct boundary</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
  </read_first>
  <files>.planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md, .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md</files>
  <action>Present this decision only when Task 00-10-01 produced validator-confirmed strict-webkit-ruleout and `00-PUBLIC-AUTH-CONTRACT.md` independently validates `native-purpose-qualified` from allowable sanitized D-02 evidence and the owner-approved bounded experiment. If strict rule-out is absent, record native not-applicable and preserve the browser branch's incomplete or terminal status without a native UI. If the native purpose contract lacks a required safe value, record `native-contract-incomplete`, keep Phase 1 blocked, and do not guess an endpoint, identity, header, or operation; missing public third-party documentation alone is not the blocker. For the eligible branch, show only the allow-listed WebKit failure category, proof of the three-part rule-out, the native purpose-contract digest, its provenance classes, and the disclosure that the disposable harness will receive the password in volatile memory for the purpose-scoped direct request. Per D-09 through D-12, make approval and rejection equally clear and persist one closed choice bound to both digests. Rejection closes unsupported; approval is necessary but not sufficient for Plan 00-11.</action>
  <verify>
    <automated>swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-native-approval .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md</automated>
  </verify>
  <decision>After exact WebKit-specific rule-out and honest native purpose-contract qualification, should the disposable harness expose one owner-operated native-direct credential boundary?</decision>
  <context>The native path gives the password directly to disposable harness memory for the exact purpose-scoped request derived from allowable sanitized evidence and must meet the same two-run proof bar.</context>
  <options>
    <option id="approve-native-direct">
      <name>Approve native attempt</name>
      <pros>Allows the only authorized fallback to be built after both technical and purpose-contract gates.</pros>
      <cons>The password enters disposable harness memory under a different exposure boundary.</cons>
    </option>
    <option id="reject-native-direct">
      <name>Reject and stop</name>
      <pros>Keeps password entry confined to provider-owned browser content.</pros>
      <cons>Phase 0 ends unsupported if browser return cannot complete.</cons>
    </option>
  </options>
  <resume-signal>Select `approve-native-direct` or `reject-native-direct`; ineligible branches resume automatically with `not-applicable`.</resume-signal>
  <done>Native-direct is approved only against exact rule-out and a qualified honest purpose contract; incomplete qualification stays blocked, while rejection or terminal outcomes close with no native credential surface.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Automated launch gate → provider surface | Every environment, safe-construction, approval, source-graph, and preflight predicate must pass first. |
| Account owner → live harness | Human alone operates account UI and supplies fixed confirmations. |
| WebKit rule-out/native purpose evidence → fallback consent | Both technical and allowable sanitized contract facts plus a separate human decision are required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-10-01 | Information Disclosure | Owner checkpoint | critical | mitigate | Executor stop boundary, owner-only interaction, fixed semantic result, no account details in resume signal. |
| T-00-10-02 | Elevation of Privilege | Launch/fallback gates | critical | mitigate | Canonical conjunctions for browser launch and native disclosure; incomplete predicates remain blocked and terminal predicates close unsupported. |
| T-00-10-03 | Denial of Service | Live attempt | high | mitigate | One attempt, owner-chosen cooldown, first unsafe event terminal, no retry or switch. |
| T-00-10-04 | Repudiation | Owner confirmations | medium | mitigate | Closed audible/cooldown/disclosure choices bound to opaque labels and canonical digests. |
| T-00-10-SC | Tampering | Dependencies | high | mitigate | No install; previously verified conditional system-framework harness only. |
</threat_model>

<verification>
- Validate the full browser launch conjunction before checkpoint presentation.
- Confirm incomplete branches create not-applicable/incomplete without provider content and terminal branches alone create canonical NO-GO.
- Confirm native approval is presentable only for strict rule-out plus native-purpose-qualified evidence.
</verification>

<success_criteria>
- Live browser activity occurs only after the safe-construction, approval, and toolchain gates; public callback documentation may remain open.
- The owner alone performs the two-run proof and every stop remains terminal.
- Native-direct remains unavailable without all locked rule-out, native-purpose-contract, and approval predicates.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-10-SUMMARY.md` when done.
</output>
