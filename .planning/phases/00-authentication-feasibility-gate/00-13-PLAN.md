---
phase: 00-authentication-feasibility-gate
plan: "13"
type: execute
wave: 9
depends_on: ["00-12"]
files_modified:
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift
  - .planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
  - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
  - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
autonomous: true
requirements: [FEAS-01, FEAS-02, FEAS-03, FEAS-04, FEAS-05]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Finalization revalidates toolchain, experiment contract, experiment approval, browser result, native approval, native result, and source-graph state from canonical bytes; it never trusts a summary or hand-authored decision."
    - "Environment/safe-construction pending, ordinary no-clean-return without D-09 proof, renewal-pending, or native-purpose-contract incomplete yields fixed incomplete with Phase 1 blocked; missing public third-party documentation is never a terminal reason."
    - "Experiment rejection, protected/challenged/rate-limited/403/429/bot/access-control/suspicious/ambiguous terminal stop, native rejection, or native terminal stop yields exactly `NO-GO unsupported` with Phase 1 blocked."
    - "GO browser-return requires exact browser complete; GO native-direct requires strict WebKit rule-out, qualified honest native purpose contract, both approvals, browser live-source retirement, and exact native complete (D-03, D-09, D-11, D-12, D-17)."
    - "Renewal-pending remains incomplete and creates no valid terminal decision (D-07)."
    - "The durable bundle contains only revision, public references/digests, selected candidate, closed outcomes, opaque run labels, rounded dates, owner cooldown, cleanup, and one derived decision (D-15, D-16)."
    - "The unchanged Phase 1 `require-phase-one-go` command succeeds only for one complete canonical GO bundle (FEAS-05)."
    - "The supersession record remains replacement-planned and Phase 1 blocked until the quartet is newly derived, atomically written, byte-validated, and classified; incomplete renewal leaves it open, NO-GO closes it blocked, and GO still requires the independent Phase 1 `auth-feasibility` gate."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md"
      provides: "Canonical sanitized format-revision-2 proof evidence"
      contains: "format_revision: 2"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-SELECTION.md"
      provides: "Exactly one validated candidate or unsupported"
      contains: "format_revision: 2"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md"
      provides: "Closed owner confirmations bound to opaque labels and contract digests"
      contains: "format_revision: 2"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      provides: "Single mechanically derived GO/NO-GO decision"
      contains: "format_revision: 2"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md"
      provides: "Replacement completion status derived only after canonical quartet regeneration and validation"
      contains: "Schema: phase-0-supersession-v1"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md"
      to: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      via: "Safe-construction qualification and experiment approval are revalidated before any live result can be considered"
      pattern: "finalize-phase"
    - from: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      to: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      via: "Decision is output-only from the exhaustive canonical state table"
      pattern: "DecisionGate.derive"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-01-PLAN.md"
      via: "Existing require-phase-one-go precondition rederives the complete quartet"
      pattern: "require-phase-one-go"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md"
      via: "Status changes from replacement-planned only after the newly generated quartet validates and the GO/NO-GO continuation class is known"
      pattern: "replacement-finalized|Replacement execution complete: yes"
  flagged_assumptions:
    - id: "SP-EDGE-FEAS-05-UNCLASSIFIED"
      statement: "Any future finalization state outside the exhaustive table remains unsupported and requires explicit human review before Phase 1."
      flagged: true
  prohibitions:
    - statement: "The durable bundle must never become a troubleshooting archive or substitute for authenticated runtime capture."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** determine whether exactly one safe SiriusXM authentication path completes two account-owner authorized proof runs, **so that** production work proceeds only on validated authentication, renewal, playback, sign-out, and cleanup evidence.

<objective>
Derive the one canonical Phase 0 outcome across prerequisite-incomplete, browser, native, and terminal branches and enforce it at the Phase 1 boundary.

Purpose: Make the safe-construction, empirical-result, and toolchain gates part of the final tamper-resistant state without converting documentation gaps into infeasibility.
Output: Exhaustive finalization tests, canonical artifact quartet, and unchanged Phase 1 GO enforcement.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-authentication-feasibility-gate/00-12-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
@.planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
@.planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
@.planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
@.planning/phases/00-authentication-feasibility-gate/00-NATIVE-PROBE.md
@.planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift

<interfaces>
- Phase 1 keeps `auth-feasibility require-phase-one-go <evidence> <selection> <owner-result> <decision>`.
- Decision raw values remain exactly `GO browser-return`, `GO native-direct`, and `NO-GO unsupported`.
- Renewal-pending is a validated live-result class but not a terminal decision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-13-01: Finalize the complete prerequisite and proof state table</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
    - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-PROBE.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift</files>
  <behavior>
    - Environment-pending, safe-construction-incomplete, ordinary browser no-clean-return without strict rule-out, renewal-pending, or native-purpose-incomplete maps to fixed incomplete with no terminal artifact write.
    - Experiment rejection, browser protected/challenged/rate-limited/403/429/bot/access-control/suspicious/ambiguous terminal stop, native rejection, or native terminal stop maps to canonical NO-GO.
    - Browser complete maps only to GO browser-return.
    - Strict rule-out plus native-purpose-qualified allowable sanitized evidence, both exact approvals, browser live-source retirement, and native complete maps only to GO native-direct.
    - Missing public third-party documentation is an audit fact only and cannot select any terminal row.
    - Mixed, duplicate, contradictory, reordered, old-revision, hand-authored, noncanonical, or unknown states fail validation.
    - The allowlist rejects free-form and secret-bearing fields before writing; every output canonical-compares byte-for-byte.
    - Supersession validation requires the exact replacement-planned, 00-05..00-13, incomplete, Phase-1-blocked planning state before finalization can run.
    - The finalizer changes supersession to replacement-finalized and current authority to the canonical quartet plus supersession only after atomic quartet replacement and successful post-write validation; GO records unlocked only after `require-phase-one-go` succeeds, while NO-GO records blocked.
  </behavior>
  <action>Validate `00-SUPERSESSION.md` first and refuse finalization unless its machine fields still identify Plans 00-05 through 00-13 as active, replacement execution incomplete, and Phase 1 blocked. Per FEAS-01 through FEAS-05, D-01 through D-03, D-07, D-09 through D-12, and D-14 through D-17, implement the exhaustive final state table in `DecisionGate` and `auth-feasibility finalize-phase`. Consume only canonical prerequisite and live-result artifacts. Environment or safe-construction gaps, ordinary no-clean-return without strict D-09 proof, renewal-pending, and native-purpose-contract gaps return fixed incomplete status without a terminal revision-2 decision or supersession closure. Explicit owner rejection and every locked protected/ambiguous terminal row write one canonical NO-GO quartet without requiring later result files. Complete browser or fully authorized complete native rows write the appropriate GO quartet. Treat documentation-open as non-dispositive in every row. Reject every contradictory branch, candidate multiplicity, wrong run cardinality, missing cleanup/renewal, unknown field, and noncanonical byte sequence. Make finalization derive all four canonical outputs into a same-directory staging area, validate and byte-compare them as a unit, atomically replace the quartet, validate the installed bytes, run the unchanged-signature Phase 1 gate, and only then derive the supersession fields `Status: replacement-finalized`, `Replacement execution complete: yes`, `Current status authority: canonical-quartet+00-SUPERSESSION.md`, and branch-consistent Phase 1 continuation. Preserve the exact Phase 1 `auth-feasibility require-phase-one-go` signature and make it rederive the entire quartet before success.</action>
  <verify>
    <automated>swift test --package-path Spikes/AuthenticationFeasibility --filter FinalizationGateTests</automated>
  </verify>
  <done>The finalizer has one exhaustive canonical mapping covering prerequisite/browser/native incomplete, browser GO, native GO, and locked terminal NO-GO without a documentation-only terminal branch.</done>
</task>

<task type="auto">
  <name>Task 00-13-02: Emit and enforce the canonical Phase 0 bundle</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
    - .planning/phases/00-authentication-feasibility-gate/00-TOOLCHAIN.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-PUBLIC-AUTH-CONTRACT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-BROWSER-PROBE.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-DIRECT-APPROVAL.md
    - .planning/phases/00-authentication-feasibility-gate/00-NATIVE-PROBE.md
    - .planning/phases/01-safe-interoperability-foundation/01-01-PLAN.md
  </read_first>
  <files>.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md, .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md, .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md</files>
  <action>Run `auth-feasibility finalize-phase` over the available canonical prerequisite and live-result artifacts with the four exact established output paths and `00-SUPERSESSION.md`. If it reports any fixed incomplete class—environment-pending, browser-experiment-incomplete, ordinary no-clean-return without D-09 proof, renewal-pending, or native-contract-incomplete—leave the historical quartet non-authoritative, retain `Status: replacement-planned`, `Replacement execution complete: no`, `Current status authority: 00-SUPERSESSION.md+00-05..00-13`, and `Phase 1 continuation: blocked`, report the phase incomplete, and do not claim a decision. Otherwise require the command to atomically install newly derived revision-2 quartet bytes, pass `auth-feasibility validate-bundle`, and update supersession current authority only afterward. For GO, run the unchanged Phase 1 `auth-feasibility require-phase-one-go` command and require success before supersession records `Phase 1 continuation: unlocked`. For NO-GO, require the same command's specified closed failure and supersession `Phase 1 continuation: blocked` before any Phase 1 source mutation. No historical quartet file may be copied forward or hand-edited into authority. Inspect only schema keys and fixed validator classes; do not print artifact values.</action>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; supersession=.planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-bundle "$evidence" "$selection" "$owner" "$decision_file" &amp;&amp; rg -q '^Status: replacement-finalized$' "$supersession" &amp;&amp; rg -q '^Replacement execution complete: yes$' "$supersession" &amp;&amp; rg -Fxq 'Current status authority: canonical-quartet+00-SUPERSESSION.md' "$supersession" &amp;&amp; decision=$(sed -n 's/^Feasibility decision: //p' "$decision_file") &amp;&amp; continuation=$(sed -n 's/^Phase 1 continuation: //p' "$decision_file") &amp;&amp; supersession_continuation=$(sed -n 's/^Phase 1 continuation: //p' "$supersession") &amp;&amp; case "$decision:$continuation:$supersession_continuation" in 'GO browser-return:unlocked:unlocked'|'GO native-direct:unlocked:unlocked') swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility require-phase-one-go "$evidence" "$selection" "$owner" "$decision_file";; 'NO-GO unsupported:blocked:blocked') ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility require-phase-one-go "$evidence" "$selection" "$owner" "$decision_file";; *) false;; esac</automated>
  </verify>
  <done>Phase 0 ends with one newly generated validated sanitized terminal bundle plus closed supersession, or an explicit fixed incomplete state with supersession still open and Phase 1 blocked; Phase 1 mechanically enforces the distinction.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Prerequisite/live artifacts → finalizer | Local files are untrusted and canonical validation precedes derivation. |
| Finalizer → durable quartet | Only closed allow-listed semantics may be written atomically. |
| Phase 0 quartet → Phase 1 | Production work requires complete-chain GO rederivation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-13-01 | Tampering | Finalization inputs/outputs | critical | mitigate | Canonical parse/re-encode, exhaustive truth table, atomic four-file write, post-write validation. |
| T-00-13-02 | Information Disclosure | Durable schema | critical | mitigate | Positive field allowlist, mutation tests, public references/digests only, fixed validator output. |
| T-00-13-03 | Elevation of Privilege | Phase 1 gate | critical | mitigate | Exact command rederives one complete GO; old/incomplete/NO-GO fails before writes. |
| T-00-13-04 | Repudiation | Derived decision | medium | mitigate | Decision is output-only and bound to canonical prerequisite/evidence bytes. |
| T-00-13-SC | Tampering | Dependencies | high | mitigate | No install; finalization uses existing Foundation-only canonical machinery. |
</threat_model>

<verification>
- Run FinalizationGateTests and the complete command-line package suite.
- Exercise every prerequisite-incomplete row, ordinary no-clean-return row, browser/native terminal row, both GO rows, and both renewal-pending rows.
- Exercise the unchanged Phase 1 gate in GO, NO-GO, incomplete, malformed, and old-revision cases without touching Phase 1 files.
</verification>

<success_criteria>
- Safe construction/provenance evidence and exact toolchain readiness are part of the canonical authority for live work and finalization.
- Exactly one safe path can yield GO only after both complete owner proof runs.
- Every unresolved safety-contract or unavailable environment remains safely incomplete without provider or production activity; locked terminal outcomes close unsupported.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-13-SUMMARY.md` when done.
</output>
