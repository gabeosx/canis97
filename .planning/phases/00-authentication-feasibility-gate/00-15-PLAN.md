---
phase: 00-authentication-feasibility-gate
plan: "15"
type: execute
wave: 11
depends_on: ["00-14"]
files_modified:
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
  - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift
  - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift
  - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
  - .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
  - .planning/phases/00-authentication-feasibility-gate/COVERAGE.md
  - .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
  - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
autonomous: true
gap_closure: true
requirements: [FEAS-03, FEAS-04, FEAS-05]
estimate:
  tokens: 14000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "Corrected Phase 0 authority explicitly supersedes the old renewal, tune/key, audible-playback, native-direct, and absolute no-cookie decisions while preserving their safety intent."
    - "browser-probe-v3 and owner-result-v3 require exactly two new ordered browser-return runs with authenticated, entitled, signed-out, cleanup-verified semantics and owner-confirmed cooldown."
    - "The finalizer accepts no owner testimony or historical v2 artifact and installs a quartet only after staging, rederivation, byte validation, and atomic replacement."
    - "Unsupported entitlement derives NO-GO unsupported with zero runs; supported entitlement requires two complete v3 runs for GO browser-return."
  artifacts:
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift"
      provides: "Strict v3 evidence and GO-only decision derivation"
      exports: ["OwnerResult", "DecisionGate", "PhaseOneGate"]
    - path: "Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift"
      provides: "Unsupported probe recorder and complete finalization CLI"
      contains: "record-browser-unsupported"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md"
      provides: "Corrected Phase 0 authority through D-23"
      contains: "Corrected Phase 0 Authority"
  key_links:
    - from: "browser-probe-v3"
      to: "owner-result-v3"
      via: "finalize-phase reparses and rederives every field"
      pattern: "finalize-phase"
    - from: "owner-result-v3"
      to: "PhaseOneGate"
      via: "exact cardinality/order and canonical-byte validation"
      pattern: "require-phase-one-go"
  prohibitions:
    - statement: "GO must never be inferred from owner testimony, prior runs, authentication-only success, or handwritten artifacts."
      status: unverified
      flagged: true
    - statement: "The corrected finish line must never reintroduce renewal, tune/key, audible playback, or native-direct fallback."
      status: unverified
      flagged: true
    - statement: "Finalization must never overwrite the blocked quartet until every staged output validates."
      status: unverified
      flagged: true
---

## Phase Goal

**As a** Sirius Mac maintainer, **I want to** derive Phase 0 from a corrected two-run evidence schema, **so that** the later owner checkpoint has one unambiguous, mechanically enforced finish line.

<objective>
Reconcile every authoritative planning artifact and implement the v3 evidence/finalization machinery before any owner activity.

Purpose: Remove contradictory historical requirements from the active path and make GO/NO-GO derivation independent of human interpretation.
Output: Corrected authority, bounded coverage/validation/runbook, strict v3 schemas, and executable finalizer commands consumed by Plan 16.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 00-15-01: Reconcile authority and implement v3 finalization</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
    - .planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
    - .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
    - .planning/phases/00-authentication-feasibility-gate/COVERAGE.md
    - .planning/phases/00-authentication-feasibility-gate/00-ENTITLEMENT-CONTRACT.md
    - .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift
    - Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift
    - Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift
  </read_first>
  <files>Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/DecisionGate.swift, Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityRunner/main.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/DecisionGateTests.swift, Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/FinalizationGateTests.swift, .planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md, .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md, .planning/phases/00-authentication-feasibility-gate/COVERAGE.md, .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md, .planning/phases/00-authentication-feasibility-gate/00-SUPERSESSION.md</files>
  <action>Add `Corrected Phase 0 Authority (Plans 00-14 through 00-16)` to 00-CONTEXT.md. Mark D-05 superseded by D-19 for browser-return: tune/key and audible playback return to Phase 2. Mark D-06 and D-07 superseded by D-20: renewal is not an owner observation or GO condition. Mark D-12 superseded because native-direct remains not-applicable after verified browser-return. Clarify D-16 through D-18: D-16 still forbids cookie/token values in artifacts and logs, while D-18 permits only the named current first-party AUTH_TOKEN in single-consumption memory. Replace D-17 with the corrected rule that login/token handoff alone is insufficient; both new runs require separately proven entitlement, visible sign-out absence, and verified cleanup. Rewrite Context's Domain, Agent Discretion, Existing Code Insights, Specific Ideas, and Deferred Ideas paragraphs that still require renewal/tune/key/playback or forbid the narrow extraction. Add D-18 through D-23 for narrow extraction, the corrected finish line, no human renewal, Phase 2 playback ownership, zero historical complete runs, and no owner activity before a supported entitlement contract plus passing suite. Require the REQUIREMENTS.md and 00-RESEARCH.md amendments completed by Plan 14 to state the same authority; fail if they do not.

Update 00-VALIDATION.md, COVERAGE.md, and 00-RUNBOOK.md to match the corrected authority. Validation must map 00-14-01, 00-14-02, 00-15-01, 00-16-01, and 00-16-02 to exact pinned commands and distinguish pre-owner from post-owner verification. Coverage integrates only player WebView, narrow extraction, profile authentication, bounded entitlement classification, sign-out/cleanup, evidence ledger, and decision gate; renewal, tune/key, playback, catalog, Keychain, and production client are reasoned OPT-OUT rows. The runbook contains only visible owner steps and app-reported results. Extend supersession active range from 00-05..00-13 to 00-05..00-16 without changing historical summaries.

Implement `browser-probe-v3` and `owner-result-v3`. Each run has exactly label, path, outcome, authentication, entitlement, signed-out, and cleanup; only run-1, owner-confirmed cooldown, then run-2 is valid. Remove renewal, tune/key, and audible playback from the v3 parser/GO predicate; reject v2 as historical blocked input. Add `record-browser-unsupported --entitlement-contract ... --output ...`, accepting only canonical unsupported and performing no network/UI work. Update `finalize-phase` to accept `--entitlement-contract`, `--browser-probe`, existing prerequisite artifacts, and explicit quartet output paths. It reparses/rederives in a private staging directory, byte-validates, atomically installs, and validates installed bytes. Supported entitlement plus exactly two complete runs derives GO browser-return; unsupported entitlement derives NO-GO unsupported with zero runs; partial/malformed/contradictory input fails without overwriting the blocked quartet. Add tests for 0/1/2/3 runs, order, duplicates, mixed paths, cooldown, incomplete/terminal fields, unsupported entitlement, stale v2, tampering, interrupted staging, canaries, and PhaseOneGate.</action>
  <verify>
    <automated>CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter DecisionGateTests &amp;&amp; CLANG_MODULE_CACHE_PATH=/tmp/sirius-auth-clang-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/sirius-auth-swiftpm-cache DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Spikes/AuthenticationFeasibility --filter FinalizationGateTests &amp;&amp; rg -n '00-14-01|00-14-02|00-15-01|00-16-01|00-16-02' .planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md &amp;&amp; ! rg -n 'Renewal pending|Renewal verified|renewal-still-pending|confirm audio' .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - All conflicting Context decisions and prose are explicitly superseded or rewritten.
    - Requirements, Research, Context, Validation, Coverage, and Runbook name one corrected finish line.
    - Exactly two ordered complete v3 runs are necessary for GO; no v2 or historical observation can count.
    - Unsupported entitlement can be finalized without provider/UI work.
    - The finalizer stages, rederives, byte-validates, atomically installs, and revalidates all outputs.
  </acceptance_criteria>
  <done>Authority and finalization are complete and Plan 16 can execute either the no-live or owner-run branch.</done>
</task>

</tasks>

## Artifacts this phase produces

- `browser-probe-v3` and `owner-result-v3` schemas.
- Updated `DecisionGate`, `FinalizationGate`, `ArtifactBundle`, and `PhaseOneGate`.
- `record-browser-unsupported` and the complete `finalize-phase` CLI.
- Corrected Context decisions D-18 through D-23 plus explicit supersession of conflicting older decisions.
- Bounded Coverage, Validation, Runbook, and Supersession authority through Plan 16.

<threat_model>
ASVS level: 1. Blocking threshold: high.

- Critical tampering: handwritten/stale artifacts produce GO. Mitigate with runtime writes, rederivation, staging, byte comparison, atomic install, and installed-byte validation.
- Critical elevation: incomplete/authentication-only runs unlock Phase 1. Mitigate with exact v3 cardinality/order and PhaseOneGate rederivation.
- High replay: historical actions count as current runs. Mitigate with v3-only evidence and v2 rejection.
- Medium interruption: partial writes corrupt authority. Mitigate with private staging and atomic replacement.
</threat_model>

<verification>
- Decision/finalization suites pass under pinned Xcode 26.6.
- The corrected authority is consistent across all planning artifacts.
- Validation maps separate Plan 16 pre-owner and post-owner commands.
</verification>

<success_criteria>
- Finalization machinery is complete before any owner activity.
- Unsupported and supported branches are both deterministic and fail closed.
- No raw sensitive material or production Phase 1/2 work enters Phase 0.
</success_criteria>
