---
context: phase
phase: 00-authentication-feasibility-gate
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-17T14:20:29.787Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through failure and must be acknowledged before proceeding.

- [x] CONSTRAINT: Empirical feasibility is required — SiriusXM publishes no third-party authentication contract, so absence of public documentation cannot produce `NO-GO unsupported`. Phase 0 must test the owner-authorized browser/native behavior it was created to decide.
- [x] CONSTRAINT: Chromium is not WKWebView — successful browser login and native token replay do not prove the actual macOS `WKWebView` runtime is accepted. A disposable native POC must prove it before selecting `browser-return`.
- [x] CONSTRAINT: Secret-bearing capture is never a planning artifact — retain only allow-listed protocol shapes and outcomes. Never commit or print credentials, cookies, token values, authorization headers, raw HAR, raw bodies, account identifiers, stream URLs, or playback keys.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Documentation-only feasibility | The original plans treated missing public SiriusXM documentation as proof that neither intended authentication path could work. This produced a premature NO-GO without testing either path. | blocking | Supersede the old premise through `$gsd-discuss-phase 0`; require owner-authorized empirical behavior as the deciding evidence. |
| Runtime substitution | A normal Chromium login can prove endpoint/session portability but cannot stand in for the embedded macOS WebKit runtime. | blocking | Require two real WKWebView login → native authenticated request → cleanup runs before recording `GO browser-return`. |
| Raw capture retention | HAR/cookie/token material is sufficient to access the subscriber account and must not become a fixture or planning record. | blocking | Record schemas, endpoint names, status classes, lifetimes, and typed outcomes only; delete raw capture immediately after sanitization. |
| Multiple production sign-in paths | Keeping browser and native-direct flows simultaneously would expand the security and compatibility surface. | advisory | Select browser-return if WKWebView succeeds; evaluate native-direct only if WKWebView is genuinely unsupported. |

<current_state>
Phase 0 executed all four original plans and then failed verification (`gaps_found`). The implementation is an offline documentation/evidence tracer and the persisted decision says `NO-GO unsupported`, but that result is not authoritative because the phase premise was wrong.

An owner-authorized empirical capture has since answered the core transport question: the normal SiriusXM login/session/playback sequence was observed, and the resulting authenticated access token successfully performed account, tuning, manifest, and playback-key requests from a standalone native-style client using an honest `SiriusMacAuthPOC` user agent. This proves native API use is feasible and not bound to Chromium cookies or its user agent.

The remaining decisive question is whether SiriusXM login itself succeeds in a real macOS `WKWebView`. No raw evidence remains: the dedicated browser was closed and every `/private/tmp/sxm-*` capture artifact was deleted.
</current_state>

<completed_work>

Completed work:
- Original Phase 0 plans 00-01 through 00-04 executed.
- Phase review and verification completed; verification status is `gaps_found`.
- Owner manually completed a normal SiriusXM login in a dedicated headed browser.
- Captured the sanitized endpoint sequence for device, anonymous session, password identity authentication, authenticated session, account/profile/subscription, tune source, stream enforcement, telemetry, manifests, and playback-key access.
- Proved an issued access token works outside Chromium with an honest native-style user agent.
- Proved native tune-source negotiation and manifest/key retrieval.
- Inspected storage shape: first-party JavaScript-readable `AUTH_TOKEN`, `DEVICE_GRANT`, and `LOGICAL_CLOCK` cookies make `WKHTTPCookieStore` handoff plausible.
- Identified `session/v1/sessions/refresh` in the public player bundle, while the actual refresh-token storage/renewal mechanism remains unknown.
- Closed the dedicated browser and deleted all raw secret-bearing files.
</completed_work>

<remaining_work>

- Re-run Phase 0 discussion and explicitly supersede the documentation-only assumptions.
- Re-plan Phase 0 around a disposable current-macOS `WKWebView` POC.
- The POC must let the owner type credentials, retrieve only allow-listed session material through `WKHTTPCookieStore`, hand it to an ephemeral native `URLSession`, perform an authenticated account request and one tune/key negotiation, then clear WebKit and native session state.
- Perform exactly two owner-operated proof runs. Stop without bypass on CAPTCHA, MFA, challenge, rate limiting, or other access-control behavior.
- Record `GO browser-return` if both WKWebView runs pass. Evaluate the already-observed native-direct password chain only if WKWebView is genuinely unsupported.
- Determine session renewal behavior without guessing token candidates or retaining secrets.
- Establish how AVFoundation will authorize the AES-128 HLS key URI; the manifests/segments are reachable without Bearer auth, but the key endpoint requires it.
- Reconcile Phase 1 plans with the single selected authentication path before execution.
</remaining_work>

<decisions_made>

- Prefer purpose-scoped WKWebView login because the subscriber password stays in the SiriusXM web surface while the resulting session is portable to native requests.
- Keep native-direct password authentication as a conditional fallback only; do not build two production sign-in paths.
- Treat the previous `NO-GO unsupported` artifact as superseded rather than as a valid project gate.
- Keep the reusable SiriusXM layer responsible for volatile protocol behavior, while WebKit presentation and Keychain integration remain at the macOS app boundary.
</decisions_made>

<blockers>
- Phase 1 remains blocked until the corrected Phase 0 records one proven GO path.
- The owner must manually operate the eventual WKWebView login; no credentials should be entered by automation.
- Session renewal and AVFoundation key-loading behavior remain bounded technical unknowns, not reasons for a premature NO-GO.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured restart state and sanitized empirical findings.
2. `.planning/phases/00-authentication-feasibility-gate/.continue-here.md` — this blocking-constraint handoff.
3. `.planning/phases/00-authentication-feasibility-gate/00-VERIFICATION.md` — confirms the original phase is `gaps_found`; its code-contract findings are secondary to the corrected premise.
4. `.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md` — must be revised to reflect empirical owner-authorized testing.
5. `.planning/ROADMAP.md` — Phase 0 goal remains useful, but its documentation-first success criteria require correction.
6. `.planning/STATE.md` — current workflow position.

## Critical Anti-Patterns (do NOT repeat these)

- Do not infer infeasibility from absent public documentation. Build the bounded owner-operated proof.
- Do not treat Chromium success as proof of WKWebView compatibility.
- Do not inspect or automate the subscriber's credential entry.
- Do not persist or commit raw provider/account/session/playback evidence.
- Do not evade CAPTCHA, MFA, bot controls, device limits, entitlement checks, DRM, or other protections.
- Do not build both authentication paths; select exactly one after evidence.

## Infrastructure State

- No browser, capture, watcher, server, or async job is running.
- Dedicated `agent-browser` session `sxm-auth-poc` was closed.
- All `/private/tmp/sxm-*` files were deleted and absence was verified.
- Working tree already contained `.planning/config.json` modifications and untracked `.gsd/`; they are unrelated and must not be included in the handoff commit.
- `.agents/AGENTS.md`, referenced by the root compatibility shim, is absent in this checkout; continue using the root instructions and installed GSD skills.

## Pre-Execution Critique Required

- Design artifact: corrected Phase 0 plans to be created after `$gsd-discuss-phase 0`.
- Critique focus: real WKWebView runtime proof, allow-listed cookie/session extraction, password non-observation, cleanup, exact two-run gate, refresh behavior, authenticated HLS key loading, and no alternate/bypass path.
- Gate: Do not begin corrected execution until the plans are reviewed against the captured empirical facts and the blocking constraints above.

<context>
The important conceptual correction is that Phase 0 is an experiment, not a documentation eligibility review. We now know the issued SiriusXM session can drive a native client. The safest likely product flow is owner-operated WKWebView login followed by native URLSession calls, but only a real WKWebView run can promote that from plausible to proven. The direct password API chain is technically viable but should be selected only if WebKit fails honestly.
</context>

<next_action>
Start with: run `$gsd-resume-work`, acknowledge all blocking constraints, then run `$gsd-discuss-phase 0` to supersede the old premise before replanning.
</next_action>
