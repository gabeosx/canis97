---
phase: 00-authentication-feasibility-gate
plan: "04"
type: execute
wave: 4
depends_on: [00-03]
files_modified:
  - .planning/phases/00-authentication-feasibility-gate/00-PROOF-READY.md
  - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
  - .planning/phases/00-authentication-feasibility-gate/00-DECISION.md
autonomous: false
requirements: [FEAS-03, FEAS-04, FEAS-05]
user_setup:
  - service: siriusxm
    why: "Only the account owner can initiate the two live proof runs, operate the real browser or native credential surface, observe entitlement, perform sign-out, choose the cooldown, and return safe semantic dispositions."
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Unsupported selection writes a valid `NO-GO unsupported` decision with blocked continuation and performs zero live runs, browser operations, account actions, cooldowns, retries, or checkpoints."
    - "A supported selection permits exactly two distinct, ordered, separately owner-initiated runs through the same sole path: run-1, owner-controlled cooldown, then run-2 (FEAS-03)."
    - "Each GO-supporting run must explicitly confirm authenticated, entitled, and clean sign-out; missing any one confirmation blocks."
    - "Zero, one, more than two, duplicate, out-of-order, mixed-path, missing-cooldown, missing-entitlement, missing-sign-out, malformed, or conflicting proof evidence blocks; the validator never repairs or normalizes it into GO."
    - "A single challenge, CAPTCHA, unavailable MFA, rejection, absent entitlement, cleanup failure, 403, 429/rate-limit, unexpected redirect, suspected bot response, protected control, unknown, or ambiguous signal is terminal, stops all remaining work, and selects NO-GO with no retry or tolerance (FEAS-04)."
    - "The final artifact contains exactly one of `GO browser-return`, `GO native-direct`, or `NO-GO unsupported` and exactly one consistent continuation value; only an exact two-run GO writes `unlocked` (FEAS-05)."
    - "Phase 0 decision is the sole Phase 1 precondition. This plan does not execute, rewrite, or repeat Phase 1 production work."
  artifacts:
    - path: ".planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md"
      provides: "Allow-listed owner proof dispositions or unsupported-no-live-attempt, with no account/session detail"
      contains: "Owner proof result"
    - path: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      provides: "Single sanitized Phase 0 GO/NO-GO and exact Phase 1 continuation gate"
      contains: "Feasibility decision"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-SELECTION.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md"
      via: "Unsupported maps directly to zero-run blocked; supported permits owner-only proof on that exact path"
      pattern: "Selected candidate"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md"
      to: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      via: "Pure decision gate accepts only the exact ordered two-pass record for GO"
      pattern: "run-1|run-2|unsupported"
    - from: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-01-PLAN.md"
      via: "Phase 1 execution requires one validated GO plus unlocked continuation; NO-GO or malformed input blocks"
      pattern: "Phase 1 continuation: (unlocked|blocked)"
  prohibitions:
    - statement: "The executor or agent must never operate, automate, inspect, screenshot, or extract from the account, real browser, native credential surface, or authenticated session."
      status: unverified
      flagged: true
    - statement: "A second sign-in method, fallback, retry, poll, parallel attempt, or bypass must never follow a stop condition or failed/partial run."
      status: unverified
      flagged: true
    - statement: "Decision and proof artifacts must never preserve credentials, tokens, cookies, account identifiers, callback URLs, raw provider values, screenshots, exact timings, or browser/developer-tools state."
      status: unverified
      flagged: true
---

<objective>
Produce the terminal Phase 0 decision: immediately block without live work for unsupported, or accept exactly two owner-operated same-path proofs and unlock Phase 1 only when every predicate passes.

Purpose: Establish authentication feasibility before any production investment while keeping the subscriber account outside automation and diagnostics.
Output: A validated `00-DECISION.md` containing the sole Phase 1 continuation precondition.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/00-authentication-feasibility-gate/00-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-RESEARCH.md
@.planning/phases/00-authentication-feasibility-gate/00-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/00-02-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-03-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
@.planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 00-04-01: Enforce the zero-run unsupported branch or arm one owner-only proof</name>
  <files>.planning/phases/00-authentication-feasibility-gate/00-PROOF-READY.md, .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md</files>
  <action>Run the synthetic tests/build, then strictly validate evidence, freshly derive and byte-compare selection, and validate selection before arming proof. Any missing, unreadable, malformed, duplicate, conflicting, or inequivalent consumed evidence/selection artifact invokes `close-unsupported --reason invalid-artifact` for the full four-artifact chain without reading the bad input. For a validated unsupported selection, remove stale `00-PROOF-READY.md` and require the canonical zero-live owner result plus freshly derived byte-identical NO-GO/blocked decision; if either is absent or invalid, invoke `close-unsupported --reason unsupported-selection` and validate the fresh canonical chain. For exactly one supported candidate, write `00-PROOF-READY.md` with selected path, evidence revision, fixed ordered labels, one-attempt assertion, closed outcome vocabulary, owner-controlled cooldown, and passed offline preflights. A missing/mismatched candidate, unsafe runbook, or failed candidate preflight invokes `close-unsupported --reason candidate-preflight-failed`, removes proof-ready and both candidates, writes prohibited-live state, validates/freshly derives/byte-compares the full canonical chain, and bypasses the checkpoint. Only failure to write or validate safe closure is an error; ordinary test/build/tool execution failures remain errors and never become GO or live work. Do not invoke the candidate, open a browser, wait a cooldown, or touch an account.</action>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; proof=.planning/phases/00-authentication-feasibility-gate/00-PROOF-READY.md; runbook=.planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; swift test --package-path Spikes/AuthenticationFeasibility || exit 1; swift build --package-path Spikes/AuthenticationFeasibility || exit 1; closed=0; if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" || ! cmp -s "$derived_selection" "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason invalid-artifact --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; selected=$(sed -n 's/^Selected candidate: //p' "$selection") &amp;&amp; case "$selected" in unsupported) if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" || ! cmp -s "$derived_decision" "$decision_file" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason unsupported-selection --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file"; fi &amp;&amp; [ ! -e "$proof" ] &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file" &amp;&amp; rg -q '^Live operation: prohibited$' "$runbook";; browser-return|native-direct) [ "$closed" -eq 0 ] &amp;&amp; test -r "$proof" &amp;&amp; rg -q '^Live operation: owner-only$' "$runbook";; *) false;; esac</automated>
  </verify>
  <done>The unsupported path is already a zero-run terminal record, or one sole candidate is armed for manual proof after every automated safety gate passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 00-04-02: Perform exactly two owner-operated proof runs or stop once</name>
  <read_first>
    - .planning/phases/00-authentication-feasibility-gate/00-RUNBOOK.md
    - .planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
    - .planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md (only when present; a validated unsupported result bypasses the checkpoint)
  </read_first>
  <files>None; Task 00-04-03 records only the safe semantic response.</files>
  <action>First run the branch-aware automated gate. If any consumed persisted artifact is missing, unreadable, malformed, duplicate, conflicting, or inequivalent, invoke canonical `close-unsupported --reason invalid-artifact`, validate/freshly derive/byte-compare the complete unsupported chain, remove proof-ready, and bypass this checkpoint. When Task 00-04-01 already produced any validated unsupported result, bypass without presenting live instructions. Otherwise the account owner—not the executor or agent—follows `00-RUNBOOK.md`. They explicitly initiate `run-1` through the sole selected path, confirm authenticated, confirm entitled, perform clean sign-out, and stop immediately on any terminal signal. Only after a complete first pass do they choose and complete a conservative cooldown; no timer or agent controls it. They then separately initiate `run-2` through the same path and apply the same three confirmations. The owner returns only the exact success record or one allow-listed terminal token. The executor must not click, type, open, observe, inspect, screenshot, automate, poll, retry, receive credentials, or obtain browser/account/session/provider data. A stop during either run ends the checkpoint immediately; run 2 does not start after a run-1 stop, and no third attempt is permitted.</action>
  <instructions>
    1. If Task 00-04-01 recorded unsupported, do not pause, present live steps, or initiate anything; continue to final decision derivation.
    2. Otherwise, the account owner alone initiates run-1 on the selected path and confirms authenticated, entitled, then clean sign-out.
    3. On the first stop/unknown/ambiguous signal, end immediately and return the matching terminal token; do not retry or use another method.
    4. Only after run-1 fully passes, the owner chooses and completes a conservative cooldown without automation, then separately initiates run-2 on the same path.
    5. Return only the exact success line or one terminal token; disclose no other browser/account/provider detail.
  </instructions>
  <verification>The executor receives only the exact two-pass line or one terminal token, verifies it against the closed vocabulary, and never obtains or inspects authenticated state. Unsupported and terminal results proceed directly to blocked decision derivation.</verification>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; proof=.planning/phases/00-authentication-feasibility-gate/00-PROOF-READY.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; swift test --package-path Spikes/AuthenticationFeasibility || exit 1; closed=0; if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" || ! cmp -s "$derived_selection" "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason invalid-artifact --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi &amp;&amp; selected=$(sed -n 's/^Selected candidate: //p' "$selection") &amp;&amp; case "$selected" in unsupported) if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" || ! cmp -s "$derived_decision" "$decision_file" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason unsupported-selection --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file"; fi &amp;&amp; [ ! -e "$proof" ] &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file";; browser-return|native-direct) [ "$closed" -eq 0 ] &amp;&amp; test -r "$proof";; *) false;; esac</automated>
    <manual>The owner returns only `run-1=pass-authenticated-entitled-signed-out;cooldown=owner-confirmed;run-2=pass-authenticated-entitled-signed-out` or one terminal token from the resume signal. They disclose no other detail.</manual>
  </verify>
  <done>The owner supplied the exact two-pass record or one terminal stop token; the executor received no authenticated/browser/account/provider detail.</done>
  <resume-signal>Reply with exactly `run-1=pass-authenticated-entitled-signed-out;cooldown=owner-confirmed;run-2=pass-authenticated-entitled-signed-out`, or one of: `stop-rejected`, `stop-not-entitled`, `stop-signout-failed`, `stop-captcha`, `stop-challenge`, `stop-mfa-unavailable`, `stop-403`, `stop-429`, `stop-rate-limit`, `stop-unexpected-redirect`, `stop-bot-signal`, `stop-device-control`, `stop-geographic-control`, `stop-subscription-control`, `stop-drm-control`, `stop-other-terminal`, `stop-unknown`, `stop-ambiguous`.</resume-signal>
</task>

<task type="auto">
  <name>Task 00-04-03: Derive the sole final decision and Phase 1 continuation gate</name>
  <reversibility rating="costly">A GO authorizes production investment on one sole path; NO-GO ends production implementation until the phase is deliberately reconsidered with new public evidence.</reversibility>
  <files>.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md, .planning/phases/00-authentication-feasibility-gate/00-DECISION.md</files>
  <action>For a pre-recorded unsupported branch, preserve and validate the canonical zero-live owner result and NO-GO/blocked decision. For a supported selection, write only the allow-listed checkpoint semantics to `00-OWNER-RESULT.md`, bound to the selected path and exact evidence revision. The exact success line becomes two distinct ordered same-path fields with authenticated, entitled, clean sign-out, and owner-confirmed cooldown; a terminal token becomes one closed terminal result with no fabricated later fields. Strictly validate evidence/selection/owner result, create `00-DECISION.md` solely with `derive-decision`, then freshly derive and byte-compare it. If any consumed or persisted evidence, selection, owner-result, or decision artifact is missing, unreadable, malformed, duplicate, conflicting, non-canonical, or inequivalent at the final gate, invoke `close-unsupported --reason invalid-artifact` without reading/copying the invalid data, then validate evidence/selection/owner/decision and freshly derive/byte-compare both links of the canonical unsupported chain. This closure replaces partial or suspect proof with zero-live unsupported state, never requests another attempt, and bypasses all live work. Only inability to write or validate safe closure is an error. Enforce exact decision/continuation cardinality; only the deterministic exact two-run result can remain GO. Do not execute or modify Phase 1.</action>
  <verify>
    <automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; closed=0; if ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" || ! cmp -s "$derived_selection" "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" || ! cmp -s "$derived_decision" "$decision_file" || ! swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file"; then swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility close-unsupported --reason invalid-artifact --evidence "$evidence" --selection "$selection" --owner-result "$owner" --decision "$decision_file" &amp;&amp; closed=1; fi &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file" &amp;&amp; [ "$(rg -c '^Feasibility decision:' "$decision_file")" -eq 1 ] &amp;&amp; [ "$(rg -c '^Phase 1 continuation:' "$decision_file")" -eq 1 ] &amp;&amp; decision=$(sed -n 's/^Feasibility decision: //p' "$decision_file") &amp;&amp; continuation=$(sed -n 's/^Phase 1 continuation: //p' "$decision_file") &amp;&amp; case "$decision:$continuation" in 'GO browser-return:unlocked'|'GO native-direct:unlocked') [ "$closed" -eq 0 ];; 'NO-GO unsupported:blocked') true;; *) false;; esac</automated>
  </verify>
  <done>Exactly one sanitized validated decision exists; only an exact same-path two-pass GO unlocks Phase 1, while every other state is a terminal blocked NO-GO.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Executor preflight → account owner | Only a validated one-path runbook crosses; unsupported bypasses live work. |
| Account owner → executor | Only one exact success record or one terminal semantic token crosses. |
| Owner result → Phase 0 decision | Pure validator enforces exact cardinality/order/path/cleanup and fail-closed continuation. |
| Phase 0 decision → Phase 1 | Only one validated GO plus `unlocked` authorizes later production execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-01 | Information Disclosure | Owner proof handoff | critical | mitigate | Semantic-only closed response; agent never operates or observes browser/account/provider state. |
| T-00-02 | Spoofing | Proof identity/path | high | mitigate | Fixed run labels and selected-path match; mixed/duplicate/malformed proof blocks. |
| T-00-03 | Tampering / Denial of Service | Two-run protocol | critical | mitigate | Exact ordered two-run cardinality, owner cooldown, one in flight, no retry/third run. |
| T-00-04 | Elevation of Privilege | Stop handling | critical | mitigate | First protected/unknown signal is terminal NO-GO and cancels remaining work. |
| T-00-05 | Tampering | Decision/continuation artifact | critical | mitigate | Closed schema, exact decision count, consistent continuation, fail-closed Phase 1 reader. |
| T-00-SC | Tampering | Tool/package use | high | mitigate | No install task; local dependency-free validator only. |
</threat_model>

<verification>
- Unsupported selection completes with zero live work and a valid blocked decision.
- A supported branch accepts only the exact owner-only two-pass record or a single terminal stop.
- `validate-decision` and shell cardinality/consistency gates pass, with no sensitive/prohibited fields.
</verification>

<success_criteria>
- Phase 0 ends in exactly one allow-listed GO/NO-GO decision.
- Phase 1 is unlocked only by two distinct ordered same-path authenticated/entitled/clean-sign-out proofs with owner-controlled cooldown.
- Every unsupported, protected, partial, malformed, or ambiguous state blocks without another method or attempt.
</success_criteria>

<output>
Create `.planning/phases/00-authentication-feasibility-gate/00-04-SUMMARY.md` when done; its first decision line must match `00-DECISION.md`. Do not execute Phase 1.
</output>
