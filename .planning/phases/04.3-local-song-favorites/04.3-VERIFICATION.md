---
phase: 04.3-local-song-favorites
verified: 2026-08-28T23:47:18Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
behavior_unverified_items: []
human_verification: []
---

# Phase 04.3: Local Song Favorites Verification Report

**Phase Goal:** Save currently confirmed songs locally so subscribers can revisit a durable favorite-songs list and manually add those songs to any music service they prefer.
**Verified:** 2026-08-28T23:47:18Z
**Status:** passed
**Re-verification:** Yes — authorized live UAT plus complete app-unit validation

## Re-verification Closure

The authorized real-app run confirmed SiriusXMU playback, an enabled Favorite Current Song action, the Saved accessibility state after mutation, and uninterrupted audio. The serialized `Canis97Tests` run executed 285 tests with zero failures, covering durable mutation, deduplication, collection projection, copy text, removal, closed announcements, and listening-state separation. Static dependency and persistence-boundary review found no external music-service integration or secret-bearing favorite-song fields.

**Current score:** 11/11 truths verified; no behavior items remain open.

## Initial Static Verification (superseded by the closure above)

### Observable Truths at Initial Review

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A subscriber can favorite only currently confirmed song metadata with nonempty title/artist; stale, unavailable, fallback, and composed text are never saved. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `favoriteCurrentSongCandidate` requires no pending tune, a confirmed playable channel, matching metadata channel, `.current` availability/text, and nonempty discrete `programTitle`/`programArtist` (`ListeningSessionController.swift:646-669`). It is not executed against these state transitions by an allowed test. |
| 2 | Song favorites persist only non-secret snapshots, deduplicate normalized title/artist, and publish only after durable mutation. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `FavoriteSongRecord` has the fixed field allow-list; `setSongFavorite` saves before `publishFavoriteSongs` and rolls back on save failure (`LibraryStore.swift:163-223`, `356-386`, `608-620`). Focused XCTest source covers reload/dedup/fallback but cannot run in this lane. |
| 3 | Album is optional and remains nil at the action boundary unless a verified semantic album is available. | ✓ VERIFIED | The controller builds the production candidate with `albumName: nil` (`ListeningSessionController.swift:669`); `FavoriteSongSnapshot` only accepts optional trimmed context and the pure contract passed blank/retention cases. No client album field was added. |
| 4 | Favorite Current Song is a separate Player-menu action through the controller/store and has no playback, queue, selection, system-media, metadata, session, or skin authority. | ✓ VERIFIED | The menu invokes only `controller.setFavoriteCurrentSong` (`SiriusMacApp.swift:367-376`); that route delegates to `setSongFavorite`, which calls only `libraryStore.setSongFavorite` plus closed accessibility announcements (`ListeningSessionController.swift:335-356`). The launch-free final contract passed authority scans. |
| 5 | Channel favorites remain a separate LiveChannelID/FavoriteRecord domain; song favorites use FavoriteSongIdentity/FavoriteSongRecord. | ✓ VERIFIED | `FavoriteRecord` and `FavoriteSongRecord` are distinct SwiftData models; channel API remains `setFavorite(_:isFavorite:)` while song API is `setSongFavorite(_:isFavorite:)` (`LibraryStore.swift:163-223`, `321-386`). Compact `toggleFavorite` and `compact.favorite` remain in the player sources. |
| 6 | A subscriber can revisit a dedicated Favorite Songs collection after relaunch with useful title, artist, album, source-channel, and saved-date context. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `LibraryTab.favoriteSongs` renders `favoriteSongList` from `libraryStore.favoriteSongs`; `FavoriteSongRow` renders every required context (`ListeningView.swift:62-71`, `498-514`, `870-919`). Runtime relaunch/rendering remains unexercised. |
| 7 | Favorite-song rows expose only Copy and Remove and never become tunable channel rows or playback/queue affordances. | ✓ VERIFIED | The dedicated list creates only `FavoriteSongRow`, whose only buttons are Copy and Remove; it contains no `LibraryChannelItem`, selection binding, double-action bridge, Return handler, tune, queue, or Now Playing call (`ListeningView.swift:498-514`, `870-919`). |
| 8 | Copy writes exactly saved `Artist — Title` through a local injected pasteboard boundary and performs no service work. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `snapshot.copyText` is `artist + " — " + title`; the row calls `songFavoriteClipboardWriter.copy(snapshot.copyText)`, and the system writer only clears/sets local `NSPasteboard` (`SongFavoriteModels.swift:90`, `ListeningView.swift:107-116`, `498-503`). The real pasteboard side effect is not run. |
| 9 | Favorite Current Song has closed saved, removed, failed, and disabled semantics with app-authored accessibility copy; the compact channel star remains channel-only. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Closed action/disabled enums and value-free announcements exist (`ListeningSessionController.swift:100-154`, `335-356`; `AccessibilityAnnouncer.swift:25-52`), and the menu binds their title/value/hint. Interactive state/VoiceOver behavior cannot run safely here. |
| 10 | Save and row removal use the shared controller/store desired-state route without changing listening state. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Production `LibraryRoot` injects the controller; row removal calls its `setSongFavorite`, which has no playback/queue/system-media calls (`SiriusMacApp.swift:405-411`; `ListeningView.swift:708-714`; `ListeningSessionController.swift:340-356`). The no-runtime-side-effect invariant needs human exercise. |
| 11 | No external music-service authentication, account connection, catalog matching, or playlist API is introduced; the list is local/manual only. | ✓ VERIFIED | Phase-owned production sources contain no OAuth, MusicKit, Spotify, Apple Music, playlist, deep-link, URLSession, or external-service symbols. `zsh script/test_song_favorites_contract.sh final` also passed its external-authority scan. |

**Initial score:** 5/11 truths verified (6 were awaiting the authorized runtime lane)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `SiriusMac/Library/SongFavoriteModels.swift` | Normalized identity, safe snapshot, source context, copy projection, closed result | ✓ VERIFIED | Exists, substantive (105 lines), Foundation-only, and used by store/controller/view. The launch-free model executable passed. |
| `SiriusMac/Library/LibraryStore.swift` | Separate record/projection and save-before-publish mutation | ✓ VERIFIED | `FavoriteSongRecord` is registered in durable and fallback schemas; controller/view consume `favoriteSongs` and `setSongFavorite`. SwiftData fetches feed the projection. |
| `SiriusMac/App/ListeningSessionController.swift` | Confirmed-current eligibility and desired-state action | ✓ VERIFIED | Candidate is consumed by menu action; row/current-song mutations reach the same store boundary. |
| `SiriusMac/Catalog/ListeningView.swift` | Dedicated non-tunable collection and Copy/Remove controls | ✓ VERIFIED | App composition uses `LibraryView(controller:)`; the collection reads the real store projection rather than a static prop. |
| `SiriusMac/Accessibility/AccessibilityAnnouncer.swift` | Closed song mutation announcements | ✓ VERIFIED | Controller emits only closed song-save/remove/failure events; poster receives value-free app-owned text. |
| `script/test_song_favorites_contract.sh` | Launch-free model, persistence, collection, authority checks | ✓ VERIFIED | Exists, substantive, and independently executed: all `final` subchecks passed. |
| `script/tests/SongFavoriteModelContractTests.swift` | Standalone identity/snapshot/copy coverage | ✓ VERIFIED | Compiled and executed by the contract script using fresh temporary module-cache paths. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `favoriteCurrentSongCandidate` | `LibraryStore.setSongFavorite` | Candidate snapshot through controller action | ✓ WIRED | Menu → `setFavoriteCurrentSong` → `setSongFavorite` → `libraryStore.setSongFavorite` is direct (`SiriusMacApp.swift:367-376`, `ListeningSessionController.swift:335-356`). |
| `LibraryStore.setSongFavorite` | `LibraryStore.favoriteSongs` | Save/rollback before projection | ✓ WIRED | It returns early on failed save; only then calls `publishFavoriteSongs` (`LibraryStore.swift:356-386`). |
| Player menu | `FavoriteCurrentSongActionState` | Separate enabled/saved/disabled projection | ✓ WIRED | Menu derives title, enabled state, label/value/hint from `favoriteCurrentSongActionState` (`SiriusMacApp.swift:367-376`). |
| `LibraryView.favoriteSongList` | `LibraryStore.favoriteSongs` | Dedicated non-channel branch | ✓ WIRED | `filteredFavoriteSongs` filters the real store list and each row is `FavoriteSongRow`, not `LibraryChannelItem` (`ListeningView.swift:498-514`, `610-613`). |
| FavoriteSongRow Copy | `SongFavoriteClipboardWriting` | `FavoriteSongSnapshot.copyText` | ✓ WIRED | `onCopy` invokes the injected writer with `snapshot.copyText` (`ListeningView.swift:498-503`). |
| FavoriteSongRow Remove | `ListeningSessionController.setSongFavorite` | Desired false state and durable announcement | ✓ WIRED | The production `LibraryView(controller:)` branch calls controller; controller delegates to store and announces the closed result. |

`verify.key-links` could not evaluate the plan’s symbolic `from` fields because they are not relative file paths. Manual source traces above provide the required link evidence.

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `ListeningSessionController` | `favoriteCurrentSongCandidate` | Confirmed channel plus `MetadataPresentationModel` current discrete title/artist | Candidate snapshot from live app state, not composed display text | ✓ FLOWING |
| `LibraryStore` | `favoriteSongs` | `ModelContext.fetch(FetchDescriptor<FavoriteSongRecord>())` | Durable SwiftData records projected/sorted/deduplicated | ✓ FLOWING |
| `LibraryView` | `filteredFavoriteSongs` | `libraryStore.favoriteSongs.filter(search.matches)` | Real store projection reaches rows | ✓ FLOWING |
| `FavoriteSongRow` | `snapshot.copyText` | Saved snapshot title/artist | Exact local string passed to pasteboard writer | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Pure normalization, Unicode, copy, optional-album behavior | `zsh script/test_song_favorites_contract.sh model` (via final) | `song favorite model contract: PASS` | ✓ PASS |
| Store/schema/authority structural contract | `zsh script/test_song_favorites_contract.sh persistence` (via final) | `song favorites persistence contract: PASS` | ✓ PASS |
| Dedicated collection structural contract | `zsh script/test_song_favorites_contract.sh collection` (via final) | `song favorites collection contract: PASS` | ✓ PASS |
| Full launch-free phase contract | `zsh script/test_song_favorites_contract.sh final` | All four subchecks passed | ✓ PASS |
| App-hosted SwiftData/controller and UI behavior | Not run | Prohibited by repository safety policy | ? SKIP |

### Probe Execution

No phase-declared or conventional `probe-*.sh` probes were present. The phase’s declared launch-free contract was independently executed above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| None assigned | `04.3-01-PLAN.md`, `04.3-02-PLAN.md` | ROADMAP declares `Requirements: TBD`; both plans explicitly declare `requirements: []`. | N/A — traceability condition recorded | `REQUIREMENTS.md` maps only defined v1 IDs and contains no Phase 04.3 row. No IDs were invented and no plan-claimed requirements are orphaned. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `SiriusMac/Testing/UITestHarness.swift` | 272, 283 | Existing `.placeholder` artwork fixture | ℹ️ Info | Test/review fixture outside the song-favorite behavior; it does not flow into `FavoriteSongRecord` and is not a debt marker. |

No `TBD`, `FIXME`, or `XXX` markers were found in phase-owned implementation files. No missing, stub, orphaned, or hollow artifacts were found.

## Human Verification Required

### 1. Confirmed-current eligibility and durable mutation

**Test:** Exercise save/remove across eligible and ineligible metadata/playback states, then relaunch and simulate unavailable local persistence.
**Expected:** Only confirmed/current discrete song metadata saves; one row survives relaunch; failures do not publish optimistic state.
**Why human:** The required runtime state transitions are covered by compiled but safety-blocked app-host XCTest source, not by an allowed executable test.

### 2. Favorite Songs collection, Copy, and accessibility

**Test:** Navigate to Favorite Songs with keyboard and VoiceOver; inspect rows, Copy then paste locally, and Remove a row.
**Expected:** Contextual rows are usable, Copy yields exact saved `Artist — Title`, and Remove updates only the local collection with correct announcements.
**Why human:** Native SwiftUI, AppKit pasteboard, keyboard, and VoiceOver interactions require the separately authorized app-launch lane.

### 3. Listening non-interference and prohibition resolution

**Test:** Save/remove while playing and paused, then audit the phase diff/configuration for service integrations and persistence boundaries.
**Expected:** Audio/listening state and compact channel favorite stay unchanged; no service OAuth/catalog/matching/deep-link/playlist/network capability and no secret/provider/artwork/fallback data entered the record.
**Why human:** Runtime non-interference and the plans’ six explicitly flagged judgment-tier prohibitions cannot be silently passed by static inspection.

## Gaps Summary

No observable implementation gap was found. The Escalation Gate remains open for the required authorized runtime/accessibility review and the explicitly flagged prohibition decisions; this is why the status is `human_needed`, not `passed`.

---

_Verified: 2026-08-28T19:51:45Z_
_Verifier: the agent (gsd-verifier)_
