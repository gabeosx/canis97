---
phase: 04.3-local-song-favorites
plan: "02"
type: execute
wave: 2
depends_on:
  - 04.3-01
files_modified:
  - SiriusMac/Catalog/ListeningView.swift
  - SiriusMac/App/ListeningSessionController.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Accessibility/AccessibilityAnnouncer.swift
  - SiriusMacTests/LibraryStoreTests.swift
  - SiriusMacTests/ListeningSessionControllerTests.swift
  - SiriusMacTests/AccessibilityContractTests.swift
  - script/test_song_favorites_contract.sh
autonomous: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A subscriber can reopen a dedicated Favorite Songs library collection and see durable newest-first rows with saved title, artist, optional verified album, source-channel context, and saved date (D-08)."
    - "Favorite-song rows expose only app-owned Copy and Remove actions; they have no selection-to-tune bridge, Tune command, double action, Return activation, captured queue role, entitlement claim, or playback affordance (D-09, D-10)."
    - "Copy writes exactly the saved `Artist — Title` projection through an injected local pasteboard boundary and performs no service lookup, authentication, matching, deep link, or network work (D-10)."
    - "Favorite Current Song has its own saved, removed, persistence-failed, and disabled semantics; the disabled reason is app-authored and the existing channel star and `compact.favorite` contract remain channel-only (D-11, D-13)."
    - "Save and row removal both pass desired durable state through the one ListeningSessionController and LibraryStore without changing playback, queue, browse selection, current metadata, Now Playing, session ownership, or audio (D-07, D-12)."
  artifacts:
    - path: "SiriusMac/Catalog/ListeningView.swift"
      provides: "Dedicated Favorite Songs tab, non-tunable rows, injected pasteboard writer, local search, empty state, saved context, and Copy/Remove controls"
      exports: ["LibraryTab.favoriteSongs", "SongFavoriteClipboardWriting", "SystemSongFavoriteClipboardWriter", "FavoriteSongRow"]
    - path: "SiriusMac/App/ListeningSessionController.swift"
      provides: "Closed accessible action state and desired-state routes for current-song save plus row removal"
    - path: "SiriusMac/Accessibility/AccessibilityAnnouncer.swift"
      provides: "Closed value-free song save/remove/failure announcements distinct from channel favorites"
    - path: "script/test_song_favorites_contract.sh"
      provides: "Collection/final source authority checks plus launch-free model execution"
  key_links:
    - from: "LibraryView.favoriteSongList"
      to: "LibraryStore.favoriteSongs"
      via: "a dedicated branch that never converts song rows to LibraryChannelItem"
      pattern: "favoriteSongs|FavoriteSongRow"
    - from: "FavoriteSongRow Copy"
      to: "SongFavoriteClipboardWriting"
      via: "FavoriteSongSnapshot.copyText only after an explicit keyboard/VoiceOver-usable action"
      pattern: "copyText|SongFavoriteClipboardWriting"
    - from: "FavoriteSongRow Remove"
      to: "ListeningSessionController.setSongFavorite"
      via: "desired false state and durable-result announcement"
      pattern: "setSongFavorite"
    - from: "ListeningCommands Favorite Current Song"
      to: "FavoriteCurrentSongActionState"
      via: "closed saved/enabled/disabled reason and app-owned accessibility semantics"
      pattern: "favoriteCurrentSongActionState"
  prohibitions:
    - statement: "Favorite-song rows never tune, join playback queues, claim entitlement/playability, or become LiveChannel rows."
      status: unverified
      flagged: true
    - statement: "No external music-service SDK, OAuth/account flow, catalog search, matching, deep link, playlist API, or network request is introduced."
      status: unverified
      flagged: true
    - statement: "The existing compact channel star, toggleFavorite action, compact.favorite identifier, channel FavoriteRecord, and channel queue semantics are not repurposed."
      status: unverified
      flagged: true
---

<objective>
Complete the subscriber-facing Favorite Songs experience around Plan 04.3-01's durable domain: a dedicated non-tunable library collection supports relaunch browsing, local search, exact Copy, and durable Remove, while the separate current-song action communicates eligibility and outcomes accessibly.

Purpose: Make saved songs useful as a manual bridge to any music service without introducing external accounts, matching, playlists, or song playback authority.
Output: A native Favorite Songs tab and row contract, injected pasteboard adapter, accessible action semantics and announcements, separation/invariance coverage, and final launch-free/build evidence.
</objective>

<assumption_delta_decision>
primary_noun: FavoriteSongIdentity for song rows; LiveChannelID for channel rows
decision: no-change to FavoriteRecord, LibraryChannelItem, PlaybackQueue, and compact channel-favorite semantics
rationale: Favorite songs are durable text/context references for manual handoff, not playable SiriusXM entities. The UI must render them through a separate row type and route, preserving D-04, D-09, and D-11.
</assumption_delta_decision>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.3-local-song-favorites/04.3-CONTEXT.md
@.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md
@.planning/phases/04.2-expressive-skin-architecture-bundled-themes/04.2-CONTEXT.md
@.planning/phases/04.3-local-song-favorites/04.3-01-SUMMARY.md
@SiriusMac/Catalog/ListeningView.swift
@SiriusMac/Library/LibraryStore.swift
@SiriusMac/App/ListeningSessionController.swift
@SiriusMac/SiriusMacApp.swift
@SiriusMac/Accessibility/AccessibilityAnnouncer.swift

<interfaces>
- Plan 04.3-01 supplies `LibraryStore.favoriteSongs`, `FavoriteSongSnapshot.copyText`, `LibraryStore.setSongFavorite`, `ListeningSessionController.setSongFavorite`, and `FavoriteCurrentSongActionState`; use those exact exports rather than duplicating normalization or persistence in the view.
- `LibraryTab` currently drives a native segmented picker and persists its raw value through `LibraryStore.setSelectedLibraryTab`; add the stable `favoriteSongs` case between channel Favorites and Recents.
- `LibraryView.channelList` owns LiveChannelID selection, double-click/Return tuning, current entitlement reconciliation, captured queue origin, channel favorite stars, and Now Playing indicators. Favorite Songs must use a separate list/row branch with none of those behaviors.
- `ListeningCommands` and `AccessibilityAnnouncer` own app semantic copy. Skin manifests and renderers provide no song labels, enabled state, announcements, clipboard behavior, or persistence routing.
</interfaces>
</context>

## Spec-less probe and UI gate note

Phase 04.3 has no assigned requirement IDs, so the spec-less probe fallback was visibly skipped and `requirements: []` remains intentional. Research is disabled and no RESEARCH.md exists. The UI gate returned `frontend:false`, `hasUiSpec:false`, `block:false`; this plan follows locked CONTEXT decisions plus the established native `LibraryView`, menu, keyboard, and accessibility patterns without inventing a Phase 04.3 UI-SPEC.

<tasks>

<task type="auto" tdd="true">
  <name>Task 04.3-02-01: Add a dedicated non-tunable Favorite Songs collection with exact Copy and durable Remove</name>
  <read_first>
    - `.planning/phases/04.3-local-song-favorites/04.3-CONTEXT.md` — implement D-08, D-09, and D-10 exactly and preserve the external-service/artwork deferred boundaries.
    - `.planning/phases/03-native-mac-listening-experience/03-CONTEXT.md` — retain native tab/search/row/keyboard patterns while keeping channel selection, queue, tune, and Now Playing semantics isolated.
    - `SiriusMac/Catalog/ListeningView.swift` — modify `LibraryTab`, `LibraryView`, collection branching, search, empty states, and row actions; do not reuse `LibraryChannelItem` for songs.
    - `SiriusMac/Library/SongFavoriteModels.swift` — consume `FavoriteSongSnapshot` display/source/date/copy helpers and identity; do not normalize or reconstruct copy text in the view.
    - `SiriusMac/Library/LibraryStore.swift` — observe `favoriteSongs` and route removal through its desired-state API; do not create a ModelContext in the view.
    - `SiriusMac/App/ListeningSessionController.swift` — use the Plan 01 generic desired-state song route for row removal when the production controller is available.
    - `SiriusMacTests/LibraryStoreTests.swift` — add native tab title/raw-value, search, sort, row-projection, and channel/song invariance test sources alongside existing library contracts.
    - `script/test_song_favorites_contract.sh` — extend `collection` mode with region-scoped checks around the song-list/row implementations and exact clipboard adapter boundary.
  </read_first>
  <files>SiriusMac/Catalog/ListeningView.swift, SiriusMacTests/LibraryStoreTests.swift, script/test_song_favorites_contract.sh</files>
  <behavior>
    - `LibraryTab.allCases` is Channels, Categories, Favorites, Favorite Songs, Recents with a stable persisted `favoriteSongs` raw value and exact `Favorite Songs` title.
    - Favorite Songs renders independently of catalog load/failure and remains available from the durable projection after store reconstruction.
    - Each row displays title, artist, optional nonempty album, source channel name/number/identity fallback, and a localized saved date.
    - Search matches title, artist, optional album, and source-channel presentation only within Favorite Songs.
    - Copy sends exactly `snapshot.copyText` to an injected local pasteboard writer; Remove sends desired false through the shared controller/store and publishes only after durable success.
    - The song list has no LiveChannel selection binding, Tune command, AppKit double-action bridge, Return tune handler, Now Playing badge, favorite star, entitlement availability, queue origin, or playback command.
  </behavior>
  <action>Start with failing focused test/source contracts. Add `.favoriteSongs` after `.favorites` and before `.recents`, with an explicit title rather than raw-value capitalization; persist that raw value through the existing preference path. Resize the native segmented picker only enough for all five app-owned titles. Branch `libraryContent` and filtering so favoriteSongs reads `libraryStore.favoriteSongs` without consulting catalog load state, `LibraryChannelItem`, current entitlement, or selection. Add a dedicated `FavoriteSongRow` and nonselecting native `List` that show saved trimmed title/artist, optional album only when present, source channel context, and app-formatted saved date; use newest-first store order unchanged (D-08/D-09). Define `SongFavoriteClipboardWriting` plus `SystemSongFavoriteClipboardWriter` in the library UI boundary, inject it for focused tests, and write only `snapshot.copyText` after explicit Copy. Add separately focusable Copy and Remove buttons with complete Help, VoiceOver labels/values, stable identifiers, and at least the existing 32-point app control target; Remove uses `ListeningSessionController.setSongFavorite(snapshot,isFavorite:false)` or the injected store route and never disappears optimistically (D-10). Add `No Favorite Songs Yet` app-owned empty copy directing subscribers to Favorite Current Song; search-empty copy names Favorite Songs. Do not add confirmation that impedes keyboard removal unless the existing native destructive pattern can restore focus deterministically. The source contract must scope its non-tuning assertions to the favorite-song list/row region so channel rows retain their legitimate tuning behavior.</action>
  <acceptance_criteria>
    - Favorite Songs is a fifth dedicated library collection with stable persistence, native search, relaunch availability, and useful title/artist/album/source/date context.
    - Copy places exactly the saved `Artist — Title` text on the pasteboard and cannot invoke another service or network path.
    - Remove is keyboard- and VoiceOver-operable, routes desired false through the shared durable boundary, and leaves the row present on failure.
    - No favorite-song row offers Tune, selection-to-play, queue navigation, entitlement/playability, Now Playing, or channel-star semantics.
    - Channels, Categories, channel Favorites, and Recents retain their current selection, tuning, queue, favorite, and reveal behavior.
  </acceptance_criteria>
  <verify>
    <automated>zsh script/test_song_favorites_contract.sh collection</automated>
  </verify>
  <done>The native library exposes a durable, searchable, copyable, removable, and explicitly non-tunable Favorite Songs collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04.3-02-02: Close accessible action semantics and prove zero listening-side effects</name>
  <read_first>
    - `.planning/phases/04.3-local-song-favorites/04.3-CONTEXT.md` — complete D-07 and D-11 through D-13 with distinct disabled/result semantics and no listening mutations.
    - `SiriusMac/App/ListeningSessionController.swift` — refine the closed candidate/action state and both current-song and row-removal desired-state routes.
    - `SiriusMac/SiriusMacApp.swift` — refine only the separate Player-menu song action; preserve `CompactPlayerAction.toggleFavorite`, compact star routing, and all playback/appearance commands.
    - `SiriusMac/Accessibility/AccessibilityAnnouncer.swift` — add only closed value-free song favorite events; never carry song/provider strings or persistence errors into announcements.
    - `SiriusMacTests/ListeningSessionControllerTests.swift` — add current/stale/fallback/empty/unconfirmed matrices, durable outcome announcements, and before/after listening-state invariants.
    - `SiriusMacTests/AccessibilityContractTests.swift` — add exact labels/hints/events, menu ownership, row action, and unchanged channel-star structural contracts.
    - `script/test_song_favorites_contract.sh` — complete `action` and `final` modes across the real source regions and pure model executable.
  </read_first>
  <files>SiriusMac/App/ListeningSessionController.swift, SiriusMac/SiriusMacApp.swift, SiriusMac/Accessibility/AccessibilityAnnouncer.swift, SiriusMacTests/ListeningSessionControllerTests.swift, SiriusMacTests/AccessibilityContractTests.swift</files>
  <behavior>
    - The current-song menu action is enabled only for the exact D-01 candidate and exposes a closed app-owned reason for no confirmed channel, pending tune, noncurrent/mismatched metadata, missing title, or missing artist.
    - Unsaved eligible state is labelled `Favorite Current Song`; saved eligible state offers explicit removal from Favorite Songs without changing the channel favorite label or value.
    - Durable save announces `Added song to Favorite Songs`; durable removal announces `Removed song from Favorite Songs`; failed persistence uses one closed failure announcement and leaves saved state truthful.
    - Executing or rejecting a song action leaves playback state, selected and confirmed channel IDs, PlaybackQueue, reveal request, metadata state, system-media publication, authentication state, and coordinator identity unchanged.
    - Channel `setFavorite`, compact star, library channel stars, and their existing announcements remain channel-only and unchanged.
  </behavior>
  <action>Write the focused matrix/invariance test sources first. Refine `FavoriteCurrentSongDisabledReason` and `FavoriteCurrentSongActionState` so every eligibility failure maps to fixed value-free app copy and the menu derives its label, enabled state, saved value, and accessibility hint solely from that state (D-13). Keep the menu action in the app-owned Player menu rather than adding a skin slot, preserving the Phase 04.2 closed compact layout and the channel-only star contract (D-11). Recompute the candidate at invocation, pass the desired state through `ListeningSessionController` to `LibraryStore`, and announce only the returned durable outcome. Add closed `songFavoriteAdded`, `songFavoriteRemoved`, and `songFavoriteMutationFailed` events with no title, artist, channel, provider, storage error, URL, token, or arbitrary text. Capture and assert all listening/session/system-media projections before and after eligible save, eligible remove, ineligible invocation, and persistence failure; only favorite-song durable state and the corresponding closed announcement may change (D-07/D-12). Add structural accessibility contracts for the menu label/reason, Favorite Songs tab/rows, Copy/Remove actions, 32-point targets, stable focus order, and unchanged `compact.favorite`/`toggleFavorite` channel path. Run only the launch-free contract and compile-only Xcode path under the safety hold.</action>
  <acceptance_criteria>
    - Every D-01 failure disables Favorite Current Song with a specific fixed app-owned accessible reason and zero mutation attempt.
    - Successful save/remove and failed persistence produce distinct closed announcements that contain no song/provider/secret/storage detail.
    - Song actions cause no changes to playback, queue, selection, confirmed metadata, system media, authentication, coordinator identity, or audio ownership.
    - `compact.favorite`, `CompactPlayerAction.toggleFavorite`, `FavoriteRecord`, and channel favorite announcements remain channel-only.
    - Final source and build gates pass without executing an app, test host, UI automation, or live provider operation.
  </acceptance_criteria>
  <verify>
    <automated>zsh -lc 'zsh script/test_song_favorites_contract.sh final; dd_path=$(mktemp -d /tmp/canis97-04.3-02-final.XXXXXX); xcodebuild build-for-testing -project SiriusMac.xcodeproj -scheme Canis97 -destination "platform=macOS" -derivedDataPath "$dd_path" CODE_SIGNING_ALLOWED=NO'</automated>
  </verify>
  <done>Favorite Current Song and Favorite Songs row actions are accessible, durable-result truthful, channel-star independent, and proven not to mutate the shared listening session.</done>
</task>

</tasks>

## Artifacts this phase produces

| File | New symbols / contents |
|---|---|
| `SiriusMac/Catalog/ListeningView.swift` | `LibraryTab.favoriteSongs`, explicit five-tab titles, `SongFavoriteClipboardWriting`, `SystemSongFavoriteClipboardWriter`, `FavoriteSongRow`, dedicated list/search/empty state, exact Copy, and durable Remove |
| `SiriusMac/App/ListeningSessionController.swift` | Complete closed disabled-reason/action-state projection, current-song desired-state route, generic row-removal route, and listening-state invariance seam |
| `SiriusMac/SiriusMacApp.swift` | Saved/unsaved/disabled Player-menu presentation for Favorite Current Song with app-owned hint and no skin authority |
| `SiriusMac/Accessibility/AccessibilityAnnouncer.swift` | `songFavoriteAdded`, `songFavoriteRemoved`, and `songFavoriteMutationFailed` closed events and fixed messages |
| `SiriusMacTests/LibraryStoreTests.swift` | Five-tab/raw-value, favorite-song search/projection, row separation, and channel collection preservation contracts |
| `SiriusMacTests/ListeningSessionControllerTests.swift` | Eligibility matrix, durable outcome, persistence failure, and zero listening/system-media side-effect contracts |
| `SiriusMacTests/AccessibilityContractTests.swift` | Menu/tab/row Copy/Remove/focus/label/target contracts and unchanged compact channel-star assertions |
| `script/test_song_favorites_contract.sh` | Completed `collection`, `action`, and `final` modes with region-scoped authority checks |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| FavoriteSongSnapshot to library row | Durable semantic values become visible local UI but gain no LiveChannel or playback authority. |
| Explicit Copy to NSPasteboard | Only exact saved artist/title text crosses after a user action; no background export or service call occurs. |
| UI desired state to controller/store | Save/remove intent crosses; observable result follows the closed durable mutation outcome. |
| Skin appearance to compact controls | Appearance can arrange existing controls only; it cannot create or redefine the song action or accessibility semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.3-02-01 | Elevation of Privilege | FavoriteSongRow | high | mitigate | Separate row/list type with no LiveChannel conversion, tune route, queue capture, entitlement claim, or double action per D-09. |
| T-04.3-02-02 | Information Disclosure | NSPasteboard adapter | medium | mitigate | Copy only exact saved Artist — Title after an explicit user action; never copy album, channel, provider, session, stream, or raw payload data per D-10. |
| T-04.3-02-03 | Spoofing | Saved/removed UI state | medium | mitigate | Derive menu/list state from LibraryStore after a successful durable mutation and retain the prior row/state on failure per D-07. |
| T-04.3-02-04 | Tampering | Skin-provided semantics | high | mitigate | Keep the song action in app-owned menu/library code with fixed labels, enabled state, hints, announcements, and persistence behavior per D-11/D-13. |
</threat_model>

<verification>
- Run `zsh script/test_song_favorites_contract.sh final`; it executes the Foundation-only model contract and source authority checks without launching an app or test host.
- Run one serialized `xcodebuild build-for-testing` with fresh temporary DerivedData and `CODE_SIGNING_ALLOWED=NO`; compile all configured products without launching tests or Canis97.
- Inspect the Favorite Songs branch to confirm it never constructs `LibraryChannelItem`, calls tune/queue routes, uses NativeListDoubleActionBridge, or publishes system media.
- Inspect Player menu and compact renderer contracts to confirm the existing channel star, `toggleFavorite`, and `compact.favorite` remain unchanged.
- Do not run `xcodebuild test`, `test-without-building`, `xctest`, `XCUIApplication.launch()`, UI automation, `script/build_and_run.sh`, any app/test host, or any live SiriusXM operation.
</verification>

<success_criteria>
- Favorite Songs is durable after relaunch and presents complete saved context in its own native library collection.
- Copy and Remove are keyboard- and VoiceOver-usable, exact, and truthful after durable outcomes.
- No song row or action gains tune, queue, entitlement, playback, system-media, authentication, or skin authority.
- The existing channel-favorite star and collection remain semantically and structurally unchanged.
- No external music-service account, matching, catalog, deep-link, or playlist integration is present.
</success_criteria>

## Multi-source coverage audit (plan set)

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Durable local favorite-songs list for manual use with any service | 01, 02 | COVERED | Separate durable model plus dedicated UI |
| GOAL | SC-01 | Only confirmed current nonempty title and artist | 01 | COVERED | Current-discrete eligibility matrix |
| GOAL | SC-02 | Non-secret normalized dedupe and durable publication | 01 | COVERED | Record allow-list/save rollback |
| GOAL | SC-03 | Relaunch list with copy/remove/context | 01, 02 | COVERED | Relaunch projection plus native row actions |
| GOAL | SC-04 | Channel distinction and no listening/accessibility regression | 01, 02 | COVERED | Separate types and invariance tests |
| GOAL | SC-05 | No external service integration | 01, 02 | COVERED | Explicit source/prohibition gates |
| REQ | — | Phase requirement IDs | — | NOT APPLICABLE | Null/TBD in roadmap/init; no ID invented |
| REQ | META-01, META-02 | Preserve confirmed semantic metadata plus explicit stale/unavailable truth | 01 | COVERED | Upstream preservation constraint, not a Phase 04.3 assignment |
| REQ | LIBR-01, LIBR-03 | Preserve channel favorite identity and non-secret local snapshots | 01, 02 | COVERED | Upstream preservation constraint, not a Phase 04.3 assignment |
| REQ | UI-02, UI-03 | Extend the native library while retaining one shared listening session | 02 | COVERED | Upstream preservation constraint, not a Phase 04.3 assignment |
| REQ | ACCS-01, ACCS-02 | Preserve keyboard/VoiceOver semantics and prevent skin semantic authority | 02 | COVERED | Upstream preservation constraint, not a Phase 04.3 assignment |
| RESEARCH | — | Phase technical research | — | NOT APPLICABLE | Disabled by configuration; no RESEARCH.md |
| CONTEXT | D-01 | Confirmed current discrete metadata eligibility | 01 | COVERED | Candidate and matrix |
| CONTEXT | D-02 | Reject stale/unavailable/fallback/composed presentation | 01 | COVERED | Candidate and offline source contract |
| CONTEXT | D-03 | Optional verified semantic album only | 01 | COVERED | Nil with current observed contract |
| CONTEXT | D-04 | Separate song SwiftData record/projection | 01 | COVERED | FavoriteSongRecord/LibraryStore |
| CONTEXT | D-05 | Fixed safe snapshot | 01 | COVERED | Exact persisted allow-list |
| CONTEXT | D-06 | Title-and-artist normalized identity | 01 | COVERED | Pure model contract |
| CONTEXT | D-07 | Durable success before publication | 01, 02 | COVERED | Store outcome plus UI truth |
| CONTEXT | D-08 | Dedicated contextual collection | 02 | COVERED | Task 01 |
| CONTEXT | D-09 | Non-tunable rows outside queues | 01, 02 | COVERED | Separate domain/row contracts |
| CONTEXT | D-10 | Accessible exact Copy and Remove | 01, 02 | COVERED | Copy model plus injected pasteboard UI |
| CONTEXT | D-11 | Separate action; channel star unchanged | 01, 02 | COVERED | Player menu and structural contracts |
| CONTEXT | D-12 | Shared controller/store route, zero side effects | 01, 02 | COVERED | Desired state and invariance matrix |
| CONTEXT | D-13 | Disabled reason and app-owned accessibility | 02 | COVERED | Task 02 |
| PROJECT | P-01 | Genuine native current-macOS UI | 02 | COVERED | SwiftUI List, Commands, NSPasteboard adapter |
| PROJECT | P-02 | Secret-safe local persistence | 01 | COVERED | Exact non-secret record fields |
| PROJECT | P-03 | Provider/auth/access boundaries unchanged | 01, 02 | COVERED | No client/adapter/live changes |
| PROJECT | P-04 | Declarative skins cannot own semantics | 01, 02 | COVERED | Menu/library action semantics remain app-owned |

Deferred external authentication/matching/deep links/playlists and durable song/album artwork remain excluded and are not audit gaps.

<output>
Create `.planning/phases/04.3-local-song-favorites/04.3-02-SUMMARY.md` when done.
</output>
