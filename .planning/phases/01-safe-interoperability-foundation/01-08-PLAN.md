---
phase: 01-safe-interoperability-foundation
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  - Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift
  - SiriusMacTests/WebAuthenticationBridgeTests.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
  - .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-01, SECR-02, SECR-03, CLNT-01, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 26000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Every legitimate finding in `00-REVIEW.md` has a blocking Phase 1 regression test."
    - "No Phase 0 artifact or caller-authored record can activate or gate production authentication."
    - "Native authentication plus native entitlement are both required before atomic active-session publication and persistence."
    - "Extraction and sign-out use the same exact first-party token predicate across apex and accepted subdomains."
    - "WebView bridge sources/tests are unconditional build members and cannot disappear when `.planning` changes."
    - "Full Phase 1 acceptance is synthetic; it neither repeats authentication experiments nor contacts SiriusXM."
  artifacts:
    - path: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      provides: "Production acceptance and Phase 2 readiness contract"
      contains: "Phase 2 readiness: ready-for-authorized-live-listening"
  prohibitions:
    - statement: "MUST NOT perform a live smoke test, validate Phase 0 GO, request owner proof, weaken a review regression, or mark readiness while any suite/static scan fails."
      status: unverified
      flagged: true
---

<objective>
Close Phase 1 with synthetic production acceptance that carries every legitimate Phase 0 review finding into a durable regression contract.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/00-REVIEW.md
@.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-08-01: Lock the Phase 0 review findings into regression suites</name>
  <files>Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift, Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift, SiriusMacTests/WebAuthenticationBridgeTests.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift</files>
  <behavior>
    - CR-01: files, summaries, and caller-provided success flags cannot create an active session; only native response classifiers inside the runtime sequence can.
    - CR-02: authentication success always triggers native entitlement verification, and absent/negative entitlement prevents activation/persistence.
    - CR-03: an accepted host-only token on any approved subdomain survives neither sign-out nor the post-delete absence check; extraction/cleanup predicate identity is asserted.
    - WR-01: no quartet is consumed; a candidate session becomes visible through one atomic actor assignment after the full sequence.
    - WR-02: bridge production/tests compile unconditionally, and build files contain no mutable-planning conditional that can skip them.
    - Cross-cutting: terminal outcomes, cleanup failures, and canary secrets remain fail-closed and redacted with zero retry/fallback/live requests.
  </behavior>
  <action>Expand the focused suites only where the review-mapping table in `01-VALIDATION.md` lacks a direct assertion. Use synthetic cookie stores, transports, clocks, stores, and diagnostics. Add a structural identity test or shared dependency proof showing extraction and cleanup call the same `FirstPartyTokenCookiePolicy`; do not duplicate expected logic in the test. Add build-graph assertions for unconditional test membership.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter WebTokenAuthenticationTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter RedactionTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WebAuthenticationBridgeTests -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests test</automated></verify>
  <acceptance_criteria>
    - Each CR/WR row above has a named deterministic test assertion.
    - Test failures cannot be hidden by planning-artifact build conditions.
    - No test uses a live account, Web service, or secret fixture.
  </acceptance_criteria>
  <done>All legitimate review findings are blocking production regressions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 01-08-02: Run full acceptance and record Phase 2 readiness</name>
  <files>.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md</files>
  <action>Run the full package and native app suites. Then scan Plans 01-01 through 01-07 and production build/source paths for references that import, read, or validate retired Phase 0 evidence/selection/owner-result/decision artifacts or invoke its continuation command. Explanatory references to `00-REVIEW.md` are allowed. If any command or dependency scan fails, stop and do not write readiness. On success, write exactly these safe fields once: `Authentication architecture: webview-token-native-requests`, `Production authentication acceptance: passed`, `Phase 0 review regressions: covered`, and `Phase 2 readiness: ready-for-authorized-live-listening`.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' test &amp;&amp; ! rg -n '@.*00-(EVIDENCE|SELECTION|OWNER-RESULT|DECISION)|require-phase-one-go' .planning/phases/01-safe-interoperability-foundation/01-0[1-7]-PLAN.md Packages/SiriusXMClient SiriusMac SiriusMacTests SiriusMac.xcodeproj &amp;&amp; rg -q '^Authentication architecture: webview-token-native-requests$' .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md &amp;&amp; rg -q '^Production authentication acceptance: passed$' .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md &amp;&amp; rg -q '^Phase 0 review regressions: covered$' .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md &amp;&amp; rg -q '^Phase 2 readiness: ready-for-authorized-live-listening$' .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md</automated></verify>
  <acceptance_criteria>
    - Full package and app suites pass.
    - Active Phase 1 execution contains no stale Phase 0 authority or alternative auth-path selection.
    - Summary readiness derives only from Phase 1 production acceptance.
  </acceptance_criteria>
  <done>Phase 1 is accepted and Phase 2 may begin without any repeated authentication experiment.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md` with the exact four safe fields when done.</output>
