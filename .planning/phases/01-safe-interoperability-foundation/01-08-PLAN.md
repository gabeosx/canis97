---
phase: 01-safe-interoperability-foundation
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/RedactionTests.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
  - .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-01, SECR-02, SECR-03, CLNT-01, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase 1 revalidates and consumes the exact Phase 0 GO path; it performs no public-evidence selection, live authentication, owner checkpoint, cooldown, or proof-run repetition."
    - "Synthetic acceptance proves the production adapter distinguishes authenticated, entitled, rejected, challenged, unsupported, and cleanup outcomes without a live SiriusXM request (AUTH-01, AUTH-02)."
    - "Synthetic success persists only the allowed reusable credential after authenticated-and-entitled state; session material stays ephemeral and semantic diagnostics contain no synthetic secret canary (SECR-01..03)."
    - "Synthetic sign-out clears actor/session memory before app-owned Keychain deletion and reports deletion failure explicitly (AUTH-03)."
    - "Every terminal/control/unknown/ambiguous synthetic outcome stops after one attempt with no retry, fallback, alternate adapter, or follow-up transport call."
    - "Phase 2 readiness is based on the already validated Phase 0 GO plus passing Phase 1 production tests, never a second live gate."
  artifacts:
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift"
      provides: "Synthetic selected-path, semantic outcome, entitlement, one-attempt, and no-fallback production acceptance"
      contains: "SelectedAuthenticationPathTests"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift"
      provides: "Synthetic memory-first sign-out and Keychain cleanup-order acceptance"
      contains: "SignOutTests"
    - path: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      provides: "Phase 1 production acceptance and Phase 2 readiness without repeated live proof"
      contains: "Production authentication acceptance"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      to: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift"
      via: "Fresh validated GO path chooses the sole production composition exercised with synthetic collaborators"
      pattern: "GO browser-return|GO native-direct"
    - from: ".planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      via: "Exact implemented path plus production-selected-path-tested readiness must match Phase 0 before acceptance passes"
      pattern: "production-selected-path-tested"
  prohibitions:
    - statement: "MUST NOT perform or request any live authentication, account/browser operation, cooldown, or repeat of the Phase 0 proof runs."
      status: unverified
      flagged: true
    - statement: "MUST NOT let synthetic acceptance weaken the Phase 0 GO prerequisite, enable a second sign-in method, or turn the unavailable default into a fallback retry path."
      status: unverified
      flagged: true
---

<objective>
Close Phase 1 with production-only synthetic acceptance of the already proven Phase 0 path, without repeating live feasibility or proof runs.

Purpose: Verify the reusable client, app composition, credential/session lifecycle, terminal-stop behavior, and redaction contract are ready for Phase 2 while keeping live account evidence exclusively in Phase 0.
Output: Expanded synthetic package/app acceptance tests and a safe Phase 1 completion summary.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
@.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
@.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
@.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
@.planning/phases/00-authentication-feasibility-gate/00-DECISION.md
@.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-08-01: Accept the selected production client with synthetic auth and cleanup scenarios</name>
  <precondition>The full Phase 0 chain freshly derives an exact GO+unlocked decision, Plan 01-06 handoff matches it, and Plan 01-07 records the same implemented path with `Continuation readiness: production-selected-path-tested`.</precondition>
  <files>Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/RedactionTests.swift</files>
  <behavior>
    - Test 1: the selected adapter maps a synthetic exact success to authenticated and entitled, stores only the allowed reusable credential after both predicates pass, and keeps session material in actor memory.
    - Test 2: rejection, absent entitlement, challenge/CAPTCHA/control, 403, 429/rate limit, unexpected redirect, suspected bot signal, unknown, and ambiguity each terminate after one synthetic transport event with no retry/fallback.
    - Test 3: sign-out clears actor/session memory before Keychain deletion; deletion failure remains explicit and leaves no active session.
    - Test 4: diagnostics, failures, fixtures, and reflected public values exclude credential/token/cookie/account/callback/request/response/URL canaries.
    - Test 5: the unselected live adapter is absent and the unavailable default cannot initiate transport; catalog, metadata, and stream-resolution remain typed unavailable with zero provider work.
  </behavior>
  <action>Revalidate the full Phase 0 artifact derivation and exact GO+unlocked path, then require Plan 01-06 and Plan 01-07 safe fields to match it before changing tests. Expand only synthetic production acceptance for the selected adapter, scripted transport, fake clock, fake credential store, and redacted diagnostic sink. Exercise semantic success, entitlement, all terminal stop classes, one-attempt/no-retry/no-fallback, post-entitlement credential persistence, memory-first sign-out, deletion failure, secret canaries, absence of the unselected adapter, safe unavailable default, and typed-unavailable Phase 2 content capabilities. Do not open a browser, contact SiriusXM, use an account/credential, review provider evidence, or recreate either proof run.</action>
  <verify>
<automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; handoff=.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md; implementation=.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file" &amp;&amp; phase0_path=$(sed -n 's/^Feasibility decision: GO //p' "$decision_file") &amp;&amp; handoff_path=$(sed -n 's/^Selected authentication result: //p' "$handoff") &amp;&amp; implemented_path=$(sed -n 's/^Implemented authentication result: //p' "$implementation") &amp;&amp; readiness=$(sed -n 's/^Continuation readiness: //p' "$implementation") &amp;&amp; [ "$phase0_path" = "$handoff_path" ] &amp;&amp; [ "$phase0_path" = "$implemented_path" ] &amp;&amp; [ "$readiness" = production-selected-path-tested ] &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SelectedAuthenticationPathTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter RedactionTests</automated>
  </verify>
  <done>The selected production client passes complete synthetic authentication, entitlement, stop, secret, and sign-out acceptance with zero live provider activity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-08-02: Accept the native composition and record Phase 2 readiness</name>
  <files>SiriusMacTests/SelectedAuthenticationCompositionTests.swift, .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md</files>
  <behavior>
    - Test 1: the native app composes exactly the same Phase 0-selected path as the library and exposes no method selector or unselected surface.
    - Test 2: a synthetic terminal outcome renders the dedicated unsupported state and cannot schedule or expose a follow-up attempt.
    - Test 3: a synthetic authenticated-and-entitled result exposes sign-out, and signed-out UI appears only after memory-first cleanup and successful Keychain deletion.
    - Test 4: app tests use synthetic collaborators only and record zero real browser, network, account, or provider interactions.
  </behavior>
  <action>Extend the existing selected-composition test with synthetic end-to-end app assertions for the exact Phase 0 path, terminal unsupported UI, no alternate method/follow-up, post-entitlement sign-out, cleanup-failure visibility, and zero live collaborators. After package and native tests pass, write `01-08-SUMMARY.md` with exactly one matching `Phase 0 path consumed: browser-return|native-direct`, `Production authentication acceptance: passed`, `Live proof repeated: no`, and `Phase 2 readiness: ready-for-authorized-live-listening`. Record no owner/run/provider/account detail. Do not perform a live smoke test; Phase 0 is the sole live feasibility proof.</action>
  <verify>
    <automated>summary=.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests test &amp;&amp; [ "$(rg -c '^Phase 0 path consumed:' "$summary")" -eq 1 ] &amp;&amp; [ "$(rg -c '^Production authentication acceptance:' "$summary")" -eq 1 ] &amp;&amp; [ "$(rg -c '^Live proof repeated:' "$summary")" -eq 1 ] &amp;&amp; [ "$(rg -c '^Phase 2 readiness:' "$summary")" -eq 1 ] &amp;&amp; consumed=$(sed -n 's/^Phase 0 path consumed: //p' "$summary") &amp;&amp; phase0=$(sed -n 's/^Feasibility decision: GO //p' "$decision_file") &amp;&amp; [ "$consumed" = "$phase0" ] &amp;&amp; rg -q '^Production authentication acceptance: passed$' "$summary" &amp;&amp; rg -q '^Live proof repeated: no$' "$summary" &amp;&amp; rg -q '^Phase 2 readiness: ready-for-authorized-live-listening$' "$summary"</automated>
  </verify>
  <done>Phase 1 closes with matching package/app synthetic acceptance and a Phase 2 readiness result that consumes, rather than repeats, Phase 0 proof.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Enforcement

## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 0/01-06/01-07 safe artifacts → synthetic acceptance | Paths must match exactly; no live evidence crosses into tests. |
| Synthetic app/client → fake transport/store/diagnostics | Only scripted non-secret fixtures exercise production semantics. |
| Acceptance summary → Phase 2 | Readiness records test outcome/path only and cannot replace the Phase 0 GO gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure | Acceptance fixtures/logs | high | mitigate | Synthetic canaries and allow-listed diagnostics; no live/provider/account values. |
| T-01-02 | Elevation of Privilege | Terminal outcome handling | critical | mitigate | Every control/unknown state stops after one scripted event with no fallback/follow-up. |
| T-01-03 | Information Disclosure | Credential/session lifecycle | high | mitigate | Post-entitlement allowed credential only, ephemeral session, memory-first cleanup. |
| T-01-04 | Denial of Service | Retry/concurrency | high | mitigate | One-attempt fake transport assertions; no timer, poll, retry, or alternate adapter. |
| T-01-05 | Spoofing / Tampering | Phase/path provenance | critical | mitigate | Fresh Phase 0 derivation and exact path equality across handoff, implementation, and summary. |
| T-01-06 | Information Disclosure | Native test output | high | mitigate | Synthetic collaborators only; no browser/account/provider operation. |
| T-01-SC | Tampering | Dependencies | high | mitigate | Existing system/package test stack only; no install task. |
</threat_model>

<verification>
- Full Phase 0 chain revalidates and matches Phase 1 handoff/implementation.
- Package selected-path, sign-out, and redaction suites pass with synthetic collaborators.
- Native selected-composition test passes and summary records no repeated live proof.
</verification>

<success_criteria>
- AUTH/SECR/CLNT production behavior is synthetically accepted without live SiriusXM activity.
- The sole Phase 0 GO path matches app and library composition.
- Phase 2 readiness consumes the existing proof and never repeats it.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md` with the exact four safe fields when done.
</output>
