---
phase: 01-safe-interoperability-foundation
plan: "08"
type: execute
wave: 6
depends_on: [01-07]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-01, SECR-02, SECR-03]
user_setup:
  - service: siriusxm
    why: "Only the account owner can manually initiate the two authorized-and-entitled proof runs with their own subscriber account and real browser or native selected path; no credential or authenticated state is shared with the executor."
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "When Plan 01-07 records blocked-unsupported, this plan records Phase continuation: blocked and performs no sign-in, browser, account, network, retry, or cooldown action (D-03)."
    - "When Plan 01-07 records supported-selected-path, exactly two separate account-owner-initiated runs use that one selected path; each signs in, confirms both authenticated and entitled, and signs out cleanly (D-03)."
    - "The account owner alone operates the account, browser, native credential surface, and live initiation; the executor never clicks, types, receives secrets, inspects browser/account state, initiates an attempt, polls, retries, waits on a timer, or automates the run."
    - "Run 2 begins only after Run 1 passes and the account owner independently chooses and completes a conservative cooldown; no agent or application scheduler controls the interval."
    - "Any CAPTCHA, challenge/interstitial, rejection, absent entitlement, sign-out cleanup failure, 403, 429, rate-limit signal, unexpected redirect, suspected bot response, or ambiguity records unsupported, ends the gate immediately, and blocks Phases 2–5."
    - "Phases 2–5 unlock only when both safe run dispositions are authenticated-and-entitled with clean sign-out; every other outcome is blocked."
  artifacts:
    - path: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      provides: "Safe two-run or unsupported continuation decision with no account/session data"
      contains: "Phase continuation"
  key_links:
    - from: ".planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      via: "Only supported-selected-path permits manual runs; blocked-unsupported maps directly to blocked continuation without authentication"
      pattern: "Continuation readiness"
    - from: "Task 01-08-02 safe owner disposition"
      to: "Task 01-08-03"
      via: "Only pass-authenticated-entitled-signed-out permits the separately initiated second run after owner-controlled cooldown"
      pattern: "pass-authenticated-entitled-signed-out"
    - from: ".planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md"
      to: ".planning/ROADMAP.md"
      via: "Later phase execution is permitted only by the exact Phase continuation: unlocked line"
      pattern: "Phase continuation: (unlocked|blocked)"
  prohibitions:
    - statement: "MUST NOT let the executor operate the SiriusXM account, real browser, native credential fields, live sign-in action, or sign-out action; these belong to the account owner alone (D-03)."
      status: unverified
      flagged: true
    - statement: "MUST NOT automate, schedule, poll, retry, parallelize, rapidly repeat, inspect, screenshot, or receive secrets from either proof run (D-03)."
      status: unverified
      flagged: true
    - statement: "MUST NOT continue to Run 2 or Phases 2–5 after any stop signal, cleanup failure, missing entitlement, or ambiguity (D-03)."
      status: unverified
      flagged: true
---

<objective>
Apply the hard Phase 1 continuation gate: record blocked without live authentication when unsupported, or accept exactly two separate account-owner-initiated authenticated-and-entitled runs with clean sign-out and a human-controlled conservative cooldown.

Purpose: Prove the one selected path is safely viable twice without turning a subscriber account into an automated test target or disclosing authenticated state.
Output: A no-secret 01-08 SUMMARY with two safe run dispositions, cooldown disposition, and exact blocked/unlocked continuation result.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-08-01: Enforce the supported-path precondition or record blocked without auth</name>
  <precondition>The executor can create a readable .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md; Plan 01-07 summary state is input to this fail-closed branch, not a prerequisite that may bypass it.</precondition>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - .planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
  </read_first>
  <files>None; the branch result is recorded only in the executor output 01-08-SUMMARY.md.</files>
  <action>Read only the exact safe result/readiness lines from Plan 01-07. If the result is unsupported or readiness is blocked-unsupported, record Selected path: unsupported, Run 1: not-attempted, Cooldown observed: not-applicable, Run 2: not-attempted, and Phase continuation: blocked in the Plan 01-08 summary, then end this plan successfully without presenting either live checkpoint or touching SiriusXM, a browser, credentials, account data, or network authentication. If and only if the result is browser-return or native-direct and readiness is supported-selected-path, run the complete local automated suite, record the selected path label only, and proceed to Task 01-08-02. Any missing or unreadable Plan 01-07 summary and any mismatched, duplicate, malformed, missing, or ambiguous safe field records that same five-field blocked contract with no Phase continuation: unlocked line; do not repair it by choosing a path.</action>
  <verify>
    <automated>upstream=.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md; summary=.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md; blocked_summary_valid() { [ -r "$summary" ] &amp;&amp; awk '/^Selected path:/ { selected_total++ } $0 == "Selected path: unsupported" { selected_closed++ } /^Run 1:/ { run1_total++ } $0 == "Run 1: not-attempted" { run1_closed++ } /^Cooldown observed:/ { cooldown_total++ } $0 == "Cooldown observed: not-applicable" { cooldown_closed++ } /^Run 2:/ { run2_total++ } $0 == "Run 2: not-attempted" { run2_closed++ } /^Phase continuation:/ { continuation_total++ } $0 == "Phase continuation: blocked" { blocked_total++ } $0 == "Phase continuation: unlocked" { unlocked_total++ } END { exit !(selected_total == 1 &amp;&amp; selected_closed == 1 &amp;&amp; run1_total == 1 &amp;&amp; run1_closed == 1 &amp;&amp; cooldown_total == 1 &amp;&amp; cooldown_closed == 1 &amp;&amp; run2_total == 1 &amp;&amp; run2_closed == 1 &amp;&amp; continuation_total == 1 &amp;&amp; blocked_total == 1 &amp;&amp; unlocked_total == 0) }' "$summary"; }; implemented_total=0; readiness_total=0; implemented=; readiness=; if [ -r "$upstream" ]; then implemented_total=$(awk '/^Implemented authentication result:/ { count++ } END { print count + 0 }' "$upstream") &amp;&amp; readiness_total=$(awk '/^Continuation readiness:/ { count++ } END { print count + 0 }' "$upstream") &amp;&amp; implemented=$(sed -n 's/^Implemented authentication result: //p' "$upstream") &amp;&amp; readiness=$(sed -n 's/^Continuation readiness: //p' "$upstream"); fi &amp;&amp; swift test --package-path Packages/SiriusXMClient &amp;&amp; xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' test &amp;&amp; case "$implemented_total:$readiness_total:$implemented:$readiness" in 1:1:unsupported:blocked-unsupported) blocked_summary_valid;; 1:1:browser-return:supported-selected-path) true;; 1:1:native-direct:supported-selected-path) true;; *) blocked_summary_valid;; esac</automated>
    <manual>Confirm the executor has read only the safe Plan 01-07 result/readiness fields and has not opened or operated a browser/account surface.</manual>
  </verify>
  <acceptance_criteria>
    - Unsupported readiness terminates with blocked and zero live attempts.
    - Only one supported selected-path readiness can reach Run 1.
    - Ambiguous summary state is blocked, never inferred.
  </acceptance_criteria>
  <done>The plan either ends with a no-attempt blocked result or advances one supported path to the first account-owner checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 01-08-02: Account owner performs authorized smoke run 1 and clean sign-out</name>
  <precondition>.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md still records browser-return or native-direct with Continuation readiness: supported-selected-path, and no attempt is in flight.</precondition>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift
    - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  </read_first>
  <files>None; the executor records only the account owner's allow-listed safe disposition in 01-08-SUMMARY.md.</files>
  <action>The account owner alone initiates one live sign-in through the selected app surface, confirms the app reports both authenticated and entitled, and invokes sign-out to a confirmed clean signed-out state. The executor must not operate or observe the account, real browser, native credential fields, live buttons, browser state, or provider response. On any stop signal, rejection, missing entitlement, cleanup failure, or ambiguity, the owner stops immediately and reports only the matching safe classification; the executor records unsupported, blocks continuation, and does not present Run 2.</action>
  <instructions>
    1. The account owner verifies no attempt is already in flight and opens the built Sirius Mac app themselves.
    2. They initiate exactly one sign-in using only the selected path shown in 01-07-SUMMARY.md. For browser-return they alone operate the real browser; for native-direct they alone enter any credential into the native app.
    3. They stop immediately for CAPTCHA, any challenge/interstitial, rejection, missing entitlement, HTTP 403 or 429, rate-limit signal, unexpected redirect, suspected bot response, or anything ambiguous. They do not try again or switch methods.
    4. Only if the app explicitly reports authenticated and entitled do they invoke Sign Out and confirm the app reports clean signed-out state with no cleanup warning.
    5. They return one allow-listed safe disposition and no credential, account identifier, cookie, token, URL, response, screenshot, browser detail, or raw error.
  </instructions>
  <verification>The executor accepts only the allow-listed safe disposition, records it without account/session detail, and may rerun local SignOutTests and RedactionTests. The executor does not inspect or independently verify the authenticated browser/account session.</verification>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter RedactionTests</automated>
    <manual>The account owner confirms pass-authenticated-entitled-signed-out only when all three semantic states were explicit in this one run; every other safe disposition blocks the phase.</manual>
  </verify>
  <acceptance_criteria>
    - The account owner, not the executor, performs exactly one selected-path initiation and sign-out.
    - A pass requires explicit authenticated, entitled, and clean signed-out evidence.
    - Every stop/failure/ambiguous result blocks immediately and prevents Run 2.
    - The recorded result contains no account, browser, credential, token, URL, or raw response data.
  </acceptance_criteria>
  <done>Run 1 is either safely recorded as pass-authenticated-entitled-signed-out or the phase is blocked unsupported with no second attempt.</done>
  <resume-signal>Reply with exactly one safe code and no other session detail: pass-authenticated-entitled-signed-out, blocked-rejected, blocked-not-entitled, blocked-captcha, blocked-challenge, blocked-403, blocked-429, blocked-rate-limit, blocked-unexpected-redirect, blocked-bot-signal, blocked-cleanup, or blocked-ambiguous.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 01-08-03: Account owner controls cooldown, performs separate run 2, and closes the gate</name>
  <precondition>The app is signed out, no attempt is in flight, and the account owner has independently chosen a conservative cooldown.</precondition>
  <reversibility rating="costly">Unlocking later authorization work relies on two independent clean proof runs; relaxing this evidence gate would invalidate the project's safety and viability assumptions (D-03).</reversibility>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
    - Task 01-08-02 safe disposition from the active execution context
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
  </read_first>
  <files>None; the executor records only cooldown-observed, the second safe disposition, and the final continuation line in 01-08-SUMMARY.md.</files>
  <action>Execute this checkpoint only when Task 01-08-02 recorded pass-authenticated-entitled-signed-out; every other Run 1 result ends the plan blocked before this checkpoint is presented. The account owner alone chooses and completes a conservative cooldown before a new initiation; if provider guidance, recent response behavior, or uncertainty suggests waiting longer or not repeating, they follow that safer choice and report blocked rather than run. The executor does not select a duration, sleep, wait, monitor, poll, schedule, or notify. After the owner-controlled cooldown, the owner independently starts a new app session, performs exactly one sign-in using the same selected path, requires explicit authenticated-and-entitled state, and signs out cleanly. Any stop signal or ambiguity records unsupported and blocks Phases 2–5. Only two passes unlock continuation.</action>
  <instructions>
    1. The account owner chooses a conservative cooldown based on current provider guidance and their own risk judgment, then completes it without asking the executor or app to time, poll, schedule, or monitor.
    2. After the cooldown, they confirm the app is signed out and no attempt is in flight, then begin a new, separately initiated run using the same selected path.
    3. They stop immediately for CAPTCHA, any challenge/interstitial, rejection, missing entitlement, HTTP 403 or 429, rate-limit signal, unexpected redirect, suspected bot response, or anything ambiguous. They do not retry or switch methods.
    4. Only if the app explicitly reports authenticated and entitled do they invoke Sign Out and confirm clean signed-out state without a cleanup warning.
    5. They return cooldown-observed plus exactly one allow-listed safe disposition and no credential, account identifier, cookie, token, URL, response, screenshot, browser detail, duration, or raw error.
  </instructions>
  <verification>The executor accepts only owner-confirmed cooldown-observed and one allow-listed safe run disposition, reruns local selected-path/sign-out/redaction tests, and writes Phase continuation: unlocked only when both runs are pass-authenticated-entitled-signed-out. It otherwise writes Phase continuation: blocked and performs no further attempt.</verification>
  <verify>
    <automated>summary=.planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md; allowed_run() { case "$1" in pass-authenticated-entitled-signed-out) return 0;; not-attempted) return 0;; blocked-rejected) return 0;; blocked-not-entitled) return 0;; blocked-captcha) return 0;; blocked-challenge) return 0;; blocked-403) return 0;; blocked-429) return 0;; blocked-rate-limit) return 0;; blocked-unexpected-redirect) return 0;; blocked-bot-signal) return 0;; blocked-cleanup) return 0;; blocked-ambiguous) return 0;; *) return 1;; esac; }; [ -r "$summary" ] &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SelectedAuthenticationPathTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter RedactionTests &amp;&amp; selected_total=$(rg -c '^Selected path:' "$summary") &amp;&amp; run1_total=$(rg -c '^Run 1:' "$summary") &amp;&amp; cooldown_total=$(rg -c '^Cooldown observed:' "$summary") &amp;&amp; run2_total=$(rg -c '^Run 2:' "$summary") &amp;&amp; continuation_total=$(rg -c '^Phase continuation:' "$summary") &amp;&amp; selected=$(sed -n 's/^Selected path: //p' "$summary") &amp;&amp; run1=$(sed -n 's/^Run 1: //p' "$summary") &amp;&amp; cooldown=$(sed -n 's/^Cooldown observed: //p' "$summary") &amp;&amp; run2=$(sed -n 's/^Run 2: //p' "$summary") &amp;&amp; continuation=$(sed -n 's/^Phase continuation: //p' "$summary") &amp;&amp; [ "$selected_total" -eq 1 ] &amp;&amp; [ "$run1_total" -eq 1 ] &amp;&amp; [ "$cooldown_total" -eq 1 ] &amp;&amp; [ "$run2_total" -eq 1 ] &amp;&amp; [ "$continuation_total" -eq 1 ] &amp;&amp; case "$selected" in browser-return) true;; native-direct) true;; unsupported) true;; *) false;; esac &amp;&amp; allowed_run "$run1" &amp;&amp; allowed_run "$run2" &amp;&amp; case "$cooldown" in owner-confirmed) true;; not-applicable) true;; *) false;; esac &amp;&amp; case "$continuation" in unlocked) true;; blocked) true;; *) false;; esac &amp;&amp; { if [ "$selected" = unsupported ]; then [ "$run1" = not-attempted ] &amp;&amp; [ "$cooldown" = not-applicable ] &amp;&amp; [ "$run2" = not-attempted ] &amp;&amp; [ "$continuation" = blocked ]; elif [ "$run1" = pass-authenticated-entitled-signed-out ] &amp;&amp; [ "$cooldown" = owner-confirmed ] &amp;&amp; [ "$run2" = pass-authenticated-entitled-signed-out ]; then [ "$continuation" = unlocked ]; else [ "$continuation" = blocked ]; fi; }</automated>
    <manual>The account owner confirms the second initiation was separate, followed their conservative cooldown, used the same sole path, reached authenticated-and-entitled, and ended with clean sign-out.</manual>
  </verify>
  <acceptance_criteria>
    - Cooldown selection and waiting remain wholly human-controlled and conservative.
    - Run 2 is a new, separate initiation using the same sole path.
    - Both runs must pass authenticated, entitled, and clean sign-out before continuation unlocks.
    - Any other outcome produces blocked and no more live attempts.
  </acceptance_criteria>
  <done>The final summary records two safe run results and unlocks Phases 2–5 only for two unambiguous passes; otherwise it records blocked unsupported.</done>
  <resume-signal>After the owner-controlled cooldown and separate run, reply with cooldown-observed plus exactly one safe code: pass-authenticated-entitled-signed-out, blocked-rejected, blocked-not-entitled, blocked-captcha, blocked-challenge, blocked-403, blocked-429, blocked-rate-limit, blocked-unexpected-redirect, blocked-bot-signal, blocked-cleanup, or blocked-ambiguous. Include no other session detail.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no source file and no code symbol. Its executor output is 01-08-SUMMARY.md with only these safe fields: Selected path: browser-return, native-direct, or unsupported; Run 1: pass-authenticated-entitled-signed-out, not-attempted, or one allow-listed blocked class; Cooldown observed: owner-confirmed or not-applicable; Run 2: pass-authenticated-entitled-signed-out, not-attempted, or one allow-listed blocked class; and Phase continuation: unlocked or blocked. It contains no account identifier, credential, cookie, token, URL, browser state, raw response, screenshot, exact timing, or provider payload.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to the account-owner/app, browser-return, session, Keychain, evidence, and phase-routing boundaries. Every critical or high-severity threat is blocking: any unverified mitigation, stop signal, or ambiguity ends live proof and records Phase continuation: blocked.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Account owner → app/real browser | The owner alone supplies secrets and operates live authentication; the executor receives no authenticated state. |
| App semantic state → safe owner disposition | Only authenticated/entitled/signed-out or an allow-listed blocked class may cross into execution evidence. |
| Run 1 → cooldown → Run 2 | Separate human initiation and conservative human-controlled waiting prevent automation or rapid repetition. |
| Safe dispositions → phase routing | Later phases unlock only for two exact passes; ambiguity is blocked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Account/browser operation | high | mitigate | Account owner alone operates live surfaces; executor receives one safe code and never browser/account/session data. |
| T-01-02 | Elevation of Privilege / Tampering | Stop-condition handling | high | mitigate | Any challenge/control signal, missing entitlement, cleanup failure, or ambiguity blocks immediately with no workaround or next run. |
| T-01-03 | Information Disclosure | Summary evidence | high | mitigate | Closed safe fields only; no identifiers, credentials, tokens, URLs, screenshots, timing, or raw provider data. |
| T-01-04 | Denial of Service / Repudiation | Two-run procedure | high | mitigate | One human initiation at a time, no executor/app automation, no retry/polling, and owner-controlled conservative cooldown. |
| T-01-05 | Spoofing / Tampering | Continuation decision | high | mitigate | Exact two-pass predicate; unsupported readiness and every non-pass/ambiguous result map to blocked. |
| T-01-06 | Information Disclosure | Verification output | high | mitigate | Automated checks use synthetic tests only and live proof contributes allow-listed semantic codes only. |
| T-01-SC | Tampering | Tools/dependencies | high | mitigate | No install or browser-automation tool is authorized; any new dependency or automated live harness blocks the plan. |
</threat_model>

<verification>
- Unsupported readiness records two not-attempted runs and blocked without presenting live checkpoints.
- Supported readiness receives exactly two separate owner-initiated dispositions only when Run 1 passes.
- Automated selected-path, sign-out, and redaction suites remain green and never touch a live account.
- 01-08-SUMMARY.md contains exactly one Phase continuation line and only the allow-listed safe fields.
- No high-severity ASVS L1 threat remains unmitigated.
</verification>

<success_criteria>
- No live account/browser action is performed by the executor.
- Unsupported or any stop/failure/ambiguity blocks Phases 2–5 immediately.
- Only two separate authenticated-and-entitled runs with clean sign-out and human-controlled cooldown unlock continuation.
- The final artifact contains no secret or authenticated provider state.
</success_criteria>

<output>
Create .planning/phases/01-safe-interoperability-foundation/01-08-SUMMARY.md with the allow-listed safe fields and exact Phase continuation line when done.
</output>
