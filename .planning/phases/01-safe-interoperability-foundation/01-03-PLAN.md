---
phase: 01-safe-interoperability-foundation
plan: "03"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/SessionTransport.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/DiagnosticRedactor.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift
  - Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift
autonomous: true
requirements: [AUTH-02, SECR-02, SECR-03, CLNT-03, CLNT-04]
estimate:
  tokens: 38000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Native authenticated requests use a client-owned ephemeral URLSession with no shared cookie or credential stores."
    - "Authorization material is attached only to the exact settled SiriusXM request contract and exact HTTPS hosts."
    - "Every initial request and redirect is validated; redirect drift stops before a follow-up request."
    - "Diagnostics accept closed semantic events only; tokens, cookies, URLs, headers, bodies, and arbitrary errors have no sink entry point."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift"
      provides: "Exact native authentication and entitlement request contract"
    - path: "Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift"
      provides: "Secret-canary exclusion proof"
  prohibitions:
    - statement: "MUST NOT infer new provider behavior, run live probes, broaden hosts, log requests, or use shared/default URLSession state."
      status: unverified
      flagged: true
---

<objective>
Implement the exact native request transport and redaction boundary required by the settled authentication architecture.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-PATTERNS.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/SemanticProofClient.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-03-01: Build exact ephemeral native authenticated transport</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/SessionTransport.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift</files>
  <behavior>
    - Test 1: live configuration is `.ephemeral`, client-owned, and has no shared cookie/credential storage.
    - Test 2: only exact HTTPS hosts and request shapes in `SiriusXMRequestContract` accept authorization material.
    - Test 3: HTTP, userinfo, suffix lookalikes, unknown paths, and unapproved hosts fail before loading.
    - Test 4: every redirect is revalidated and any drift cancels with zero follow-up requests.
    - Test 5: cancellation performs no retry and releases credential-bearing request state.
  </behavior>
  <action>Port the settled native authentication and entitlement request contract from the Phase 0 implementation references into one internal typed contract. This is productionization, not feasibility: do not contact SiriusXM or change the contract based on experiments. Implement injected `SessionTransport`, exact `DirectHostPolicy`, and a private ephemeral URLSession delegate. Apply authorization only after contract validation and do not persist/cache the credential.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter EphemeralSessionTests</automated></verify>
  <acceptance_criteria>
    - Token-bearing requests can reach only exact approved native endpoints.
    - Redirects cannot forward authorization outside the contract.
    - Tests are fully synthetic and deterministic.
  </acceptance_criteria>
  <done>Native authenticated requests are ephemeral, exact-host/path constrained, and redirect safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03-02: Make diagnostics and fixtures safe by construction</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/DiagnosticRedactor.swift, Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift</files>
  <behavior>
    - Test 1: event APIs accept only safe operation/outcome enums and a non-identifying handle.
    - Test 2: credential, token, cookie, authorization, URL, account, request, response, and raw-error canaries never appear in events, logs, fixture output, or failures.
    - Test 3: recursive structural redaction rejects sensitive keys before fixture promotion.
  </behavior>
  <action>Create closed `SafeDiagnosticEvent`, privacy-qualified OSLog sink, and structural `DiagnosticRedactor`. Keep transport values and arbitrary `Error` descriptions outside the sink type system. Use invented canaries only; create no live fixture, response snapshot, raw export, `print`, or `debugPrint` path.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter RedactionTests</automated></verify>
  <acceptance_criteria>
    - Secret-bearing values cannot enter diagnostics by API construction.
    - All representations tested exclude canaries.
    - No export feature or live fixture is added.
  </acceptance_criteria>
  <done>Diagnostics structurally exclude upstream and secret material.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md` when done.</output>
