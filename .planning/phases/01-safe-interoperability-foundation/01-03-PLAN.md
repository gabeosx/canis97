---
phase: 01-safe-interoperability-foundation
plan: "03"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/SessionTransport.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/RedactedDiagnosticsSink.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/DiagnosticRedactor.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift
  - Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift
autonomous: true
requirements: [AUTH-02, SECR-02, SECR-03, CLNT-03, CLNT-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The production transport owns an ephemeral URLSession, has no shared cookie/credential store, accepts only HTTPS requests to evidence-approved exact SiriusXM hosts, and begins with an empty host allow-list."
    - "Redirects outside the evidence-approved contract are rejected before a follow-up request; transport errors never include raw request/response material in public or diagnostic output."
    - "Diagnostics accept only allow-listed semantic enums and non-identifying run handles; credentials, tokens, cookies, authorization values, token-bearing URLs, raw bodies, and arbitrary Error descriptions have no entry point."
    - "Synthetic canaries cannot survive the structural fixture redactor or appear in diagnostic descriptions/test failures."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift"
      provides: "Client-owned ephemeral direct-request implementation"
      contains: "URLSessionConfiguration.ephemeral"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift"
      provides: "Exact HTTPS host and redirect policy with an empty pre-evidence default"
      contains: "DirectHostPolicy"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift"
      provides: "Allow-listed diagnostic schema"
      contains: "SafeDiagnosticEvent"
    - path: "Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift"
      provides: "Synthetic canary exclusion tests"
      contains: "RedactionTests"
  key_links:
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift"
      via: "Every request and redirect is authorized before URLSession follows it"
      pattern: "DirectHostPolicy"
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/RedactedDiagnosticsSink.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift"
      via: "Sink accepts only the allow-listed event value"
      pattern: "emit.*SafeDiagnosticEvent"
  prohibitions:
    - statement: "MUST NOT turn redacted diagnostics into a raw log archive or sensitive support export; Phase 1 exposes only allow-listed local classifications."
      status: unverified
      flagged: true
---

<objective>
Build the library trust-boundary infrastructure: a fail-closed ephemeral direct-host transport and diagnostics that are safe by type construction and synthetic redaction tests.

Purpose: Ensure a later evidence-selected adapter cannot accidentally persist session state, contact an unapproved host, follow drifted redirects, or disclose sensitive material.
Output: Transport seam/live implementation, host policy, safe diagnostic schema/sink/redactor, and security tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md

<interfaces>
`SessionTransport` and all request/response carriers are internal. `DirectHostPolicy` receives exact evidence-approved host names only from the selected adapter composition in Plan 01-07. `RedactedDiagnosticsSink` is the public semantic seam from Plan 01-01; its live implementation accepts only `SafeDiagnosticEvent`, never arbitrary strings or Error values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-03-01: Enforce ephemeral direct-host transport before any live adapter exists</name>
  <read_first>
    - Packages/SiriusXMClient/Package.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/SessionTransport.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift</files>
  <behavior>
    - Test 1: live transport uses a client-owned ephemeral configuration with no shared cookie or credential storage.
    - Test 2: the default empty host policy rejects every request before URL loading.
    - Test 3: only exact HTTPS hosts added from evidence pass; scheme changes, userinfo, subdomain lookalikes, and unapproved redirects fail closed.
    - Test 4: redirect drift causes cancellation of the redirect and no follow-up request.
  </behavior>
  <action>Define internal `SessionTransport`, `TransportRequest`, and `TransportResponse` values plus `DirectHostPolicy` and `EphemeralURLSessionTransport`. Configure a privately owned `URLSessionConfiguration.ephemeral`, explicitly remove cookie and URL credential storage, avoid shared session state, and validate the initial request and each redirect before sending/following it. The approved-host set must be empty in the default/unavailable composition; Plan 01-07 may populate exact hosts only from the recorded selection evidence. Do not add proxying, browser emulation, user-agent spoofing, speculative hosts, retry scheduling, or response logging.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter EphemeralSessionTests</automated>
    <manual>Review the live initializer and delegate path: no shared/default URLSession, cookie store, credential store, permissive suffix host check, or automatic redirect follow remains.</manual>
  </verify>
  <acceptance_criteria>
    - Empty policy makes the pre-selection live transport unreachable.
    - Exact approved HTTPS hosts are the only network destinations.
    - Redirect drift cannot create a second request.
  </acceptance_criteria>
  <done>The internal production transport is ephemeral, direct-host constrained, redirect-safe, and disabled until evidence supplies an exact allow-list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-03-02: Make diagnostics and fixtures safe by construction</name>
  <read_first>
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/SafeDiagnosticEvent.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/RedactedDiagnosticsSink.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Diagnostics/DiagnosticRedactor.swift, Packages/SiriusXMClient/Tests/FixtureTests/RedactionTests.swift</files>
  <behavior>
    - Test 1: diagnostic events can contain only capability, safe outcome/stop class, and a non-identifying opaque run handle.
    - Test 2: credential, authorization, cookie, session/token, tokenized-URL, and raw-response canaries never appear in event/log/fixture descriptions.
    - Test 3: arbitrary Error and URL/request/response values cannot be passed to the sink API.
    - Test 4: structural fixture redaction rejects or removes sensitive keys recursively while preserving safe synthetic classification fields.
  </behavior>
  <action>Create `SafeDiagnosticEvent` from closed enums, `OSLogRedactedDiagnosticsSink` using static messages with deliberately private dynamic safe values, and `DiagnosticRedactor` for synthetic fixture promotion. Keep raw network values outside every sink initializer/method signature. Tests must use invented canary credentials/tokens/URLs only and assert across event descriptions, redacted fixtures, and failure renderings. Do not add support-bundle export, raw-log export, live response fixtures, `print`/`debugPrint`, or arbitrary localized error text.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter RedactionTests</automated>
    <manual>Inspect the diagnostic API surface: callers can select only safe enums and opaque non-identifying handles, never supply free-form upstream content.</manual>
  </verify>
  <acceptance_criteria>
    - Canary values are absent from every diagnostic/fixture/failure representation under test.
    - Live logging accepts only closed safe event values and privacy-qualifies dynamic fields.
    - No sensitive export feature or live fixture is created.
  </acceptance_criteria>
  <done>Diagnostic and fixture paths structurally exclude secret-bearing material before any logging, testing, or presentation consumer can receive it.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `SessionTransport.swift`, `DirectHostPolicy.swift`, `EphemeralURLSessionTransport.swift`, `SafeDiagnosticEvent.swift`, `RedactedDiagnosticsSink.swift`, `DiagnosticRedactor.swift`, `EphemeralSessionTests.swift`, and `RedactionTests.swift`. Internal symbols: `SessionTransport`, `TransportRequest`, `TransportResponse`, `DirectHostPolicy`, `EphemeralURLSessionTransport`, `SafeDiagnosticEvent`, `OSLogRedactedDiagnosticsSink`, and `DiagnosticRedactor`. It implements the public `RedactedDiagnosticsSink` protocol without adding public wire values.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to its primary trust boundaries. Every critical or high-severity threat below is blocking: if its mitigation cannot be implemented or verified, stop the plan without weakening the control.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal adapter → network | A request may leave the Mac only after exact destination and redirect policy approval. |
| Internal values → diagnostics/fixtures | Potentially sensitive runtime/test data must be reduced to an allow-listed semantic event before observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Session transport | high | mitigate | No browser state API exists; only client-owned ephemeral direct requests are possible after evidence approval. |
| T-01-03 | Information Disclosure | URLSession / fixtures | high | mitigate | Ephemeral session, absent shared cookie/credential stores, structural canary redaction, and no live fixtures. |
| T-01-05 | Spoofing / Tampering | DirectHostPolicy / redirect delegate | high | mitigate | Exact HTTPS allow-list and per-redirect validation; empty list before selection. |
| T-01-06 | Information Disclosure / Repudiation | Diagnostics and future support evidence | high | mitigate | Closed semantic event schema, private OSLog fields, no raw export, and canary tests across representations. |
| T-01-SC | Tampering | Package dependency graph | high | mitigate | This plan uses Foundation/OSLog only and authorizes no package install without a legitimacy audit. |
</threat_model>

<verification>
- `swift test --package-path Packages/SiriusXMClient --filter EphemeralSessionTests` passes.
- `swift test --package-path Packages/SiriusXMClient --filter RedactionTests` passes.
- The default composition has no approved host, no persistent URLSession stores, and no raw diagnostic/export entry point.
</verification>

<success_criteria>
- Only direct, evidence-approved HTTPS destinations can ever receive session material.
- Redirect drift and unapproved destinations stop before a follow-up request.
- Sensitive values cannot enter diagnostics or synthetic fixture artifacts by API construction.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md` when done.
</output>
