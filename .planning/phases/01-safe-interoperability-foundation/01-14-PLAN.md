---
phase: 01-safe-interoperability-foundation
plan: "14"
type: execute
wave: 11
depends_on: [01-12]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift
autonomous: true
gap_closure: true
requirements: [AUTH-02, SECR-02, CLNT-03, CLNT-04]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The actual URLSession redirect delegate callback records every redirect attempt before resolving it."
    - "The redirect callback always completes with nil, so authorization material is never forwarded to a follow-up request."
    - "A deterministic test invokes the production delegate method, observes the count change from zero to one, captures a nil completion request, and confirms active request state is not retained."
    - "Redirect instrumentation remains internal and records only a count; it exposes no destination, request, header, response, body, or credential value."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift"
      provides: "Lock-protected redirect-attempt instrumentation in the production URLSessionTaskDelegate callback"
      contains: "redirectAttemptCount"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift"
      provides: "Direct production-delegate callback regression proving nil follow-up behavior"
  key_links:
    - from: "EphemeralURLSessionTransport.urlSession(_:task:willPerformHTTPRedirection:newRequest:completionHandler:)"
      to: "RequestState"
      via: "record the redirect attempt under the existing lock before invoking completionHandler(nil)"
      pattern: "recordRedirectAttempt"
    - from: "EphemeralSessionTests"
      to: "URLSessionTaskDelegate callback"
      via: "invoke the concrete delegate method with synthetic URLSessionTask/HTTPURLResponse inputs and capture its completion argument"
      pattern: "willPerformHTTPRedirection"
  prohibitions:
    - statement: "MUST NOT follow, replay, or authorize any redirect, or retain/log its destination, request headers, response body, or credential material."
      status: resolved
      verification: judgment
      resolution: "Production callback records a scalar attempt count then completes with nil; the focused test asserts both facts."
---

<objective>
Instrument and prove the real ephemeral-transport redirect callback so the test demonstrates cancellation rather than reading a permanently zero counter.

Purpose: Close IN-01 and retain the fail-closed direct-request boundary for authorization material.
Output: Lock-protected redirect-attempt telemetry and a deterministic direct-delegate regression.

**As a** SiriusXM subscriber, **I want to** establish or end an authorized session, **so that** I can listen safely on my Mac.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-REVIEW.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift

<interfaces>
- `EphemeralURLSessionTransport` is the concrete `URLSessionTaskDelegate` used by its client-owned ephemeral session.
- `urlSession(_:task:willPerformHTTPRedirection:newRequest:completionHandler:)` currently calls `completionHandler(nil)` but never mutates the existing `RequestState.redirects` counter.
- `RequestState` is a private lock-protected reference object already used for test-safe request-state observations.
- `redirectDecision(for:) -> .cancel` is a policy helper; it is not evidence that the URLSession delegate callback ran.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `EphemeralURLSessionTransport.swift` — replaces the misleading follow-up counter with an actual redirect-delegate invocation count and records it inside the real callback.
- `EphemeralSessionTests.swift` — invokes the production callback directly with synthetic Foundation objects and observes both callback count and nil completion.

### New symbols

- Internal `EphemeralURLSessionTransport.redirectAttemptCount` test observation.
- Private `RequestState.recordRedirectAttempt()` lock-protected mutator.

### New files or public API

- None. No public client type or transport contract changes.

## Spec-less edge resolution

- SECR-02 concurrency/effect ordering is explicit: callback entry → count increment under lock → optional contract check → `completionHandler(nil)`; no follow-up state exists.
- CLNT-03 keeps redirect mechanics internal; CLNT-04 uses a deterministic direct callback rather than a network redirect or timing assumption.
- AUTH-02 remains fail closed for every redirect regardless of destination validity.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 01-14-01: Observe one real redirect callback and prove nil follow-up</name>
  <reversibility rating="reversible">The change replaces a private test observation and private counter without altering public API or request behavior.</reversibility>
  <read_first>
    - `EphemeralURLSessionTransport.swift` — trace session construction, `redirectDecision(for:)`, the concrete delegate callback, and the unused `RequestState.redirects` field.
    - `EphemeralSessionTests.swift` — replace the policy-only zero-counter assertion with a direct delegate invocation and completion capture.
    - `SiriusXMRequestContract.swift` and `DirectHostPolicy.swift` — preserve exact host/path authorization and do not add a redirect exception.
    - `01-REVIEW.md` IN-01 — close the evidence defect exactly; production redirect behavior already cancels and must remain unchanged.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/EphemeralURLSessionTransport.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/EphemeralSessionTests.swift</files>
  <behavior>
    - Test 1: a fresh transport exposes zero redirect attempts.
    - Test 2: directly invoking the production URLSession redirect delegate method increments attempts to one before its completion is observed.
    - Test 3: completion receives nil for both an otherwise contract-shaped redirect and an unapproved destination; no follow-up URLRequest is returned.
    - Test 4: redirect handling does not mark a credential-bearing request active, retain request state, retry, or expose redirect/request values.
  </behavior>
  <action>Start RED by replacing the existing `redirectDriftCancelsBeforeFollowUp` assertion with a test that constructs a synthetic `HTTPURLResponse`, a harmless `URLSessionTask`, and a destination request, then invokes `EphemeralURLSessionTransport.urlSession(_:task:willPerformHTTPRedirection:newRequest:completionHandler:)` directly. Capture the optional request passed to completion and assert the production callback changes the observed attempt count from zero to one before returning nil. Repeat or parameterize for a contract-shaped and an unapproved destination so cancellation is unconditional.

Rename the private observation to `redirectAttemptCount`; remove the misleading `followUpRequestCount` meaning. Add `RequestState.recordRedirectAttempt()` and call it synchronously under the existing lock at the start of the actual delegate callback, before `completionHandler(nil)`. Retain the contract-policy evaluation only as a fail-closed inspection with no branch that follows. The only stored redirect data is the integer count; do not store or render URLs, requests, responses, headers, bodies, or credentials. Keep `completionHandler(nil)` unconditional and add no URLProtocol, live server, retry, or redirect replay.</action>
  <acceptance_criteria>
    - The focused test fails against the old implementation because the production delegate count stays zero, then passes when the callback records one invocation.
    - The captured completion request is nil for every tested destination and active request state remains false after the callback.
    - No redirect destination or authorization-bearing request is persisted or exposed by instrumentation.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter EphemeralSessionTests</automated>
  </verify>
  <done>The package deterministically proves that the real URLSession redirect delegate ran, recorded one attempt, and authorized no follow-up request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URLSession redirect response → transport delegate | An untrusted server-directed location reaches the concrete delegate while a request may contain authorization material. |
| Transport delegate → test observation | Only a scalar callback count may cross; redirect/request contents remain unavailable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14-01 | Information Disclosure / Elevation of Privilege | URLSession redirect callback | high | mitigate | Increment a scalar count then always complete with nil; test approved and unapproved destinations with no follow-up request. |
| T-01-14-02 | Tampering | redirect instrumentation | medium | mitigate | Mutate the count under the existing lock inside the real delegate method and assert the transition directly. |
| T-01-14-03 | Repudiation | redirect test evidence | medium | mitigate | Remove the permanently-zero proxy and prove actual callback entry plus nil completion in one focused test. |
| T-01-14-04 | Denial of Service | redirect loops | high | mitigate | Follow zero redirects and schedule no retry, so a redirect chain terminates at the first callback. |
</threat_model>

<verification>
- Focused ephemeral-transport tests pass without network access.
- Direct delegate invocation observes one attempt and nil completion.
- Full package verification in Plan 01-13 remains compatible with the renamed internal observation.
</verification>

<success_criteria>
- IN-01 is closed with behavior evidence from the production callback rather than a policy helper.
- AUTH-02 and SECR-02 remain fail closed: authorization is never forwarded across redirects.
- CLNT-03 and CLNT-04 remain intact: the instrumentation is internal, secret-free, and deterministic.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-14-SUMMARY.md` when done.
</output>
