---
phase: 01-safe-interoperability-foundation
plan: "10"
type: execute
wave: 8
depends_on: [01-09]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/AuthenticationView.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
autonomous: true
gap_closure: true
requirements: [AUTH-03, SECR-01, SECR-02, CLNT-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A fresh app composition exposes an explicit local-session cleanup action even when no in-memory session is active."
    - "Fresh-state cleanup retires actor material first, then attempts both app Keychain deletion and exact WebView residue cleanup before reporting completion."
    - "Concurrent cleanup requests share one cleanup operation; every later explicit cleanup remains safe and idempotent, while no failure is retried automatically."
    - "Cleanup never reads a Keychain credential into session state, never authenticates, and never restores a session after failure."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      provides: "Fresh-state, coalesced, memory-first local cleanup lifecycle"
      contains: "signOut"
    - path: "SiriusMac/Authentication/AuthenticationView.swift"
      provides: "Always-reachable explicit Clear Local Session action outside an active session"
    - path: "SiriusMacTests/SelectedAuthenticationCompositionTests.swift"
      provides: "Fresh-composition persisted-Keychain and WebView-residue removal regression"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift"
      provides: "Signed-out initial state, concurrency coalescing, explicit failure retry, and idempotence tests"
  key_links:
    - from: "SiriusMac/Authentication/AuthenticationView.swift"
      to: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      via: "Clear Local Session invokes a semantic explicit cleanup intent from the fresh waiting state"
      pattern: "clearLocalSession"
    - from: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      via: "ComposedAuthenticationPresentationFlow.signOut delegates through SiriusXMClient to the coordinator"
      pattern: "signOut"
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      to: "SiriusMac/Security/KeychainCredentialStore.swift and WebAuthenticationBridge"
      via: "injected CredentialStore.erase and AuthenticationResidueCleaner.removeAuthenticationResidue are both awaited after actor state retirement"
      pattern: "credentialStore.erase|removeAuthenticationResidue"
  prohibitions:
    - "MUST NOT require an active in-memory session before explicit local cleanup can delete Keychain material and browser residue."
    - "MUST NOT report complete cleanup before both injected cleaners succeed, restore a session after cleanup failure, read stored credentials into authentication state, or retry cleanup automatically."
---

<objective>
Make safe local-session cleanup reachable after a fresh app composition, including persisted Keychain material, without restoring credentials or requiring an active in-memory session.

Purpose: Close AUTH-03's restart lifecycle gap while preserving memory-first retirement, exact browser cleanup, honest aggregate failure, and no-bypass behavior.
Output: Coalesced fresh-state cleanup semantics, a native explicit cleanup action, and package/app lifecycle regressions.

**As a** SiriusXM subscriber, **I want to** establish or end an authorized session, **so that** I can listen safely on my Mac.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-VERIFICATION.md
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-09-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
@SiriusMac/Authentication/AuthenticationPresentationModel.swift
@SiriusMac/Authentication/AuthenticationView.swift
@SiriusMac/Security/KeychainCredentialStore.swift
@SiriusMacTests/SelectedAuthenticationCompositionTests.swift

<interfaces>
- Existing `SessionCoordinator.signOut() async -> SignOutOutcome` currently returns `.alreadySignedOut` before invoking cleaners when actor state starts signed out.
- Existing `CredentialStore.erase() async throws` is the only client-facing Keychain deletion seam; production `KeychainCredentialStore.erase()` deletes the app-scoped generic-password item without exposing stored data.
- Existing `AuthenticationResidueCleaner.removeAuthenticationResidue() async -> AuthenticationResidueCleanupOutcome` is implemented by `WebAuthenticationBridge` with the exact shared token predicate and a post-delete rescan.
- Existing `AuthenticationPresentationModel.signOut()` is entitled-only; the fresh composition starts at `.waitingForWebView` and therefore needs a separate explicit cleanup intent.
- Existing `ComposedAuthenticationPresentationFlow.signOut()` delegates semantically to `SiriusXMClient.signOut()` and must remain the only app-to-client cleanup route.
</interfaces>
</context>

## Artifacts this phase produces

### Existing artifacts modified

- `SessionCoordinator.signOut()` — extend the existing memory-first pipeline so every explicit request, including the initial signed-out state, performs safe local cleanup and concurrent callers share its result.
- `AuthenticationPresentationModel` and `AuthenticationView` — expose a semantic explicit cleanup intent and reachable native control from a fresh composition without exposing credential restoration.
- `SignOutTests` and `SelectedAuthenticationCompositionTests` — extend existing deterministic suites with restart, concurrency, idempotence, and explicit retry behavior.

### New symbols

- `AuthenticationPresentationModel.clearLocalSession()` — explicit subscriber action that delegates to the existing semantic sign-out/cleanup flow while no session is active.
- Private `SessionCoordinator.cleanupTask` — actor-owned in-flight cleanup result shared by concurrent callers.

### New files

- None.

## Flagged assumptions

- **[FLAGGED — AUTH-03 / SECR-01 / SECR-02 / CLNT-04 lifecycle]** The verification gap permits either restore or always-reachable cleanup. This plan deliberately chooses cleanup: fresh composition never reads Keychain material into an authentication credential or silently restores a session; the subscriber must authenticate explicitly again.
- **[FLAGGED — SECR-02 / CLNT-04 concurrency]** No plain phase SPEC defines simultaneous public cleanup-call semantics. Coalesce callers onto one actor-owned task so cleaners run once and every caller observes the same result; verify this deterministically rather than allowing duplicate Keychain/WebKit operations.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 01-10-01: Clear persisted local auth material from a fresh app composition</name>
  <reversibility rating="reversible">The behavior extends the existing semantic cleanup seam and native presentation locally; it changes no stored format, published credential representation, or provider contract.</reversibility>
  <read_first>
    - `Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift` — preserve actor-state retirement before external effects and inspect the current signed-out early return.
    - `Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift` — extend the existing injected store/cleaner and blocking-cleaner patterns.
    - `SiriusMac/Authentication/AuthenticationPresentationModel.swift` — preserve the main-actor attempt ID and semantic `AuthenticationPresentationFlow` boundary.
    - `SiriusMac/Authentication/AuthenticationView.swift` — make cleanup reachable from the initial waiting state and terminal states without adding a second sign-in method.
    - `SiriusMac/Security/KeychainCredentialStore.swift` — use the existing `erase()`/`removeStoredCredential()` behavior; do not add a restore/read path.
    - `SiriusMacTests/SelectedAuthenticationCompositionTests.swift` — extend the synthetic composition with a randomized app-scoped Keychain item and injected browser residue.
    - `.planning/phases/01-safe-interoperability-foundation/01-VERIFICATION.md` — implement only the open fresh-composition cleanup gap; do not rewrite completed plans or the corrected roadmap goal.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift</files>
  <behavior>
    - Test 1: a newly initialized signed-out coordinator invokes `CredentialStore.erase()` and `AuthenticationResidueCleaner.removeAuthenticationResidue()` exactly once when cleanup is explicitly requested, then returns `.signedOut` only when both succeed.
    - Test 2: two overlapping cleanup calls share one operation and observe the same semantic result; they do not double-run either cleaner.
    - Test 3: after successful cleanup, another explicit cleanup safely invokes each idempotent cleaner once again and returns `.signedOut`; after cleanup failure, no session is restored and a later explicit request may retry both cleaners once.
    - Test 4: an explicit cleanup after any newly started WebView attempt still invokes both cleaners even if an earlier cleanup succeeded; terminal paths schedule no automatic cleanup retry.
    - Test 5: a fresh native composition with a randomized synthetic Keychain credential and synthetic matching browser residue exposes `clearLocalSession()`, removes both through the production seams, and ends in `.signedOut` without invoking authentication or entitlement.
    - Test 6: Keychain or browser cleanup failure presents `.cleanupFailed` safely, leaves actor state signed out, and keeps the explicit cleanup action reachable.
  </behavior>
  <action>Start RED in the package sign-out suite and the existing app composition suite. Replace the coordinator's initial signed-out early return with actor-owned cleanup lifecycle state: clear attempt/session/transient/entitlement state before launching external cleanup, and share one in-flight cleanup task across overlapping callers so both observe the same aggregate result. Each later explicit cleanup request must run a new idempotent Keychain erase plus exact browser-residue removal, even after an earlier success, because a newly operated WebView can introduce residue without first entering the client actor; never cache a completed-cleanup shortcut. Add `AuthenticationPresentationModel.clearLocalSession()` as a separate explicit semantic intent available while no action is in flight and no entitled-session sign-out button applies; it delegates to `flow.signOut()`, uses the existing attempt-ID completion guard, and maps aggregate outcomes through the existing safe states. Render `Clear Local Session` in `AuthenticationView` from the initial waiting state and non-verifying terminal/signed-out/cleanup-failed states, including the isolated unsupported surface, while retaining the ordinary entitled `Sign Out` action. In the app-lifecycle regression, save only synthetic material under a randomized Keychain identity, build a fresh bridge/client/model composition, seed only the injected synthetic cookie store, invoke the explicit cleanup intent, and assert Keychain absence, predicate-clean browser state, zero authentication/entitlement calls, and truthful presentation. Do not call `readStoredCredential()` from production, do not construct an `AuthenticationCredential` from Keychain data, and do not add automatic cleanup, authentication, fallback, or restoration.</action>
  <acceptance_criteria>
    - A fresh app composition provides a visible explicit local cleanup action without requiring entitlement or active in-memory session state.
    - The action removes the randomized Keychain item and matching injected browser residue through production cleanup seams, with zero authentication/entitlement requests.
    - Memory is retired before cleanup, concurrent callers share one result, sequential explicit requests remain idempotent by safely re-running both cleaners, and failure remains signed out plus explicitly retryable.
    - Complete cleanup is reported only after both Keychain and browser operations succeed; aggregate failures remain safe and exact.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SignOutTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests test</automated>
  </verify>
  <done>A subscriber can explicitly clear Keychain and exact WebView authentication residue after a fresh app composition, and the client reports the complete or incomplete cleanup truthfully without restoring or authorizing a session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native cleanup control → presentation/client | A subscriber can request local cleanup without an active session; the action must not become an authentication or restoration path. |
| Session actor → Keychain cleaner | Actor state is retired before app-owned persistent material is deleted, and deletion failure is classified without secret detail. |
| Session actor → WebView residue cleaner | Exact first-party cookie cleanup is awaited and combined with Keychain outcome before completion is reported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10-01 | Information Disclosure | fresh composition / Keychain lifecycle | high | mitigate | Provide explicit cleanup without reading the stored value; lifecycle regression writes synthetic material, then proves absence through the existing app-owned store. |
| T-01-10-02 | Tampering | `SessionCoordinator.signOut()` ordering | high | mitigate | Retire actor state first, await both cleaners, mark completion only on dual success, and keep failure signed out plus safely classified. |
| T-01-10-03 | Denial of Service / Repudiation | concurrent cleanup requests | medium | mitigate | Coalesce overlapping callers onto one actor-owned cleanup task, count each cleaner once per explicit operation, return one shared result, and start another operation only from a later user action. |
| T-01-10-04 | Elevation of Privilege | `clearLocalSession()` | high | mitigate | Make the action cleanup-only: no Keychain read, no credential construction, no native verification request, no session restoration, and regression assertions for zero auth/entitlement calls. |
| T-01-10-05 | Spoofing | cleanup-complete presentation | high | mitigate | Map `.signedOut` only from confirmed dual cleanup success; map exact aggregate failures to `.cleanupFailed` and keep the action reachable for explicit retry. |
</threat_model>

## Multi-Source Coverage Audit (gap-closure set)

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Establish or end an authorized session safely | 01-09, 01-10 | COVERED | 01-09 restores repeatable establishment; 01-10 makes ending/cleanup reachable after restart. |
| REQ | AUTH-01 | Explicit sign-in outcomes remain usable across attempts | 01-09 | COVERED | Closes the verifier-blocked retry/re-login lifecycle. |
| REQ | AUTH-02 | Unknown/prohibited flows fail closed | 01-01–01-08 | COVERED | Existing implementation remains unchanged; both gap plans preserve no retry/fallback/bypass. |
| REQ | AUTH-03 | Sign-out clears active and stored credentials | 01-10 | COVERED | Closes fresh-composition cleanup reachability. |
| REQ | SECR-01 | Keychain-only persistent secret storage | 01-10 | COVERED | Uses existing Keychain erase seam and deliberately adds no restore or alternate store. |
| REQ | SECR-02 | Tokens remain ephemeral/direct-only | 01-09, 01-10 | COVERED | Stale handoffs are discarded; cleanup makes no provider request. |
| REQ | SECR-03 | Diagnostics/fixtures exclude secrets | 01-01–01-08 | COVERED | Existing safe diagnostic boundary is untouched; new tests use invented synthetic values only. |
| REQ | CLNT-01 | Independent SwiftPM client | 01-01–01-08 | COVERED | No public product/package boundary change is planned. |
| REQ | CLNT-02 | Typed async semantic API | 01-01–01-08 | COVERED | Existing semantic outcomes are reused; no wire detail is added. |
| REQ | CLNT-03 | Volatile adapters remain internal | 01-01–01-08 | COVERED | Both fixes remain behind existing bridge/session adapters. |
| REQ | CLNT-04 | Injected deterministic collaborators | 01-09, 01-10 | COVERED | Existing cookie/store/cleaner/client fakes prove retry, cleanup, and concurrency. |
| RESEARCH | gap retry | Serialized, fail-closed authentication with no automatic retry | 01-09 | COVERED | Explicit new-attempt re-arm only; client verification authority unchanged. |
| RESEARCH | gap cleanup | Memory-first idempotent Keychain and residue cleanup | 01-10 | COVERED | Fresh-state cleanup, coalescing, aggregate failure, and explicit retry are tested. |
| CONTEXT | auth path | Sole user-operated nonpersistent WebView and one volatile handoff per attempt | 01-09 | COVERED | No second method or browser-state expansion. |
| CONTEXT | runtime authority | Native authentication plus entitlement remain mandatory before active state | 01-09 | COVERED | Reset changes attempt lifecycle only, never authorization. |
| CONTEXT | secret/sign-out | Clear memory, Keychain, and exact matching cookies or report failure | 01-10 | COVERED | Fresh composition reaches the existing injected cleaners and reports aggregate result. |
| CONTEXT | testing | Deterministic unconditional WebView/native-request tests | 01-09, 01-10 | COVERED | Existing XCTest/Swift Testing targets and synthetic collaborators are extended. |

Excluded as non-gaps: the roadmap user-story formatting failure was closed by commit `fd464e6`; catalog/playback/skins/release work belongs to later phases; superseded research recommendations for alternate authentication feasibility remain historical.

<verification>
- Focused package sign-out tests pass for fresh-state cleanup, concurrency coalescing, sequential idempotent cleanup, explicit failure retry, and memory-first ordering.
- Focused app composition tests pass for randomized persisted Keychain material plus synthetic matching browser residue after a fresh composition.
- The fresh cleanup path records zero authentication and entitlement calls and presents `.signedOut` only after both cleaners succeed.
</verification>

<success_criteria>
- The open verification truth "A subscriber can sign out and clear persisted credentials after a fresh app composition" is covered by package and app-lifecycle regressions.
- AUTH-03 is no longer blocked by the coordinator's signed-out early return or the UI's entitled-only cleanup reachability.
- SECR-01, SECR-02, and CLNT-04 remain intact: no stored material is restored, cleanup is deterministic and coalesced, and failures remain fail closed.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-10-SUMMARY.md` when done.
</output>
