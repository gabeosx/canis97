---
phase: 01-safe-interoperability-foundation
plan: "13"
type: execute
wave: 11
depends_on: [01-12]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SanitizedNativeResponseFixtures.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-02, SECR-03, CLNT-01, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A 2xx profile-v4 response is accepted only when its body is non-empty, valid JSON with an object root and no recognized control signal; no invented profile field is required."
    - "A subscription-v1 response is entitled only when the settled nested `subscription.status` string is `active`, is not entitled when it is `inactive`, and is unsupported for missing, non-string, or unknown values."
    - "Sanitized multi-field profile and subscription fixtures tolerate unrelated fields and complete the runtime-owned authentication then entitlement transaction through active session publication."
    - "Redirects, non-JSON content, malformed bodies, 401/403, 429, challenge/control markers, and ambiguous entitlement evidence remain terminal with no retry or alternate method."
    - "Versioned provider response decoders remain internal; public Apple-platform consumers continue to see only typed semantic outcomes and no raw schema, body, endpoint, header, or credential detail."
    - "Fixture data contains no credential, token, account identifier, PII, authorization header, URL, or captured provider response."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      provides: "Internal ProfileResponseV4Decoder and SubscriptionStatusResponseV1Decoder with settled fail-closed semantics"
      contains: "ProfileResponseV4Decoder"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SanitizedNativeResponseFixtures.swift"
      provides: "Invented, secret-free, multi-field profile-v4 and subscription-v1 contract fixtures"
      contains: "SanitizedNativeResponseFixtures"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift"
      provides: "End-to-end representative native authentication then entitlement transaction regression"
  key_links:
    - from: "AuthenticationFlowAdapter.classifyAuthentication"
      to: "ProfileResponseV4Decoder"
      via: "preflight and control-signal checks precede the settled non-empty valid-JSON profile predicate"
      pattern: "ProfileResponseV4Decoder"
    - from: "AuthenticationFlowAdapter.classifyEntitlement"
      to: "SubscriptionStatusResponseV1Decoder"
      via: "nested subscription.status is reduced to active, inactive, or unsupported"
      pattern: "SubscriptionStatusResponseV1Decoder"
    - from: "SessionCoordinator.attemptSession"
      to: "SanitizedNativeResponseFixtures"
      via: "injected native verifiers feed representative multi-field responses through both production classifiers"
      pattern: "profileV4Authenticated|subscriptionV1Active"
  prohibitions:
    - statement: "MUST NOT conduct a live account experiment, infer an additional provider field, or turn arbitrary response data into a public contract while closing the response-adapter gap."
      status: resolved
      verification: judgment
      resolution: "Use only the settled Phase 0 profile and subscription predicates, invented sanitized fixtures, internal decoder types, and synthetic transports."
    - statement: "MUST NOT change Phase 1 signing settings; signed and notarized distribution remains Phase 5 requirement REL-01."
      status: resolved
      verification: judgment
      resolution: "No task in this plan edits the Xcode project or release configuration."
---

<objective>
Replace the synthetic one-field success adapter with versioned internal response decoders grounded only in settled Phase 0 evidence, then prove representative multi-field responses can establish an active session.

Purpose: Close CR-02 and the verifier's only remaining authorization gap without inventing provider semantics or weakening fail-closed control handling.
Output: Internal profile/subscription decoders, sanitized representative fixtures, and full native transaction regressions.

**As a** SiriusXM subscriber, **I want to** establish or end an authorized session, **so that** I can listen safely on my Mac.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-VERIFICATION.md
@.planning/phases/01-safe-interoperability-foundation/01-REVIEW.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
@Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
@Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
@Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityCore/EntitlementContract.swift
@Spikes/AuthenticationFeasibility/Tests/AuthFeasibilityCoreTests/WebSessionBridgeTests.swift

<interfaces>
- `AuthenticationFlowAdapter.classifyAuthentication(_:) -> AdapterAuthenticationResult` is the sole internal profile-response classifier used by `SessionCoordinator`.
- `AuthenticationFlowAdapter.classifyEntitlement(_:) -> AdapterEntitlementResult` is the separate subscription-response classifier used only after authentication succeeds.
- `NativeTransportResponse` carries status, content type, body bytes, and optional redirect location internally; none of these fields is public.
- Phase 0 `NativeWebSessionVerifier.classify` established profile authentication from a 2xx response with a non-empty body; this plan additionally requires that the body decode as a JSON object but requires no guessed profile identifier.
- Phase 0 `EntitlementContract` established the exact field/value contract `subscription.status == active`, with `inactive` as denial and missing/non-string/other values malformed.
- `SessionCoordinator.attemptSession()` remains the only authority that sequences the two classifiers and publishes `.active` after both succeed.
</interfaces>
</context>

## Phase 1 Closure Matrix

| Finding | Disposition | Plan / evidence |
|---------|-------------|-----------------|
| CR-01 — release signing disabled | DEFER | Phase 5 owns signed/notarized release behavior through REL-01. Plans 01-13–01-16 must not change signing settings. |
| CR-02 — one-field synthetic response adapter | FIX | Plan 01-13 replaces it with internal profile-v4/subscription-v1 decoders and representative transaction tests. |
| WR-01 — Keychain credential is write-only | FIX | Plan 01-16 adds one bounded app-owned restore source that still uses the same native authentication then entitlement transaction and erases rejected material. |
| WR-02 — WebKit state remains after sign-out | FIX | Plan 01-15 retires the app-owned nonpersistent website session after exact token deletion/rescan and presents a fresh store/WebView. |
| WR-03 — insecure/overbroad token-cookie acceptance | FIX | Plan 01-15 requires Secure and restricts issuers to evidence-backed `siriusxm.com` and `www.siriusxm.com`. |
| IN-01 — redirect counter never observes the delegate | FIX | Plan 01-14 instruments the actual redirect callback and proves one callback, a nil completion request, and no follow-up. |
| Verifier gap — representative responses cannot activate | FIX | Plan 01-13 runs sanitized multi-field profile then subscription responses through the production classifiers and actor-owned transaction to `.active`. |

## Artifacts this phase produces

### Existing artifacts modified

- `AuthenticationFlowAdapter.swift` — replaces cardinality/synthetic-key success with named versioned internal decoders while retaining status, content-type, redirect, and control preflight.
- `AuthenticationOutcomeTests.swift` — exercises representative multi-field, unrelated-field, active/inactive, malformed, unknown, and control cases.
- `SessionCoordinatorTests.swift`, `SignOutTests.swift`, and `WebTokenAuthenticationTests.swift` — replace synthetic success booleans with the sanitized settled-response fixtures and prove the ordered transaction.

### New files and symbols

- `SanitizedNativeResponseFixtures.swift` — test-only fixture namespace containing invented, secret-free `profileV4Authenticated`, `subscriptionV1Active`, and `subscriptionV1Inactive` response bodies.
- Internal `ProfileResponseV4Decoder` — validates the settled profile-v4 predicate without requiring a guessed profile field.
- Internal `SubscriptionStatusResponseV1Decoder` — decodes only the settled `subscription.status` field and ignores unrelated fields.

### Public API changes

- None. `SiriusXMClient`, `AuthenticationOutcome`, and `EntitlementAvailability` remain semantic and wire-schema-free.

## Spec-less edge resolution

- AUTH-01/AUTH-02 resolve to explicit fixture-to-active and terminal-control assertions; no live provider compatibility claim is made.
- AUTH-03 is preserved by running the sign-out suite after replacing every synthetic success response.
- SECR-01 is preserved because response work reads or writes no persistent credential material.
- SECR-02 resolves the effect order as credential → profile verification → subscription verification → one active-state assignment → allowed save.
- SECR-03 resolves to a source-controlled invented fixture namespace with a canary scan and no raw captured body.
- CLNT-01/CLNT-02/CLNT-03 resolve by keeping all decoder/fixture symbols internal or test-only and running the independent public-consumer suite.
- CLNT-04 resolves through injected verifier responses and deterministic full-package tests.
- Assumption retained explicitly: future upstream drift beyond the settled profile-v4 and subscription-v1 evidence is unsupported until sanitized evidence updates the internal adapter; the implementation does not guess.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 01-13-01: Decode one representative profile then subscription success path</name>
  <reversibility rating="reversible">Both response decoders remain internal behind the existing classifier functions, so a future compatibility repair changes no public client contract.</reversibility>
  <read_first>
    - `AuthenticationFlowAdapter.swift` — replace `exactJSONObject` and the synthetic success keys while preserving preflight and semantic result mapping.
    - `AuthenticationOutcomeTests.swift` — convert success coverage to the sanitized versioned fixtures and retain terminal/control assertions.
    - `SessionCoordinatorTests.swift` — add the thinnest raw-response-to-active tracer through the unchanged actor-owned sequence.
    - Phase 0 `WebSessionBridge.swift` — use only its settled 2xx non-empty profile-body predicate; do not copy headers, user-agent behavior, or live transport logic.
    - Phase 0 `EntitlementContract.swift` and `WebSessionBridgeTests.swift` — use only `subscription.status`, `active`, `inactive`, and missing/non-string failure semantics.
    - `01-REVIEW.md` and `01-VERIFICATION.md` — close CR-02 and the sole remaining verifier gap without altering already-verified bridge/session authority.
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SanitizedNativeResponseFixtures.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift</files>
  <behavior>
    - Test 1: a 2xx, JSON content-type, non-empty valid object-root profile-v4 fixture with multiple fields maps to `.authenticatedPendingEntitlement`; no profile identifier or invented field is required.
    - Test 2: a multi-field subscription-v1 fixture maps `subscription.status: active` to `.entitled` and `inactive` to `.authenticatedButNotEntitled`, while unrelated sibling/envelope fields are ignored.
    - Test 3: missing/non-string/unknown subscription status, empty body, non-object JSON, malformed JSON, non-JSON content, redirects, 401/403, 429, and recognized challenge/bot control signals are terminal.
    - Test 4: the representative profile fixture followed by the active subscription fixture makes `SessionCoordinator.attemptSession()` return `.active`, records authentication before entitlement, and saves once only after active publication.
    - Test 5: fixture source contains invented neutral values only and no credential, bearer value, account identity, PII, token-bearing URL, header, or raw captured provider payload.
  </behavior>
  <action>Start RED by creating `SanitizedNativeResponseFixtures` with three multiline, sorted, multi-field JSON bodies: a schema-neutral profile-v4 object that proves only non-empty valid JSON, and subscription-v1 active/inactive objects that use the settled nested field plus unrelated synthetic fields. Name every synthetic value as a fixture marker so no field is represented as observed provider schema beyond `subscription.status`; include no account, profile, credential, token, authorization, URL, or PII-like value.

Replace the one-key helper with internal named `ProfileResponseV4Decoder` and `SubscriptionStatusResponseV1Decoder`. After existing redirect/content-type/status preflight and recognized challenge/bot control detection, the profile decoder accepts a non-empty response body only when JSON parsing yields an object root; it must not require a profile ID or any additional exact field. The subscription decoder uses Decodable or a bounded object traversal for only `subscription.status`; map exact lowercase `active` and `inactive`, and fail closed for every other case. Extra fields at either level must not affect the settled predicate. Remove production success handling for caller-shaped `authenticated` and `entitled` booleans. Keep the raw response and decoder types internal, emit no raw error/body description, and preserve the existing public semantic result mapping.

Add the actor tracer using the fixture bodies through injected native verifiers. Assert ordered authentication then entitlement calls, no partial active state, one active assignment, and one post-entitlement save. Do not contact SiriusXM, add endpoint fields, or derive any new provider predicate.</action>
  <acceptance_criteria>
    - Representative multi-field profile and active-subscription responses classify successfully through production adapter code and the actor publishes one active session.
    - Inactive subscription remains a distinct authenticated-but-not-entitled result; unknown/malformed/control responses remain terminal.
    - No synthetic `authenticated`/`entitled` production success key remains, and no guessed profile field becomes mandatory.
    - Fixture values are invented, sanitized, and structurally representative without containing secrets, identifiers, PII, authorization material, or a captured provider body.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter AuthenticationOutcomeTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests</automated>
  </verify>
  <done>The production adapter can reduce settled representative profile-v4 and subscription-v1 evidence to an active actor-owned session without inventing a provider schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-13-02: Replace every synthetic success stub and lock full-package compatibility</name>
  <reversibility rating="reversible">The test conversion changes only internal fixture inputs and preserves the existing public semantic client API.</reversibility>
  <read_first>
    - `SignOutTests.swift` — replace helper responses used to enter/cancel active work without changing memory-first cleanup assertions.
    - `WebTokenAuthenticationTests.swift` — make the existing end-to-end client transaction use the representative fixtures and add a named multi-field active-session regression.
    - `SessionCoordinatorTests.swift` — reuse the one test-only fixture namespace rather than duplicating response JSON.
    - `PublicConsumerTests.swift` — verify the independent non-`@testable` consumer remains wire-schema-free; no edit is expected unless compilation exposes leakage.
    - `RedactionTests.swift` — retain structural secret rejection and run it against the new fixture namespace through the full suite.
  </read_first>
  <files>Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift</files>
  <behavior>
    - Test 1: all successful coordinator, sign-out setup, and web-token transaction paths use the shared profile-v4/subscription-v1 fixtures rather than one-field booleans.
    - Test 2: the full client transaction calls authentication once, entitlement once, publishes active state, and persists once for active; inactive/terminal cases persist zero times.
    - Test 3: the public-consumer target compiles without importing or naming either versioned decoder or test fixture type.
    - Test 4: all package tests remain deterministic, make no network request, and contain no mutable planning-artifact dependency.
  </behavior>
  <action>Convert every package test helper that currently manufactures `authenticated` or `entitled` success booleans to the shared sanitized fixture bodies. Keep HTTP status/control tests synthetic and explicit. Add a named end-to-end `WebTokenAuthenticationTests` case that sends the multi-field profile fixture followed by active subscription through `SessionCoordinator` and asserts semantic auth, semantic entitlement, ordered calls, active state, and post-entitlement persistence. Keep sign-out/concurrency tests behavior-identical apart from their settled response inputs. Run the independent consumer and redaction targets as part of the full package suite; do not expose fixture loaders or decoder types publicly and do not add a `.planning` runtime lookup.</action>
  <acceptance_criteria>
    - No package test relies on the removed one-field success booleans.
    - The complete client package suite proves active, inactive, terminal, sign-out, redaction, and public-consumer behavior from the settled adapter semantics.
    - Decoder and fixture types remain unavailable to ordinary client consumers.
  </acceptance_criteria>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer swift test --package-path Packages/SiriusXMClient</automated>
  </verify>
  <done>The entire package test graph uses the settled response contract, and representative multi-field native responses complete the production transaction without public wire leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native SiriusXM response bytes → internal decoder | Untrusted status, content type, redirect metadata, and JSON cross into semantic authentication/entitlement classifications. |
| Internal classifications → session actor | Only authenticated profile plus active subscription may authorize one immutable active session. |
| Synthetic fixtures → test/report output | Invented test data must remain secret-free and must not be mistaken for captured provider schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13-01 | Spoofing / Tampering | profile/subscription decoders | high | mitigate | Apply redirect/content/status/control preflight, use only settled predicates, fail closed on malformed/unknown entitlement evidence, and test all terminal classes. |
| T-01-13-02 | Elevation of Privilege | `SessionCoordinator` active publication | high | mitigate | Require profile authentication then exact active entitlement, preserve one actor assignment, and assert zero persistence for inactive/terminal results. |
| T-01-13-03 | Information Disclosure | fixture and diagnostic paths | high | mitigate | Use invented neutral fixture markers, keep raw bodies internal, run redaction/public-consumer suites, and forbid captured account/token/authorization data. |
| T-01-13-04 | Denial of Service | response parsing | medium | mitigate | Parse one bounded native response already held by transport, avoid recursive schema discovery, and make malformed data terminal with no retry. |
| T-01-13-05 | Repudiation | compatibility evidence | medium | mitigate | Name the decoder versions and fixture cases explicitly so future drift repairs can identify which settled contract passed or failed without logging a body. |
</threat_model>

## Multi-Source Coverage Audit (Phase 1 closure set)

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Establish or end an authorized session safely | 01-13–01-16 | COVERED | Response decoding, redirect stop, browser cleanup, cookie trust, and bounded restore close the current findings. |
| REQ | AUTH-01 | Explicit sign-in outcomes and usable authorized success | 01-13, 01-16 | COVERED | Representative responses activate; restored and WebView attempts use the same transaction. |
| REQ | AUTH-02 | Unknown/prohibited flows stop without bypass | 01-13, 01-14, 01-16 | COVERED | Decoders, redirects, and rejected restore all fail closed. |
| REQ | AUTH-03 | Sign-out clears active and stored credentials | 01-15, 01-16 | COVERED | Exact-cookie cleanup, store retirement, and rejected-restore erasure are explicit. |
| REQ | SECR-01 | Keychain is the sole persistent secret store | 01-16 | COVERED | One bounded Keychain loader/source is added; no fallback store exists. |
| REQ | SECR-02 | Tokens remain ephemeral and direct-only | 01-14, 01-15, 01-16 | COVERED | Redirects follow none; WebKit store rotates; restored material is one-attempt and direct-verification only. |
| REQ | SECR-03 | Diagnostics/fixtures exclude sensitive material | 01-13, 01-15, 01-16 | COVERED | Sanitized fixtures, opaque cookie/restore seams, and semantic errors only. |
| REQ | CLNT-01 | Independent SwiftPM client | 01-13 | COVERED | Full public-consumer target remains green with no new app dependency. |
| REQ | CLNT-02 | Typed async semantic API | 01-13 | COVERED | Public outcomes stay semantic while response versions remain internal. |
| REQ | CLNT-03 | Volatile schema details stay replaceable/internal | 01-13, 01-14 | COVERED | Versioned decoders and redirect instrumentation remain package-internal. |
| REQ | CLNT-04 | Injected deterministic collaborators | 01-13–01-16 | COVERED | Scripted responses, direct delegate callback, store lifecycle seam, and credential loader are deterministic. |
| RESEARCH | internal repairable adapter | Provider drift is contained behind a semantic façade | 01-13 | COVERED | Named internal response versions replace synthetic one-key success. |
| RESEARCH | ephemeral direct transport | Authorization never follows a redirect or enters shared state | 01-14 | COVERED | Actual callback count and nil completion are tested. |
| RESEARCH | Keychain and cleanup lifecycle | Persistent material is app-owned and cleanup is truthful | 01-15, 01-16 | COVERED | Restore is bounded; rejected material and browser state are retired. |
| CONTEXT | authentication architecture | One user-operated nonpersistent WebView plus one volatile token handoff | 01-15, 01-16 | COVERED | Allowlist and store rotation preserve the sole path; restore reuses approved material, not a second method. |
| CONTEXT | runtime authority | Native authentication then entitlement exclusively authorize state | 01-13, 01-16 | COVERED | Both WebView and restored credentials traverse the same client actor transaction. |
| CONTEXT | secret lifecycle/sign-out | Memory, Keychain, and exact browser residue clear or fail explicitly | 01-15, 01-16 | COVERED | Cleanup rotates the store; invalid/rejected restores erase Keychain material. |
| CONTEXT | deterministic testing | No live proof run or planning-artifact runtime dependency | 01-13–01-16 | COVERED | Every plan uses injected synthetic collaborators and existing test targets. |
| REVIEW | CR-01 | Release signing | Phase 5 | EXCLUDED | REL-01 explicitly owns public signing/notarization; no Phase 1 signing edit is planned. |
| REVIEW | CR-02 / WR-01 / WR-02 / WR-03 / IN-01 | Current Phase 1 findings | 01-13–01-16 | COVERED | Each finding appears in the closure matrix with executable tests or explicit deferral. |
| VERIFICATION | remaining gap | Multi-field native responses can establish active state | 01-13 | COVERED | Sanitized representative fixture transaction is a blocking test. |

`01-CONTEXT.md` contains locked decision groups but no D-NN identifiers, so this audit cites the exact groups without inventing IDs. Superseded authentication research, catalog, playback, metadata, skins, and release distribution are excluded by the active phase boundary.

<verification>
- Focused response-classification and coordinator suites pass with sanitized multi-field fixtures.
- The full package suite passes, including public-consumer, redaction, sign-out, and web-token transaction tests.
- Static review confirms decoder types are internal and no new endpoint, public wire type, live probe, raw body log, or captured fixture exists.
</verification>

<success_criteria>
- CR-02 and the verifier's remaining gap are closed by a production adapter that accepts the settled representative profile/subscription evidence.
- Unknown/control/redirect/status/malformed cases retain fail-closed semantic outcomes and zero retry/fallback behavior.
- AUTH-01, AUTH-02, SECR-03, and CLNT-01–CLNT-04 remain enforceable through one deterministic full-package suite.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-13-SUMMARY.md` when done.
</output>
