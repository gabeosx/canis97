---
phase: 01-safe-interoperability-foundation
plan: "06"
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified:
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac/Authentication/FirstPartyTokenCookiePolicy.swift
  - SiriusMac/Authentication/WebAuthenticationBridge.swift
  - SiriusMac/Authentication/WebAuthenticationView.swift
  - SiriusMacTests/WebAuthenticationBridgeTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-02, SECR-03, CLNT-04]
estimate:
  tokens: 40000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The app uses a user-operated `WKWebView` with `WKWebsiteDataStore.nonPersistent()` and no shared browser profile."
    - "Only explicit `Use Logged-In Session` may query cookies, and it selects exactly one current `AUTH_TOKEN` using one shared first-party policy."
    - "The policy applies identically to extraction and sign-out across `siriusxm.com` and accepted true subdomains, including expiry and path rules."
    - "Multiple matches, unsupported domain/path, malformed payload, missing `session.accessToken`, or any remaining match after cleanup fails closed."
    - "Only `session.accessToken` crosses once as an opaque volatile credential; raw cookies/payloads never enter logs, fixtures, summaries, or persistence."
    - "WebAuthenticationBridge and its tests always compile independently of `.planning` artifact contents."
  artifacts:
    - path: "SiriusMac/Authentication/FirstPartyTokenCookiePolicy.swift"
      provides: "Single exact extraction/cleanup predicate"
    - path: "SiriusMac/Authentication/WebAuthenticationBridge.swift"
      provides: "Nonpersistent WebView credential source and residue cleaner"
    - path: "SiriusMacTests/WebAuthenticationBridgeTests.swift"
      provides: "Extraction, ambiguity, cleanup-parity, and unconditional-build regression tests"
  prohibitions:
    - statement: "MUST NOT enumerate unrelated cookies/storage, inject JavaScript, use developer tools, share Safari state, persist raw token cookies, or gate compilation on planning artifacts."
      status: unverified
      flagged: true
---

<objective>
Productionize the settled nonpersistent WebView token bridge with one exact token predicate shared by extraction and sign-out.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-REVIEW.md
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/WebSessionBridge.swift
@Spikes/AuthenticationFeasibility/Sources/AuthFeasibilityHarness/LiveBrowserRuntime.swift
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-06-01: Extract exactly one current first-party token after explicit consent</name>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/Authentication/FirstPartyTokenCookiePolicy.swift, SiriusMac/Authentication/WebAuthenticationBridge.swift, SiriusMac/Authentication/WebAuthenticationView.swift, SiriusMacTests/WebAuthenticationBridgeTests.swift</files>
  <behavior>
    - Test 1: the bridge creates only a nonpersistent data store and does not access Safari/shared cookie state.
    - Test 2: without explicit `Use Logged-In Session`, cookie-store access count is zero.
    - Test 3: exactly one unexpired `AUTH_TOKEN` on the apex or a boundary-correct accepted subdomain/path is accepted.
    - Test 4: zero, multiple, expired, suffix-lookalike, unsupported path/domain, malformed JSON, or missing/non-string `session.accessToken` is a typed terminal result.
    - Test 5: accepted input decodes only `session.accessToken`, clears intermediate cookie/JSON buffers, and emits one opaque credential once.
  </behavior>
  <action>Adapt the proven bridge into `WebAuthenticationBridge`, but replace its duplicated predicates with `FirstPartyTokenCookiePolicy`. Normalize domains safely and require a label boundary so `evil-siriusxm.com` cannot match. Encode the approved cookie name, expiry, and path semantics once. Query the WebKit cookie store only from the explicit user action, decode a bounded payload into a minimal private struct containing only `session.accessToken`, then immediately discard the cookie/payload and transfer one opaque credential to the client seam.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WebAuthenticationBridgeTests test</automated></verify>
  <acceptance_criteria>
    - Explicit consent, exact cardinality, domain/path/expiry validation, and minimal decoding are tested.
    - Only opaque access-token material leaves the bridge once in volatile memory.
    - No broad browser-state inspection API exists.
  </acceptance_criteria>
  <done>The app safely converts one explicit WebView login into one opaque volatile credential.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-06-02: Apply the same token predicate to sign-out and keep tests unconditional</name>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/Authentication/FirstPartyTokenCookiePolicy.swift, SiriusMac/Authentication/WebAuthenticationBridge.swift, SiriusMacTests/WebAuthenticationBridgeTests.swift</files>
  <behavior>
    - Test 1: sign-out enumerates the nonpersistent store and deletes every cookie matching the exact extraction predicate, including host-only tokens on accepted subdomains.
    - Test 2: apex-only cleanup cannot pass while an accepted subdomain token remains.
    - Test 3: multiple matches, unsupported path/domain ambiguity, delete failure, or any matching token on post-delete rescan returns cleanup-failed.
    - Test 4: successful cleanup requires a post-delete rescan with zero predicate matches.
    - Test 5: project/package build membership for bridge source and tests is unconditional and contains no `.planning` file checks or conditional `canImport` skip path.
  </behavior>
  <action>Make `WebAuthenticationBridge` conform to the Plan 01-04 residue-cleaner seam. Use `FirstPartyTokenCookiePolicy` for both candidate selection and post-sign-out presence—never a second apex-only helper. Delete all exact matches, rescan, and return only safe cleanup classifications. Add a build-graph regression assertion that the production bridge and deterministic tests are always members; remove all planning-artifact-dependent source/test inclusion.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/WebAuthenticationBridgeTests test &amp;&amp; ! rg -n '\.planning|canImport\(AuthFeasibilityHarness\)' SiriusMac.xcodeproj Packages/SiriusXMClient/Package.swift SiriusMacTests/WebAuthenticationBridgeTests.swift</automated></verify>
  <acceptance_criteria>
    - Extraction and cleanup call the same policy implementation.
    - Remaining first-party subdomain tokens block successful cleanup.
    - Browser bridge tests cannot be silently excluded by mutable planning state.
  </acceptance_criteria>
  <done>WebView sign-out is symmetric with extraction and browser-bridge regressions always execute.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md` when done.</output>
