---
phase: 01-safe-interoperability-foundation
plan: "06"
type: execute
wave: 4
depends_on: [01-04, 01-05]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: siriusxm
    why: "The account owner may need an active subscriber account and the real first-party browser to evaluate a clean return; the agent must not operate or inspect the authenticated browser session."
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Browser-return feasibility is accepted only with public first-party evidence for a fixed app-bound callback and a user-initiated real-browser return that requires no browser-state extraction."
    - "Native-direct feasibility is considered only after browser return is ruled out and only with evidence for an honest app identity, exact direct host/request contract, strict entitlement predicate, and no provider-control workaround."
    - "The resulting selection is exactly browser-return, native-direct, or unsupported; no selector, fallback, dual implementation, or preference-based choice is permitted (D-01)."
    - "Any challenge/interstitial, CAPTCHA, 403, 429, rate-limit, redirect drift, suspected bot signal, or ambiguous entitlement evidence immediately selects/stays unsupported and ends evaluation."
  artifacts:
    - path: ".planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md"
      provides: "Non-secret evidence disposition and exactly one selected compatibility result"
      contains: "Selected authentication result"
  key_links:
    - from: ".planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-07-PLAN.md"
      via: "Plan 01-07 implements only the recorded evidence-selected result"
      pattern: "browser-return|native-direct|unsupported"
  prohibitions:
    - statement: "MUST NOT inspect or export browser cookies, storage, tokens, profiles, developer-tools data, or authenticated session state while selecting the path (D-01)."
      status: unverified
      flagged: true
    - statement: "MUST NOT select a sign-in path by preference or keep a second candidate as fallback (D-01)."
      status: unverified
      flagged: true
---

<objective>
Resolve the phase's costly evidence gate without agent-operated live authentication: evaluate browser-return evidence first, then honest native-direct evidence only if needed, and select exactly one result or unsupported.

Purpose: Supply Plan 01-07 with a trustworthy, non-secret implementation decision while keeping all authenticated browser/account operation under the account owner's control.
Output: A Plan 01-06 SUMMARY recording public evidence references, safe stop classifications, and exactly one selected compatibility result.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/COVERAGE.md
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 01-06-01: Evaluate ordered authentication feasibility with the account owner</name>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
    - .planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md
  </read_first>
  <files>None; the safe evidence disposition is captured in the plan SUMMARY after the checkpoint.</files>
  <action>First collect only public, first-party SiriusXM evidence for a fixed app-bound real-browser return. If the account owner chooses to initiate the real-browser flow, they operate it themselves; the agent does not click, type, read, screenshot, automate, inspect, or extract anything from the authenticated browser session. Only when browser return lacks the required contract may the account owner evaluate whether non-secret first-party/authorized evidence defines one honest native direct request and strict entitlement predicate. Do not initiate a native live sign-in in this task. The owner returns exactly one classification from this closed vocabulary: `browser-return-evidenced`; `browser-ruled-out/native-direct-evidenced`; `unsupported-no-supported-contract`; `unsupported-rejected`; `unsupported-not-entitled`; `unsupported-captcha`; `unsupported-challenge`; `unsupported-403`; `unsupported-429`; `unsupported-rate-limit`; `unsupported-unexpected-redirect`; `unsupported-bot-signal`; `unsupported-access-control`; `unsupported-other-terminal`; or `unsupported-ambiguous`. The executor records public first-party reference URLs from its own public-source review, not from authenticated browser/account state. Every `unsupported-*` classification ends evaluation and maps mechanically to the unsupported result.</action>
  <instructions>
    1. The account owner alone reviews public first-party references and, only if they choose, initiates one real-browser feasibility check themselves.
    2. They do not expose the authenticated browser, account, cookies, storage, tokens, developer tools, screenshots, raw redirects, or response data to the executor.
    3. After completing the ordered review, they return exactly one allow-listed classification and nothing else: browser-return-evidenced; browser-ruled-out/native-direct-evidenced; unsupported-no-supported-contract; unsupported-rejected; unsupported-not-entitled; unsupported-captcha; unsupported-challenge; unsupported-403; unsupported-429; unsupported-rate-limit; unsupported-unexpected-redirect; unsupported-bot-signal; unsupported-access-control; unsupported-other-terminal; or unsupported-ambiguous.
    4. Only after the browser-return contract is ruled out may they review non-secret public/authorized evidence for an exact honest native-direct contract; they do not perform a native live sign-in in this plan.
    5. Rejection maps to unsupported-rejected; missing entitlement maps to unsupported-not-entitled; CAPTCHA maps to unsupported-captcha; a challenge/interstitial maps to unsupported-challenge; 403 maps to unsupported-403; 429 maps to unsupported-429; an explicit rate-limit maps to unsupported-rate-limit; an unexpected redirect maps to unsupported-unexpected-redirect; a suspected bot response maps to unsupported-bot-signal; any other MFA/device/geographic/DRM/subscription control maps to unsupported-access-control; any other terminal condition maps to unsupported-other-terminal; and missing, conflicting, or unclear evidence maps to unsupported-ambiguous. If neither ordered candidate has its exact required contract, use unsupported-no-supported-contract.
  </instructions>
  <verification>The executor accepts only one allow-listed safe classification, verifies no native live attempt was requested, and records no owner-supplied browser/account/session material. The executor cannot and must not inspect the authenticated browser to verify the owner's classification.</verification>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient &amp;&amp; xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' test</automated>
    <manual>The account owner returns only one allow-listed classification for the browser-first result and, only if browser is ruled out, the native-direct evidence result. They disclose no credential, cookie, token, account identifier, raw response, browser storage value, developer-tools output, or public-reference annotation.</manual>
  </verify>
  <acceptance_criteria>
    - Evidence evaluation follows browser return → native direct → unsupported order.
    - The agent never operates or inspects an authenticated browser/account session.
    - Safe evidence is sufficient to decide one candidate, or the result is unsupported.
  </acceptance_criteria>
  <done>The account owner has supplied exactly one allow-listed non-secret evidence classification for the ordered candidate procedure without authorizing a live agent-operated attempt.</done>
  <resume-signal>Reply with exactly one classification token and nothing else: browser-return-evidenced; browser-ruled-out/native-direct-evidenced; unsupported-no-supported-contract; unsupported-rejected; unsupported-not-entitled; unsupported-captcha; unsupported-challenge; unsupported-403; unsupported-429; unsupported-rate-limit; unsupported-unexpected-redirect; unsupported-bot-signal; unsupported-access-control; unsupported-other-terminal; or unsupported-ambiguous.</resume-signal>
</task>

<task type="auto">
  <name>Task 01-06-02: Record the sole evidence-selected authentication result</name>
  <reversibility rating="costly">The selected sign-in surface affects the public client composition, app shell, security policy, and all downstream authorization work (D-01).</reversibility>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
    - Task 01-06-01 safe evidence disposition from the active execution context
  </read_first>
  <files>.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md</files>
  <action>Apply D-01 mechanically to the single Task 01-06-01 classification. Map `browser-return-evidenced` to `browser-return`. Map `browser-ruled-out/native-direct-evidenced` to `native-direct`. Map every allow-listed `unsupported-*` classification to `unsupported`. A missing, duplicated, malformed, or non-allow-listed classification also maps to `unsupported`; do not request or infer additional browser/account evidence. Record one exact selected-result line, the safe classification, and independently reviewed public first-party reference URLs only. Do not preserve an alternate candidate, fallback, selector, copied token, speculative host, raw protocol sample, browser/session state, or developer-tools output.</action>
  <verify>
    <automated>summary=.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md; [ -r "$summary" ] &amp;&amp; total=$(rg -c '^Selected authentication result:' "$summary") &amp;&amp; candidate=$(sed -n 's/^Selected authentication result: //p' "$summary") &amp;&amp; [ "$total" -eq 1 ] &amp;&amp; case "$candidate" in browser-return) true;; native-direct) true;; unsupported) true;; *) false;; esac</automated>
    <manual>Confirm the selected result follows the locked evidence order and every non-selected path is excluded, not retained as fallback.</manual>
  </verify>
  <acceptance_criteria>
    - Exactly one result is recorded with non-secret public evidence.
    - Unsupported is mandatory when evidence is absent, control-protected, or ambiguous.
    - The summary contains no browser/session/account secret or raw provider response.
  </acceptance_criteria>
  <done>Plan 01-06 ends with exactly one evidence-selected result that Plan 01-07 can implement without choosing another path.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates no source file and no public/internal code symbol. Its required executor output is `01-06-SUMMARY.md`, containing `Selected authentication result: browser-return|native-direct|unsupported`, public first-party reference URLs, safe stop classifications, and an explicit no-secrets/no-fallback attestation. The summary is the costly D-01 decision artifact consumed by Plan 01-07.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to the human-browser evidence and evidence-to-implementation boundaries. Every critical or high-severity threat below is blocking: if a mitigation cannot be maintained or evidence is ambiguous, record unsupported and stop.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Account owner/real browser → planning evidence | Only safe disposition and public references may cross; authenticated browser/session material stays private. |
| Evidence → implementation choice | Weak/ambiguous evidence must produce unsupported, never speculative implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Human-operated real browser | high | mitigate | Account owner operates browser; agent receives only safe disposition/public URLs and no authenticated state. |
| T-01-02 | Elevation of Privilege / Tampering | Path selection | high | mitigate | Evidence order plus terminal-stop policy; unsupported is mandatory on control protection or ambiguity. |
| T-01-04 | Denial of Service | Feasibility evaluation | high | mitigate | No automated/live native sign-in in this plan, no polling, and only one human browser initiation if needed. |
| T-01-05 | Spoofing / Tampering | Evidence interpretation | high | mitigate | Exact first-party contract predicate; visible website behavior alone is insufficient. |
| T-01-06 | Information Disclosure | Plan summary | high | mitigate | Allow-listed result/reference fields only; no credential/account/session/raw-response content. |
| T-01-SC | Tampering | Tool/package use | high | mitigate | No install task; evaluation uses existing public-source access and human browser only. |
</threat_model>

<verification>
- Baseline automated package/app suites remain green before human evaluation.
- The executor/agent performs no live sign-in, browser operation, storage inspection, or token transfer.
- `01-06-SUMMARY.md` records exactly one evidence-compelled result with safe fields only.
</verification>

<success_criteria>
- Exactly one browser-return, native-direct, or unsupported result exists.
- No selector/fallback or browser-state extraction is authorized.
- Unsupported remains a fully valid Phase 1 outcome and blocks later authorization-dependent work.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md` with the exact selected-result line and safe evidence fields when done.
</output>
