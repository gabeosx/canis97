---
phase: 01-safe-interoperability-foundation
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/AuthenticationView.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-01, SECR-02, SECR-03, CLNT-01, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The implementation result is exactly the validated Phase 0 GO path consumed by Plan 01-06: browser-return or native-direct (D-01)."
    - "The selected result is implemented only from exact public first-party/authorized evidence for its callback or direct-request contract, exact HTTPS hosts, and strict authenticated-and-entitled predicate; missing or ambiguous evidence stops before source changes and leaves the existing unavailable composition intact."
    - "The mechanically safe unavailable composition remains the default until every Phase 0 validation/derivation and selected-path precondition passes; invalid input creates no live adapter or credential effect (D-02)."
    - "A supported result ships one sign-in surface only, with no method selector, alternate adapter, runtime fallback, speculative endpoint, speculative host, or speculative callback."
    - "Every challenge/interstitial, CAPTCHA, 403, 429, rate-limit, unexpected redirect, suspected bot response, rejection, absent entitlement, and ambiguous response stops the single attempt without retry or fallback."
    - "Sign-out remains reachable after authenticated-and-entitled success and clears memory before app-owned Keychain deletion."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      provides: "Strict classifier plus only the validated Phase 0-selected live adapter; unavailable remains the blocked default before composition"
      contains: "AuthenticationFlowAdapter"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift"
      provides: "Empty blocked-default policy until exact evidence-derived HTTPS host policy is composed"
      contains: "DirectHostPolicy"
    - path: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      provides: "One selected sign-in action with the complete unsupported compatibility state retained for terminal runtime failures"
      contains: "AuthenticationPresentationModel"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift"
      provides: "Synthetic selected-result, terminal-stop, and no-fallback contract tests"
      contains: "SelectedAuthenticationPathTests"
    - path: ".planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md"
      provides: "Implemented-result and continuation-readiness contract consumed by Plan 01-08"
      contains: "Continuation readiness"
  key_links:
    - from: ".planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      via: "The exact selected-result line and sufficient public evidence choose one compile-time composition; they never become a runtime method selector"
      pattern: "Selected authentication result"
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift"
      via: "Only the selected supported adapter may supply exact evidence-approved HTTPS hosts"
      pattern: "DirectHostPolicy"
    - from: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift"
      via: "The app awaits one public semantic sign-in operation and public sign-out operation"
      pattern: "SiriusXMClient"
    - from: ".planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md"
      to: ".planning/phases/01-safe-interoperability-foundation/01-08-PLAN.md"
      via: "Plan 01-08 performs production synthetic acceptance only for the validated Phase 0 path"
      pattern: "production-selected-path-tested"
  prohibitions:
    - statement: "MUST NOT implement more than the one result recorded by Plan 01-06, retain another path as fallback, or expose a method selector (D-01)."
      status: unverified
      flagged: true
    - statement: "MUST NOT infer, guess, scrape, or copy an endpoint, host, callback, cookie, token, browser-storage value, app identity, request shape, response shape, or entitlement predicate."
      status: unverified
      flagged: true
---

<objective>
Implement only the authentication result already proven and consumed from Phase 0, preserving the explicit unsupported runtime state and unavailable composition as fail-closed defaults rather than repeating feasibility.

Purpose: Convert the costly evidence decision into one repairable internal composition while preserving the public semantic API, app-owned secret boundary, and fail-closed continuation gate.
Output: Exactly one selected-path production composition, safe unavailable defaults, synthetic contract/app tests, and an exact production-readiness summary.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-PATTERNS.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md
@.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
@.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
@.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
@.planning/phases/00-authentication-feasibility-gate/00-DECISION.md

<interfaces>
Keep the public SiriusXMClient, AuthenticationOutcome, EntitlementState, AuthenticationCapability, AuthenticationCredential, CredentialStore, RedactedDiagnosticsSink, SignOutOutcome, ChannelID, ChannelCatalog, NowPlayingMetadata, LiveStreamResource, CatalogCapability, MetadataCapability, LiveStreamResolutionCapability, CatalogError, MetadataError, and LiveStreamResolutionError contracts created in Plans 01-01 through 01-04. The catalog, metadata, and live-stream-resolution APIs remain typed unavailable in Phase 1; this plan must not add provider endpoints, catalog fetching, metadata loops, stream resolution, or playback. AuthenticationFlowAdapter, all callback/direct-request values, DirectHostPolicy, wire decoders, and active session material remain internal. The app composes one internal authentication result and never exposes a path enum or selector to subscribers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-07-01: Implement exactly the evidence-selected library composition</name>
  <precondition>The complete Phase 0 artifact chain freshly derives byte-identical selection/decision with exact GO+unlocked, and 01-06-SUMMARY.md contains exactly the matching browser-return or native-direct result plus consumed unlocked state.</precondition>
  <reversibility rating="costly">The one selected sign-in surface and its internal composition constrain the app shell and later authorization work, while the volatile wire implementation remains replaceable behind the public client boundary (D-01).</reversibility>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Transport/DirectHostPolicy.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SelectedAuthenticationPathTests.swift</files>
  <behavior>
    - Test 1: before validated composition, or for any malformed/mismatched Phase 0/01-06 input, the empty approved-host policy and unavailable adapter remain unchanged with zero transport/credential calls.
    - Test 2: the selected Phase 0 GO path recognizes only its evidence-recorded callback or direct response plus strict entitlement predicate; every altered, missing, or ambiguous field is terminal unsupported at runtime.
    - Test 3: challenge/interstitial, CAPTCHA, 403, 429, rate limit, redirect drift, suspected bot signal, rejection, and missing entitlement each produce a terminal result with one attempt and no follow-up call.
    - Test 4: public authentication, entitlement, catalog, metadata, and live-stream-resolution values contain no endpoint, host, callback, request, response, header, cookie, token, session, or raw-schema detail; the three content operations remain typed unavailable with zero transport calls.
    - Test 5: sign-out after synthetic entitled success clears actor state before credential deletion and remains explicit when deletion fails.
  </behavior>
  <action>Revalidate the full Phase 0 derivation and require the Plan 01-06 normalized result to match before editing source. If the chain is missing/malformed/mismatched/NO-GO, or the selected result lacks any exact callback/direct-request contract, exact HTTPS host set, honest client-identity rule, strict response classification, or strict authenticated-and-entitled predicate, halt with the existing unavailable composition unchanged; do not infer, re-evaluate, or test a provider value. Then execute exactly one GO branch. For browser-return, implement one internal browser-return adapter from the exact recorded first-party contract, accept only the expected app-bound return with in-memory correlation, use the user's real external browser from the app task, and reject every unrecognized return without reading browser state. For native-direct, implement one internal direct adapter from the exact recorded authorized request/response contract, identify Sirius Mac honestly, and make only exact approved HTTPS requests. Remove the unused live-path implementation rather than retaining it, preserve the actor's one-attempt/no-retry behavior, retain the unavailable adapter as the terminal/default safety behavior rather than a selectable second method, and persist only a validated reusable credential after authenticated-and-entitled success when required. Preserve the CLNT-02 content contracts and typed unavailable zero-transport behavior. Do not perform public-evidence review, live authentication, or either proof run.</action>
  <verify>
<automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; handoff=.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file" &amp;&amp; phase0_path=$(sed -n 's/^Feasibility decision: GO //p' "$decision_file") &amp;&amp; handoff_path=$(sed -n 's/^Selected authentication result: //p' "$handoff") &amp;&amp; consumed=$(sed -n 's/^Phase 0 continuation consumed: //p' "$handoff") &amp;&amp; [ "$phase0_path" = "$handoff_path" ] &amp;&amp; [ "$consumed" = unlocked ] &amp;&amp; case "$phase0_path" in browser-return|native-direct) true;; *) false;; esac &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SelectedAuthenticationPathTests</automated>
    <manual>Compare the implementation against the validated Phase 0 path: only one live branch exists, every host/callback/request predicate is evidence-derived, and the unavailable default cannot initiate provider work.</manual>
  </verify>
  <acceptance_criteria>
    - The selected result alone is compiled into the library composition.
    - Selected code contains no guessed provider detail and invalid preconditions leave the no-live-adapter default intact.
    - All locked terminal signals stop after one attempt and no alternate method is reachable.
    - Public API and diagnostics remain semantic and non-secret, and all three content capabilities remain typed unavailable without provider work.
  </acceptance_criteria>
  <done>The library implements exactly the recorded result with synthetic fail-closed proof and no speculative or alternate interoperability path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-07-02: Expose only the selected app surface and clean sign-out</name>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md
    - SiriusMac/SiriusMacApp.swift
    - SiriusMac/Authentication/AuthenticationPresentationModel.swift
    - SiriusMac/Authentication/UnsupportedAuthenticationView.swift
    - SiriusMac/Security/KeychainCredentialStore.swift
  </read_first>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/SiriusMacApp.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift</files>
  <behavior>
    - Test 1: before/after terminal unsupported runtime state, the dedicated compatibility screen exposes no alternate sign-in form/player shell, retains no credential, and produces zero follow-up attempts.
    - Test 2: browser-return selection exposes one browser sign-in action through the system external browser and accepts only the evidence-recorded app return; it contains no native credential form, WebView, browser automation, or browser-state inspection.
    - Test 3: native-direct selection exposes one transient native credential action and contains no browser sign-in action; input is cleared after every outcome and saved only after authenticated-and-entitled success if required.
    - Test 4: every supported success exposes sign-out, and sign-out renders signed out only after memory-first cleanup and successful Keychain deletion; cleanup failure remains explicit.
    - Test 5: Retry or Sign In permits at most one in-flight client call and schedules no timer, polling, retry loop, or alternate path.
  </behavior>
  <action>Compose the native app for the same validated GO result and no other. For browser-return, create one native AuthenticationView action that opens the exact evidence-approved initiation in the user's real system browser and handles only the exact OS-delivered app return named by the evidence; register callback metadata only when exact evidence defines it, keep correlation/session values in memory, and never use a WebView, browser automation, cookies, storage, screenshots, or browser inspection. For native-direct, create one transient native credential surface that invokes the sole public semantic action, clears UI input after every result, and never imitates a browser. Surface authenticated-and-entitled distinctly, expose sign-out through the existing memory-first cleanup path, and map every terminal failure to the dedicated typed compatibility state with no follow-up. If evidence cannot determine registration/initiation exactly, stop before project changes and leave the unsupported root/default intact. Tests remain synthetic and perform no live authentication or feasibility proof.</action>
  <verify>
    <automated>xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests test</automated>
    <manual>Launch only with synthetic collaborators: the app shows exactly one selected surface, never a method picker, and every terminal unsupported state exposes no alternate sign-in or follow-up provider attempt.</manual>
  </verify>
  <acceptance_criteria>
    - App and library compositions agree on exactly one result.
    - Unsupported remains a complete, reachable, credential-free product state with no live adapter.
    - A supported surface has one user action, one in-flight attempt, semantic outcomes, and reachable clean sign-out.
    - No browser wrapper/automation, secondary path, fallback, or later-phase catalog/playback UI is introduced.
  </acceptance_criteria>
  <done>The native app exposes only the Phase 0-selected result and is ready for production synthetic acceptance; no live feasibility or two-run gate is repeated.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan modifies the public client composition, internal authentication adapter/classifier, exact direct-host policy, app root/presentation model, and synthetic selected-path tests. It may create AuthenticationView.swift and evidence-required Xcode callback metadata for the sole Phase 0 GO path. The unavailable composition remains the safe default/terminal state and exact host set stays empty until the validated composition is installed. The executor output 01-07-SUMMARY.md must contain exactly one `Implemented authentication result: browser-return|native-direct` line and exactly one `Continuation readiness: production-selected-path-tested` line. No public provider wire symbol is added.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to the app-to-client, client-to-provider, browser-return, credential, and diagnostic boundaries. Every critical or high-severity threat below is blocking: if its mitigation or automated proof cannot be implemented, stop the plan and keep authentication unsupported.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated Phase 0 chain + Plan 01-06 handoff → source composition | Only the exact matching GO path may authorize one implementation; ambiguity leaves the safe default unchanged. |
| Native app/account owner → selected client action | Transient user/browser input enters one public semantic operation and must not leak or select another path. |
| Internal adapter → SiriusXM | Only a supported branch may make one honest, exact, direct request/return exchange to approved HTTPS hosts. |
| Session actor → Keychain | Only validated post-entitlement reusable credentials may cross into app-owned storage; sign-out clears memory first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Browser-return composition | high | mitigate | Real external browser only, exact app-bound return, in-memory correlation, and no WebView/cookie/storage/inspection API. |
| T-01-02 | Elevation of Privilege / Tampering | Selected authentication adapter | high | mitigate | One evidence-selected branch; all control-protection and ambiguous signals terminate unsupported with no workaround/fallback. |
| T-01-03 | Information Disclosure | Credential/session lifecycle | high | mitigate | Transient input, post-entitled-success Keychain effect only, memory-only session, and memory-first sign-out. |
| T-01-04 | Denial of Service / Repudiation | Authentication action | high | mitigate | Existing actor lease plus app in-flight guard; synthetic tests prove one call and no polling/retry/fallback. |
| T-01-05 | Spoofing / Tampering | Evidence-derived host/callback/response contract | high | mitigate | Exact predicates from Plan 01-06 only; missing evidence halts before source changes and unknown shapes cannot authenticate. |
| T-01-06 | Information Disclosure | Diagnostics and tests | high | mitigate | Semantic allow-listed events and synthetic fixtures only; no live response, raw error, account value, or token enters artifacts. |
| T-01-SC | Tampering | Package/project dependencies | high | mitigate | Use only OS-bundled frameworks and existing manifests; any new dependency install blocks pending a package-legitimacy audit. |
</threat_model>

<verification>
- The exact selected-result and evidence preconditions pass before source changes.
- Package and app selected-composition tests pass without a live SiriusXM request.
- Full package/app suites remain green.
- Source and project composition contain one selected path plus a non-selectable fail-closed unavailable state, never a selector/fallback.
- 01-07-SUMMARY.md contains the exact implemented-result and continuation-readiness lines consumed by Plan 01-08.
</verification>

<success_criteria>
- The freshly validated Phase 0 result and matching Plan 01-06 handoff determine implementation.
- No speculative callback, host, endpoint, request, response, identity, or entitlement rule exists.
- Unsupported produces no live adapter and supported produces one path with clean sign-out.
- No high-severity ASVS L1 threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md with exact implemented-result and continuation-readiness lines when done.
</output>
