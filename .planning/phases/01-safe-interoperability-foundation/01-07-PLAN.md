---
phase: 01-safe-interoperability-foundation
plan: "07"
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/WebAuthenticationBridge.swift
  - SiriusMacTests/SelectedAuthenticationCompositionTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-01, SECR-02, SECR-03, CLNT-01, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 48000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The sole production sequence is explicit WebView token extraction → native authentication request → native entitlement request → atomic session publication."
    - "The extracted token is consumed once and remains volatile; native requests are direct to exact SiriusXM hosts through the ephemeral transport."
    - "The runtime—not a caller, summary, or planning artifact—derives authentication and entitlement from native responses."
    - "Keychain save and entitled UI occur only after both native verifiers succeed."
    - "Every terminal/control/unknown outcome stops with no retry, alternate method, follow-up request, or partial session."
    - "Sign-out clears actor state, WebView token cookies, and Keychain material or reports aggregate cleanup failure."
  artifacts:
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift"
      provides: "End-to-end synthetic production sequence tests"
    - path: "SiriusMacTests/SelectedAuthenticationCompositionTests.swift"
      provides: "Native WebView-to-client composition tests"
  prohibitions:
    - statement: "MUST NOT branch on Phase 0 artifacts, accept self-asserted success, skip entitlement, retain a native credential form, or keep an alternate auth adapter."
      status: unverified
      flagged: true
---

<objective>
Compose the settled WebView token source with native authentication and entitlement verification through the reusable client.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-REVIEW.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-07-01: Execute native authentication and entitlement as one runtime-owned transaction</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/SiriusXMRequestContract.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/WebTokenAuthenticationTests.swift</files>
  <behavior>
    - Test 1: one opaque credential produces exactly one native authentication request followed by exactly one entitlement request.
    - Test 2: authenticated response without entitled response yields authenticated-but-not-entitled, zero active session, and zero persistence.
    - Test 3: both recognized successes create one immutable active session and one allowed Keychain save after entitlement.
    - Test 4: caller-authored flags/files cannot bypass either verifier; production initializers accept collaborators/responses, not success claims.
    - Test 5: challenge, rejection, 403/429, redirect drift, malformed/unknown/ambiguous response, or cancellation stops with cleared credential/session and no follow-up/retry.
  </behavior>
  <action>Wire the Plan 01-02 coordinator to Plan 01-03 native request contracts. Treat the credential as single-consumption secret data. Execute authentication and entitlement in order inside the actor-owned attempt, building the candidate `ActiveSession` locally and assigning it only after both strict classifiers pass. Persist allowed reusable material only after that assignment point. Delete any leftover owner-result/decision input API and any unused alternate authentication adapter from production composition.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter WebTokenAuthenticationTests &amp;&amp; swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests</automated></verify>
  <acceptance_criteria>
    - Native runtime observations are the sole authentication authority.
    - Entitlement is mandatory and ordered after authentication.
    - Active state is atomic and terminal outcomes leave no residue or follow-up work.
  </acceptance_criteria>
  <done>The reusable client owns one complete WebView-token/native-verification authentication transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-07-02: Compose the one native app surface and complete sign-out</name>
  <files>SiriusMac/SiriusMacApp.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/WebAuthenticationBridge.swift, SiriusMacTests/SelectedAuthenticationCompositionTests.swift</files>
  <behavior>
    - Test 1: the app composes `WebAuthenticationBridge` as the credential source and the client as native verifier; no method selector or alternate source exists.
    - Test 2: explicit Use Logged-In Session transfers one credential and renders verifying-auth then verifying-entitlement before entitled.
    - Test 3: authenticated-but-not-entitled and every terminal result return to typed compatibility UI with no player, alternate method, or automatic retry.
    - Test 4: sign-out clears memory before WebView/Keychain cleanup; complete signed-out appears only after both succeed and aggregate failure is visible otherwise.
    - Test 5: all app tests use synthetic cookie stores/transports and make zero real provider requests.
  </behavior>
  <action>Replace the temporary unavailable composition with the one settled bridge/client composition. Keep the WebView app-owned and the native request/response contracts client-owned; only the opaque credential seam crosses between them. Drive presentation from semantic progress/outcomes. Inject the Plan 01-06 residue cleaner into the Plan 01-04 sign-out pipeline. Remove any external-browser or native-credential path.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/SelectedAuthenticationCompositionTests test</automated></verify>
  <acceptance_criteria>
    - App and client form one WebView-token/native-request path.
    - Entitlement and cleanup state are visible and truthful.
    - No live test, alternate auth UI, fallback, or partial player shell exists.
  </acceptance_criteria>
  <done>The native app exposes the settled authentication architecture end to end with clean sign-out.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-07-SUMMARY.md` when done.</output>
