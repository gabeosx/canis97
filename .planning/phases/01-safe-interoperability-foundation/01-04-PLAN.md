---
phase: 01-safe-interoperability-foundation
plan: "04"
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac/Security/KeychainCredentialStore.swift
  - SiriusMacTests/KeychainCredentialStoreTests.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
autonomous: true
requirements: [AUTH-03, SECR-01, SECR-02, CLNT-04]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Credentials can be persisted only through the app-owned Keychain adapter, only after the selected path returns authenticated-and-entitled, and only when that path requires a reusable credential."
    - "Rejected, challenge, unsupported, ambiguous, cancelled, and authenticated-but-not-entitled outcomes perform no Keychain write."
    - "Sign-out retires/cancels the active session and clears all actor-held material before it attempts Keychain deletion."
    - "A Keychain deletion failure returns an explicit safe failure while memory remains signed out; the client never claims complete cleanup falsely."
  artifacts:
    - path: "SiriusMac/Security/KeychainCredentialStore.swift"
      provides: "App-owned Security.framework credential CRUD adapter"
      contains: "SecItemDelete"
    - path: "SiriusMacTests/KeychainCredentialStoreTests.swift"
      provides: "Isolated Keychain add/read/update/delete and post-success ordering tests"
      contains: "KeychainCredentialStoreTests"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift"
      provides: "Memory-first sign-out and honest deletion-failure tests"
      contains: "SignOutTests"
  key_links:
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      to: "SiriusMac/Security/KeychainCredentialStore.swift"
      via: "Public CredentialStore protocol; app injects the Keychain implementation"
      pattern: "CredentialStore"
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      via: "Public signOut delegates to actor-owned memory-first cleanup"
      pattern: "signOut"
  prohibitions:
    - statement: "MUST NOT retain a credential after an unsupported/control-protected outcome or claim sign-out succeeded when Keychain deletion failed."
      status: unverified
      flagged: true
---

<objective>
Add the sole Phase 1 secure persistence integration and truthful sign-out semantics: app-owned Keychain CRUD, post-success-only storage, and memory-first cleanup.

Purpose: Make credential/session lifecycle safe regardless of whether the final selected authentication result is supported or unsupported.
Output: Keychain adapter/integration tests, session sign-out ordering, and explicit deletion-failure result.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md

<interfaces>
Implement the public `CredentialStore` protocol created by Plan 01-01. `SessionCoordinator` owns ordering and never imports Security; `KeychainCredentialStore` is app-internal and maps OSStatus to safe app errors without exposing account, service, credential, or token values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-04-01: Implement isolated app-owned Keychain credential CRUD</name>
  <precondition>`xcodebuild -version` succeeds under a selected full Xcode developer directory so Security.framework integration tests can run.</precondition>
  <read_first>
    - SiriusMac.xcodeproj/project.pbxproj
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
  </read_first>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/Security/KeychainCredentialStore.swift, SiriusMacTests/KeychainCredentialStoreTests.swift</files>
  <behavior>
    - Test 1: a unique test service/account can add, read, update, and delete synthetic credential bytes using SecItem APIs.
    - Test 2: duplicate save updates the intended app-scoped item rather than creating an ambiguous duplicate.
    - Test 3: missing item and OSStatus failures map to safe typed errors with no credential/service/account description.
    - Test 4: rejected/challenge/unsupported/not-entitled/cancelled paths never invoke save.
  </behavior>
  <action>Link Security.framework and implement `KeychainCredentialStore`, `KeychainStoreError`, and an internal `KeychainItemDescriptor` using `SecItemAdd`, `SecItemCopyMatching`, `SecItemUpdate`, and `SecItemDelete`. Scope production items to the app bundle/service and use randomized synthetic test service/account identifiers with guaranteed cleanup. Conform to the semantic `CredentialStore` protocol; do not add UserDefaults, SwiftData, file, cloud, fixture, or library-owned persistence. Do not include OSStatus-derived arbitrary text or secret-bearing query values in diagnostics.</action>
  <verify>
    <automated>xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/KeychainCredentialStoreTests test</automated>
    <manual>Inspect the test cleanup and production query: tests touch only randomized app-scoped entries, and every failure message is a safe classification.</manual>
  </verify>
  <acceptance_criteria>
    - All four SecItem lifecycle operations are exercised against isolated synthetic entries.
    - No non-Keychain disk fallback exists.
    - Non-success authentication outcomes have zero persistence calls.
  </acceptance_criteria>
  <done>The app owns a narrowly scoped, tested Keychain CRUD adapter with safe errors and no alternative secret store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-04-02: Enforce memory-first sign-out and honest deletion failure</name>
  <read_first>
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - SiriusMac/Security/KeychainCredentialStore.swift
    - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift</files>
  <behavior>
    - Test 1: sign-out retires any active attempt/session and erases actor-held material before calling credential deletion.
    - Test 2: successful deletion returns signed out and a second sign-out is idempotent.
    - Test 3: failed deletion leaves memory signed out but returns a safe `credentialCleanupFailed` result instead of success.
    - Test 4: cancellation during sign-out cannot restore session state or skip the attempted deletion result.
  </behavior>
  <action>Add `SiriusXMClient.signOut()` and implement its ordering in `SessionCoordinator`: cancel/retire active attempt, clear actor-private session/credential references, then call the injected store deletion exactly once. Return semantic `SignOutOutcome.signedOut` or `.credentialCleanupFailed` without raw OSStatus/error strings. Preserve signed-out memory state even when deletion fails so callers can surface that local stored credentials still require attention. Do not retry deletion automatically or report complete cleanup optimistically.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter SignOutTests</automated>
    <manual>Review the spy store event sequence: memory cleanup precedes deletion in every case, and failure remains visible to the caller.</manual>
  </verify>
  <acceptance_criteria>
    - Event-order tests prove memory-first cleanup.
    - Sign-out is idempotent and never reactivates session state.
    - Deletion failure is explicit, safe, and not retried automatically.
  </acceptance_criteria>
  <done>Sign-out clears volatile state first, deletes app-owned credentials once, and reports incomplete local cleanup honestly.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `KeychainCredentialStore.swift`, `KeychainCredentialStoreTests.swift`, and `SignOutTests.swift`, and modifies the Xcode project, public client façade, and `SessionCoordinator`. App-internal symbols: `KeychainCredentialStore`, `KeychainStoreError`, and `KeychainItemDescriptor`. Public symbol behavior expanded: `SiriusXMClient.signOut()` and `SignOutOutcome`. No Keychain implementation symbol becomes public in the library.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to its primary trust boundaries. Every critical or high-severity threat below is blocking: if its mitigation cannot be implemented or verified, stop the plan without weakening the control.

## Trust Boundaries

| Boundary | Description |
|---|---|
| App → Keychain Services | Reusable credential bytes cross from transient app state into the only authorized persistence service. |
| Session actor → credential store | Sign-out and post-success save ordering determine whether secrets survive incorrectly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03 | Information Disclosure | KeychainCredentialStore | high | mitigate | SecItem-only app-scoped storage, post-entitled-success save, safe error mapping, isolated synthetic tests, no fallback store. |
| T-01-04 | Denial of Service / Repudiation | Sign-out cleanup | medium | mitigate | One deletion attempt, no automatic retry, idempotent state, and explicit failure outcome. |
| T-01-06 | Information Disclosure | Keychain errors/diagnostics | high | mitigate | Error types expose classification only; no account/service/query/OSStatus text reaches safe diagnostics. |
| T-01-SC | Tampering | Xcode/package dependencies | high | mitigate | Security.framework is OS-bundled; no package-manager install is authorized. |
</threat_model>

<verification>
- Full-Xcode Keychain integration tests pass with isolated synthetic entries and cleanup.
- Package sign-out ordering/idempotency/failure tests pass.
- Repository search and project review find no alternate secret persistence implementation.
</verification>

<success_criteria>
- Only an entitled success may trigger app-owned Keychain persistence.
- Sign-out always clears in-memory state before storage deletion.
- Incomplete Keychain cleanup is visible without disclosing sensitive details.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md` when done.
</output>
