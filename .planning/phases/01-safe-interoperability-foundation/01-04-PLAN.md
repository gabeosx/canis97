---
phase: 01-safe-interoperability-foundation
plan: "04"
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac/Security/KeychainCredentialStore.swift
  - SiriusMacTests/KeychainCredentialStoreTests.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift
autonomous: true
requirements: [AUTH-03, SECR-01, SECR-02, CLNT-04]
estimate:
  tokens: 36000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Only an authenticated-and-entitled runtime result may save allowed reusable credential material to app-owned Keychain storage."
    - "Sign-out cancels work and clears actor-held material before any external cleanup."
    - "Keychain deletion and browser-residue cleanup are injected, independently reported operations; failure never restores session state or claims complete cleanup."
    - "No preferences, SwiftData, file, fixture, or library-owned fallback stores secrets."
  artifacts:
    - path: "SiriusMac/Security/KeychainCredentialStore.swift"
      provides: "App-owned Security.framework credential CRUD"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift"
      provides: "Memory-first, multi-cleaner, honest-failure ordering tests"
  prohibitions:
    - statement: "MUST NOT persist before entitlement, retry cleanup automatically, or report signed out cleanly while any injected cleaner failed."
      status: unverified
      flagged: true
---

<objective>
Add app-owned Keychain persistence and a memory-first sign-out pipeline that can incorporate the WebView cookie cleaner from Plan 01-06.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-04-01: Implement isolated app-owned Keychain CRUD</name>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/Security/KeychainCredentialStore.swift, SiriusMacTests/KeychainCredentialStoreTests.swift</files>
  <behavior>
    - Test 1: randomized app-scoped entries add, read, update, and delete synthetic secret bytes with SecItem APIs.
    - Test 2: duplicate save updates one intended item.
    - Test 3: missing/failure outcomes map to safe classifications without service/account/secret/OSStatus text.
    - Test 4: every outcome other than authenticated-and-entitled produces zero save calls.
  </behavior>
  <action>Implement `KeychainCredentialStore` with `SecItemAdd`, `SecItemCopyMatching`, `SecItemUpdate`, and `SecItemDelete`. Scope items to the app; clean randomized test entries deterministically. Conform to the semantic store protocol and create no alternate secret persistence.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/KeychainCredentialStoreTests test</automated></verify>
  <acceptance_criteria>
    - All Keychain lifecycle operations are integration tested.
    - Persistence is post-entitlement only.
    - Error presentation contains classifications only.
  </acceptance_criteria>
  <done>The app owns one narrow Keychain store with safe lifecycle behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-04-02: Enforce memory-first aggregate sign-out cleanup</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SignOutTests.swift</files>
  <behavior>
    - Test 1: sign-out retires the attempt/session and clears actor memory before invoking Keychain or browser-residue cleaners.
    - Test 2: both cleaners are attempted once; success is idempotent.
    - Test 3: either/both cleaner failures produce an explicit aggregate cleanup-failed outcome while memory stays signed out.
    - Test 4: cancellation cannot restore session state or skip recording invoked cleaner outcomes.
  </behavior>
  <action>Add `AuthenticationResidueCleaner` as a narrow semantic injected cleanup seam and implement `SiriusXMClient.signOut()` in `SessionCoordinator`. Clear internal state first, then invoke Keychain deletion and residue cleanup exactly once, aggregating safe failure classes. Plan 01-06 provides the WebKit implementation; tests here use fakes.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter SignOutTests</automated></verify>
  <acceptance_criteria>
    - Event-order tests prove memory-first cleanup.
    - Partial cleanup is explicit and never reactivates the session.
    - No cleaner is automatically retried.
  </acceptance_criteria>
  <done>Sign-out clears volatile state first and reports all incomplete local cleanup honestly.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-04-SUMMARY.md` when done.</output>
