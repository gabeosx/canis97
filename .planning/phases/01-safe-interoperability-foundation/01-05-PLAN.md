---
phase: 01-safe-interoperability-foundation
plan: "05"
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/UnsupportedAuthenticationView.swift
  - SiriusMacTests/AuthenticationPresentationModelTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, SECR-03, CLNT-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The app exposes only the typed authentication/entitlement/cleanup state supplied by SiriusXMClient and never inspects adapter, browser, cookie, header, or response detail."
    - "Unsupported compatibility is a dedicated native screen stating that authentication is unsupported, no credential was retained, and no workaround was attempted (D-02)."
    - "Retry is an explicit user action that invokes at most one client attempt and never selects another method or schedules a retry."
    - "The official-site action opens a fixed first-party HTTPS URL in the user's real browser and never reads return/browser state."
    - "Visible diagnostics are limited to safe semantic classification/version values; no raw log/support export exists."
  artifacts:
    - path: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      provides: "Main-actor projection of typed client state and explicit user actions"
      contains: "AuthenticationPresentationModel"
    - path: "SiriusMac/Authentication/UnsupportedAuthenticationView.swift"
      provides: "Dedicated fail-closed compatibility UI"
      contains: "UnsupportedAuthenticationView"
    - path: "SiriusMacTests/AuthenticationPresentationModelTests.swift"
      provides: "Retry cardinality, semantic projection, and redaction tests"
      contains: "AuthenticationPresentationModelTests"
  key_links:
    - from: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift"
      via: "Main-actor model awaits public typed operations only"
      pattern: "SiriusXMClient"
    - from: "SiriusMac/Authentication/UnsupportedAuthenticationView.swift"
      to: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      via: "View renders semantic state and forwards explicit Retry/site actions"
      pattern: "AuthenticationPresentationModel"
  prohibitions:
    - statement: "MUST NOT present a method picker, automatic browser/native fallback, partially authenticated player shell, or workaround language (D-01, D-02)."
      status: unverified
      flagged: true
---

<objective>
Turn typed client outcomes into the complete native unsupported-authentication experience, including explicit Retry, official-site navigation, and safe diagnostic wording.

Purpose: Give subscribers an honest, useful Phase 1 experience even when no safe SiriusXM authentication path can be supported.
Output: Presentation model, dedicated SwiftUI compatibility screen, app composition, and deterministic UI-state tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md

<interfaces>
The presentation model may import only the public package product and maps `AuthenticationOutcome`, `EntitlementState`, `AuthenticationCapability`, `SignOutOutcome`, and safe diagnostic classifications into view state. It must not import Foundation networking or internal package modules.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-05-01: Wire typed client outcomes into a reachable dedicated compatibility screen</name>
  <precondition>`xcodebuild -version` succeeds under a selected full Xcode developer directory.</precondition>
  <read_first>
    - SiriusMac/SiriusMacApp.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
  </read_first>
  <files>SiriusMac/SiriusMacApp.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/UnsupportedAuthenticationView.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift</files>
  <behavior>
    - Test 1: unsupported state renders the three required facts—unsupported, no retained credential, no workaround—without player/library state.
    - Test 2: rejection, challenge, not-entitled, cleanup-failure, and unknown/unsupported remain distinct presentation states.
    - Test 3: only public domain values enter the model; safe diagnostics render fixed labels and non-identifying version/classification values.
  </behavior>
  <action>Create `@MainActor @Observable AuthenticationPresentationModel`, `AuthenticationPresentationState`, and `UnsupportedAuthenticationView`, then make it the reachable app scene. The model consumes only `SiriusXMClient` public semantic operations and translates each outcome into explicit copy. The unsupported state must state D-02's three facts and must not embed a player, library, WebView, browser session, response viewer, raw error, log archive, or authentication method selector. Keep accessibility labels/keyboard reachability native even though later visual design is out of scope.</action>
  <verify>
    <automated>xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests test</automated>
    <manual>Launch the app locally and verify the dedicated screen states the three required facts and exposes no player/library controls; this is UI observation only, not live authentication.</manual>
  </verify>
  <acceptance_criteria>
    - The compatibility screen is the app's reachable root when authentication is unavailable.
    - Every semantic outcome is distinct and no raw upstream detail is displayed.
    - No partial player shell, WebView, or sign-in method selector exists.
  </acceptance_criteria>
  <done>The native app presents a truthful, typed, fail-closed compatibility state as its complete Phase 1 user surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-05-02: Add explicit single-attempt Retry and fixed official-site navigation</name>
  <read_first>
    - SiriusMac/Authentication/AuthenticationPresentationModel.swift
    - SiriusMac/Authentication/UnsupportedAuthenticationView.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
  </read_first>
  <files>SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/UnsupportedAuthenticationView.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift</files>
  <behavior>
    - Test 1: one Retry click creates one awaited client call and disables/rejects another while it is in flight.
    - Test 2: unsupported/challenge/rate-limit outcomes do not schedule a retry or select an alternate method.
    - Test 3: official-site action exposes only the fixed first-party HTTPS destination through the system openURL environment.
    - Test 4: diagnostic details contain only safe allow-listed labels and cannot accept arbitrary strings.
  </behavior>
  <action>Add explicit Retry and official SiriusXM-site controls to the compatibility screen. Retry must await exactly one public client action, reflect in-flight state, and return to the semantic result without a timer, loop, fallback, or rapid re-enable policy; the conservative cooldown belongs to the human viability gate. Use SwiftUI's system URL-opening environment with a fixed official HTTPS destination and never observe the browser after opening it. Show only safe classification/version diagnostics already admitted by the typed sink.</action>
  <verify>
    <automated>xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests test</automated>
    <manual>With the unavailable adapter, click Retry twice quickly: only one in-flight call is represented and the result stays unsupported. The site control opens the real browser; the app receives no browser data.</manual>
  </verify>
  <acceptance_criteria>
    - Retry is user-initiated, single-attempt, and never automatic.
    - Official-site navigation is fixed, external, and one-way.
    - Diagnostics remain allow-listed and non-exporting.
  </acceptance_criteria>
  <done>The unsupported experience offers useful explicit actions without adding a second path, browser observation, automatic retry, or sensitive diagnostics.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `AuthenticationPresentationModel.swift`, `UnsupportedAuthenticationView.swift`, and `AuthenticationPresentationModelTests.swift`, and modifies `SiriusMacApp.swift`. App-internal symbols: `AuthenticationPresentationModel`, `AuthenticationPresentationState`, `UnsupportedAuthenticationView`, and fixed official-site action helpers. It consumes the public `SiriusXMClient` symbols and creates no public library symbol.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to its primary trust boundaries. Every critical or high-severity threat below is blocking: if its mitigation cannot be implemented or verified, stop the plan without weakening the control.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Public client → presentation model | Typed semantic state becomes subscriber-visible copy and actions. |
| App → real browser | The app may open a fixed official site but must receive or inspect no authenticated browser state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Official-site navigation | high | mitigate | System openURL only; fixed HTTPS destination; no WebView/cookie/storage/return inspection. |
| T-01-02 | Elevation of Privilege | Retry and compatibility UI | high | mitigate | Typed unsupported/control state, no workaround language, no alternate method, no player shell. |
| T-01-04 | Denial of Service | Retry action | high | mitigate | Main-actor in-flight guard and actor-side lease; tests assert one client call and no timer/loop. |
| T-01-06 | Information Disclosure | Diagnostic details UI | high | mitigate | Closed allow-listed labels only; no raw log archive or export action. |
| T-01-SC | Tampering | App dependencies | high | mitigate | SwiftUI is OS-bundled; no package-manager install is authorized. |
</threat_model>

<verification>
- Full-Xcode presentation model tests pass.
- Local non-live launch shows the dedicated compatibility screen and explicit actions.
- Source dependency review shows no app networking/browser-state code and no internal package import.
</verification>

<success_criteria>
- Subscribers receive honest, actionable unsupported/rejection/challenge/entitlement/cleanup states.
- Retry remains a single explicit action and official-site navigation remains one-way.
- The app cannot imply supported authentication before the later manual gate.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md` when done.
</output>
