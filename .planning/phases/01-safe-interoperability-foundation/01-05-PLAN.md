---
phase: 01-safe-interoperability-foundation
plan: "05"
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - SiriusMac/SiriusMacApp.swift
  - SiriusMac/Authentication/AuthenticationPresentationModel.swift
  - SiriusMac/Authentication/AuthenticationView.swift
  - SiriusMac/Authentication/UnsupportedAuthenticationView.swift
  - SiriusMacTests/AuthenticationPresentationModelTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SECR-03, CLNT-01]
estimate:
  tokens: 32000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The app has one sign-in method: the settled in-app WebView path."
    - "Presentation consumes semantic client/bridge state and never displays token, cookie, request, response, or raw error detail."
    - "Authentication, authenticated-but-not-entitled, entitled, rejection, challenge, unsupported, signed-out, and cleanup-failure states remain distinct."
    - "A user action starts at most one attempt; no method selector, external-browser fallback, credential form, timer, or automatic retry exists."
    - "Terminal unsupported state contains no partial player/library shell."
  artifacts:
    - path: "SiriusMac/Authentication/AuthenticationPresentationModel.swift"
      provides: "Main-actor projection of the single WebView/native-request flow"
    - path: "SiriusMac/Authentication/AuthenticationView.swift"
      provides: "Native sign-in container for the WebView bridge"
  prohibitions:
    - statement: "MUST NOT reintroduce real-browser callback selection, native credentials, official-site fallback, browser/native method choice, or player UI."
      status: unverified
      flagged: true
---

<objective>
Create the complete typed native authentication presentation for the one settled WebView-token/native-request path.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md
@.planning/phases/01-safe-interoperability-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-05-01: Render the complete semantic authentication state machine</name>
  <files>SiriusMac/SiriusMacApp.swift, SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMac/Authentication/UnsupportedAuthenticationView.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift</files>
  <behavior>
    - Test 1: waiting/WebView, verifying-authentication, verifying-entitlement, authenticated-not-entitled, entitled, rejection, challenge, unsupported, signed-out, and cleanup-failure states render distinct fixed copy.
    - Test 2: only the entitled state exposes sign-out/readiness; no earlier state exposes player/library controls.
    - Test 3: terminal compatibility copy states that the flow is unsupported, no workaround was attempted, and cleanup status is truthful.
    - Test 4: safe diagnostics accept fixed classifications only.
  </behavior>
  <action>Create `@MainActor @Observable AuthenticationPresentationModel` and reachable `AuthenticationView`. The view reserves a native container for the WebKit bridge implemented in Plan 01-06 and renders only semantic state. Keep `UnsupportedAuthenticationView` as the terminal state within this single method, not as another composition. Preserve keyboard/VoiceOver semantics.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests test</automated></verify>
  <acceptance_criteria>
    - All auth/entitlement/cleanup states are semantically distinct.
    - Entitled is the only state that unlocks continuation.
    - No raw upstream or secret detail enters presentation.
  </acceptance_criteria>
  <done>The native app has a complete typed Phase 1 authentication surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-05-02: Enforce one explicit in-flight WebView sign-in action</name>
  <files>SiriusMac/Authentication/AuthenticationPresentationModel.swift, SiriusMac/Authentication/AuthenticationView.swift, SiriusMac/Authentication/UnsupportedAuthenticationView.swift, SiriusMacTests/AuthenticationPresentationModelTests.swift</files>
  <behavior>
    - Test 1: Sign In starts one bridge action and disables/rejects another while in flight.
    - Test 2: Retry resets and repeats only the same WebView path after a terminal result.
    - Test 3: challenge/rate-limit/unknown/cleanup failures schedule no timer, poll, alternate method, or follow-up request.
    - Test 4: sign-out is visible only for an active entitled session and preserves cleanup failure detail safely.
  </behavior>
  <action>Wire explicit Sign In, Use Logged-In Session, Retry, and Sign Out intents to injected semantic collaborators. `Use Logged-In Session` is the only action that asks the later bridge to extract a token. Keep one main-actor in-flight guard in addition to the client actor lease. Do not add official-site navigation as an authentication alternative.</action>
  <verify><automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/AuthenticationPresentationModelTests test</automated></verify>
  <acceptance_criteria>
    - Every attempt is explicit and single-flight.
    - The only retry repeats the same settled path.
    - No alternate sign-in surface or partial player shell exists.
  </acceptance_criteria>
  <done>The UI safely drives one explicit WebView authentication lifecycle.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-05-SUMMARY.md` when done.</output>
