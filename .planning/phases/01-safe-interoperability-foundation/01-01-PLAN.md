---
phase: 01-safe-interoperability-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme
  - SiriusMac/SiriusMacApp.swift
  - SiriusMacTests/CompatibilityTracerTests.swift
  - Packages/SiriusXMClient/Package.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
  - Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift
autonomous: true
requirements: [CLNT-01, CLNT-02, CLNT-04]
estimate:
  tokens: 36000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Production scaffolding begins immediately; no Phase 0 artifact, GO string, harness command, or authentication experiment is a precondition."
    - "A native macOS target consumes an independently testable SiriusXMClient SwiftPM product."
    - "Public APIs are semantic and typed; endpoints, cookie names, tokens, headers, requests, responses, and raw schemas stay out of public models."
    - "The app can render a typed unavailable/waiting authentication state before the later WebView bridge is composed."
    - "Catalog, metadata, and live-stream resolution remain typed unavailable with zero provider work in Phase 1."
  artifacts:
    - path: "SiriusMac.xcodeproj/project.pbxproj"
      provides: "Native app/test targets with local SiriusXMClient dependency"
    - path: "Packages/SiriusXMClient/Package.swift"
      provides: "Reusable library and unconditional test targets"
    - path: "Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift"
      provides: "Independent-consumer compile contract"
  key_links:
    - from: "SiriusMac/SiriusMacApp.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift"
      via: "App awaits semantic client state"
  prohibitions:
    - statement: "MUST NOT read or validate Phase 0 planning artifacts, run the feasibility harness, contact SiriusXM, or conditionally exclude tests based on .planning contents."
      status: unverified
      flagged: true
---

<objective>
Create the native walking skeleton and reusable public client boundary without any Phase 0 execution gate.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-PATTERNS.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01-01: Create the native app-to-client walking skeleton</name>
  <precondition>`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -version` succeeds. This toolchain check is the only precondition.</precondition>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/SiriusMacApp.swift, SiriusMacTests/CompatibilityTracerTests.swift, Packages/SiriusXMClient/Package.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift</files>
  <behavior>
    - Test 1: the app target launches a main-actor presentation model backed by the local package product.
    - Test 2: before authentication composition exists, the client returns a typed unavailable/waiting result with zero transport calls.
    - Test 3: repeated explicit presentation actions create no background retry work.
  </behavior>
  <action>Create a macOS 26 SwiftUI app/test target with Swift 6 mode and complete concurrency checking. Add the local `Packages/SiriusXMClient` product with no external dependencies. Build a minimal authentication/compatibility scene using a deterministic unavailable adapter as temporary composition only; it is replaced by Plans 01-06/01-07, not exposed as a second sign-in method. Do not inspect Phase 0, start a WebView, or contact SiriusXM.</action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/CompatibilityTracerTests test</automated>
  </verify>
  <acceptance_criteria>
    - Native app and app tests build through full Xcode.
    - App-to-package dependency direction is one-way.
    - No Phase 0 artifact or provider request participates in the build/test path.
  </acceptance_criteria>
  <done>The native walking skeleton reaches a real semantic client outcome with deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-02: Publish the semantic client contract and independent-consumer proof</name>
  <files>SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme, Packages/SiriusXMClient/Package.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift, Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift</files>
  <behavior>
    - Test 1: a non-`@testable` consumer imports `SiriusXMClient` and awaits authentication, entitlement, sign-out, catalog, metadata, and stream-resolution operations.
    - Test 2: public values expose semantic outcomes only and have no endpoint/cookie/token/header/request/response/raw-schema fields.
    - Test 3: content operations return domain-specific typed unavailable results with zero transport calls.
    - Test 4: all package test targets compile unconditionally and the manifest has no `.planning`-dependent source inclusion.
  </behavior>
  <action>Define the public façade and Sendable semantic models/capabilities. Include an opaque, non-Codable, non-describable `AuthenticationCredential` and narrow `CredentialSource`/`CredentialStore` seams so the later app-owned WebView bridge can transfer secret bytes without exposing SiriusXM cookie semantics. Keep raw storage private and prevent debug/description reflection from rendering it. Keep all content domains typed unavailable in Phase 1. Add a shared scheme and unconditional package test targets.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter PublicConsumerTests &amp;&amp; DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' build</automated>
  </verify>
  <acceptance_criteria>
    - Another Apple-platform target consumes the product without internal imports.
    - Secret/wire details do not appear in public field labels or descriptions.
    - Package/test compilation is independent of mutable planning files.
  </acceptance_criteria>
  <done>The project has a stable semantic client contract, native consumer, and unconditional test graph.</done>
</task>

</tasks>

<verification>
- Focused app and public-consumer tests pass.
- Production/project files contain no dependency on the Phase 0 harness or planning artifacts.
</verification>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md` when done.</output>
