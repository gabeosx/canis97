---
phase: 01-safe-interoperability-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - SiriusMac.xcodeproj/project.pbxproj
  - SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme
  - SiriusMac/SiriusMacApp.swift
  - SiriusMacTests/CompatibilityTracerTests.swift
  - Packages/SiriusXMClient/Package.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
  - Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift
autonomous: true
requirements: [CLNT-01, CLNT-02, CLNT-04]
user_setup:
  - service: xcode
    why: "A selected full Xcode developer directory is required to build and test the native macOS target; installation or selection requires user authorization."
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before any production file is created, Phase 0 evidence, selection, owner result, and decision pass strict validation, fresh deterministic derivation/equality, and exactly `GO browser-return|GO native-direct` plus `Phase 1 continuation: unlocked`; missing, malformed, NO-GO, or mismatched artifacts block execution."
    - "A subscriber can launch the native app, invoke Retry, and receive an explicit unsupported compatibility outcome through SiriusXMClient without any live SiriusXM request (D-02)."
    - "An independent Apple-platform consumer can import the SiriusXMClient product and call typed async authentication, entitlement, catalog, metadata, and live-stream-resolution capabilities without endpoint, cookie, header, or raw-schema knowledge (CLNT-02)."
    - "Until later phases add evidence-backed content adapters, catalog, metadata, and live-stream-resolution operations return their typed unavailable capability/error outcomes without a transport call, catalog fetch, metadata loop, stream resolution, or playback work."
    - "The app target depends on the local package product; the package does not import SwiftUI, AppKit, Security, or app state."
    - "Full Xcode absence stops native build/test execution transparently instead of weakening verification."
  artifacts:
    - path: "SiriusMac.xcodeproj/project.pbxproj"
      provides: "Native macOS app and app-test target consuming the local SwiftPM product"
      contains: "SiriusXMClient"
    - path: "Packages/SiriusXMClient/Package.swift"
      provides: "Independently consumable SiriusXMClient library and test products"
      contains: ".library"
    - path: "SiriusMac/SiriusMacApp.swift"
      provides: "Native walking-skeleton scene and explicit compatibility interaction"
      contains: "@main"
    - path: "Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift"
      provides: "External-consumer compile/use contract for authentication, entitlement, catalog, metadata, and live-stream resolution"
      contains: "import SiriusXMClient"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift"
      provides: "Endpoint-free public domain models, capability values, and typed errors for catalog, metadata, and live-stream resolution"
      contains: "LiveStreamResolutionError"
  key_links:
    - from: ".planning/phases/00-authentication-feasibility-gate/00-DECISION.md"
      to: "SiriusMac.xcodeproj/project.pbxproj"
      via: "The first production task begins with Phase 0 validate/derive/compare and exact GO+unlocked gate before creating any file"
      pattern: "GO browser-return|GO native-direct"
    - from: "SiriusMac/SiriusMacApp.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift"
      via: "The SwiftUI action awaits the public semantic client operation"
      pattern: "SiriusXMClient"
    - from: "SiriusMac.xcodeproj/project.pbxproj"
      to: "Packages/SiriusXMClient/Package.swift"
      via: "Local Swift package reference and product dependency"
      pattern: "XCLocalSwiftPackageReference"
    - from: "Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift"
      via: "A non-@testable consumer compiles and awaits each CLNT-02 semantic capability/operation while observing typed unavailable errors only"
      pattern: "CatalogCapability|MetadataCapability|LiveStreamResolutionCapability"
  prohibitions:
    - statement: "MUST NOT present a partially authenticated player shell or imply playback availability when authentication compatibility is unsupported (D-02)."
      status: unverified
      flagged: true
---

<objective>
Create the production walking skeleton: a real native macOS interaction crosses the app-to-library boundary and ends in a truthful fail-closed compatibility outcome without probing a live account.

Purpose: Prove the native project, reusable package boundary, typed async contract, and local build/test loop before expanding the volatile interoperability surface.
Output: Native Xcode scaffold, local SwiftPM product, compatibility tracer, shared scheme, and public-consumer contract test.
</objective>

## Phase Goal

**As a** SiriusXM subscriber, **I want to** establish or end an authorized SiriusXM session without exposing secrets or weakening access controls, **so that** the reusable Apple-platform client can support a dependable native player safely.

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-PATTERNS.md
@.planning/phases/01-safe-interoperability-foundation/01-VALIDATION.md
@.planning/phases/01-safe-interoperability-foundation/SKELETON.md
@.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md
@.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md
@.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md
@.planning/phases/00-authentication-feasibility-gate/00-DECISION.md

<interfaces>
This greenfield plan creates the first interface contracts. Keep the public surface semantic: `SiriusXMClient`, `AuthenticationOutcome`, `EntitlementState`, `AuthenticationCapability`, `SignOutOutcome`, `CredentialStore`, `RedactedDiagnosticsSink`, `ChannelID`, `ChannelCatalog`, `NowPlayingMetadata`, `LiveStreamResource`, `CatalogCapability`, `MetadataCapability`, `LiveStreamResolutionCapability`, `CatalogError`, `MetadataError`, and `LiveStreamResolutionError`. The app imports these values only. Network requests, responses, paths, hosts, redirects, cookies, headers, raw JSON, adapter decoders, and resolved resource internals remain internal.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 01-01-01: Prove the native app-to-client unsupported path end to end</name>
  <precondition>The Phase 0 harness strictly validates evidence/owner result, freshly derives byte-identical selection/decision, and the decision is exactly GO browser-return or GO native-direct with Phase 1 continuation unlocked; `xcodebuild -version` also succeeds under a selected full Xcode developer directory. Any failed predicate blocks before a production file is created.</precondition>
  <reversibility rating="costly">The native app plus first-class SwiftPM package boundary is inherited by every later phase and changing it would coordinate app, library, test, and release call sites.</reversibility>
  <read_first>
    - .planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
    - .planning/phases/01-safe-interoperability-foundation/SKELETON.md
  </read_first>
  <files>SiriusMac.xcodeproj/project.pbxproj, SiriusMac/SiriusMacApp.swift, SiriusMacTests/CompatibilityTracerTests.swift, Packages/SiriusXMClient/Package.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift</files>
  <behavior>
    - Test 1: launching the presentation model and invoking its explicit Retry action awaits `SiriusXMClient` and yields the typed unsupported state.
    - Test 2: the tracer records zero transport/authentication attempts because no evidence-selected live adapter exists.
    - Test 3: a second explicit Retry remains deterministic and cannot create background retry work.
  </behavior>
  <action>Before creating or editing any production path, run the exact Phase 0 gate from this task's automated command through the decision/continuation assertions. Stop immediately on missing/unreadable/malformed evidence, selection, owner result, or decision; derivation mismatch; `NO-GO unsupported`; blocked continuation; or unavailable full Xcode. Do not create a package, project, source, test, or placeholder on gate failure. After the gate passes, create a native macOS 26 SwiftUI app target and test target in `SiriusMac.xcodeproj`, with Swift 6 language mode and complete concurrency checking. Add a local package reference to `Packages/SiriusXMClient`, whose manifest declares an Apple-platform library product with no external dependencies. Implement a production `UnavailableAuthenticationAdapter` behind `SiriusXMClient` so the app's visible Retry interaction returns the D-02 unsupported compatibility outcome without constructing a request, reading browser state, or touching credentials. This remains the mechanically safe production default until later Phase 1 tasks compose the already validated Phase 0 selected path; it does not re-evaluate feasibility or perform live authentication. The app scene must show authentication unsupported, no credential retained, and no workaround attempted; do not add catalog, playback, skins, or a player shell. Start with the failing app test, then make the single path pass.</action>
  <verify>
<automated>evidence=.planning/phases/00-authentication-feasibility-gate/00-EVIDENCE.md; selection=.planning/phases/00-authentication-feasibility-gate/00-SELECTION.md; owner=.planning/phases/00-authentication-feasibility-gate/00-OWNER-RESULT.md; decision_file=.planning/phases/00-authentication-feasibility-gate/00-DECISION.md; derived_selection=$(mktemp) || exit 1; derived_decision=$(mktemp) || { rm -f "$derived_selection"; exit 1; }; trap 'rm -f "$derived_selection" "$derived_decision"' EXIT; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-evidence "$evidence" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-selection --evidence "$evidence" --output "$derived_selection" &amp;&amp; cmp -s "$derived_selection" "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-selection "$selection" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-owner-result "$owner" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility derive-decision --selection "$selection" --evidence "$evidence" --owner-result "$owner" --output "$derived_decision" &amp;&amp; cmp -s "$derived_decision" "$decision_file" &amp;&amp; swift run --package-path Spikes/AuthenticationFeasibility auth-feasibility validate-decision "$decision_file" &amp;&amp; decision=$(sed -n 's/^Feasibility decision: //p' "$decision_file") &amp;&amp; continuation=$(sed -n 's/^Phase 1 continuation: //p' "$decision_file") &amp;&amp; case "$decision:$continuation" in 'GO browser-return:unlocked'|'GO native-direct:unlocked') true;; *) false;; esac &amp;&amp; xcodebuild -version &gt;/dev/null &amp;&amp; xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' -only-testing:SiriusMacTests/CompatibilityTracerTests test</automated>
    <manual>After the automated test passes, the local run command in SKELETON.md may be used to observe the same unsupported screen; this observation is not a live-auth or approval gate.</manual>
  </verify>
  <acceptance_criteria>
    - The native target builds through full Xcode and consumes the local package product.
    - Retry crosses app presentation into the client and returns unsupported with zero network attempts.
    - The screen contains no playback/library shell and makes no supported-authentication claim.
  </acceptance_criteria>
  <done>The native walking skeleton proves a production app-to-client fail-closed path with an automated end-to-end test and no live account interaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-02: Publish the semantic client contract and independent-consumer proof</name>
  <reversibility rating="costly">The public semantic contract will be consumed by the app and later Apple-platform clients; coordinated changes remain possible before a release but affect multiple phases.</reversibility>
  <read_first>
    - Packages/SiriusXMClient/Package.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - SiriusMac.xcodeproj/project.pbxproj
    - .planning/phases/01-safe-interoperability-foundation/01-PATTERNS.md
  </read_first>
  <files>SiriusMac.xcodeproj/xcshareddata/xcschemes/SiriusMac.xcscheme, Packages/SiriusXMClient/Package.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift, Packages/SiriusXMClient/Tests/PublicAPITests/PublicConsumerTests.swift</files>
  <behavior>
    - Test 1: an external-style test target imports only `SiriusXMClient` and exercises typed async capability, sign-in, entitlement, sign-out, catalog, metadata, and live-stream-resolution values.
    - Test 2: catalog, metadata, and live-stream-resolution capability queries and operations each return their domain-specific typed unavailable state/error in Phase 1 and record zero transport calls.
    - Test 3: public Sendable values and their reflected public field labels contain no endpoint, URL request/response, header, cookie, token, or raw-schema/payload detail; the consumer test uses no `@testable` import or internal adapter symbol.
    - Test 4: the package manifest resolves with zero external package dependencies.
  </behavior>
  <action>Expand the tracer contract into the stable public façade required by CLNT-01/02/04. Define `SiriusXMClient`, `AuthenticationOutcome`, `EntitlementState`, `AuthenticationCapability`, `AuthenticationChallenge`, `UnsupportedAuthenticationReason`, `SignOutOutcome`, `AuthenticationCredential`, `CredentialStore`, and `RedactedDiagnosticsSink` as domain-oriented Sendable values/protocols. In the public model file, add semantic `ChannelID`, `ChannelCatalog`, `NowPlayingMetadata`, and opaque `LiveStreamResource` domain values; `CatalogCapability`, `MetadataCapability`, and `LiveStreamResolutionCapability`; and domain-specific `CatalogError`, `MetadataError`, and `LiveStreamResolutionError`. Expose exact typed async `SiriusXMClient` entry points `catalogCapability()`, `catalog()`, `metadataCapability(for:)`, `metadata(for:)`, `liveStreamResolutionCapability(for:)`, and `resolveLiveStream(for:)`; the three operations return their domain model with their domain-specific typed error. Their production Phase 1 implementation must return/throw the corresponding typed unavailable outcome before any transport call: this task defines the CLNT-02 repair boundary and does not implement an endpoint, catalog fetch, metadata loop, stream resolution, or playback. Keep the sole authentication path choice internal, and keep endpoint, host, callback, cookie, header, token-bearing URL, raw schema/payload, and resolved-resource internals out of public declarations. Add a shared Xcode scheme and a separate `PublicAPITests` SwiftPM test target that behaves like another Apple-platform consumer, uses no `@testable` import, compiles/awaits all five CLNT-02 domains, asserts the three content domains are typed unavailable with zero transport attempts, and audits the public value surface for forbidden wire-detail field labels.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter PublicConsumerTests &amp;&amp; xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' build</automated>
    <manual>Review the generated public declarations as an Apple-platform consumer: every operation is semantic and typed, and no caller must know how SiriusXM communicates on the wire.</manual>
  </verify>
  <acceptance_criteria>
    - The standalone product imports and compiles outside the app target.
    - Public APIs describe typed authentication, entitlement, catalog, metadata, and live-stream-resolution capabilities/outcomes/errors rather than upstream transport.
    - All three Phase 1 content operations return typed unavailable without provider-facing work, while independent-consumer tests compile and use their semantic contracts.
    - Package and Xcode manifests contain no third-party dependency installation.
  </acceptance_criteria>
  <done>The reusable library has endpoint-free typed async contracts across every CLNT-02 domain, deterministic unavailable content behavior, an independent consumer proof, and a shared native build scheme.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `SiriusMac.xcodeproj/project.pbxproj`, the shared `SiriusMac.xcscheme`, `SiriusMac/SiriusMacApp.swift`, `CompatibilityTracerTests.swift`, the client `Package.swift`, the public façade/model files, and `PublicConsumerTests.swift`. Public symbols: `SiriusXMClient`, `AuthenticationOutcome`, `EntitlementState`, `AuthenticationCapability`, `AuthenticationChallenge`, `UnsupportedAuthenticationReason`, `SignOutOutcome`, `AuthenticationCredential`, `CredentialStore`, `RedactedDiagnosticsSink`, `ChannelID`, `ChannelCatalog`, `NowPlayingMetadata`, `LiveStreamResource`, `CatalogCapability`, `MetadataCapability`, `LiveStreamResolutionCapability`, `CatalogError`, `MetadataError`, and `LiveStreamResolutionError`. Internal symbols: `UnavailableAuthenticationAdapter`, unavailable content-capability implementations, and the tracer presentation state.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Authorized session establishment/end through a safe reusable boundary | 01-01..01-08 | COVERED | Code ends safely unsupported unless the manual gate proves one path. |
| REQ | AUTH-01 | Explicit authentication and entitlement outcomes | 01-02, 01-05..01-08 | COVERED | Synthetic classification plus manual support gate. |
| REQ | AUTH-02 | Unknown/control-protected flows fail closed | 01-02, 01-03, 01-06..01-08 | COVERED | Stop matrix, zero retry, evidence-selected path. |
| REQ | AUTH-03 | Sign-out clears memory and stored credential | 01-04, 01-07, 01-08 | COVERED | Memory-first ordering and manual proof. |
| REQ | SECR-01 | Keychain-only credential persistence | 01-04, 01-07 | COVERED | App-owned adapter, post-success write. |
| REQ | SECR-02 | Ephemeral direct-only session material | 01-02, 01-03, 01-07 | COVERED | Actor ownership and direct-host transport. |
| REQ | SECR-03 | Redaction/exclusion by construction | 01-03, 01-05, 01-07, 01-08 | COVERED | Allow-listed event types and safe evidence. |
| REQ | CLNT-01 | Independent SwiftPM product | 01-01 | COVERED | Public-consumer target. |
| REQ | CLNT-02 | Typed async domain APIs, models, capabilities, and errors for authentication, entitlement, catalog, metadata, and live-stream resolution | 01-01, 01-02, 01-07 | COVERED | Phase 1 publishes every endpoint-free semantic contract; content operations return typed unavailable with zero provider work until later phases supply adapters. |
| REQ | CLNT-03 | Replaceable internal adapters | 01-02, 01-03, 01-07 | COVERED | Selected adapter remains internal. |
| REQ | CLNT-04 | Injected deterministic collaborators | 01-01..01-04, 01-07 | COVERED | Transport, clock, store, and safe diagnostics seams. |
| RESEARCH | R-TRANSPORT | Ephemeral URLSession plus evidence-derived direct-host allow-list | 01-03, 01-07 | COVERED | Default host set is empty before selection. |
| RESEARCH | R-STATE | One actor, one in-flight attempt, zero automatic auth retries | 01-02 | COVERED | Cancellation and parallel-call predicates are explicit. |
| RESEARCH | R-SECRETS | Post-success Keychain write and memory-first sign-out | 01-04 | COVERED | No database/SwiftData/files fallback. |
| RESEARCH | R-DIAG | Typed allow-listed diagnostics and synthetic canary fixtures | 01-03, 01-05 | COVERED | Support export is explicitly opted out. |
| RESEARCH | R-XCODE | Full Xcode required for native build/tests | 01-01, 01-04, 01-05, 01-07 | COVERED | Runnable preconditions stop when unavailable. |
| CONTEXT | D-01 | Evidence-select exactly one authentication path | 01-06, 01-07 | COVERED | Browser first, then honest native, else unsupported. |
| CONTEXT | D-02 | Dedicated unsupported compatibility experience | 01-01, 01-05, 01-07 | COVERED | No partial player shell or retained credentials. |
| CONTEXT | D-03 | Two manual authorized runs gate later phases | 01-08 | COVERED | Separate human initiations and conservative cooldown. |

### Spec-less edge resolution

The ten unresolved probe rows are resolved into plan predicates rather than silently dropped: AUTH-01 has the explicit five-class outcome plus entitlement predicate; AUTH-02 has the full terminal-stop/no-follow-up-request matrix; AUTH-03 has memory-first deletion and honest failure reporting; SECR-01 has post-success Keychain-only persistence; SECR-02 has cancellation-safe single-actor memory ownership; SECR-03 has synthetic canary exclusion; CLNT-01 has external-consumer compilation; CLNT-02 has endpoint-free Sendable async authentication, entitlement, catalog, metadata, and live-stream-resolution contracts with typed Phase 1 unavailable behavior for the three content domains; CLNT-03 has actor-owned adapter isolation under concurrency; CLNT-04 has deterministic injected transport, clock, credential store, and diagnostics.

### Prohibition recall breadcrumbs

Generic secret disclosure, request-rate abuse, malformed-input handling, and transport integrity are canon security items handled in the STRIDE registers and later `$gsd-secure-phase` verification, so they are not duplicated as bespoke prohibitions. The bespoke project prohibitions retained across plan frontmatter are: no multiple sign-in paths/fallback, no partial-authentication player shell, and no continuation to Phases 2–5 without both unambiguous proof runs.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to its primary trust boundaries. Every critical or high-severity threat below is blocking: if its mitigation cannot be implemented or verified, stop the plan without weakening the control.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Native app → public client | App input crosses into a semantic library contract that must expose no wire or secret-bearing implementation detail. |
| Public client → internal adapter | Typed operations cross into volatile behavior; the tracer must remain unavailable until evidence selects a path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information Disclosure / Elevation of Privilege | Browser-return boundary | high | mitigate | Tracer has no browser inspection API and no live adapter; later browser feasibility is human-operated only. |
| T-01-02 | Elevation of Privilege | Authentication entry | high | mitigate | Default client returns typed unsupported and offers no challenge/control workaround or alternate method. |
| T-01-05 | Spoofing / Tampering | Public API and adapter seam | high | mitigate | Public consumer sees only semantic values; unavailable adapter rejects unproven protocol behavior. |
| T-01-SC | Tampering | Package manifests / package-manager installs | high | mitigate | Both manifests have zero external dependencies; any npm/pip/cargo or new Swift package install halts because no package-legitimacy audit authorizes it. |
</threat_model>

<verification>
- `swift test --package-path Packages/SiriusXMClient` passes.
- Full Xcode is selected and `xcodebuild -project SiriusMac.xcodeproj -scheme SiriusMac -destination 'platform=macOS' test` passes.
- The tracer invokes no live network path and the app/public-library dependency direction is one-way.
- `PublicConsumerTests` imports without `@testable`, calls every CLNT-02 domain contract, observes typed unavailable catalog/metadata/live-stream outcomes, and exposes no wire-detail fields.
</verification>

<success_criteria>
- A native subscriber-facing interaction reaches a real typed compatibility result end to end.
- Another Apple-platform target consumes authentication, entitlement, catalog, metadata, and live-stream-resolution contracts without upstream wire knowledge.
- The skeleton stops honestly when full Xcode is unavailable and never installs it without authorization.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md` when done.
</output>
