---
phase: 01-safe-interoperability-foundation
plan: "02"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionState.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, SECR-02, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 40000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Authentication and entitlement are distinct native verification steps with distinct semantic outcomes."
    - "Only the runtime-owned sequence can create active session state; caller-authored booleans, result files, and planning artifacts have no authorization API."
    - "The actor permits one attempt, consumes one credential once, and performs no automatic retry or alternate method."
    - "Session state is published atomically only after both authentication and entitlement succeed."
    - "Unknown, challenged, rejected, not-entitled, cancelled, or ambiguous results clear transient state and cannot persist credentials."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      provides: "Runtime-owned authentication → entitlement → atomic-session state machine"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift"
      provides: "Authority, sequencing, concurrency, cancellation, and atomicity tests"
  key_links:
    - from: "SessionCoordinator.swift"
      to: "AuthenticationFlowAdapter.swift"
      via: "Coordinator passes raw internal native responses to strict classifiers"
  prohibitions:
    - statement: "MUST NOT expose an API that accepts authenticated/entitled claims, activate on profile success alone, or schedule retry/fallback."
      status: unverified
      flagged: true
---

<objective>
Create fail-closed native response classification and a single actor-owned authentication/entitlement session sequence.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/00-authentication-feasibility-gate/00-REVIEW.md
@.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-02-01: Classify native authentication and entitlement responses</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift</files>
  <behavior>
    - Test 1: a recognized native authentication response maps to authenticated-pending-entitlement, never final success.
    - Test 2: a recognized entitlement response maps to entitled or authenticated-but-not-entitled.
    - Test 3: rejection, challenge, CAPTCHA/MFA/control, 403, 429, redirect drift, bot signal, malformed, unknown, or ambiguous shapes are terminal typed outcomes.
    - Test 4: no public or internal convenience initializer can turn caller-authored `authenticated=true`/`entitled=true` claims into success.
  </behavior>
  <action>Implement closed internal response classifiers for the already settled native request contract. Preserve separate `AdapterAuthenticationResult` and `AdapterEntitlementResult` types. Classifiers receive internal transport responses, not files or caller claims, and unknown/multiple interpretations return unsupported. Public outcomes contain no raw body, URL, host, cookie, header, or token.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter AuthenticationOutcomeTests</automated></verify>
  <acceptance_criteria>
    - Profile/authentication success alone cannot produce an active or entitled public result.
    - Every terminal/control shape is explicit and fail-closed.
    - No self-asserted owner/result record is accepted by production types.
  </acceptance_criteria>
  <done>Native responses reduce to strict semantic authentication and entitlement classifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02-02: Own the complete sequence and publish session state atomically</name>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionState.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift</files>
  <behavior>
    - Test 1: one attempt consumes one credential, invokes authentication once, then entitlement once, in that order.
    - Test 2: session remains absent after authentication success until entitlement succeeds.
    - Test 3: entitled success swaps one immutable `ActiveSession` into actor state atomically; observers never see partial state.
    - Test 4: parallel attempts are rejected before collaborator work; cancellation and every terminal result zero transient state and schedule no retry.
    - Test 5: persistence is invoked only after the atomic entitled transition and never for not-entitled/terminal outcomes.
  </behavior>
  <action>Implement `SessionCoordinator` as the sole owner of `AttemptLease`, transient credential, pending verification, and `ActiveSession`. Model the sequence explicitly. Keep the new active session in a local immutable value until both verifiers succeed, then perform one actor-state assignment. Expose snapshots/semantic outcomes only. Inject credential source, authentication verifier, entitlement verifier, credential store, clock, and diagnostics for deterministic tests.</action>
  <verify><automated>swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests</automated></verify>
  <acceptance_criteria>
    - Runtime observations—not files or claims—are the only session authority.
    - Entitlement is mandatory before activation or persistence.
    - No observer can see a mixed/partial session and no failure can leave transient material.
  </acceptance_criteria>
  <done>The actor owns one complete, atomic, fail-closed authentication lifecycle.</done>
</task>

</tasks>

<output>Create `.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md` when done.</output>
