---
phase: 01-safe-interoperability-foundation
plan: "02"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionState.swift
  - Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift
  - Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift
autonomous: true
requirements: [AUTH-01, AUTH-02, SECR-02, CLNT-02, CLNT-03, CLNT-04]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Synthetic known shapes map to authenticated-and-entitled, authenticated-but-not-entitled, rejected, challenge, and unsupported semantic outcomes; malformed or ambiguous evidence is unsupported."
    - "CAPTCHA/interstitial, 403, 429, explicit rate limit, unexpected redirect, suspected bot response, and ambiguous entitlement each terminate the attempt with zero automatic retry or alternate path (D-01)."
    - "Exactly one authentication attempt can own the actor at a time; a parallel call is rejected locally before transport, and cancellation clears transient state before another explicit attempt is permitted."
    - "Session material exists only inside the actor and cannot be encoded, persisted, logged, or returned publicly."
  artifacts:
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      provides: "Internal strict classification seam for volatile upstream behavior"
      contains: "AuthenticationFlowAdapter"
    - path: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      provides: "Single-attempt actor and memory-only session transitions"
      contains: "actor SessionCoordinator"
    - path: "Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift"
      provides: "Serialized concurrency, cancellation, and stop-condition verification"
      contains: "@Suite(.serialized)"
  key_links:
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift"
      via: "Actor awaits one adapter classification and owns all subsequent transitions"
      pattern: "AuthenticationFlowAdapter"
    - from: "Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift"
      to: "Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift"
      via: "Internal classifications are projected to domain-only public outcomes"
      pattern: "AuthenticationOutcome"
  prohibitions:
    - statement: "MUST NOT add a browser/native method selector, an automatic fallback, or more than one shipped sign-in path (D-01)."
      status: unverified
      flagged: true
---

<objective>
Expand the tracer into a deterministic, fail-closed authentication compatibility engine with strict semantic classification and one actor-owned attempt/session state.

Purpose: Make protocol drift, control-protection signals, concurrency, and cancellation safe before any real provider path can be selected.
Output: Internal classifier, serialized session actor, memory-only state, and synthetic outcome/concurrency tests.
</objective>

<execution_context>
@/Users/gabe/.codex/gsd-core/workflows/execute-plan.md
@/Users/gabe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-safe-interoperability-foundation/01-CONTEXT.md
@.planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
@.planning/phases/01-safe-interoperability-foundation/01-01-SUMMARY.md

<interfaces>
Use the public symbols created by Plan 01-01 unchanged at the app boundary. Internal adapter input may contain response/status/redirect/body detail but must return an internal `AdapterAuthenticationResult`; only `SessionCoordinator` projects it to `AuthenticationOutcome`. No raw internal value may conform to a public protocol or appear in a public signature.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-02-01: Classify every known and terminal authentication outcome</name>
  <reversibility rating="costly">The semantic outcome taxonomy is a shared library contract used by the app and later phases, though volatile wire classifiers remain replaceable.</reversibility>
  <read_first>
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/SiriusXMClient.swift
    - .planning/phases/01-safe-interoperability-foundation/01-RESEARCH.md
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift</files>
  <behavior>
    - Test 1: synthetic authenticated evidence with the strict entitlement predicate maps to authenticated-and-entitled; authenticated evidence without entitlement maps distinctly and never unlocks viability.
    - Test 2: explicit rejection maps to rejected without retry eligibility.
    - Test 3: challenge/CAPTCHA/interstitial, 403, 429/rate-limit, redirect drift, suspected bot response, malformed shape, and ambiguous evidence each map to a terminal unsupported/control-protection class.
    - Test 4: public outcome values contain no raw body, URL, host, status response, cookie, header, or token field.
  </behavior>
  <action>Write synthetic tests first, then implement `AuthenticationFlowAdapter`, `AdapterAuthenticationResult`, `AdapterStopReason`, and narrow internal response/redirect decoder values. Define an allow-list of exact recognized semantic shapes; unknown fields/shapes and any ambiguity must map to unsupported, never to authenticated. Preserve distinct public success, rejection, challenge/control-protection, unsupported, and entitlement results required by AUTH-01. Do not guess an endpoint, host, client identity, entitlement signal, or browser return: the adapter has no live configuration until Plan 01-07 consumes evidence from the manual selection gate.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter AuthenticationOutcomeTests</automated>
    <manual>Inspect the test matrix and confirm every locked stop condition has an explicit terminal row and no raw upstream value appears in the public model.</manual>
  </verify>
  <acceptance_criteria>
    - Every required outcome class is test-covered with synthetic data.
    - Unknown, malformed, redirected, rate-limited, challenged, bot-like, and ambiguous inputs are unsupported.
    - No live provider request or speculative endpoint is introduced.
  </acceptance_criteria>
  <done>The internal classifier has a complete fail-closed semantic matrix with no live configuration and no public wire leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-02-02: Serialize attempts and memory-only session transitions in one actor</name>
  <read_first>
    - Packages/SiriusXMClient/Sources/SiriusXMClient/InternalAdapters/AuthenticationFlowAdapter.swift
    - Packages/SiriusXMClient/Sources/SiriusXMClient/Public/AuthenticationModels.swift
    - Packages/SiriusXMClient/Tests/SiriusXMClientTests/AuthenticationOutcomeTests.swift
  </read_first>
  <files>Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionState.swift, Packages/SiriusXMClient/Sources/SiriusXMClient/Session/SessionCoordinator.swift, Packages/SiriusXMClient/Tests/SiriusXMClientTests/SessionCoordinatorTests.swift</files>
  <behavior>
    - Test 1: two parallel explicit calls create one adapter/transport invocation; the second receives a local attempt-in-progress result.
    - Test 2: every terminal stop result clears transient input/session data and schedules no task, timer, fallback, or retry.
    - Test 3: cancellation retires the attempt lease and clears transient state before a later user action can begin another attempt.
    - Test 4: entitled success creates actor-private session state; no session/token value is returned or encoded.
  </behavior>
  <action>Create `SessionCoordinator` as the sole actor owning `SessionState`, `AttemptLease`, and actor-private `ActiveSession`. Inject the adapter, transport-facing seam, clock, credential store, and redacted diagnostic sink through narrow initializers used by deterministic tests. Reject concurrent attempts locally before invoking a collaborator. Treat task cancellation and every D-01/D-03 stop condition as cleanup boundaries; never spawn an authentication retry or fallback task. Keep cooldown as a manual proof concern rather than a production scheduler.</action>
  <verify>
    <automated>swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests</automated>
    <manual>Review recorded collaborator calls: each explicit action has at most one attempt, and stop/cancellation cases have no calls after the terminal result.</manual>
  </verify>
  <acceptance_criteria>
    - Parallel calls yield one in-flight attempt and one local rejection.
    - Cancellation/stop outcomes leave signed-out memory state with no scheduled work.
    - Only an authenticated-and-entitled classification creates actor-private session state.
  </acceptance_criteria>
  <done>The library has a deterministic one-attempt actor whose concurrency and interruption guarantees are proven by serialized Swift Testing cases.</done>
</task>

</tasks>

## Artifacts this phase produces

This plan creates `AuthenticationFlowAdapter.swift`, `SessionState.swift`, `SessionCoordinator.swift`, `AuthenticationOutcomeTests.swift`, and `SessionCoordinatorTests.swift`, and expands `AuthenticationModels.swift`. Internal symbols: `AuthenticationFlowAdapter`, `AdapterAuthenticationResult`, `AdapterStopReason`, internal response/redirect decoder values, `SessionCoordinator`, `SessionState`, `AttemptLease`, and `ActiveSession`. No new public wire symbol is created; the existing public semantic outcomes are refined without exposing internal types.

<threat_model>
## ASVS L1 Enforcement

This plan applies OWASP ASVS Level 1 to its primary trust boundaries. Every critical or high-severity threat below is blocking: if its mitigation cannot be implemented or verified, stop the plan without weakening the control.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Internal response → adapter classifier | Untrusted/volatile provider-shaped input becomes a semantic compatibility result. |
| Concurrent app calls → session actor | Multiple tasks may attempt authentication but only one may reach a provider-facing collaborator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-02 | Elevation of Privilege / Tampering | AuthenticationFlowAdapter | high | mitigate | Exact known-shape classifier maps challenges/control signals to terminal unsupported; no alternate or workaround branch exists. |
| T-01-04 | Denial of Service / Repudiation | SessionCoordinator | high | mitigate | Actor-owned attempt lease rejects parallel calls locally; tests assert one collaborator call and zero retries after stop/cancel. |
| T-01-05 | Spoofing / Tampering | Adapter classification | high | mitigate | Unknown, malformed, redirected, and ambiguous evidence cannot produce authenticated/entitled state. |
| T-01-SC | Tampering | Package dependency graph | high | mitigate | No dependency install is permitted; any newly required package stops execution pending a legitimacy audit. |
</threat_model>

<verification>
- `swift test --package-path Packages/SiriusXMClient --filter AuthenticationOutcomeTests` passes.
- `swift test --package-path Packages/SiriusXMClient --filter SessionCoordinatorTests` passes.
- The tests prove all stop conditions, one in-flight attempt, cancellation cleanup, and zero automatic retry/fallback.
</verification>

<success_criteria>
- Protocol drift cannot be misclassified as supported authentication.
- Concurrent/interrupted callers cannot cause a request storm or persist session material.
- Public consumers continue to see semantic typed async results only.
</success_criteria>

<output>
Create `.planning/phases/01-safe-interoperability-foundation/01-02-SUMMARY.md` when done.
</output>
