// The project currently compiles the library's small semantic models together
// in LibraryStore.swift. PlaybackQueue, QueueDirection, QueueDirectionAvailability,
// and LibraryRevealRequest are defined there so SwiftData's local-state boundary
// can be reviewed with the queue's stable-ID-only contract.
